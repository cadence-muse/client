---
phase: 06-foundation-info-settings-polish
plan: 04
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - lib/features/setlists/setlist_list_screen.dart
  - lib/features/setlists/setlist_detail_screen.dart
  - test/features/setlists/setlist_list_screen_test.dart
  - test/features/setlists/setlist_detail_screen_test.dart
autonomous: true
requirements: [SETL-11]

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Setlist list rows show a location icon (Icons.location_on) + value only when SetlistListItem.eventLocation is present, and a duration icon (Icons.timer) + value always -- e.g. \"Tonight's Show  <pin> The Fillmore  <timer> 45:30\" (SETL-11, UI-SPEC populated row)"
    - "When eventLocation is null/absent, only the duration icon+value renders in the trailing slot -- no placeholder (D-06 graceful degradation, UI-SPEC partial row)"
    - "The old 'N tracks, Xm Ys' trailing text (tracksAndDuration) is replaced by the location+duration icon row on this per-band list screen only -- lib/features/setlists/setlist_formatting.dart's tracksAndDuration/pluralizeTracks helpers are NOT deleted or modified, since the separate cross-band Setlists tab (lib/features/setlists/setlists_screen.dart, out of this phase's scope) still calls tracksAndDuration directly"
    - "Long location names truncate with an ellipsis in the list row; tapping the location icon+text shows the full untruncated text in a SnackBar without triggering the row's own navigation (UI-SPEC 'Setlist list metadata icons' overflow row)"
    - "Setlist detail screen shows icon rows (not label-prefixed text) for location (when present) and duration (always), replacing the old plain eventLocation Text and the old 'Duration: ...' prefixed text"
    - "Setlist list/detail reuse their existing screen-level loading/error states -- no new per-row skeleton or per-row error UI for this addition"
  artifacts:
    - lib/features/setlists/setlist_list_screen.dart
    - lib/features/setlists/setlist_detail_screen.dart
  key_links:
    - "SetlistListItem.eventLocation (added in 06-01) -> SetlistListScreen trailing icon row"
    - "SetlistListScreen location Text -> GestureDetector -> ScaffoldMessenger SnackBar (tap-to-expand, does not intercept the row's own onTap navigation)"
---

<objective>
Deliver SETL-11: event location and duration as icon-based at-a-glance indicators on the per-band Setlist list and detail screens, per D-04/D-07's icon+inline-row style, mirroring Plan 06-03's Track icon-row pattern.

Purpose: Completes the phase's third and final display vertical, reusing the icon-row conventions (SizedBox-constrained trailing Row, unprefixed detail rows) established by Plan 06-03.
Output: Setlist list rows and the Setlist detail screen both show icon-based location/duration indicators.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-foundation-info-settings-polish/06-CONTEXT.md
@.planning/phases/06-foundation-info-settings-polish/06-UI-SPEC.md
@lib/features/setlists/setlist_list_screen.dart
@lib/features/setlists/setlist_detail_screen.dart
@lib/features/setlists/setlist_formatting.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setlist list -- location + duration icon row (replaces tracksAndDuration trailing)</name>
  <files>lib/features/setlists/setlist_list_screen.dart, test/features/setlists/setlist_list_screen_test.dart</files>
  <read_first>
    lib/features/setlists/setlist_list_screen.dart (lines 87-111 -- `_buildContent`'s `ListView.separated`/`itemBuilder`, and the current `trailing: Text(tracksAndDuration(tracksCount, durationSeconds))` at line 99)
    lib/features/setlists/setlist_formatting.dart (`tracksAndDuration`/`pluralizeTracks`/`DurationFormatting.asMinutesAndSeconds` -- confirm these stay untouched; only stop calling `tracksAndDuration` from this one screen)
    lib/features/setlists/setlists_screen.dart (line 114 -- confirms the separate cross-band tab still calls `tracksAndDuration`, which is why `setlist_formatting.dart` itself must not change)
    .planning/phases/06-foundation-info-settings-polish/06-UI-SPEC.md (`## Setlist List Metadata Icons` section and the "Setlist list metadata icons" overflow row in `## UI Considerations`)
    test/features/setlists/setlist_list_screen_test.dart (the two tests at lines 87-125 and 127-176 that assert `find.text('3 tracks, 10m 0s')` / `find.text('1 track, 1m 0s')` / `find.text('8 tracks, 42m 35s')` -- these assert the OLD trailing format and must be rewritten, not just extended, since track-count text no longer appears in the trailing slot)
  </read_first>
  <action>
    In `lib/features/setlists/setlist_list_screen.dart`'s `_buildContent`, inside `itemBuilder`, replace `trailing: Text(tracksAndDuration(tracksCount, durationSeconds))` with an icon row. Read `final eventLocation = setlist['eventLocation'] as String?;` alongside the existing reads (keep `tracksCount`'s read only if still needed elsewhere in the row -- it is not used by the new trailing, but the row's other fields (`name`, `eventDate` subtitle) are unaffected). Build: `final colorScheme = Theme.of(context).colorScheme;` then `trailing: SizedBox(width: 150, child: Row(mainAxisSize: MainAxisSize.min, mainAxisAlignment: MainAxisAlignment.end, children: [ if (eventLocation != null) ...[GestureDetector(onTap: () => ScaffoldMessenger.of(context).showSnackBar(SnackBar(content: Text(eventLocation))), child: Row(mainAxisSize: MainAxisSize.min, children: [Icon(Icons.location_on, size: 18, color: colorScheme.primary), const SizedBox(width: 4), Text(eventLocation, style: const TextStyle(fontSize: 12), maxLines: 1, overflow: TextOverflow.ellipsis)])), const SizedBox(width: 8)], Icon(Icons.timer, size: 18, color: colorScheme.primary), const SizedBox(width: 4), Text(durationSeconds.asMinutesAndSeconds, style: const TextStyle(fontSize: 12)) ]))`. The `GestureDetector` wraps only the location icon+text pair (not the whole trailing Row and not the ListTile), so tapping it shows the SnackBar without triggering the ListTile's own `onTap` navigation to `SetlistDetailScreen` -- Flutter's hit-testing resolves the tap to the innermost gesture-handling widget. Duration always renders unconditionally (matching the always-shown behavior `tracksAndDuration` had before).

    Rewrite the two existing tests that assert the old trailing format: in `'a cached setlist with no eventDate shows "No date set"'`, change `expect(find.text('3 tracks, 10m 0s'), findsOneWidget);` to `expect(find.text('10m 0s'), findsOneWidget);` and add `expect(find.byIcon(Icons.timer), findsOneWidget);`. In `'track-count pluralization: 1 track is singular, 8 tracks is plural'`, rename the test to `'setlist list shows duration text regardless of track count'` (pluralization is no longer under test here since track count is no longer displayed) and change its two assertions to `expect(find.text('1m 0s'), findsOneWidget);` and `expect(find.text('42m 35s'), findsOneWidget);`.

    Add three new tests: (1) a setlist with `'eventLocation': 'The Fillmore'` shows `find.byIcon(Icons.location_on)`, `find.text('The Fillmore')`, and `find.byIcon(Icons.timer)`; (2) a setlist with no `eventLocation` key shows `find.byIcon(Icons.location_on)` `findsNothing` while `find.byIcon(Icons.timer)` still `findsOneWidget`; (3) tapping the location text of a setlist with a long `eventLocation` (e.g. 60+ characters) shows a SnackBar containing that full string (`await tester.tap(find.text(eventLocation)); await tester.pump(); expect(find.text(eventLocation), findsWidgets);` -- `findsWidgets` because both the (possibly-truncated-but-still-matched) row text and the SnackBar text share the same string) and does NOT navigate to `SetlistDetailScreen` (`expect(find.byType(SetlistDetailScreen), findsNothing);`).
  </action>
  <verify>
    <automated>flutter test test/features/setlists/setlist_list_screen_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - Setlist list trailing renders `Icons.location_on` + eventLocation text only when present, wrapped in a tap-to-expand `GestureDetector` that does not trigger row navigation.
    - Setlist list trailing always renders `Icons.timer` + `asMinutesAndSeconds` duration text.
    - `lib/features/setlists/setlist_formatting.dart` is unmodified; `lib/features/setlists/setlists_screen.dart` still compiles and its own tests (`test/features/setlists/setlists_screen_test.dart`) are unaffected since neither file changed.
    - `flutter test test/features/setlists/setlist_list_screen_test.dart` passes, including the two rewritten tests and the three new tests.
  </acceptance_criteria>
  <done>Setlist list rows show location icon+value (when present, tap-to-expand) and duration icon+value (always), replacing the old "N tracks, Xm Ys" trailing text.</done>
</task>

<task type="auto">
  <name>Task 2: Setlist detail -- icon rows for location/duration</name>
  <files>lib/features/setlists/setlist_detail_screen.dart, test/features/setlists/setlist_detail_screen_test.dart</files>
  <read_first>
    lib/features/setlists/setlist_detail_screen.dart (lines 248-297 -- `_buildContent`'s header `Column`: the existing plain `Text(eventLocation)` block and the `Text('Duration: ${durationSeconds.asMinutesAndSeconds}')` line, plus the already-declared `colorScheme` local var)
    .planning/phases/06-foundation-info-settings-polish/06-UI-SPEC.md (`## Setlist List Metadata Icons` fields list, which also documents the Detail-screen icon choices)
    test/features/setlists/setlist_detail_screen_test.dart (lines 99-148 -- the 'a full BandSetlist response...' test, which asserts `find.text('The Venue')` (stays valid, unprefixed already) and `find.text('Duration: 42m 35s')` (must change -- prefix is being dropped) and lines 150+ -- the "eventLocation/eventDate omitted" test, which must keep passing unchanged)
  </read_first>
  <action>
    In `lib/features/setlists/setlist_detail_screen.dart`'s `_buildContent` header `Column`, replace the `if (eventLocation != null) ...[const SizedBox(height: 16), Text(eventLocation)]` block's `Text` with `Row(children: [Icon(Icons.location_on, size: 20, color: colorScheme.primary), const SizedBox(width: 8), Expanded(child: Text(eventLocation))])` -- the visible string itself (`eventLocation`, unprefixed) does not change, only an icon is added, so no existing `find.text(eventLocation)` assertion breaks. Leave the `eventDate` block (`Text(formatEventDate(eventDate))`) completely untouched -- eventDate is out of SETL-11's scope (only location/duration are required).

    Replace `Text('Duration: ${durationSeconds.asMinutesAndSeconds}')` with `Row(children: [Icon(Icons.timer, size: 20, color: colorScheme.primary), const SizedBox(width: 8), Text(durationSeconds.asMinutesAndSeconds)])` (drops the "Duration:" prefix -- the icon now carries that meaning, per D-07).

    Update the existing test 'a full BandSetlist response renders name/location/date/duration/tracks': change `expect(find.text('Duration: 42m 35s'), findsOneWidget);` to `expect(find.text('42m 35s'), findsOneWidget);`; the `expect(find.text('The Venue'), findsOneWidget);` line needs no change. Add `expect(find.byIcon(Icons.location_on), findsOneWidget);` and `expect(find.byIcon(Icons.timer), findsOneWidget);` to the same test. Verify (do not need to change) the "eventLocation/eventDate are omitted entirely... when both are null" test still passes as-is -- since the `if (eventLocation != null)` guard is unchanged, only its inner widget changed.
  </action>
  <verify>
    <automated>flutter test test/features/setlists/setlist_detail_screen_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - Setlist detail's location row (when present) is icon+unprefixed-text; duration row is icon+unprefixed-text (no "Duration:" label).
    - eventDate row is unchanged (not part of SETL-11).
    - `flutter test test/features/setlists/setlist_detail_screen_test.dart` passes, including the updated full-response test and the unmodified omitted-fields test.
  </acceptance_criteria>
  <done>Setlist detail screen shows icon rows for location (when present) and duration (always), matching the Track detail screen's icon+inline-row style.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| SetlistListItem.eventLocation / BandSetlist.eventLocation -> icon rows | Server-returned setlist metadata is rendered as-is; no new mutation surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-04-01 | Information Disclosure | SetlistListScreen location icon row | low | accept | `eventLocation` was already visible to the same authenticated band member via `SetlistDetailScreen` (full `BandSetlist`); surfacing it on the list is a display convenience, not new exposure. |
| T-06-04-02 | Information Disclosure | Location tap-to-expand SnackBar | low | accept | The SnackBar only re-displays the same already-rendered `eventLocation` string (untruncated), not additional data. |
</threat_model>

<verification>
- `flutter test test/features/setlists/` passes (both files).
- `flutter analyze` reports no new errors.
- `flutter test test/features/setlists/setlists_screen_test.dart` (the untouched cross-band tab) still passes, confirming `setlist_formatting.dart`'s `tracksAndDuration` was not broken.
</verification>

<success_criteria>
- Setlist list rows show location (when present, tap-to-expand) + duration icons.
- Setlist detail screen shows icon rows for location (when present) and duration.
</success_criteria>

<output>
Create `.planning/phases/06-foundation-info-settings-polish/06-04-SUMMARY.md` when done
</output>
