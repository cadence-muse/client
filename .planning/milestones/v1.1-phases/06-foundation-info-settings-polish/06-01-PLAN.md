---
phase: 06-foundation-info-settings-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/api/publicapi.yml
  - lib/api/public_api.dart
  - lib/features/profile/change_password_screen.dart
  - lib/features/profile/profile_screen.dart
  - test/features/profile/change_password_screen_test.dart
autonomous: true
requirements: [USER-03]

estimate:
  tokens: 78000
  raw_tokens: 78000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "User can navigate to Profile -> Change password and see three obscured password fields: Current password, New password, Confirm new password (D-02)"
    - "Initially all fields are empty and the submit button is enabled (UI-SPEC Password Change Form -> State)"
    - "Submit is blocked until all three fields pass validation: current password non-empty ('This field is required'), new password >= 8 chars ('At least 8 characters'), confirm matches new ('Passwords don't match')"
    - "While submitting, the button is disabled and shows a loading spinner; the button disables only during the in-flight call, not before submit is pressed"
    - "On success: SnackBar 'Password changed successfully' shown and the screen pops back to Profile"
    - "On a 400 response with code 'invalid_input' (wrong current password, per lib/api/publicapi.yml's BadRequest response for ChangeUserPassword -- NOT 401): inline red error text 'Current password is incorrect' is shown, fields are preserved, submit re-enables"
    - "On any other ApiException: inline red error text shows the server's e.message, fields preserved, submit re-enables"
    - "Editing any field after an error clears the stale error message (RESEARCH.md Pitfall 5)"
  artifacts:
    - lib/features/profile/change_password_screen.dart
    - test/features/profile/change_password_screen_test.dart
  key_links:
    - "ProfileScreen 'Change password' ListTile -> Navigator.push -> ChangePasswordScreen"
    - "ChangePasswordScreen._submit -> ref.read(publicApiProvider).changePassword -> ApiClient.send('POST', '/api/me/password') -> ChangeUserPasswordRequestBody{currentPassword, password}"
    - "lib/api/publicapi.yml TrackListItem.key / SetlistListItem.eventLocation / BandListItem.ownerId (added by Task 2) -> consumed by Plan 06-02/06-03/06-04's display work in Wave 2"
---

<objective>
Deliver USER-03 end-to-end (password change on the Profile screen, matching D-01/D-02) as this phase's tracer slice, and extend `lib/api/publicapi.yml` with the three remaining client-first schema fields (`TrackListItem.key`, `SetlistListItem.eventLocation`, `BandListItem.ownerId` per D-03/D-04/D-05/D-06) that Wave 2's display plans depend on.

Purpose: Password change is the phase's only feature that spans every layer (spec -> service -> form UI -> error handling), making it the representative end-to-end path to prove first. Bundling the remaining spec additions into this same Wave-1 plan means Wave 2's three display plans (Band/Track/Setlist) never touch `publicapi.yml` themselves, so they have zero file overlap with each other and can run fully in parallel.
Output: Working password-change flow (screen + API method + spec field), plus `lib/api/publicapi.yml` carrying all four of this phase's schema additions.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-foundation-info-settings-polish/06-CONTEXT.md
@.planning/phases/06-foundation-info-settings-polish/06-UI-SPEC.md
@lib/api/publicapi.yml
@lib/api/public_api.dart
@lib/api/api_exception.dart
@lib/features/auth/login_screen.dart
@lib/features/profile/profile_screen.dart
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Password change end-to-end -- spec field, API method, form screen, wiring</name>
  <files>lib/api/publicapi.yml, lib/api/public_api.dart, lib/features/profile/change_password_screen.dart, lib/features/profile/profile_screen.dart, test/features/profile/change_password_screen_test.dart</files>
  <read_first>
    lib/api/publicapi.yml (lines 90-108 `/api/me/password` operation; lines 727-734 `ChangeUserPasswordRequestBody`; lines 646-667 `BadRequestResponseBody`/`ErrorCode` -- confirms the ONLY documented error responses for this endpoint are '200' and '400', never '401')
    lib/api/public_api.dart (existing `register`/`login`/`logout` method shapes to mirror)
    lib/api/api_exception.dart (`ApiException.fromResponse` -- `statusCode`/`code`/`message` shape)
    lib/features/auth/login_screen.dart (full form pattern: `_formKey`, controllers, `_isSubmitting`/`_errorMessage` state, validator style, `ApiException` catch-and-translate, `FilledButton` with spinner)
    lib/features/profile/profile_screen.dart (current `_buildContent` ListTile list -- Settings then Log out -- to insert the new tile between them)
    .planning/phases/06-foundation-info-settings-polish/06-UI-SPEC.md (`## Password Change Form` and `## Form Validation Rules` sections -- exact copy strings)
    test/features/bands/band_detail_screen_test.dart (`wrapWithListRoot` pattern, lines 59-88 -- mirror for a push-then-pop test harness)
    test/features/profile/profile_screen_test.dart (`buildApiClient`/`wrap` helper pattern to mirror for the new test file)
  </read_first>
  <behavior>
    - Test: renders exactly 3 `TextFormField`s labeled 'Current password', 'New password', 'Confirm new password', all `obscureText: true`.
    - Test: tapping submit with an empty current password shows 'This field is required' and does not call the API.
    - Test: a new password under 8 characters shows 'At least 8 characters'.
    - Test: a confirm value that doesn't match the new password shows "Passwords don't match".
    - Test: a valid submit calls `changePassword` with the typed `currentPassword`/`newPassword`, shows SnackBar 'Password changed successfully', and pops back to the pushing root.
    - Test: a 400 response with `{"code": "invalid_input", "message": "..."}` shows inline error 'Current password is incorrect' (not the raw server message) and re-enables the button.
    - Test: a 500 response with `{"code": "server_error", "message": "Something broke"}` shows inline error 'Something broke' (the raw `e.message`) verbatim.
    - Test: while the API call is in flight, the submit button's `onPressed` is `null` and a `CircularProgressIndicator` is shown inside it.
    - Test: after an error is shown, typing in any field clears the error text.
  </behavior>
  <action>
    In `lib/api/publicapi.yml`, extend the `ChangeUserPasswordRequestBody` schema (currently only has `password`) to also declare `currentPassword` (`type: string`, `format: password`) and add it to the schema's `required` list alongside `password` (per D-01 -- client always collects and sends it; server implements validation separately). Do not touch the `/api/me/password` path's `responses` block -- it already correctly documents only `'200'` and `'400'` (`$ref: '#/components/responses/BadRequest'`); there is no `'401'` response for this operation in the spec, despite 06-UI-SPEC.md's Password Change Form section saying "Server returns 401" -- that line is a documentation slip in the UI-SPEC against the actual API contract in publicapi.yml (the source of truth per CLAUDE.md). Implement against 400 + `ErrorCode.invalid_input`, which is what 06-RESEARCH.md's Code Examples section and the existing `BadRequestResponseBody`/`ErrorCode` schemas actually support.

    In `lib/api/public_api.dart`, add `Future<void> changePassword({required String currentPassword, required String newPassword})` following the `logout()`/`updateBand()` void-return shape: `await _client.send('POST', '/api/me/password', body: {'currentPassword': currentPassword, 'password': newPassword})`. Add a doc comment noting the 400+`invalid_input` error contract and that `currentPassword` is a client-first D-01 addition.

    Create `lib/features/profile/change_password_screen.dart` with a `ChangePasswordScreen` `ConsumerStatefulWidget` (mirrors `LoginScreen`'s structure exactly): `GlobalKey<FormState> _formKey`, three `TextEditingController`s (`_currentPasswordController`, `_newPasswordController`, `_confirmPasswordController`, all disposed), `bool _isSubmitting`, `String? _errorMessage`. Each `TextFormField` sets `onChanged: (_) => setState(() => _errorMessage = null)` (RESEARCH.md Pitfall 5). Validators use the exact copy from 06-UI-SPEC.md's Form Validation Rules table: current password empty -> `'This field is required'`; new password empty -> `'This field is required'`, under 8 chars -> `'At least 8 characters'`; confirm empty -> `'This field is required'`, mismatch -> `"Passwords don't match"`. `_submit()`: validate via `_formKey.currentState!.validate()`, set `_isSubmitting=true`/`_errorMessage=null`, call `ref.read(publicApiProvider).changePassword(currentPassword: ..., newPassword: ...)`; on success (`if (mounted)`) show `ScaffoldMessenger.of(context).showSnackBar(const SnackBar(content: Text('Password changed successfully')))` then `Navigator.of(context).pop()`; on `ApiException catch (e)`, if `e.statusCode == 400 && e.code == 'invalid_input'` set `_errorMessage = 'Current password is incorrect'`, else `_errorMessage = e.message`; `finally` guard `if (mounted) setState(() => _isSubmitting = false)`. `Scaffold(appBar: AppBar(title: const Text('Change password')), body: ...)` with the `Form` wrapped in `SingleChildScrollView(padding: const EdgeInsets.all(24))`, 16px `SizedBox` spacing between fields (per UI-SPEC Layout), `OutlineInputBorder()` on each field, error text styled with `Theme.of(context).colorScheme.error`, and a full-width `FilledButton` labeled `'Change password'` with the standard 20x20 `CircularProgressIndicator(strokeWidth: 2)` spinner swap while `_isSubmitting`.

    In `lib/features/profile/profile_screen.dart`, import `change_password_screen.dart` and insert a new `ListTile` (leading `Icons.lock_outline`, title `'Change password'`, trailing `Icons.chevron_right`) between the existing `'Settings'` and `'Log out'` tiles in `_buildContent`, navigating via `Navigator.of(context).push(MaterialPageRoute(builder: (context) => const ChangePasswordScreen()))`.

    Write `test/features/profile/change_password_screen_test.dart` covering every case in `<behavior>` above, following the `buildApiClient`/`wrap` helper pattern from `test/features/profile/profile_screen_test.dart` (ApiClient with `MockClient`, `ProviderScope` overriding `apiClientProvider`/`cacheServiceProvider`). For the success-pop test, mirror `band_detail_screen_test.dart`'s `wrapWithListRoot` pattern: build a root `Scaffold` with a button that pushes `ChangePasswordScreen`, so popping is observable (`find.byType(ChangePasswordScreen)` becomes `findsNothing` and the root text reappears) -- `ChangePasswordScreen` doesn't need `CacheService` for its own logic, but the shared `wrap` helper should still accept and pass through a `CacheService.inMemory()` override for provider-tree consistency with the rest of the suite.
  </action>
  <reversibility rating="costly">D-01's `currentPassword` field name/shape is a client-first spec addition (06-CONTEXT.md explicitly rates it "costly" -- once backend implements against this field name, renaming it later requires a coordinated client+backend change). Not one-way (no data loss, no destructive action), so no blocking checkpoint -- flagged for awareness only.</reversibility>
  <verify>
    <automated>flutter test test/features/profile/change_password_screen_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `lib/api/publicapi.yml`'s `ChangeUserPasswordRequestBody` has `currentPassword` (type string, format password) in `properties` and in `required`, alongside the pre-existing `password`.
    - `PublicApi.changePassword({required currentPassword, required newPassword})` exists in `lib/api/public_api.dart`, POSTs to `/api/me/password` with body `{'currentPassword': ..., 'password': ...}`, returns `void`.
    - `ChangePasswordScreen` exists with 3 obscured `TextFormField`s, the exact validator copy strings listed in `<action>`, and the 400+`invalid_input` -> "Current password is incorrect" branch (not a 401 check).
    - `ProfileScreen` has a "Change password" `ListTile` between "Settings" and "Log out" that pushes `ChangePasswordScreen`.
    - `flutter test test/features/profile/change_password_screen_test.dart` passes with all 9 behaviors from `<behavior>` covered.
  </acceptance_criteria>
  <done>Password change works end-to-end from the Profile screen: happy path succeeds with a snackbar+pop, wrong current password is rejected with a clear inline error, and client-side validation blocks incomplete submits -- all proven by a passing automated widget test.</done>
</task>

<task type="auto">
  <name>Task 2: Extend TrackListItem/SetlistListItem/BandListItem schemas (spec-only, unblocks Wave 2)</name>
  <files>lib/api/publicapi.yml</files>
  <read_first>
    lib/api/publicapi.yml (lines 757-771 `BandListItem`; lines 873-889 `TrackListItem`; lines 1012-1032 `SetlistListItem`; lines 941-960 `UpdateBandTrackRequestBody` for the project's `nullable: true` convention on *clearable request* fields, to confirm it does NOT apply here -- these are response-item additions, which follow the plain "omit from `required`" convention already used by e.g. `TrackListItem.durationSeconds`)
    .planning/phases/06-foundation-info-settings-polish/06-CONTEXT.md (D-03, D-04, D-05, D-06, and the "Claude's Discretion" note on required/nullable marking)
  </read_first>
  <action>
    In `lib/api/publicapi.yml`, add exactly three new optional properties, none added to any `required` list (matching D-06's graceful-degradation contract and the existing convention where e.g. `TrackListItem.durationSeconds` is a property but not in `required`):
    1. `TrackListItem` (properties: id/title/artist/durationSeconds, required: id/title/artist) -- add `key: { type: string }` (per D-03: list rows get key, NOT notes -- notes stays detail-screen-only via the existing full `BandTrack` schema, unchanged).
    2. `SetlistListItem` (properties: id/name/tracksCount/durationSeconds/eventDate, required: id/name/tracksCount/durationSeconds) -- add `eventLocation: { type: string }` (per D-04, matching the field name already used in `BandSetlist`).
    3. `BandListItem` (properties: id/name/membersCount, required: id/name/membersCount) -- add `ownerId: { type: string, format: uuid }` (per D-05, matching the `format: uuid` convention already used for `id` in the same schema).
    Do not add `nullable: true` to any of the three -- that flag is this spec's convention for *update-request* bodies where an explicit `null` means "clear this field" (see `UpdateBandTrackRequestBody`), not for read-only list-item response fields, which the codebase already models as plain optional (absent-from-`required`) properties.
  </action>
  <verify>
    <automated>awk '/^    TrackListItem:/,/^$/' lib/api/publicapi.yml | grep -q 'key:' && awk '/^    SetlistListItem:/,/^$/' lib/api/publicapi.yml | grep -q 'eventLocation:' && awk '/^    BandListItem:/,/^$/' lib/api/publicapi.yml | grep -q 'ownerId:' && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `TrackListItem` schema block contains a `key` property; `key` is absent from `TrackListItem`'s `required` list.
    - `SetlistListItem` schema block contains an `eventLocation` property; `eventLocation` is absent from `SetlistListItem`'s `required` list.
    - `BandListItem` schema block contains an `ownerId` property (format uuid); `ownerId` is absent from `BandListItem`'s `required` list.
    - The verify command's `awk`-scoped greps all match (prints `PASS`).
  </acceptance_criteria>
  <done>All three remaining schema fields exist in publicapi.yml as optional properties, unblocking Plans 06-02/06-03/06-04's display work in Wave 2.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| ChangePasswordScreen form -> POST /api/me/password | User-typed `currentPassword`/`newPassword` cross from client UI into an authenticated HTTP request body |
| API error response -> inline UI text | Server-returned `code`/`message` (BadRequestResponseBody) is inspected and partially rendered to the user |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01-01 | Tampering | ChangePasswordScreen validators | low | accept | Client-side validation (length/match) is UX-only and trivially bypassable via a direct API call -- per ASVS V5 this is acceptable because the server is the authoritative validator (400+`invalid_input` on a bad `currentPassword`); no client-side check is treated as a security control. |
| T-06-01-02 | Information Disclosure | ChangePasswordScreen error display | medium | mitigate | Never render the raw server `message` for the wrong-current-password case -- always substitute the fixed string `'Current password is incorrect'` when `statusCode==400 && code=='invalid_input'`, so no server-internal detail (e.g. hash-mismatch wording) reaches the UI. Other error codes still surface `e.message` (already user-safe generic text per existing `ApiException` contract). |
| T-06-01-03 | Information Disclosure | Password TextFormFields | low | mitigate | All three fields set `obscureText: true`, verified by the widget test's field-rendering assertion; no password value is ever logged (no `print`/logging calls added). |
| T-06-01-04 | Tampering / Spoofing | Transport (MITM on `/api/me/password`) | low | accept | Covered by the existing app-wide HTTPS-only `ApiClient` transport (unchanged this phase); no new mitigation needed, out of this plan's scope. |

No new packages are installed this task (RESEARCH.md's Package Legitimacy Audit confirms zero new dependencies for Phase 6), so no package-legitimacy checkpoint applies.
</threat_model>

<verification>
- `flutter test test/features/profile/change_password_screen_test.dart` passes (Task 1).
- The `awk`-scoped grep check in Task 2's `<verify>` prints `PASS`.
- `flutter analyze` reports no new errors introduced by either task.
</verification>

<success_criteria>
- A user can open Profile -> Change password, submit valid current/new/confirm values, and see success feedback (SnackBar) with the screen returning to Profile.
- A wrong current password is rejected with the clear message "Current password is incorrect", distinct from any other error.
- `lib/api/publicapi.yml` carries all four of this phase's client-first field additions (`currentPassword`, `key`, `eventLocation`, `ownerId`), unblocking Wave 2.
</success_criteria>

<output>
Create `.planning/phases/06-foundation-info-settings-polish/06-01-SUMMARY.md` when done
</output>
