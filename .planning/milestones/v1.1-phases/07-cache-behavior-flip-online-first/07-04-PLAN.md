---
phase: 07-cache-behavior-flip-online-first
plan: 04
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - lib/providers/setlists_provider.dart
  - test/providers/setlists_provider_test.dart
  - lib/features/setlists/setlists_screen.dart
  - lib/features/setlists/setlist_list_screen.dart
  - lib/features/setlists/setlist_detail_screen.dart
  - test/features/setlists/setlists_screen_test.dart
  - test/features/setlists/setlist_list_screen_test.dart
  - test/features/setlists/setlist_detail_screen_test.dart
autonomous: true
requirements: [OFFL-07, OFFL-08]

estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Opening the global cross-band Setlists tab while online always shows freshly-fetched server data, and re-selecting it after it was already built triggers a fresh fetch (OFFL-07, D-01)"
    - "Opening a band's per-band setlist list or a setlist detail screen (pushed routes) while online always fetches fresh, with no new tab-switch wiring needed (D-02)"
    - "All three setlist screens show cached data offline instead of a blank/error screen, and the dedicated 'No cached data' state (no Retry) when nothing was ever cached, auto-recovering on reconnect (OFFL-08, D-06, D-07)"
    - "SyncStatusBadge no longer renders on any of the 3 setlist screens (OFFL-08)"
  artifacts: []
  key_links:
    - "SetlistListData/SetlistDetailData/UserSetlistsListData.build() -> ref.watch(isOnlineProvider) (online-first fetch decision, D-01/D-03)"
    - "SetlistsScreen.build() -> ref.listen(selectedTabIndexProvider) -> ref.invalidate(userSetlistsListDataProvider) (D-01 tab-switch refetch, tab index 3)"
---

<objective>
Apply the online-first pattern established by 07-01's tracer to the three
setlist providers (`SetlistListData` per-band, `SetlistDetailData`
per-setlist, `UserSetlistsListData` cross-band tab) and their three screens.
Mirrors 07-03's Tracks-bundle plan exactly, one entity level down (Setlists
instead of Tracks).

Purpose: extend OFFL-07/OFFL-08 coverage to every setlist-related screen
using the exact `build()` rewrite shape 07-01 proved, distinguishing the one
tab screen (cross-band `SetlistsScreen`, D-01 tab-switch wiring) from the two
pushed-route screens (`SetlistListScreen`/`SetlistDetailScreen`, D-02 no new
wiring).

Output: `setlists_provider.dart`'s three providers rewritten to online-first;
`setlists_screen.dart`/`setlist_list_screen.dart`/`setlist_detail_screen.dart`
rewired accordingly.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-cache-behavior-flip-online-first/07-CONTEXT.md
@.planning/phases/07-cache-behavior-flip-online-first/07-UI-SPEC.md
@.planning/phases/07-cache-behavior-flip-online-first/07-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: setlists_provider.dart — SetlistListData, SetlistDetailData, UserSetlistsListData online-first</name>
  <files>lib/providers/setlists_provider.dart, test/providers/setlists_provider_test.dart</files>
  <read_first>lib/providers/bands_provider.dart (07-01's rewritten BandsListData/BandDetailData.build() — canonical template), lib/providers/tracks_provider.dart (07-03's rewritten sibling — same 3-provider-in-one-file shape), lib/providers/setlists_provider.dart, test/providers/setlists_provider_test.dart, lib/providers/offline_no_cache_exception.dart</read_first>
  <behavior>
    Apply 07-01's 6 provider-level cases to all THREE providers in this
    file:
    - `SetlistListData.build(bandId)` — has a `_version` guard, preserve it.
    - `SetlistDetailData.build(bandId, setlistId)` — has a `_version` guard,
      preserve it; note this provider ALSO has `updateFields()` and
      `reorderTracks()` local-mutation methods beyond the usual one — both
      stay untouched, only `build()` changes.
    - `UserSetlistsListData.build()` — watches
      `selectedSetlistBandIdFilterProvider`; has the same never-mutated
      `final int _version = 0;` field as `UserTracksListData` — preserve the
      capture/compare pattern as-is.
  </behavior>
  <action>
    Rewrite all three `build()` methods in
    `lib/providers/setlists_provider.dart` to the online-first shape from
    07-01 (watch `isOnlineProvider` and `cacheServiceProvider`; online
    branch tries `_fetchAndCache(...)` falling back to the matching
    `cache.readX(...)` call on failure, setting the matching `XxxSyncedAt`
    provider on a cache hit; offline branch reads cache the same way or
    throws `const OfflineNoCacheException()`):
    - `SetlistListData.build(String bandId)`: online try
      `_fetchAndCache(bandId)`, catch falls back to
      `cache.readBandSetlists(bandId)`; offline reads the same or throws.
    - `SetlistDetailData.build(String bandId, String setlistId)`: online try
      `_fetchAndCache(bandId, setlistId)`, catch falls back to
      `cache.readSetlistDetail(bandId, setlistId)`; offline reads the same
      or throws.
    - `UserSetlistsListData.build()`: keep
      `final bandIdFilter = ref.watch(selectedSetlistBandIdFilterProvider);`
      as the first line (unchanged), then apply online-first: online try
      `_fetchAndCache(bandIdFilter)`, catch falls back to
      `cache.readUserSetlists(bandIdFilter)`; offline reads the same or
      throws.
    Delete all three now-dead `_refresh(...)` private methods (same
    `unused_element` reason as 07-01/07-03). Leave every `_fetchAndCache`,
    `refresh()`, `_doRefresh()`, `_version`, and local-mutation method
    (`removeFromList`, `updateFields`, `reorderTracks`) completely
    untouched. Add imports for `connectivity_provider.dart` and
    `offline_no_cache_exception.dart`.

    Update `test/providers/setlists_provider_test.dart` (896 lines,
    pre-existing coverage for all three providers plus `reorderTracks`):
    apply the 07-01-established rewrite pattern to each provider's
    build()-related test block; keep every `_version`-guard/dedup/
    local-mutation/`reorderTracks` test unchanged in intent — only the
    "cache-hit returns cached data immediately" family of tests changes.
  </action>
  <verify>
    <automated>flutter test test/providers/setlists_provider_test.dart && flutter analyze lib/providers/setlists_provider.dart</automated>
  </verify>
  <acceptance_criteria>
    - `SetlistListData.build(bandId)`, `SetlistDetailData.build(bandId,
      setlistId)`, and `UserSetlistsListData.build()` each call their
      `_fetchAndCache(...)` directly when online, even with a populated
      cache present.
    - Each falls back silently to its matching cache read when online but
      the fetch throws (D-03), and each throws `OfflineNoCacheException`
      when offline with no cache (D-06).
    - All pre-existing `_version`-guard/dedup/`reorderTracks`/local-mutation
      tests still pass unmodified in intent.
    - `flutter analyze lib/providers/setlists_provider.dart` reports zero
      `unused_element` warnings (all three `_refresh()` methods removed).
  </acceptance_criteria>
  <done>SetlistListData, SetlistDetailData, and UserSetlistsListData are all
  online-first with D-03/D-06 fallback, `_version` guards and
  `reorderTracks`/`updateFields` preserved unchanged; all listed tests
  pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Setlist screens — SetlistsScreen (tab), SetlistListScreen + SetlistDetailScreen (pushed routes)</name>
  <files>lib/features/setlists/setlists_screen.dart, lib/features/setlists/setlist_list_screen.dart, lib/features/setlists/setlist_detail_screen.dart, test/features/setlists/setlists_screen_test.dart, test/features/setlists/setlist_list_screen_test.dart, test/features/setlists/setlist_detail_screen_test.dart</files>
  <read_first>lib/features/bands/bands_screen.dart (07-01's rewritten tab screen — canonical template), lib/features/bands/band_detail_screen.dart (07-01's rewritten pushed-route screen — canonical template), lib/features/setlists/setlists_screen.dart, lib/features/setlists/setlist_list_screen.dart, lib/features/setlists/setlist_detail_screen.dart, test/features/setlists/setlists_screen_test.dart, test/features/setlists/setlist_list_screen_test.dart, test/features/setlists/setlist_detail_screen_test.dart, lib/providers/offline_no_cache_exception.dart, lib/widgets/offline_no_cache_view.dart</read_first>
  <behavior>
    - `SetlistsScreen` (cross-band tab, index 3): mirror `BandsScreen`'s
      07-01 shape — tab-switch listener invalidating
      `userSetlistsListDataProvider` on `current == 3` (D-01), `AppBar.bottom`
      `LinearProgressIndicator` gated on
      `userSetlistsAsync.isLoading && userSetlistsAsync.hasValue` (D-08),
      `OfflineNoCacheView` on `OfflineNoCacheException` (D-06).
    - `SetlistListScreen`/`SetlistDetailScreen` (pushed routes, family
      providers): mirror `BandDetailScreen`'s 07-01 shape — NO tab-switch
      listener (D-02), NO `AppBar.bottom` indicator, just badge removal +
      `OfflineNoCacheView` error branch. Note `SetlistDetailScreen` is a
      `ConsumerStatefulWidget` (not a plain `ConsumerWidget`) — the same
      error-branch and badge-removal edits apply inside its `State.build()`
      method; its `_editMode`/reorder/remove-track logic is untouched.
  </behavior>
  <action>
    Rewire `lib/features/setlists/setlists_screen.dart`: this screen
    currently watches `userSetlistsSyncedAtProvider` at the top-level
    `build()` (passed down into `_buildSetlistsBody`) — remove that
    `final syncedAt = ...` line and the `sync_status_badge.dart` import.
    Add `ref.listen<int>(selectedTabIndexProvider, (previous, current) { if
    (current == 3) ref.invalidate(userSetlistsListDataProvider); });` at the
    top of `build()` (D-01; tab index 3 = Setlists), registered before the
    `bands.isEmpty` early-return so a tab-switch always re-triggers a fetch
    attempt. Lift a `ref.watch(userSetlistsListDataProvider)` read up to
    `build()` (or thread the already-watched value from
    `_buildSetlistsBody`) so `build()` can render an `AppBar.bottom`
    `LinearProgressIndicator` gated on `isLoading && hasValue` (D-08).
    Simplify `_buildSetlistsBody`'s `data:` branch to directly
    `_buildContent(context, setlists)` (drop the
    `Column([SyncStatusBadge, Expanded])` wrapper). Add the
    `OfflineNoCacheView` branch to `error:` before the existing
    `_buildError(...)` fallback. Add imports for `navigation_provider.dart`,
    `offline_no_cache_exception.dart`, `offline_no_cache_view.dart`.

    Rewire `lib/features/setlists/setlist_list_screen.dart` and
    `lib/features/setlists/setlist_detail_screen.dart` identically to how
    07-01 Task 2 rewired `band_detail_screen.dart`: remove the
    `sync_status_badge.dart` import and each screen's
    `final syncedAt = ref.watch(...SyncedAtProvider(...));` line; simplify
    the `data:` branch to drop the `Column([SyncStatusBadge, Expanded])`
    wrapper (in `setlist_detail_screen.dart` this is inside
    `_SetlistDetailScreenState.build()`); add the `OfflineNoCacheView`
    branch to each `error:` callback; add imports for
    `offline_no_cache_exception.dart` and `offline_no_cache_view.dart`. No
    tab-switch listener, no `AppBar.bottom` indicator on either (D-02).
    `setlist_detail_screen.dart`'s edit-mode/reorder/remove-track/add
    -tracks logic and its `_removeTrack`/`_handleReorder` methods are
    entirely out of scope — do not touch them.

    Update `test/features/setlists/setlists_screen_test.dart`,
    `test/features/setlists/setlist_list_screen_test.dart`, and
    `test/features/setlists/setlist_detail_screen_test.dart`: replace/add
    tests per the `<behavior>` block, mirroring 07-01's
    `bands_screen_test.dart`/`band_detail_screen_test.dart` updates.
    `setlists_screen_test.dart`'s `wrap()` helper does not currently
    override `isOnlineProvider` — add the same default-true override
    parameter 07-02/07-03 added elsewhere, for the same reason.
    `setlist_list_screen_test.dart` and `setlist_detail_screen_test.dart`
    already override `isOnlineProvider` — no change needed there. Watch for
    the same `tester.pump()` -> `await tester.pumpAndSettle()` adjustment
    flagged in 07-01/07-02/07-03.
  </action>
  <verify>
    <automated>flutter test test/features/setlists/setlists_screen_test.dart test/features/setlists/setlist_list_screen_test.dart test/features/setlists/setlist_detail_screen_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `SetlistsScreen` refetches on a second Setlists-tab selection; shows
      the `AppBar` in-flight indicator only when loading with existing
      data; shows `OfflineNoCacheView` on `OfflineNoCacheException`.
    - `SetlistListScreen`/`SetlistDetailScreen` show `OfflineNoCacheView`
      (no Retry) on the same exception, with no tab-switch listener and no
      `AppBar.bottom` indicator present in either file.
    - `SetlistDetailScreen`'s reorder/remove-track/add-tracks behavior is
      unchanged (existing tests for those flows still pass).
    - No file under `lib/features/setlists/` imports
      `sync_status_badge.dart` anymore.
  </acceptance_criteria>
  <done>All 3 setlist screens are wired to the online-first providers from
  Task 1: SetlistsScreen refetches on tab-switch with an in-flight
  indicator, SetlistListScreen/SetlistDetailScreen need no new wiring
  (D-02), all show the offline-no-cache view, none render SyncStatusBadge;
  all listed tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Device connectivity signal -> fetch/cache policy | `isOnlineProvider` now gates whether all three setlist providers trust the network or fall back to local cache |
| Cross-band filter state -> fetch key | `selectedSetlistBandIdFilterProvider` still determines `UserSetlistsListData`'s cache/fetch key — unchanged by this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-08 | Tampering | `SetlistListData`/`SetlistDetailData` `_version` guard | medium | mitigate | Preserved verbatim across the online-first rewrite, identical mitigation to T-07-01/T-07-06 — a slow in-flight fetch cannot clobber a newer `removeFromList`/`updateFields`/`reorderTracks` local mutation |
| T-07-09 | Spoofing | `isOnlineProvider` (radio state, not reachability) | low | accept | Same accepted tradeoff as T-07-02/T-07-04/T-07-07 — D-03's silent cache fallback absorbs a wrong online read |
</threat_model>

<verification>
Run `flutter test test/providers/setlists_provider_test.dart test/features/setlists/setlists_screen_test.dart test/features/setlists/setlist_list_screen_test.dart test/features/setlists/setlist_detail_screen_test.dart` — all green.
Run `flutter analyze` on the full `lib/` tree — zero new warnings from this plan's files.
</verification>

<success_criteria>
- All three setlist providers and their three screens are fully online-first (OFFL-07) with offline-cache fallback, offline-no-cache state, and no SyncStatusBadge (OFFL-08).
- SetlistsScreen has D-01 tab-switch wiring; SetlistListScreen/SetlistDetailScreen correctly have none (D-02).
- Reorder/remove-track/add-tracks setlist flows are unaffected.
- All automated tests pass; `flutter analyze` is clean on every file this plan touches.
</success_criteria>

<output>
Create `.planning/phases/07-cache-behavior-flip-online-first/07-04-SUMMARY.md` when done
</output>
