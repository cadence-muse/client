---
phase: 07-cache-behavior-flip-online-first
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - lib/providers/homepage_provider.dart
  - lib/features/home/home_screen.dart
  - test/providers/homepage_provider_test.dart
  - test/features/home/home_screen_test.dart
  - lib/providers/profile_provider.dart
  - lib/features/profile/profile_screen.dart
  - test/providers/profile_provider_test.dart
  - test/features/profile/profile_screen_test.dart
autonomous: true
requirements: [OFFL-07, OFFL-08]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Opening the Home tab while online always shows freshly-fetched server data, and re-selecting it after it was already built triggers a fresh fetch (OFFL-07, D-01)"
    - "Opening the Profile tab while online always shows freshly-fetched server data, and re-selecting it after it was already built triggers a fresh fetch (OFFL-07, D-01)"
    - "Home and Profile tabs show cached data offline instead of a blank/error screen, and the dedicated 'No cached data' state (no Retry) when nothing was ever cached, auto-recovering on reconnect (OFFL-08, D-06, D-07)"
    - "SyncStatusBadge no longer renders on the Home or Profile tab (OFFL-08)"
  artifacts: []
  key_links:
    - "HomepageData.build()/ProfileData.build() -> ref.watch(isOnlineProvider) (online-first fetch decision, D-01/D-03)"
    - "HomeScreen/ProfileScreen.build() -> ref.listen(selectedTabIndexProvider) -> provider invalidation (D-01 tab-switch refetch, tab indices 0 and 4)"
---

<objective>
Apply the online-first pattern established by 07-01's tracer to the two
remaining simple tab providers — Homepage and Profile. Both have no
`_version` guard and no family/detail counterpart, making this the smallest
expansion slice in the phase.

Purpose: extend OFFL-07/OFFL-08 coverage to the Home and Profile tabs using
the exact `build()` rewrite shape 07-01 proved, plus the shared
`OfflineNoCacheException`/`OfflineNoCacheView` artifacts it created.

Output: `HomepageData`/`ProfileData` rewritten to online-first;
`HomeScreen`/`ProfileScreen` rewired with tab-switch refetch (D-01), in
-flight indicator (D-08/D-09), and offline-no-cache handling (D-06/D-07).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-cache-behavior-flip-online-first/07-CONTEXT.md
@.planning/phases/07-cache-behavior-flip-online-first/07-UI-SPEC.md
@.planning/phases/07-cache-behavior-flip-online-first/07-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Home tab — HomepageData + HomeScreen online-first</name>
  <files>lib/providers/homepage_provider.dart, lib/features/home/home_screen.dart, test/providers/homepage_provider_test.dart, test/features/home/home_screen_test.dart</files>
  <read_first>lib/providers/bands_provider.dart (07-01's rewritten BandsListData.build() — the canonical template), lib/features/bands/bands_screen.dart (07-01's rewritten screen — canonical template), lib/providers/homepage_provider.dart, lib/features/home/home_screen.dart, test/providers/homepage_provider_test.dart, test/features/home/home_screen_test.dart, lib/providers/offline_no_cache_exception.dart, lib/widgets/offline_no_cache_view.dart</read_first>
  <behavior>
    Mirror 07-01 Task 1's 6 provider-level cases (online+no-cache fetches
    directly; online+stale-cache still returns fresh; online+fetch-fails
    +cache falls back silently D-03; online+fetch-fails+no-cache rethrows;
    offline+cache returns cached; offline+no-cache throws
    `OfflineNoCacheException` D-06) applied to `HomepageData.build()` — note
    this provider has NO `_version` guard, so there is no version-capture
    step to preserve, just `_fetchAndCache()`/cache read directly. Plus
    2 widget cases: `HomeScreen` shows `OfflineNoCacheView` (no Retry) on
    that exception; re-selecting the Home tab (index 0) after it was already
    built triggers a second `GET /api/homepage` call (D-01).
  </behavior>
  <action>
    Rewrite `HomepageData.build()` in `lib/providers/homepage_provider.dart`
    to the online-first shape from 07-01 (watch `isOnlineProvider`, online
    branch tries `_fetchAndCache()` falling back to `cache.readHomepage()`
    on failure, offline branch reads cache or throws
    `const OfflineNoCacheException()`). This provider has no `_version`
    field — nothing to capture/compare. Delete the now-dead `_refresh()`
    private method (same `unused_element` reason as 07-01). Leave
    `_fetchAndCache()`, `refresh()`, `_doRefresh()` untouched. Add imports
    for `connectivity_provider.dart` and `offline_no_cache_exception.dart`.

    Rewire `lib/features/home/home_screen.dart`: remove the
    `sync_status_badge.dart` import and the
    `final syncedAt = ref.watch(homepageSyncedAtProvider);` line. Add
    `ref.listen<int>(selectedTabIndexProvider, (previous, current) { if
    (current == 0) ref.invalidate(homepageDataProvider); });` at the top of
    `build()` (D-01; tab index 0 = Home). Add an `AppBar.bottom`
    `PreferredSize` `LinearProgressIndicator` gated on
    `homeAsync.isLoading && homeAsync.hasValue` (D-08; existing
    `loading:` branch stays as the D-09 cold-start spinner). Simplify the
    `data:` branch to directly `_buildContent(context, data)` (drop the
    `Column([SyncStatusBadge, Expanded])` wrapper). Add the
    `OfflineNoCacheView` branch to `error:` before the existing
    `_buildError(...)` fallback. Add imports for `navigation_provider.dart`,
    `offline_no_cache_exception.dart`, `offline_no_cache_view.dart`.

    Update `test/providers/homepage_provider_test.dart` and
    `test/features/home/home_screen_test.dart` per the `<behavior>` block.
    In `home_screen_test.dart`'s `wrap()` helper, add
    `isOnlineProvider.overrideWithValue(isOnline)` with an `isOnline = true`
    default parameter (currently `wrap()` does NOT override
    `isOnlineProvider` at all, which now matters — under the old cache-first
    behavior this was harmless since `HomeScreen` never gated on
    connectivity, but under online-first `HomepageData.build()` reads
    `isOnlineProvider`, and leaving it unmocked resolves to the fail-safe
    `false` default per `connectivity_provider.dart`'s doc comment, silently
    exercising the offline branch in every existing test). Replace the
    `'SyncStatusBadge is present...'` test with the offline-no-cache view
    test and the tab-switch-refetch test; existing tests seeding cache via
    `cacheService.writeHomepage(...)` and asserting after a single
    `tester.pump()` may need `await tester.pumpAndSettle()` instead, per
    07-01's note on the same issue.
  </action>
  <verify>
    <automated>flutter test test/providers/homepage_provider_test.dart test/features/home/home_screen_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `HomepageData.build()` calls `GET /api/homepage` directly when online,
      even with a populated cache present; returns cache directly (no
      network call) when offline with cache; throws
      `OfflineNoCacheException` when offline with no cache.
    - `HomeScreen` renders `OfflineNoCacheView` (no Retry) on that
      exception; a second Home-tab selection triggers a second network call.
    - `flutter analyze lib/providers/homepage_provider.dart
      lib/features/home/home_screen.dart` reports zero new warnings.
  </acceptance_criteria>
  <done>HomepageData is online-first with D-03/D-06 fallback; HomeScreen
  refetches on tab-switch, shows the in-flight indicator and offline-no
  -cache view, and no longer renders SyncStatusBadge; all listed tests
  pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Profile tab — ProfileData + ProfileScreen online-first</name>
  <files>lib/providers/profile_provider.dart, lib/features/profile/profile_screen.dart, test/providers/profile_provider_test.dart, test/features/profile/profile_screen_test.dart</files>
  <read_first>lib/providers/bands_provider.dart (07-01's rewritten BandsListData.build() — canonical template), lib/features/bands/bands_screen.dart (07-01's rewritten screen — canonical template), lib/providers/profile_provider.dart, lib/features/profile/profile_screen.dart, test/providers/profile_provider_test.dart, test/features/profile/profile_screen_test.dart, lib/providers/offline_no_cache_exception.dart, lib/widgets/offline_no_cache_view.dart</read_first>
  <behavior>
    Identical shape to Task 1's `<behavior>` block, applied to
    `ProfileData.build()` (also no `_version` guard) and `ProfileScreen`
    (tab index 4 = Profile, per `root_scaffold.dart`'s destination order).
  </behavior>
  <action>
    Rewrite `ProfileData.build()` in `lib/providers/profile_provider.dart`
    to the identical online-first shape (watch `isOnlineProvider`, online
    branch tries `_fetchAndCache()` falling back to `cache.readProfile()` on
    failure, offline branch reads cache or throws
    `const OfflineNoCacheException()`). No `_version` field here either.
    Delete the now-dead `_refresh()` private method. Leave
    `_fetchAndCache()`, `refresh()`, `_doRefresh()` untouched. Add imports
    for `connectivity_provider.dart` and `offline_no_cache_exception.dart`.

    Rewire `lib/features/profile/profile_screen.dart`: remove the
    `sync_status_badge.dart` import and the
    `final syncedAt = ref.watch(profileSyncedAtProvider);` line. Add
    `ref.listen<int>(selectedTabIndexProvider, (previous, current) { if
    (current == 4) ref.invalidate(profileDataProvider); });` at the top of
    `build()` (D-01; tab index 4 = Profile). Add the same `AppBar.bottom`
    `LinearProgressIndicator` pattern gated on
    `profileAsync.isLoading && profileAsync.hasValue` (D-08). Simplify the
    `data:` branch to directly `_buildContent(context, ref, profile)`. Add
    the `OfflineNoCacheView` branch to `error:`. Add imports for
    `navigation_provider.dart`, `offline_no_cache_exception.dart`,
    `offline_no_cache_view.dart`.

    Update `test/providers/profile_provider_test.dart` and
    `test/features/profile/profile_screen_test.dart` per the `<behavior>`
    block. `profile_screen_test.dart`'s `wrap()` helper also does not
    currently override `isOnlineProvider` — add the same default-true
    override parameter as Task 1's `HomeScreen` fix, for the same reason.
  </action>
  <verify>
    <automated>flutter test test/providers/profile_provider_test.dart test/features/profile/profile_screen_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `ProfileData.build()` calls `GET /api/me` directly when online, even
      with a populated cache present; falls back silently to cache on a
      failed online fetch (D-03); throws `OfflineNoCacheException` when
      offline with no cache.
    - `ProfileScreen` renders `OfflineNoCacheView` (no Retry) on that
      exception; a second Profile-tab selection triggers a second network
      call.
    - `flutter analyze lib/providers/profile_provider.dart
      lib/features/profile/profile_screen.dart` reports zero new warnings.
  </acceptance_criteria>
  <done>ProfileData is online-first with D-03/D-06 fallback; ProfileScreen
  refetches on tab-switch, shows the in-flight indicator and offline-no
  -cache view, and no longer renders SyncStatusBadge; all listed tests
  pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Device connectivity signal -> fetch/cache policy | `isOnlineProvider` now gates whether `HomepageData`/`ProfileData` trust the network or fall back to local cache |
| Cached Hive data -> UI render | Previously-fetched, already-trusted JSON rendered as-is on the offline/fetch-fail path |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-04 | Spoofing | `isOnlineProvider` (radio state, not reachability) | low | accept | Same accepted tradeoff as 07-01/T-07-02 — D-03's silent cache fallback absorbs a wrong online read; no auth/session impact from `ProfileData`/`HomepageData` |
| T-07-05 | Information Disclosure | Profile data served from cache while offline | low | accept | `ProfileData` already cached profile (own-user data, previously fetched over an authenticated session) is unchanged by this phase — no new exposure surface, same Hive-local storage as before |
</threat_model>

<verification>
Run `flutter test test/providers/homepage_provider_test.dart test/providers/profile_provider_test.dart test/features/home/home_screen_test.dart test/features/profile/profile_screen_test.dart` — all green.
Run `flutter analyze` on the full `lib/` tree — zero new warnings from this plan's files.
</verification>

<success_criteria>
- Home and Profile tabs are fully online-first (OFFL-07) with offline-cache fallback, offline-no-cache state, and no SyncStatusBadge (OFFL-08).
- Both providers reuse 07-01's shared `OfflineNoCacheException`/`OfflineNoCacheView` unchanged.
- All automated tests pass; `flutter analyze` is clean on every file this plan touches.
</success_criteria>

<output>
Create `.planning/phases/07-cache-behavior-flip-online-first/07-02-SUMMARY.md` when done
</output>
