---
phase: 06.1-api-contract-catch-up-migrate-removesetlisttrack-to-removese
verified: 2026-08-21T10:30:00Z
status: passed
score: 6/6 must-haves verified
behavior_unverified: 0
overrides_applied: 0
---

# Phase 06.1: API Contract Catch-Up (RemoveSetlistTrack Batch + ListUserTracks/ListUserSetlists POST) Verification Report

**Phase Goal:** Migrate `public_api.dart`'s `removeSetlistTrack`, `listUserTracks`, and `listUserSetlists` methods to match the current `publicapi.yml` wire contract (post `fe72e78`), unblocking setlist track removal and cross-band track/setlist lists against the live API.

**Verified:** 2026-08-21T10:30:00Z  
**Status:** PASSED  
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Removing a track from a setlist succeeds by calling the batch RemoveSetlistTracks endpoint (DELETE .../tracks with a JSON trackIds array), not the removed single-track path route. | ✓ VERIFIED | `lib/api/public_api.dart` lines 364-370 send `DELETE /api/band/$bandId/setlist/$setlistId/tracks` with body `{'trackIds': [trackId]}`; schema match confirmed in `publicapi.yml` lines 547-565; unit test in `test/api/public_api_test.dart` lines 26-56 passes. |
| 2 | Listing the current user's cross-band tracks (Tracks tab / no-filter view) succeeds via POST /api/track/list, not the removed GET route. | ✓ VERIFIED | `lib/api/public_api.dart` lines 243-249 send `POST /api/track/list`; schema match confirmed in `publicapi.yml` lines 383-405; unit tests in `test/api/public_api_test.dart` lines 74-139 pass (3 tests: bandIdFilter, no-filter, searchQuery). |
| 3 | Listing the current user's cross-band setlists succeeds via POST /api/setlist/list, not the removed GET route. | ✓ VERIFIED | `lib/api/public_api.dart` lines 399-405 send `POST /api/setlist/list`; schema match confirmed in `publicapi.yml` lines 407-429; unit tests in `test/api/public_api_test.dart` lines 141-206 pass (3 tests: bandIdFilter, no-filter, searchQuery). |
| 4 | bandId filtering on both list endpoints is still sent as a query parameter (BandIdFilter), never moved into the JSON body. | ✓ VERIFIED | Both `listUserTracks` and `listUserSetlists` use `queryParameters: bandIdFilter == null ? null : {'bandId': bandIdFilter}` at correct lines; `publicapi.yml` line 629-635 confirms `BandIdFilter` is `in: query, required: false`; provider tests in `test/providers/tracks_provider_test.dart` lines 525, 556 and `test/providers/setlists_provider_test.dart` lines 860, 892 capture and assert the query parameter. |
| 5 | Existing call sites (_removeTrack in setlist_detail_screen.dart, UserTracksListData._fetchAndCache, UserSetlistsListData._fetchAndCache) require no behavior change to their public signatures — only public_api.dart's internal HTTP method/path/body changes. | ✓ VERIFIED | `setlist_detail_screen.dart` line 46-90 _removeTrack call is unchanged (`bandId`, `setlistId`, `trackId` params remain the same); `tracks_provider.dart` line 313-322 listUserTracks call is unchanged (`bandIdFilter` only); `setlists_provider.dart` line 358-369 listUserSetlists call is unchanged (`bandIdFilter` only). Widget-level regression test in `setlist_detail_screen_test.dart` lines 337-410 confirms the end-to-end flow (tap remove → batch DELETE → refresh → UI update) works correctly. |
| 6 | flutter analyze and the full flutter test suite pass with zero regressions, confirming ListBandTracks, ListBandSetlists, and AddSetlistTracks (unchanged endpoints) were not inadvertently touched. | ✓ VERIFIED | `flutter analyze` output: "No issues found! (ran in 0.9s)"; `flutter test` output: "All tests passed!" with 315/315 tests passing; unchanged endpoints (ListBandTracks GET /api/band/{bandId}/track/list, ListBandSetlists GET /api/band/{bandId}/setlist/list, AddSetlistTracks POST .../tracks) remain untouched and pass their existing tests. |

**Score:** 6/6 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `lib/api/public_api.dart` | Updated removeSetlistTrack, listUserTracks, listUserSetlists methods | ✓ VERIFIED | All three methods present and substantive; removeSetlistTrack: lines 359-371; listUserTracks: lines 239-250; listUserSetlists: lines 395-406. All methods have doc comments explaining the migration. |
| `test/api/public_api_test.dart` | New focused unit-test file with 8 tests | ✓ VERIFIED | File created; 8 tests total: 2 for removeSetlistTrack, 3 for listUserTracks, 3 for listUserSetlists; all tests pass. |
| `test/features/setlists/setlist_detail_screen_test.dart` | Corrected DELETE path and body assertion | ✓ VERIFIED | Test at lines 337-410 updated to assert DELETE path `/api/band/b1/setlist/s1/tracks` and body `{'trackIds': ['t1']}`; test passes. |
| `test/providers/tracks_provider_test.dart` | Added POST method assertion to bandIdFilter test | ✓ VERIFIED | Test at lines 504-558 captures `request.method` and asserts `everyElement('POST')`; test passes. |
| `test/providers/setlists_provider_test.dart` | Added POST method assertion to mirror bandIdFilter test | ✓ VERIFIED | Test mirrors tracks_provider_test.dart pattern; lines 857-894 capture and assert POST method; test passes. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `public_api.dart#removeSetlistTrack` | `setlist_detail_screen.dart#_removeTrack` | Single trackId wrapped in 1-element trackIds array | ✓ WIRED | Caller passes single `trackId`; method wraps it internally; widget test confirms end-to-end flow (lines 337-410 of setlist_detail_screen_test.dart). |
| `public_api.dart#listUserTracks` | `tracks_provider.dart#UserTracksListData._fetchAndCache` | bandIdFilter stays query param; optional searchQuery in body (not yet used) | ✓ WIRED | Provider test lines 504-558 of tracks_provider_test.dart confirms POST method and query param handling. |
| `public_api.dart#listUserSetlists` | `setlists_provider.dart#UserSetlistsListData._fetchAndCache` | bandIdFilter stays query param; optional searchQuery in body (not yet used) | ✓ WIRED | Provider test lines 838-894 of setlists_provider_test.dart (mirrored from tracks_provider_test.dart) confirms POST method and query param handling. |
| `ApiClient#send(method, path, {body, queryParameters, requireAuth})` | All three migrated methods | Method/path/body changes only; existing ApiClient capabilities used | ✓ WIRED | ApiClient.send already supports DELETE/POST with body and query parameters (lib/api/api_client.dart); no plumbing changes needed. |

### Wire Contract Verification (publicapi.yml Schema)

| Endpoint | Method | Path | Query Params | Request Body | Response | Status |
|----------|--------|------|--------------|--------------|----------|--------|
| RemoveSetlistTracks | DELETE | `/api/band/{bandId}/setlist/{setlistId}/tracks` | (none) | `RemoveSetlistTracksRequestBody` with `trackIds: string[]` (maxItems: 100, required) | 204 No Content | ✓ VERIFIED (lines 547-565, 1196-1206 of publicapi.yml) |
| ListUserTracks | POST | `/api/track/list` | `BandIdFilter` (name: bandId, in: query, required: false) | `ListUserTracksRequestBody` with optional `searchQuery: string` | 200 with `ListUserTracksResponseBody` (`items: UserTrackListItem[]`) | ✓ VERIFIED (lines 383-405, 970-975 of publicapi.yml) |
| ListUserSetlists | POST | `/api/setlist/list` | `BandIdFilter` (name: bandId, in: query, required: false) | `ListUserSetlistsRequestBody` with optional `searchQuery: string` | 200 with `ListUserSetlistsResponseBody` (`items: UserSetlistListItem[]`) | ✓ VERIFIED (lines 407-429, 1044-1049 of publicapi.yml) |

### Behavioral Spot-Checks

| Behavior | Test | Command | Result | Status |
|----------|------|---------|--------|--------|
| removeSetlistTrack sends correct DELETE | test/api/public_api_test.dart#removeSetlistTrack[0] | `flutter test test/api/public_api_test.dart` | "removeSetlistTrack sends DELETE to /api/band/{bandId}/setlist/{setlistId}/tracks with body {trackIds: [trackId]}" PASSED | ✓ PASS |
| removeSetlistTrack 204 response resolves | test/api/public_api_test.dart#removeSetlistTrack[1] | `flutter test test/api/public_api_test.dart` | "a 204 response resolves without throwing" PASSED | ✓ PASS |
| listUserTracks with bandIdFilter sends POST | test/api/public_api_test.dart#listUserTracks[0] | `flutter test test/api/public_api_test.dart` | "calling with bandIdFilter sends POST to /api/track/list with bandId as a query parameter" PASSED | ✓ PASS |
| listUserTracks no filters sends POST | test/api/public_api_test.dart#listUserTracks[1] | `flutter test test/api/public_api_test.dart` | "calling with no bandIdFilter and no searchQuery sends POST with no bandId query parameter" PASSED | ✓ PASS |
| listUserTracks with searchQuery sends body | test/api/public_api_test.dart#listUserTracks[2] | `flutter test test/api/public_api_test.dart` | "calling with searchQuery sends a JSON body {searchQuery: value}" PASSED | ✓ PASS |
| listUserSetlists with bandIdFilter sends POST | test/api/public_api_test.dart#listUserSetlists[0] | `flutter test test/api/public_api_test.dart` | "calling with bandIdFilter sends POST to /api/setlist/list with bandId as a query parameter" PASSED | ✓ PASS |
| listUserSetlists no filters sends POST | test/api/public_api_test.dart#listUserSetlists[1] | `flutter test test/api/public_api_test.dart` | "calling with no bandIdFilter and no searchQuery sends POST with no bandId query parameter" PASSED | ✓ PASS |
| listUserSetlists with searchQuery sends body | test/api/public_api_test.dart#listUserSetlists[2] | `flutter test test/api/public_api_test.dart` | "calling with searchQuery sends a JSON body {searchQuery: value}" PASSED | ✓ PASS |
| Widget-level setlist track removal | test/features/setlists/setlist_detail_screen_test.dart | `flutter test test/features/setlists/setlist_detail_screen_test.dart` | "tapping a row's remove icon calls removeSetlistTrack with that trackId and refreshes via a second getSetlist call" PASSED | ✓ PASS |
| Provider bandIdFilter with POST method | test/providers/tracks_provider_test.dart | `flutter test test/providers/tracks_provider_test.dart` | "changing selectedBandIdFilterProvider triggers a rebuild whose listUserTracks call receives the new bandIdFilter" + method assertion PASSED | ✓ PASS |
| Provider setlist bandIdFilter with POST | test/providers/setlists_provider_test.dart | `flutter test test/providers/setlists_provider_test.dart` | "changing selectedSetlistBandIdFilterProvider triggers a rebuild whose listUserSetlists call receives the new bandIdFilter" + method assertion PASSED | ✓ PASS |

### Full Test Suite & Static Analysis

| Check | Result | Status |
|-------|--------|--------|
| `flutter analyze` | No issues found! (ran in 0.9s) | ✓ PASSED |
| `flutter test` (full suite) | All tests passed! (315/315) | ✓ PASSED |
| Test files modified | 4 files (1 created, 4 modified) | ✓ VERIFIED |
| No regressions | All unchanged tests still passing | ✓ VERIFIED |

### Requirements Coverage

Phase 06.1 declares `requirements: []` (frontmatter line 14 of PLAN) — intentional, as this is an inserted urgent catch-up phase with no formal REQUIREMENTS.md IDs. No requirement traceability checks required.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| N/A | N/A | No TBD/FIXME/XXX/TODO debt markers found in modified files | N/A | N/A |
| N/A | N/A | No hardcoded empty data or placeholder implementations | N/A | N/A |
| N/A | N/A | All methods substantive with real HTTP calls | N/A | N/A |

## Summary

**All must-haves verified.** All 6 observable truths confirmed against the actual codebase, all artifacts substantive and wired correctly, all tests passing (315/315), zero regressions, and the three migrated methods now match the current `publicapi.yml` wire contract post-`fe72e78`. Existing call sites require zero behavior change; only `public_api.dart`'s internal HTTP method/path/body changes were needed. Phase goal achieved.

---

_Verified: 2026-08-21T10:30:00Z_  
_Verifier: Claude (gsd-verifier)_
