---
phase: 13-string-extraction-screen-localization
plan: 05
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - lib/features/home/home_screen.dart
  - test/features/home/home_screen_test.dart
  - lib/features/home/band_picker_sheet.dart
  - test/features/home/band_picker_sheet_test.dart
  - test/locale_live_switch_test.dart
autonomous: true
requirements: [I18N-04]

estimate:
  tokens: 60000
  raw_tokens: 40000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Switching to Russian replaces every visible string on the Home tab and the shared band-picker bottom sheet — no hardcoded English remains in either"
    - "home_screen_test.dart and band_picker_sheet_test.dart assert against tester.strings.* instead of hardcoded English literals"
    - "An IndexedStack tab (Home) not visible at the moment of a language switch renders fully localized text — not just a locale code — once the user navigates back to it"
  artifacts:
    - lib/features/home/home_screen.dart
    - lib/features/home/band_picker_sheet.dart
  key_links:
    - "tester.strings (from 13-01's test/test_strings.dart) -> home_screen_test.dart, band_picker_sheet_test.dart, locale_live_switch_test.dart"
  prohibitions: []
---

<objective>
Localize the Home tab (`home_screen.dart`) and the shared "Add Song"/"Add Setlist" band-picker bottom sheet (`band_picker_sheet.dart`), migrate their tests, then strengthen `test/locale_live_switch_test.dart`'s existing I18N-02 criterion-5 test to assert actual rendered Home-tab text (not just `Localizations.localeOf(...)`) — closing the gap RESEARCH.md's Validation Architecture section flags.

Purpose: Home is one of the 5 `IndexedStack`-kept-alive bottom-nav tabs (ROADMAP success criterion 4) — it is the specific tab Phase 12's existing test already targets for the "kept-alive tab picks up the new locale" check, so this plan is the natural place to prove that check against real rendered copy instead of just the ambient locale value.
Output: Home tab and band-picker sheet fully localized; locale_live_switch_test.dart proves cross-tab locale propagation against real text.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-string-extraction-screen-localization/13-CONTEXT.md
@.planning/phases/13-string-extraction-screen-localization/13-RESEARCH.md
@.planning/phases/13-string-extraction-screen-localization/13-01-PLAN.md
</context>

<artifacts_produced>
## Artifacts this plan adds to app_en.arb / app_ru.arb (landed by 13-01 Task 2, consumed here)

- homeAppBarTitle -> "Home" -> "Главная" (identical value to `navHome` — kept as a separate key since it's the screen's own AppBar title, a distinct semantic role from the nav-bar label, even though text matches)
- homeWelcomeMessage({username}) -> "Welcome, {username}" -> "Добро пожаловать, {username}"
- homeQuickActionsHeader -> "Quick Actions" -> "Быстрые действия"
- homeAddBandButton -> "Add Band" -> "Добавить группу"
- homeAddSongButton -> "Add Song" -> "Добавить песню"
- homeAddSetlistButton -> "Add Setlist" -> "Добавить сетлист"
- homeErrorTitle -> "Couldn't load home" -> "Не удалось загрузить главную страницу"
- bandPickerErrorMessage -> "Could not load bands. Please try again." -> "Не удалось загрузить группы. Попробуйте ещё раз."

No task in this plan touches the ARB files.
</artifacts_produced>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: home_screen.dart + test</name>
  <files>lib/features/home/home_screen.dart, test/features/home/home_screen_test.dart</files>
  <read_first>
    lib/features/home/home_screen.dart
    test/features/home/home_screen_test.dart
    lib/l10n/app_en.arb
  </read_first>
  <action>
    Add `import '../../generated/app_localizations.dart';`. In `build()`, add `final l10n = AppLocalizations.of(context)!;` and pass it (or re-derive via `AppLocalizations.of(context)!`) into `_buildContent`/`_buildError` since they receive their own `context`. Replace `const Text('Home')` with `Text(l10n.homeAppBarTitle)`. Replace `tooltip: 'Refresh'` with `tooltip: l10n.commonRefresh`. Replace `'Welcome, $username'` with `l10n.homeWelcomeMessage(username)`. Replace `'Quick Actions'` with `l10n.homeQuickActionsHeader`. Replace `const Text('Add Band')` with `Text(l10n.homeAddBandButton)`. Replace `const Text('Add Song')` with `Text(l10n.homeAddSongButton)`. Replace `const Text('Add Setlist')` with `Text(l10n.homeAddSetlistButton)`. Replace `_buildError`'s `"Couldn't load home"`/`'Please check your connection and try again.'`/`'Retry'` with `l10n.homeErrorTitle`/`l10n.commonConnectionError`/`l10n.commonRetry`.

    Migrate the test file: add `import '../../test_strings.dart';`, replace every app-copy `find.text('...')` with `tester.strings.keyName` (composing `homeWelcomeMessage(username)` with the same `username` test-fixture value). Test fixtures (usernames) stay hardcoded.
  </action>
  <verify>
    <automated>flutter test test/features/home/home_screen_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "l10n\." lib/features/home/home_screen.dart` >= 8
    - `grep -c "tester.strings" test/features/home/home_screen_test.dart` >= 1
    - `flutter test test/features/home/home_screen_test.dart` passes
  </acceptance_criteria>
  <done>home_screen.dart and its test are fully localized; flutter test passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: band_picker_sheet.dart + test</name>
  <files>lib/features/home/band_picker_sheet.dart, test/features/home/band_picker_sheet_test.dart</files>
  <read_first>
    lib/features/home/band_picker_sheet.dart
    test/features/home/band_picker_sheet_test.dart
    lib/l10n/app_en.arb
  </read_first>
  <action>
    Add `import '../../generated/app_localizations.dart';`. `showBandPickerSheet`'s inner `Consumer` builder receives its own `context` — add `final l10n = AppLocalizations.of(context)!;` there. Replace `const Text('Could not load bands. Please try again.')` with `Text(l10n.bandPickerErrorMessage)`. The `ListTile` titles showing `band['name'] as String` are data, not UI copy — leave unchanged.

    Migrate the test file: add `import '../../test_strings.dart';`, replace the error-message `find.text('...')` assertion with `tester.strings.bandPickerErrorMessage`. Band-name test fixtures stay hardcoded.
  </action>
  <verify>
    <automated>flutter test test/features/home/band_picker_sheet_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "l10n.bandPickerErrorMessage" lib/features/home/band_picker_sheet.dart` >= 1
    - `flutter test test/features/home/band_picker_sheet_test.dart` passes
  </acceptance_criteria>
  <done>band_picker_sheet.dart and its test are fully localized; flutter test passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Strengthen locale_live_switch_test.dart's I18N-02 criterion-5 check</name>
  <files>test/locale_live_switch_test.dart</files>
  <read_first>
    test/locale_live_switch_test.dart
    lib/features/home/home_screen.dart
  </read_first>
  <action>
    This task runs after Task 1 lands `l10n.homeAppBarTitle` in `home_screen.dart`, so it can now assert against real rendered Home-tab text. Add `import 'test_strings.dart';` (same-directory import, this file lives at `test/locale_live_switch_test.dart`). In the `testWidgets('I18N-02 success criterion 5: ...')` block, before the language switch, add an assertion that Home renders `tester.strings.homeAppBarTitle` (the English AppBar title) while still on the Home tab (right after the initial `pumpAndSettle()`, before `goToSettings(tester)` navigates away). After switching to Russian and navigating back to Home (the existing `find.descendant(of: find.byType(NavigationBar), matching: find.text('Home'))` tap needs to become `find.text(tester.strings.navHome)` too, since the nav-bar label itself is now localized — but `tester.strings` at that point in the test still reads off whichever locale is CURRENTLY active before the tap navigates, so keep using `tester.strings.navHome` evaluated fresh right before that tap, not a value captured earlier), replace the existing final assertion (`Localizations.localeOf(tester.element(find.byType(HomeScreen))).languageCode == 'ru'`) with an ADDED assertion (keep the locale-code check too, as a cheap sanity check) that `find.text(tester.strings.homeAppBarTitle)` finds the Home AppBar title rendered in Russian ("Главная") — proving the previously-inactive Home tab actually re-rendered its text content, not just that the ambient `Localizations` value changed.
  </action>
  <verify>
    <automated>flutter test test/locale_live_switch_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "tester.strings.homeAppBarTitle" test/locale_live_switch_test.dart` >= 1
    - `flutter test test/locale_live_switch_test.dart` passes
  </acceptance_criteria>
  <done>locale_live_switch_test.dart's I18N-02 criterion-5 test asserts real rendered Home-tab text before and after the switch, not just the ambient Localizations locale code.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | Home tab and band-picker sheet render already-approved ARB strings interpolated with the authenticated user's own `username` (already-trusted display data). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|--------------|------------------|
| T-13-07 | Information Disclosure | `homeWelcomeMessage({username})` interpolation | low | accept | `username` is the authenticated user's own account name, already displayed pre-phase; no new data surfaced. |

No package installs in this plan; package-legitimacy gate not applicable.
</threat_model>

<verification>
- `flutter test test/features/home/home_screen_test.dart test/features/home/band_picker_sheet_test.dart test/locale_live_switch_test.dart` — all pass
- `flutter analyze lib/features/home/home_screen.dart lib/features/home/band_picker_sheet.dart` — no new errors
</verification>

<success_criteria>
Home tab and band-picker sheet fully localized; locale_live_switch_test.dart proves the IndexedStack-kept-alive-tab locale propagation against real rendered text, not just the ambient locale code.
</success_criteria>

<output>
Create `.planning/phases/13-string-extraction-screen-localization/13-05-SUMMARY.md` when done
</output>
