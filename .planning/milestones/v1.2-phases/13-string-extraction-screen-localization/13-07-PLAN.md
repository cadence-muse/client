---
phase: 13-string-extraction-screen-localization
plan: 07
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - lib/navigation/root_scaffold.dart
  - lib/widgets/offline_banner.dart
  - test/widgets/offline_banner_test.dart
  - test/offline_cross_tab_test.dart
autonomous: true
requirements: [I18N-04]

estimate:
  tokens: 55000
  raw_tokens: 37000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Switching to Russian replaces all 5 bottom-navigation labels (Home/Bands/Tracks/Setlists/Profile) — this is an always-visible surface regardless of which tab is active, per D-13"
    - "Switching to Russian replaces the offline banner text shown above the IndexedStack on every tab when offline with cached data, per D-15"
    - "offline_banner_test.dart and offline_cross_tab_test.dart assert against tester.strings.* instead of hardcoded English literals"
  artifacts:
    - lib/navigation/root_scaffold.dart
    - lib/widgets/offline_banner.dart
  key_links:
    - "tester.strings (from 13-01's test/test_strings.dart) -> offline_banner_test.dart, offline_cross_tab_test.dart"
  prohibitions: []
---

<!-- planner-discipline-allow: const bannerText -->

<objective>
Localize `root_scaffold.dart`'s 5 `NavigationDestination` labels (D-13) and `offline_banner.dart`'s single message (D-15), then migrate `test/widgets/offline_banner_test.dart` and `test/offline_cross_tab_test.dart` off their hardcoded English literals — both of which assert against this plan's exact strings.

Purpose: These are the two most-widely-visible shared surfaces in the app (nav bar is on every screen; the offline banner sits above every tab's `IndexedStack` content whenever offline with cached data) — CONTEXT.md D-13/D-15 explicitly call these in scope even though ROADMAP's success-criteria list names only the 5 tab screens, since leaving them hardcoded would visibly contradict "no screen retains hardcoded English."
Output: root_scaffold.dart and offline_banner.dart fully localized; both cross-cutting tests updated to match.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-string-extraction-screen-localization/13-CONTEXT.md
@.planning/phases/13-string-extraction-screen-localization/13-01-PLAN.md
</context>

<artifacts_produced>
## Artifacts this plan adds to app_en.arb / app_ru.arb (landed by 13-01 Task 2, consumed here)

- navHome / navBands / navTracks / navSetlists / navProfile -> "Home"/"Bands"/"Tracks"/"Setlists"/"Profile" -> "Главная"/"Группы"/"Треки"/"Сетлисты"/"Профиль"
- offlineBannerMessage -> "Showing cached data — may be out of date" -> "Показаны сохранённые данные — они могут быть устаревшими"

No task in this plan touches the ARB files. (`navHome`..`navProfile` are already reused by `band_detail_screen.dart` in 13-01 Task 3 for its "Tracks"/"Setlists" ListTile labels — this plan is what actually produces the nav-bar rendering of those same keys.)
</artifacts_produced>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: root_scaffold.dart — 5 nav labels</name>
  <files>lib/navigation/root_scaffold.dart</files>
  <read_first>
    lib/navigation/root_scaffold.dart
    lib/l10n/app_en.arb
  </read_first>
  <action>
    Add `import '../generated/app_localizations.dart';`. `root_scaffold.dart` currently builds its `NavigationBar`'s `destinations` list as a `const [...]` literal — since the labels must now come from `AppLocalizations.of(context)!`, the list can no longer be `const`. Change `destinations: const [` to `destinations: [` and drop `const` from each individual `NavigationDestination(` entry (the `Icon(...)` children can stay `const`). Add `final l10n = AppLocalizations.of(context)!;` at the top of `build()`. Replace `label: 'Home'` -> `label: l10n.navHome`, `label: 'Bands'` -> `label: l10n.navBands`, `label: 'Tracks'` -> `label: l10n.navTracks`, `label: 'Setlists'` -> `label: l10n.navSetlists`, `label: 'Profile'` -> `label: l10n.navProfile`, preserving the existing icon/order.
  </action>
  <verify>
    <automated>flutter analyze lib/navigation/root_scaffold.dart</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "l10n.nav" lib/navigation/root_scaffold.dart` == 5
    - `flutter analyze lib/navigation/root_scaffold.dart` reports no new errors
  </acceptance_criteria>
  <done>All 5 NavigationDestination labels render via AppLocalizations.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: offline_banner.dart + test</name>
  <files>lib/widgets/offline_banner.dart, test/widgets/offline_banner_test.dart</files>
  <read_first>
    lib/widgets/offline_banner.dart
    test/widgets/offline_banner_test.dart
    lib/l10n/app_en.arb
  </read_first>
  <action>
    Add `import '../generated/app_localizations.dart';`. In `build()`, after the `if (isOnline) return const SizedBox.shrink();` early return, add `final l10n = AppLocalizations.of(context)!;`. Replace `'Showing cached data — may be out of date'` with `l10n.offlineBannerMessage`.

    Migrate the test file: add `import '../test_strings.dart';`, replace both `find.text('Showing cached data — may be out of date')` assertions with `find.text(tester.strings.offlineBannerMessage)`.
  </action>
  <verify>
    <automated>flutter test test/widgets/offline_banner_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "l10n.offlineBannerMessage" lib/widgets/offline_banner.dart` >= 1
    - `grep -c "tester.strings.offlineBannerMessage" test/widgets/offline_banner_test.dart` >= 2
    - `flutter test test/widgets/offline_banner_test.dart` passes
  </acceptance_criteria>
  <done>offline_banner.dart and its test are fully localized; flutter test passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: test/offline_cross_tab_test.dart — migrate nav-label and banner-text constants</name>
  <files>test/offline_cross_tab_test.dart</files>
  <read_first>
    test/offline_cross_tab_test.dart
  </read_first>
  <action>
    This file pumps the full `RootScaffold` and navigates across tabs while asserting on the nav-bar labels and the offline-banner text — both now sourced from Task 1/Task 2's ARB keys. Add `import 'test_strings.dart';`. The file currently declares two top-level `const` values inside `main()`: `const bannerText = 'Showing cached data — may be out of date';` and `const tabLabels = ['Home', 'Bands', 'Tracks', 'Setlists', 'Profile'];`. These can no longer be `const` `String`/`List<String>` literals since their values now depend on `tester.strings` (only available after a widget is pumped) — replace both declarations with local functions/getters computed at point of use instead: wherever `bannerText` is referenced, substitute `tester.strings.offlineBannerMessage`; wherever `tabLabels` is referenced (e.g. iterated to tap each nav destination or assert all 5 are present), substitute a list built at that point in the test: `[tester.strings.navHome, tester.strings.navBands, tester.strings.navTracks, tester.strings.navSetlists, tester.strings.navProfile]`. Remove the now-unused top-level `const` declarations. Test-fixture literals (band name `'B.A.T.H.'`, usernames) stay hardcoded.
  </action>
  <verify>
    <automated>flutter test test/offline_cross_tab_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "tester.strings" test/offline_cross_tab_test.dart` >= 1
    - `grep -c "const bannerText" test/offline_cross_tab_test.dart` == 0
    - `flutter test test/offline_cross_tab_test.dart` passes
  </acceptance_criteria>
  <done>offline_cross_tab_test.dart asserts against tester.strings for both nav labels and banner text; flutter test passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | Nav labels and offline-banner text are static app chrome; no user-controlled input reaches either. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|--------------|------------------|
| T-13-09 | Denial of Service (UI) | Longer Russian nav labels ("Сетлисты", "Профиль") in the fixed-width `NavigationBar` | low | accept | `NavigationDestination` labels already clip/ellipsize by Material's `NavigationBar` at narrow widths; no functional break, at most a visual clip already exercised by existing longer English labels like "Setlists". |

No package installs in this plan; package-legitimacy gate not applicable.
</threat_model>

<verification>
- `flutter test test/widgets/offline_banner_test.dart test/offline_cross_tab_test.dart` — both pass
- `flutter analyze lib/navigation/root_scaffold.dart lib/widgets/offline_banner.dart` — no new errors
</verification>

<success_criteria>
Bottom nav bar and offline banner — the two most-visible shared chrome surfaces — are fully localized; both cross-cutting tests that assert on them pass against the localized text.
</success_criteria>

<output>
Create `.planning/phases/13-string-extraction-screen-localization/13-07-SUMMARY.md` when done
</output>
