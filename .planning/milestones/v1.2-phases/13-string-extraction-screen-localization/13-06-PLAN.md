---
phase: 13-string-extraction-screen-localization
plan: 06
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - lib/features/profile/profile_screen.dart
  - test/features/profile/profile_screen_test.dart
  - lib/features/profile/change_password_screen.dart
  - test/features/profile/change_password_screen_test.dart
autonomous: true
requirements: [I18N-04]

estimate:
  tokens: 55000
  raw_tokens: 37000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Switching to Russian replaces every visible string on the Profile tab and the change-password screen — no hardcoded English remains in either"
    - "profile_screen_test.dart and change_password_screen_test.dart assert against tester.strings.* instead of hardcoded English literals"
  artifacts:
    - lib/features/profile/profile_screen.dart
    - lib/features/profile/change_password_screen.dart
  key_links:
    - "tester.strings (from 13-01's test/test_strings.dart) -> both migrated test files"
  prohibitions: []
---

<objective>
Localize the Profile tab (`profile_screen.dart`, one of the 5 `IndexedStack`-kept-alive bottom-nav tabs) and the change-password screen (`change_password_screen.dart`), and migrate their tests.

Note: this phase explicitly does NOT touch `lib/features/settings/settings_screen.dart` — it was fully localized in Phase 12 and is out of this phase's scope per 13-CONTEXT.md's domain boundary. `profile_screen.dart`'s own "Settings" ListTile label (the nav entry pointing INTO that already-localized screen) IS in scope — it is profile_screen's own copy, distinct from settings_screen's internal strings.

Purpose: Profile is the last of the 5 always-visible bottom-nav tabs not yet covered — completing it closes out ROADMAP success criterion 1's "Home, Songs/Tracks, Bands, Setlists, and Profile" list for this tab (Songs/Tracks and Setlists land in later plans in this same wave).
Output: Both screens and their tests localized end-to-end.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-string-extraction-screen-localization/13-CONTEXT.md
@.planning/phases/13-string-extraction-screen-localization/13-01-PLAN.md
</context>

<artifacts_produced>
## Artifacts this plan adds to app_en.arb / app_ru.arb (landed by 13-01 Task 2, consumed here)

- profileAppBarTitle -> "Profile" -> "Профиль" (identical value to `navProfile` — kept separate per the same rationale as `homeAppBarTitle` in 13-05)
- profileIdLabel -> "ID" -> "ID" (kept untranslated — a technical field label conventionally left in Latin script even in Russian UIs)
- profileSettingsLabel -> "Settings" -> "Настройки"
- profileChangePasswordLabel -> "Change password" -> "Сменить пароль"
- profileLogOutLabel -> "Log out" -> "Выйти"
- profileErrorTitle -> "Couldn't load profile" -> "Не удалось загрузить профиль"
- changePasswordAppBarTitle -> "Change password" -> "Смена пароля"
- changePasswordSuccessSnackbar -> "Password changed successfully" -> "Пароль успешно изменён"
- changePasswordIncorrectCurrentError -> "Current password is incorrect" -> "Неверный текущий пароль"
- changePasswordCurrentLabel -> "Current password" -> "Текущий пароль"
- changePasswordNewLabel -> "New password" -> "Новый пароль"
- changePasswordConfirmLabel -> "Confirm new password" -> "Подтвердите новый пароль"
- changePasswordMismatchError -> "Passwords don't match" -> "Пароли не совпадают"
- changePasswordSubmitButton -> "Change password" -> "Сменить пароль"

No task in this plan touches the ARB files.
</artifacts_produced>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: profile_screen.dart + test</name>
  <files>lib/features/profile/profile_screen.dart, test/features/profile/profile_screen_test.dart</files>
  <read_first>
    lib/features/profile/profile_screen.dart
    test/features/profile/profile_screen_test.dart
    lib/l10n/app_en.arb
  </read_first>
  <action>
    Add `import '../../generated/app_localizations.dart';`. In `build()`, add `final l10n = AppLocalizations.of(context)!;`, and re-derive `l10n` in `_buildContent`/`_buildError` from their own `context` parameters. Replace `const Text('Profile')` with `Text(l10n.profileAppBarTitle)`. Replace `tooltip: 'Refresh'` with `tooltip: l10n.commonRefresh`. Replace `const Text('ID')` with `Text(l10n.profileIdLabel)`. Replace `const Text('Settings')` with `Text(l10n.profileSettingsLabel)`. Replace `const Text('Change password')` with `Text(l10n.profileChangePasswordLabel)`. Replace `const Text('Log out')` with `Text(l10n.profileLogOutLabel)`. Replace `_buildError`'s `"Couldn't load profile"`/`'Please check your connection and try again.'`/`'Retry'` with `l10n.profileErrorTitle`/`l10n.commonConnectionError`/`l10n.commonRetry`.

    Migrate the test file: add `import '../../test_strings.dart';`, replace every app-copy `find.text('...')` with `tester.strings.keyName`. `id`/`username` test fixtures stay hardcoded.
  </action>
  <verify>
    <automated>flutter test test/features/profile/profile_screen_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "l10n\." lib/features/profile/profile_screen.dart` >= 8
    - `grep -c "tester.strings" test/features/profile/profile_screen_test.dart` >= 1
    - `flutter test test/features/profile/profile_screen_test.dart` passes
  </acceptance_criteria>
  <done>profile_screen.dart and its test are fully localized; flutter test passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: change_password_screen.dart + test</name>
  <files>lib/features/profile/change_password_screen.dart, test/features/profile/change_password_screen_test.dart</files>
  <read_first>
    lib/features/profile/change_password_screen.dart
    test/features/profile/change_password_screen_test.dart
    lib/l10n/app_en.arb
  </read_first>
  <action>
    Add `import '../../generated/app_localizations.dart';`. In `build()`, add `final l10n = AppLocalizations.of(context)!;`. Replace `AppBar(title: const Text('Change password'))` with `AppBar(title: Text(l10n.changePasswordAppBarTitle))`. Replace `labelText: 'Current password'` with `labelText: l10n.changePasswordCurrentLabel`. Replace `labelText: 'New password'` with `labelText: l10n.changePasswordNewLabel`. Replace `labelText: 'Confirm new password'` with `labelText: l10n.changePasswordConfirmLabel`. Replace all three `'This field is required'` validator branches with `l10n.commonFieldRequired`. Replace `'At least 8 characters'` with `l10n.commonAtLeast8Chars`. Replace `"Passwords don't match"` with `l10n.changePasswordMismatchError`. Replace `const Text('Change password')` (submit button) with `Text(l10n.changePasswordSubmitButton)`. In `_submit()`, capture `l10n` via `AppLocalizations.of(context)!` and replace `const SnackBar(content: Text('Password changed successfully'))` with `SnackBar(content: Text(l10n.changePasswordSuccessSnackbar))`, and `'Current password is incorrect'` (the 400/`invalid_input` branch) with `l10n.changePasswordIncorrectCurrentError`.

    Migrate the test file: add `import '../../test_strings.dart';`, replace every app-copy `find.text('...')`/`enterText`-adjacent validator assertion with `tester.strings.keyName`. Test-fixture password strings stay hardcoded.
  </action>
  <verify>
    <automated>flutter test test/features/profile/change_password_screen_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "l10n\." lib/features/profile/change_password_screen.dart` >= 9
    - `grep -c "tester.strings" test/features/profile/change_password_screen_test.dart` >= 1
    - `flutter test test/features/profile/change_password_screen_test.dart` passes
  </acceptance_criteria>
  <done>change_password_screen.dart and its test are fully localized; flutter test passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | Profile/change-password screens render already-approved ARB strings; password values themselves are never interpolated into any ARB string (validators reference field state, not value content). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|--------------|------------------|
| T-13-08 | Information Disclosure | Change-password error copy | low | accept | `changePasswordIncorrectCurrentError` is a generic message with no account-enumeration detail — identical disclosure surface to the pre-phase English string, just now localized. |

No package installs in this plan; package-legitimacy gate not applicable.
</threat_model>

<verification>
- `flutter test test/features/profile/profile_screen_test.dart test/features/profile/change_password_screen_test.dart` — both pass
- `flutter analyze lib/features/profile/profile_screen.dart lib/features/profile/change_password_screen.dart` — no new errors
</verification>

<success_criteria>
Profile tab and change-password screen fully localized; the last of the 5 bottom-nav tabs (Profile) is now complete for this plan's scope.
</success_criteria>

<output>
Create `.planning/phases/13-string-extraction-screen-localization/13-06-SUMMARY.md` when done
</output>
