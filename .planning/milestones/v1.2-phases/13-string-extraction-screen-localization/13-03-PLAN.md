---
phase: 13-string-extraction-screen-localization
plan: 03
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - lib/features/bands/confirm_rotate_invite_code_dialog.dart
  - test/features/bands/confirm_rotate_invite_code_dialog_test.dart
  - lib/features/bands/confirm_transfer_ownership_dialog.dart
  - test/features/bands/confirm_transfer_ownership_dialog_test.dart
autonomous: true
requirements: [I18N-04]

estimate:
  tokens: 40000
  raw_tokens: 27000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Switching to Russian replaces every visible string in the rotate-invite-code and transfer-ownership dialogs — no hardcoded English remains in either"
    - "confirm_rotate_invite_code_dialog_test.dart and confirm_transfer_ownership_dialog_test.dart assert against tester.strings.* instead of hardcoded English literals"
  artifacts:
    - lib/features/bands/confirm_rotate_invite_code_dialog.dart
    - lib/features/bands/confirm_transfer_ownership_dialog.dart
  key_links:
    - "tester.strings (from 13-01's test/test_strings.dart) -> both migrated test files"
  prohibitions:
    - { statement: "Semantically distinct messages must not be silently conflated under one shared/deduplicated ARB key such that users lose meaningful distinction between different error/status conditions.", status: unverified, verification: manual }
---

<objective>
Localize the two band-owner-privileged confirm dialogs (BAND-11 rotate invite code, BAND-12 transfer ownership) and migrate their existing widget tests off hardcoded English literals.

Purpose: Both dialogs are owner-only, highest-consequence mutations (invite code invalidation, ownership handoff) — accurate Russian copy here matters more than most, since a mistranslation could cause an owner to misunderstand an irreversible action.
Output: Both dialogs and their tests localized end-to-end.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-string-extraction-screen-localization/13-CONTEXT.md
@.planning/phases/13-string-extraction-screen-localization/13-01-PLAN.md
</context>

<artifacts_produced>
## Artifacts this plan adds to app_en.arb / app_ru.arb (landed by 13-01 Task 2, consumed here)

- confirmRotateInviteCodeTitle -> "Rotate invite code?" -> "Сменить код приглашения?"
- confirmRotateInviteCodeBody -> "The current invite code will stop working immediately. Any member who hasn't joined yet will need the new code." -> "Текущий код приглашения перестанет действовать немедленно. Участникам, которые ещё не присоединились, понадобится новый код."
- confirmRotateInviteCodeSnackbar -> "Invite code rotated" -> "Код приглашения изменён"
- confirmTransferOwnershipTitle({memberUsername}) -> "Transfer ownership to {memberUsername}?" -> "Передать права владельца {memberUsername}?"
- confirmTransferOwnershipBody({memberUsername},{bandName}) -> "{memberUsername} will become the owner of this band.\n\nYou will no longer be the owner of {bandName}." -> "{memberUsername} станет владельцем этой группы.\n\nВы больше не будете владельцем группы «{bandName}»."
- confirmTransferOwnershipButton -> "Transfer" -> "Передать"
- confirmTransferOwnershipSnackbar -> "Ownership transferred" -> "Права владельца переданы"

No task in this plan touches the ARB files.
</artifacts_produced>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: confirm_rotate_invite_code_dialog.dart + test</name>
  <files>lib/features/bands/confirm_rotate_invite_code_dialog.dart, test/features/bands/confirm_rotate_invite_code_dialog_test.dart</files>
  <read_first>
    lib/features/bands/confirm_rotate_invite_code_dialog.dart
    test/features/bands/confirm_rotate_invite_code_dialog_test.dart
    lib/l10n/app_en.arb
  </read_first>
  <action>
    Add `import '../../generated/app_localizations.dart';`. In `build()`, add `final l10n = AppLocalizations.of(context)!;`. Replace `const Text('Rotate invite code?')` with `Text(l10n.confirmRotateInviteCodeTitle)`. Replace the two-line `const Text("The current invite code will stop working immediately. Any member who hasn't joined yet will need the new code.")` with `Text(l10n.confirmRotateInviteCodeBody)`. Replace `const Text('Cancel')` with `Text(l10n.commonCancel)`. Replace `message: isOnline ? '' : 'Requires connection'` with `isOnline ? '' : l10n.commonRequiresConnection`. Replace `Text(isOnline ? 'Rotate' : 'Requires connection')` with `Text(isOnline ? l10n.commonRotate : l10n.commonRequiresConnection)`. In `_rotate()`, capture `l10n` via `AppLocalizations.of(context)!` and replace the SnackBar `const SnackBar(content: Text('Invite code rotated'))` with `SnackBar(content: Text(l10n.confirmRotateInviteCodeSnackbar))` and `_errorMessage = 'Something went wrong. Please try again.'` with `_errorMessage = l10n.commonSomethingWentWrong`.

    Migrate the test file: add `import '../../test_strings.dart';`, replace every app-copy `find.text('...')`/button-text assertion with `tester.strings.keyName`. Test fixtures (`bandName`, mock invite codes) stay hardcoded.
  </action>
  <verify>
    <automated>flutter test test/features/bands/confirm_rotate_invite_code_dialog_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "l10n\." lib/features/bands/confirm_rotate_invite_code_dialog.dart` >= 6
    - `grep -c "tester.strings" test/features/bands/confirm_rotate_invite_code_dialog_test.dart` >= 1
    - `flutter test test/features/bands/confirm_rotate_invite_code_dialog_test.dart` passes
  </acceptance_criteria>
  <done>confirm_rotate_invite_code_dialog.dart and its test are fully localized; flutter test passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: confirm_transfer_ownership_dialog.dart + test</name>
  <files>lib/features/bands/confirm_transfer_ownership_dialog.dart, test/features/bands/confirm_transfer_ownership_dialog_test.dart</files>
  <read_first>
    lib/features/bands/confirm_transfer_ownership_dialog.dart
    test/features/bands/confirm_transfer_ownership_dialog_test.dart
    lib/l10n/app_en.arb
  </read_first>
  <action>
    Add `import '../../generated/app_localizations.dart';`. In `build()`, add `final l10n = AppLocalizations.of(context)!;`. Replace `Text('Transfer ownership to ${widget.memberUsername}?')` with `Text(l10n.confirmTransferOwnershipTitle(widget.memberUsername))`. Replace the interpolated body `'${widget.memberUsername} will become the owner of this band.\n\nYou will no longer be the owner of ${widget.bandName}.'` with `l10n.confirmTransferOwnershipBody(widget.memberUsername, widget.bandName)` — confirm the generated method signature takes both placeholders in that declared order (matches `@confirmTransferOwnershipBody`'s placeholder declaration order from 13-01 Task 2; if build_runner generated a different parameter order, match that order instead). Replace `const Text('Cancel')` with `Text(l10n.commonCancel)`. Replace `message: isOnline ? '' : 'Requires connection'` with `isOnline ? '' : l10n.commonRequiresConnection`. Replace `Text(isOnline ? 'Transfer' : 'Requires connection')` with `Text(isOnline ? l10n.confirmTransferOwnershipButton : l10n.commonRequiresConnection)`. In `_transfer()`, capture `l10n` and replace `const SnackBar(content: Text('Ownership transferred'))` with `SnackBar(content: Text(l10n.confirmTransferOwnershipSnackbar))` and `_errorMessage = 'Something went wrong. Please try again.'` with `_errorMessage = l10n.commonSomethingWentWrong`.

    Migrate the test file: add `import '../../test_strings.dart';`, replace every app-copy `find.text('...')` with `tester.strings.keyName(...)`. Test fixtures stay hardcoded.
  </action>
  <verify>
    <automated>flutter test test/features/bands/confirm_transfer_ownership_dialog_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "l10n\." lib/features/bands/confirm_transfer_ownership_dialog.dart` >= 7
    - `grep -c "tester.strings" test/features/bands/confirm_transfer_ownership_dialog_test.dart` >= 1
    - `flutter test test/features/bands/confirm_transfer_ownership_dialog_test.dart` passes
  </acceptance_criteria>
  <done>confirm_transfer_ownership_dialog.dart and its test are fully localized; flutter test passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | Both dialogs render already-approved ARB strings interpolated with server-returned `memberUsername`/`bandName`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|--------------|------------------|
| T-13-05 | Repudiation | Ownership-transfer/invite-rotate confirm copy | medium | mitigate | Accurate, unambiguous Russian body text (D-04's requirement to explicitly state the acting owner's own demotion) is verified by the widget test asserting the exact composed string, preventing a silent mistranslation that could make an owner misunderstand an irreversible action. |

No package installs in this plan; package-legitimacy gate not applicable.
</threat_model>

<verification>
- `flutter test test/features/bands/confirm_rotate_invite_code_dialog_test.dart test/features/bands/confirm_transfer_ownership_dialog_test.dart` — both pass
- `flutter analyze lib/features/bands/confirm_rotate_invite_code_dialog.dart lib/features/bands/confirm_transfer_ownership_dialog.dart` — no new errors
</verification>

<success_criteria>
Both dialogs and their tests fully localized; no hardcoded English string literal remains.
</success_criteria>

<output>
Create `.planning/phases/13-string-extraction-screen-localization/13-03-SUMMARY.md` when done
</output>
