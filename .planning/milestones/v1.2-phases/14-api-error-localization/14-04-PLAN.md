---
phase: 14-api-error-localization
plan: 04
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - lib/features/setlists/create_setlist_screen.dart
  - lib/features/setlists/edit_setlist_screen.dart
  - lib/features/setlists/confirm_delete_setlist_dialog.dart
  - lib/features/setlists/setlist_detail_screen.dart
  - lib/features/setlists/add_setlist_tracks_dialog.dart
  - test/features/setlists/create_setlist_screen_test.dart
  - test/features/setlists/add_setlist_tracks_dialog_test.dart
autonomous: true
requirements: [I18N-05]

estimate:
  tokens: 50000
  raw_tokens: 33000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Every setlist-feature `on ApiException catch (e)` site (create setlist, edit setlist, delete setlist, remove track from setlist, add tracks to setlist) shows the ARB-localized generic message for a known error code."
    - "setlist_detail_screen.dart's remove-track catch site, which displays errors via a SnackBar (not the inline-Text `_errorMessage` pattern every other site uses), is updated to the same localized-message call without changing its SnackBar display mechanism."
  artifacts:
    - "create_setlist_screen.dart and add_setlist_tracks_dialog.dart each gain a `final l10n = AppLocalizations.of(context)!;` fetch inside their `on ApiException catch (e)` block, since (unlike every other in-scope site) their existing `l10n` local is declared only inside the try block's success path and is not in lexical scope in the catch block."
    - "setlist_detail_screen.dart's `_removeTrack()` `on ApiException catch (e)` block gains the same local `l10n` fetch, placed after its existing `if (!mounted) return;` guard, matching the pattern already used one branch below it in the file's own `catch (_)` block."
  key_links:
    - "edit_setlist_screen.dart and confirm_delete_setlist_dialog.dart both already have `l10n` in scope before their `try` block (verified per-file during planning) — a bare call-site swap, no new fetch, unlike the other 3 files in this plan."
---

<objective>
Wire the 5 remaining setlist-feature catch sites into `ApiExceptionLocalization.localizedMessage()`. Three of the five (`create_setlist_screen.dart`, `add_setlist_tracks_dialog.dart`, `setlist_detail_screen.dart`) need an explicit local `l10n` fetch added inside the catch block itself, because — unlike every site handled in 14-02/14-03 — their existing `l10n` local is declared only inside the `try` block's success path (or, for `setlist_detail_screen.dart`, not declared at all in that method) and is therefore not in lexical scope at the `on ApiException catch (e)` site.

Purpose: Complete Setlists-feature coverage, the last feature area with in-scope catch sites (I18N-05).
Output: 5 files updated (2 mechanical swaps, 3 with an added `l10n` fetch); 2 test files extended.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-api-error-localization/14-CONTEXT.md
@lib/api/api_exception.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire create/edit/delete-setlist catch sites (one needs a new l10n fetch)</name>
  <files>lib/features/setlists/create_setlist_screen.dart, lib/features/setlists/edit_setlist_screen.dart, lib/features/setlists/confirm_delete_setlist_dialog.dart, test/features/setlists/create_setlist_screen_test.dart</files>
  <read_first>
    lib/api/api_exception.dart (confirm the `ApiExceptionLocalization.localizedMessage(l10n)` signature from 14-01)
    lib/features/setlists/create_setlist_screen.dart lines 41-91 (`_submit()` — `l10n` is declared at line 70, INSIDE the try block's success path only, AFTER the `if (!mounted) return;` at line 69 — it is NOT in scope at the `on ApiException catch (e)` block at line 82; the sibling `catch (_)` block at lines 84-87 already fetches its own local `l10n` for exactly this reason, at line 86)
    lib/features/setlists/edit_setlist_screen.dart lines 52-115 (`_submit()` — `l10n` fetched at line 55, before `try` — already in scope, mechanical swap)
    lib/features/setlists/confirm_delete_setlist_dialog.dart lines 36-70 (`_delete()` — `l10n` fetched at line 37, before `try` — already in scope, mechanical swap)
    test/features/setlists/create_setlist_screen_test.dart (existing test scaffold/patterns to model the new test on)
  </read_first>
  <action>
    In `create_setlist_screen.dart`, change the `on ApiException catch (e) { setState(() => _errorMessage = e.message); }` block to `on ApiException catch (e) { final l10n = AppLocalizations.of(context)!; setState(() => _errorMessage = e.localizedMessage(l10n)); }` — this file's `AppLocalizations.of(context)!` import is already present, only a new local variable is added inside this specific catch block. Do not add a `mounted` guard before the fetch (the pre-existing `catch (_)` block below it does guard with `mounted`, but the `on ApiException` block never did before this change and this phase's scope is error-message text only, not new guard logic).

    In `edit_setlist_screen.dart` and `confirm_delete_setlist_dialog.dart`, change each `on ApiException catch (e)` block's body from `setState(() => _errorMessage = e.message);` to `setState(() => _errorMessage = e.localizedMessage(l10n));` — `l10n` is already in scope in both methods, no new fetch needed.

    In `test/features/setlists/create_setlist_screen_test.dart`, add one new `testWidgets` block modeled on the file's existing error-rendering test: mock the createSetlist POST to return `http.Response(jsonEncode({'code': 'permission_denied', 'message': 'raw server text'}), 400)`, submit the form with a valid name, and assert `find.text(tester.strings.commonErrorPermissionDenied)` is found while `find.text('raw server text')` is not found.
  </action>
  <verify>
    <automated>flutter test test/features/setlists/create_setlist_screen_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `create_setlist_screen.dart`'s `on ApiException catch (e)` block contains its own `final l10n = AppLocalizations.of(context)!;` line, separate from the one inside the `try` block's success path.
    - `edit_setlist_screen.dart` and `confirm_delete_setlist_dialog.dart` call `e.localizedMessage(l10n)` with no new `l10n` fetch added.
    - `flutter test test/features/setlists/create_setlist_screen_test.dart` passes, including the new `permission_denied` case.
    - `flutter analyze` reports no new issues in the 3 modified `.dart` files (in particular, no "unused variable" or duplicate-declaration warnings from the new `l10n` local in `create_setlist_screen.dart`).
  </acceptance_criteria>
  <reversibility rating="reversible">Client-side string-display change plus one added local-variable fetch; trivially revertable via git.</reversibility>
  <done>create_setlist_screen.dart, edit_setlist_screen.dart, and confirm_delete_setlist_dialog.dart all show the localized generic message for known error codes; proven by a passing widget test on the create-setlist path.</done>
</task>

<task type="auto">
  <name>Task 2: Wire remove-track (SnackBar) and add-tracks catch sites (both need a new l10n fetch)</name>
  <files>lib/features/setlists/setlist_detail_screen.dart, lib/features/setlists/add_setlist_tracks_dialog.dart, test/features/setlists/add_setlist_tracks_dialog_test.dart</files>
  <read_first>
    lib/api/api_exception.dart (confirm the `ApiExceptionLocalization.localizedMessage(l10n)` signature from 14-01)
    lib/features/setlists/setlist_detail_screen.dart lines 43-89 (`_removeTrack()` — its `on ApiException catch (e)` block at lines 73-77 shows the error via `ScaffoldMessenger`/`SnackBar`, NOT the `_errorMessage` inline-Text pattern every other site in this phase uses, and has no `l10n` local anywhere in the method until the sibling `catch (_)` block fetches its own at line 80)
    lib/features/setlists/add_setlist_tracks_dialog.dart lines 86-130 (`_submit()` — `l10n` is declared at line 116, INSIDE the try block's success path only, AFTER `if (!mounted) return;` — it is NOT in scope at the `on ApiException catch (e)` block at line 121; the sibling `catch (_)` block at lines 123-126 already fetches its own local `l10n` for exactly this reason, at line 125)
    test/features/setlists/add_setlist_tracks_dialog_test.dart (existing test scaffold/patterns to model the new test on)
  </read_first>
  <action>
    In `setlist_detail_screen.dart`'s `_removeTrack()`, change the `on ApiException catch (e) { if (!mounted) return; ScaffoldMessenger.of(context).showSnackBar(SnackBar(content: Text(e.message))); }` block to `on ApiException catch (e) { if (!mounted) return; final l10n = AppLocalizations.of(context)!; ScaffoldMessenger.of(context).showSnackBar(SnackBar(content: Text(e.localizedMessage(l10n)))); }` — keep the existing `mounted` guard before the fetch (this file's existing pattern, unlike `create_setlist_screen.dart`'s `on ApiException` block, already guards with `mounted` in its sibling `catch (_)` block, so match that local convention). Do not change the SnackBar display mechanism itself.

    In `add_setlist_tracks_dialog.dart`, change the `on ApiException catch (e) { setState(() => _errorMessage = e.message); }` block to `on ApiException catch (e) { final l10n = AppLocalizations.of(context)!; setState(() => _errorMessage = e.localizedMessage(l10n)); }` — no `mounted` guard, matching this block's own pre-existing behavior (the `catch (_)` block below it does guard with `mounted`, but the `on ApiException` block never did).

    In `test/features/setlists/add_setlist_tracks_dialog_test.dart`, add one new `testWidgets` block modeled on the file's existing error-rendering test: mock the addSetlistTracks POST to return `http.Response(jsonEncode({'code': 'operation_rejected', 'message': 'raw server text'}), 400)`, select at least one track and submit, and assert `find.text(tester.strings.commonErrorOperationRejected)` is found while `find.text('raw server text')` is not found.
  </action>
  <verify>
    <automated>flutter test test/features/setlists/add_setlist_tracks_dialog_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `setlist_detail_screen.dart`'s `on ApiException catch (e)` block in `_removeTrack()` still displays the error via `ScaffoldMessenger`/`SnackBar` (not converted to the `_errorMessage` inline-Text pattern), now calling `e.localizedMessage(l10n)` with a locally-fetched `l10n`.
    - `add_setlist_tracks_dialog.dart`'s `on ApiException catch (e)` block contains its own `final l10n = AppLocalizations.of(context)!;` line, separate from the one inside the `try` block's success path.
    - `flutter test test/features/setlists/add_setlist_tracks_dialog_test.dart` passes, including the new `operation_rejected` case.
    - `flutter analyze` reports no new issues in the 2 modified `.dart` files.
  </acceptance_criteria>
  <reversibility rating="reversible">Client-side string-display change plus two added local-variable fetches; trivially revertable via git. No change to the SnackBar-vs-inline-Text display mechanism in either file.</reversibility>
  <done>setlist_detail_screen.dart's remove-track SnackBar and add_setlist_tracks_dialog.dart both show the localized generic message for known error codes; proven by a passing widget test on the add-tracks path. All ~20 in-scope catch sites across the app now use `localizedMessage()` — Phase 14 coverage is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server HTTP error response → `ApiException.code`/`message` → client UI text | Same boundary as 14-01/14-02/14-03, now exercised at the final 5 Setlists sites, completing coverage of every in-scope catch site in the app. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-09 | Tampering | 5 Setlists-feature catch sites | low | mitigate | Mechanical call-site swap plus 3 added local `l10n` fetches, no new lookup/switch logic — same mitigation as T-14-01. |
| T-14-10 | Information Disclosure | `setlist_detail_screen.dart` SnackBar display | low | accept | Unchanged from pre-phase behavior for unmapped codes (raw `e.message` still shown via SnackBar); this task only adds localization for the 5 known codes, consistent with every other site in this phase. |

No new authentication, authorization, or data-flow surface is introduced. This plan completes Phase 14's catch-site coverage — after it lands, P-03 (no orphaned raw-`e.message` site) from 14-01's prohibition recall is fully satisfied.
</threat_model>

<verification>
- `flutter test test/features/setlists/create_setlist_screen_test.dart` passes.
- `flutter test test/features/setlists/add_setlist_tracks_dialog_test.dart` passes.
- `flutter analyze` shows no new issues across the 5 modified files.
- Full suite sanity check: `flutter test` (all files) still passes after all of 14-01 through 14-04 have landed.
</verification>

<success_criteria>
- All 5 setlist-feature `on ApiException catch (e)` sites call `e.localizedMessage(l10n)`, with a correctly-scoped local `l10n` fetch added wherever the pre-existing local wasn't visible at the catch site.
- `setlist_detail_screen.dart`'s SnackBar-based error display is preserved as-is, only its message source changes.
- Combined with 14-01/14-02/14-03, every in-scope `on ApiException catch (e)` site in the app (~20 total) now calls `localizedMessage()` — ROADMAP Phase 14 success criteria 1 and 2 are fully met.
</success_criteria>

<output>
Create `.planning/phases/14-api-error-localization/14-04-SUMMARY.md` when done
</output>

## Artifacts this phase produces

(Complete list across all of Phase 14's plans — 14-01 through 14-04.)

- `ApiExceptionLocalization` extension on `ApiException`, method `String localizedMessage(AppLocalizations l10n, {Map<String, String>? overrides})` — defined in `lib/api/api_exception.dart` (created in 14-01).
- ARB keys `commonErrorInvalidInput`, `commonErrorNotFound`, `commonErrorPermissionDenied`, `commonErrorOperationRejected`, `commonErrorAlreadyExists` in `lib/l10n/app_en.arb` and `lib/l10n/app_ru.arb`, and their generated `AppLocalizations` getters in `lib/generated/app_localizations*.dart` (created in 14-01).
- The `overrides` named parameter on `localizedMessage` (the D-04 override mechanism) (created in 14-01, consumed in 14-03).
- `test/features/auth/login_screen_test.dart` — new test file, did not exist before this phase (created in 14-03).
