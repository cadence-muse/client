---
phase: 14-api-error-localization
plan: 03
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - lib/features/tracks/create_track_screen.dart
  - lib/features/tracks/edit_track_screen.dart
  - lib/features/tracks/confirm_delete_track_dialog.dart
  - lib/features/auth/login_screen.dart
  - lib/features/profile/change_password_screen.dart
  - test/features/tracks/create_track_screen_test.dart
  - test/features/auth/login_screen_test.dart
  - test/features/profile/change_password_screen_test.dart
autonomous: true
requirements: [I18N-05]

estimate:
  tokens: 55000
  raw_tokens: 37000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Every track-feature `on ApiException catch (e)` site (create track, edit track, delete track) shows the ARB-localized generic message for a known error code."
    - "login_screen.dart's already_exists override (registration) and change_password_screen.dart's invalid_input override keep their exact pre-existing wording (loginUsernameTakenError / changePasswordIncorrectCurrentError) after being refactored to route through `localizedMessage`'s `overrides` parameter (D-04)."
    - "login_screen.dart's 401-statusCode-driven loginInvalidCredentialsError special case is untouched — still not code-driven, still out of this phase's scope."
    - "A known error code other than the 2 existing overrides (e.g. not_found) on login or change-password now shows the shared generic localized message instead of raw server text — a behavior expansion this phase intentionally makes (I18N-05), not present before."
  artifacts:
    - "login_screen.dart's `_submit()` no longer has a nested try/catch/rethrow around the `register()` call — the ApiException from a failed register propagates directly to the outer `on ApiException catch (e)` block, which now calls `e.localizedMessage(l10n, overrides: {'already_exists': l10n.loginUsernameTakenError})`."
    - "change_password_screen.dart's `on ApiException catch (e)` block calls `e.localizedMessage(l10n, overrides: {'invalid_input': l10n.changePasswordIncorrectCurrentError})` in place of its inline statusCode/code conditional."
    - "test/features/auth/login_screen_test.dart is a new file (LoginScreen had no test coverage before this phase)."
  key_links:
    - "login_screen.dart's 401 statusCode-check block (lines 68-77) still independently throws a wrapped ApiException with `code: e.code` (which is null for a 401, per publicapi.yml's BadRequestResponseBody/ErrorCode being scoped to 400 responses only) — the outer catch's `localizedMessage` call falls through to the null-code branch and returns that wrapped `message` unchanged, so the 401 override keeps working with zero code changes to that block."
---

<objective>
Wire the 3 remaining track-feature catch sites (mechanical swap, same as 14-02), and refactor the 2 pre-existing screen-specific overrides — login's `already_exists` and change-password's `invalid_input` — to route through `localizedMessage`'s `overrides` parameter (D-04), so every in-scope catch site in the app uses one consistent call shape with no orphaned duplicate error-handling logic.

Purpose: Complete Tracks-feature coverage and retire the 2 bespoke override implementations that predate this phase's shared mechanism, without changing their user-visible wording.
Output: 3 track files updated with 1 new regression test; `login_screen.dart` and `change_password_screen.dart` refactored; a new `login_screen_test.dart` (LoginScreen currently has zero test coverage).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-api-error-localization/14-CONTEXT.md
@lib/api/api_exception.dart
@lib/features/auth/login_screen.dart
@lib/features/profile/change_password_screen.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire create/edit/delete-track catch sites</name>
  <files>lib/features/tracks/create_track_screen.dart, lib/features/tracks/edit_track_screen.dart, lib/features/tracks/confirm_delete_track_dialog.dart, test/features/tracks/create_track_screen_test.dart</files>
  <read_first>
    lib/api/api_exception.dart (confirm the `ApiExceptionLocalization.localizedMessage(l10n)` signature from 14-01)
    lib/features/tracks/create_track_screen.dart lines 79-126 (`_submit()` — `l10n` fetched at line 80, before `try`)
    lib/features/tracks/edit_track_screen.dart lines 110-182 (`_submit()` — `l10n` fetched at line 111, before `try`)
    lib/features/tracks/confirm_delete_track_dialog.dart lines 36-75 (`_delete()` — `l10n` fetched at line 37, before `try`)
    test/features/tracks/create_track_screen_test.dart (existing test scaffold/patterns to model the new test on)
  </read_first>
  <action>
    In each of `create_track_screen.dart`, `edit_track_screen.dart`, `confirm_delete_track_dialog.dart`: change the `on ApiException catch (e)` block's body from `setState(() => _errorMessage = e.message);` to `setState(() => _errorMessage = e.localizedMessage(l10n));`. `l10n` is already a local in scope in every one of these 3 methods — do not add a new fetch. Leave the `catch (_)` fallback block and `finally` block untouched in all 3 files.

    In `test/features/tracks/create_track_screen_test.dart`, add one new `testWidgets` block modeled on the file's existing error-rendering test: mock the createBandTrack POST to return `http.Response(jsonEncode({'code': 'invalid_input', 'message': 'raw server text'}), 400)`, submit the form with valid required fields, and assert `find.text(tester.strings.commonErrorInvalidInput)` is found while `find.text('raw server text')` is not found.
  </action>
  <verify>
    <automated>flutter test test/features/tracks/create_track_screen_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - All 3 files' `on ApiException catch (e)` blocks call `e.localizedMessage(l10n)`, no file introduces a new `l10n` fetch (all 3 already have one in scope).
    - `flutter test test/features/tracks/create_track_screen_test.dart` passes, including the new `invalid_input` case.
    - `flutter analyze` reports no new issues in the 3 modified `.dart` files.
  </acceptance_criteria>
  <reversibility rating="reversible">Client-side string-display swap only; trivially revertable via git.</reversibility>
  <done>create_track_screen.dart, edit_track_screen.dart, and confirm_delete_track_dialog.dart all show the localized generic message for known error codes; proven by a passing widget test on the create-track path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor login/change-password overrides onto the shared `overrides` mechanism (D-04)</name>
  <files>lib/features/auth/login_screen.dart, lib/features/profile/change_password_screen.dart, test/features/auth/login_screen_test.dart, test/features/profile/change_password_screen_test.dart</files>
  <read_first>
    lib/api/api_exception.dart (confirm the `overrides` parameter shape from 14-01: `Map<String, String>?`)
    lib/features/auth/login_screen.dart (full file, 187 lines — both the `already_exists` code-driven override at lines 48-60 and the separate 401 statusCode-driven override at lines 62-77, which stays untouched)
    lib/features/profile/change_password_screen.dart (full file, 161 lines — the `invalid_input` override at lines 62-67)
    test/features/profile/change_password_screen_test.dart lines 264-296 (existing "400 invalid_input shows..." test — must keep passing verbatim after the refactor) and lines 298-320 (existing "500 server_error shows the raw e.message verbatim" test — proves the unmapped-code fallback still works)
    test/features/bands/create_band_screen_test.dart lines 20-62 (buildApiClient/wrap scaffold pattern to model the new login_screen_test.dart on)
    lib/api/public_api.dart lines 13-35 (register hits `POST /api/register`, login hits `POST /api/login` — needed to route the mock handler by path in the new test)
  </read_first>
  <behavior>
    - Registering with a username that already exists (`POST /api/register` → 400, `code: 'already_exists'`) still shows `loginUsernameTakenError`, not the generic `commonErrorAlreadyExists`.
    - Registering and hitting a different known code (e.g. `POST /api/register` → 400, `code: 'not_found'`) now shows the generic `commonErrorNotFound` message — this is new behavior this phase adds (previously any non-`already_exists` register failure showed raw `e.message`).
    - Logging in with wrong credentials (`POST /api/login` → 401) still shows `loginInvalidCredentialsError`, unchanged — this is the out-of-scope statusCode-driven path, not touched by this task.
    - Submitting the wrong current password on change-password (`POST /api/me/password` → 400, `code: 'invalid_input'`) still shows `changePasswordIncorrectCurrentError`, not the generic `commonErrorInvalidInput`.
    - A different known code on change-password (e.g. `code: 'not_found'`) now shows the generic `commonErrorNotFound` message — new behavior, previously showed raw `e.message`.
  </behavior>
  <action>
    In `lib/features/auth/login_screen.dart`'s `_submit()`: remove the nested `try { await publicApi.register(...); } on ApiException catch (e) { if (e.statusCode == 400 && e.code == 'already_exists') { throw ApiException(...); } rethrow; }` block entirely, replacing it with a bare `await publicApi.register(username: username, password: password);` call (still inside the existing `if (_mode == _AuthMode.signUp) { ... }` guard) — the ApiException now propagates directly to the outer catch instead of being pre-wrapped. Leave the second nested try/catch (the 401 statusCode-driven `login()` wrap, lines 62-77) completely untouched — it is out of this phase's scope. Change the outer `on ApiException catch (e) { setState(() => _errorMessage = e.message); }` block to `on ApiException catch (e) { setState(() => _errorMessage = e.localizedMessage(l10n, overrides: {'already_exists': l10n.loginUsernameTakenError})); }`.

    In `lib/features/profile/change_password_screen.dart`'s `_submit()`: change the `on ApiException catch (e) { setState(() { _errorMessage = (e.statusCode == 400 && e.code == 'invalid_input') ? l10n.changePasswordIncorrectCurrentError : e.message; }); }` block to `on ApiException catch (e) { setState(() => _errorMessage = e.localizedMessage(l10n, overrides: {'invalid_input': l10n.changePasswordIncorrectCurrentError})); }`.

    Create `test/features/auth/login_screen_test.dart` from scratch, modeling `buildApiClient`/`wrap` on `test/features/bands/create_band_screen_test.dart`'s scaffold (`apiClientProvider.overrideWithValue`, `MaterialApp` with the 4 localization delegates, `home: const LoginScreen()`). Route the mock handler by `request.url.path` (`/api/register` vs `/api/login`). Add 3 `testWidgets` tests: (1) toggle to sign-up mode (tap the button labeled `tester.strings.loginToggleToSignUp`), fill valid username/password (>=8 chars), submit; mock `/api/register` to return `http.Response(jsonEncode({'code': 'already_exists', 'message': 'raw'}), 400)`; assert `find.text(tester.strings.loginUsernameTakenError)` found. (2) same sign-up flow but mock `/api/register` to return `http.Response(jsonEncode({'code': 'not_found', 'message': 'raw'}), 400)`; assert `find.text(tester.strings.commonErrorNotFound)` found. (3) stay in log-in mode (default), fill valid username/password, submit; mock `/api/login` to return `http.Response('', 401)`; assert `find.text(tester.strings.loginInvalidCredentialsError)` found — this is the regression proof that the untouched 401 path still works.

    In `test/features/profile/change_password_screen_test.dart`, add one new `testWidgets` block modeled on the existing "400 invalid_input shows..." test: mock the changePassword POST to return `http.Response(jsonEncode({'code': 'not_found', 'message': 'raw server text'}), 400)`, submit the form, and assert `find.text(tester.strings.commonErrorNotFound)` is found while `find.text('raw server text')` is not found. Do not modify the existing `invalid_input` test (line 264) or the existing `server_error` fallback test (line 298) — both must keep passing unmodified as regression proof.
  </action>
  <verify>
    <automated>flutter test test/features/auth/login_screen_test.dart test/features/profile/change_password_screen_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `login_screen.dart` no longer contains a nested try/catch around the `register()` call.
    - `flutter test test/features/auth/login_screen_test.dart` passes all 3 new tests.
    - `flutter test test/features/profile/change_password_screen_test.dart` passes, including both pre-existing tests (line 264, line 298) unmodified and the new `not_found` test.
    - `flutter analyze` reports no new issues in the 2 modified `.dart` files.
  </acceptance_criteria>
  <reversibility rating="reversible">Client-side error-handling refactor with no change to network calls, request bodies, or navigation; trivially revertable via git. The 2 existing override wordings are explicitly preserved (P-02), keeping this a behavior-neutral refactor for the already-covered cases and a scoped, intentional behavior expansion (I18N-05) for the previously-uncovered codes.</reversibility>
  <done>login_screen.dart's already_exists override and change_password_screen.dart's invalid_input override both route through `localizedMessage`'s overrides parameter with unchanged wording; login_screen.dart has test coverage for the first time; the 401 statusCode-driven path is proven unaffected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server HTTP error response → `ApiException.code`/`message` → client UI text | Same boundary as 14-01/14-02, now exercised at 3 Tracks sites plus the 2 refactored Auth/Profile override sites. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-06 | Tampering | 3 Tracks-feature catch sites | low | mitigate | Mechanical call-site swap only, no new logic — same mitigation as T-14-01. |
| T-14-07 | Tampering | login/change-password `overrides` map usage | low | mitigate | The `overrides` map's keys (`'already_exists'`, `'invalid_input'`) are compile-time string literals written by the client, not attacker-influenced; a server response cannot inject new override keys, it can only supply a `code` value that is or isn't a key already present in the map. |
| T-14-08 | Repudiation/Tampering | Removal of login's inner register try/catch/rethrow | low | mitigate | Behavior-preserving refactor verified by 3 new automated tests (already_exists override, a different known code, and the untouched 401 path) — see task 2's `<behavior>` block. |

No new authentication, authorization, or data-flow surface is introduced.
</threat_model>

<verification>
- `flutter test test/features/tracks/create_track_screen_test.dart` passes.
- `flutter test test/features/auth/login_screen_test.dart` passes (new file, 3 tests).
- `flutter test test/features/profile/change_password_screen_test.dart` passes (2 pre-existing + 1 new test).
- `flutter analyze` shows no new issues across the 5 modified/created `.dart` files.
</verification>

<success_criteria>
- All 3 track-feature `on ApiException catch (e)` sites call `e.localizedMessage(l10n)`.
- login_screen.dart and change_password_screen.dart both route their pre-existing overrides through `localizedMessage`'s `overrides` parameter with unchanged wording, and login_screen.dart gains test coverage for the first time.
</success_criteria>

<output>
Create `.planning/phases/14-api-error-localization/14-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces

(Complete list across all of Phase 14's plans — 14-01 through 14-04.)

- `ApiExceptionLocalization` extension on `ApiException`, method `String localizedMessage(AppLocalizations l10n, {Map<String, String>? overrides})` — defined in `lib/api/api_exception.dart` (created in 14-01).
- ARB keys `commonErrorInvalidInput`, `commonErrorNotFound`, `commonErrorPermissionDenied`, `commonErrorOperationRejected`, `commonErrorAlreadyExists` in `lib/l10n/app_en.arb` and `lib/l10n/app_ru.arb`, and their generated `AppLocalizations` getters in `lib/generated/app_localizations*.dart` (created in 14-01).
- The `overrides` named parameter on `localizedMessage` (the D-04 override mechanism) (created in 14-01, consumed in 14-03).
- `test/features/auth/login_screen_test.dart` — new test file, did not exist before this phase (created in 14-03).
