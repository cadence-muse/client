---
phase: 16
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - test/providers/setlists_provider_test.dart
  - test/features/setlists/setlist_detail_screen_test.dart
  - test/features/tracks/track_list_screen_test.dart
  - test/features/tracks/confirm_delete_track_dialog_test.dart
  - test/features/tracks/create_track_screen_test.dart
  - test/features/tracks/track_detail_screen_test.dart
  - test/cache/cache_service_test.dart
autonomous: true
requirements: [RENAME-01]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "flutter test (full project suite) passes with 0 failures after all rename changes land (ROADMAP criterion 4)"
    - "No file under lib/ or test/ contains the substring 'song' (case-insensitive), except the explicitly out-of-scope lib/api/publicapi.yml Songs tag deferred to Phase 17 per D-04 (ROADMAP criteria 2-4)"
    - "All 51 test-fixture 'Song'-named sample track titles across the 7 files this plan touches now read their 'Track' equivalents, with each fixture value and its matching assertion kept in sync (D-05)"
    - "lib/features/songs/ does not exist and lib/generated/app_localizations*.dart carry no homeAddSongButton getter — confirmed as a closing cross-check on top of Plan 16-01's work (ROADMAP criteria 2 and 4)"
  artifacts:
    - path: "test/providers/setlists_provider_test.dart"
      provides: "Track One/Track Two fixture titles (renamed from Song One/Song Two)"
    - path: "test/cache/cache_service_test.dart"
      provides: "Track One/Track Two cache fixture titles (renamed from Song One/Song Two)"
  key_links:
    - from: "test fixture 'title' values across the 7 files"
      to: "their corresponding find.text/expect assertions in the same test"
      via: "matching string literal (e.g. a fixture 'title': 'Track One' and its find.text('Track One') assertion)"
      pattern: "'Track One'|'Track Two'|'Track Three'|'My Track'|'Cached Track'"
  prohibitions:
    - statement: "MUST NOT touch or rename lib/api/publicapi.yml's Songs tag or any other publicapi.yml content — explicitly deferred to Phase 17 (API Contract Sync) per D-04"
      status: unresolved
    - statement: "MUST NOT change any assertion structure, test names, or non-title fixture fields (artist, id, band name) while renaming title literals — this is a pure string-literal value rename with zero test-behavior change"
      status: unresolved
---

<objective>
RENAME-01's client-code-only rename sweep, part 2 of 2. Task 1 renames the ~51 arbitrary "Song"-named sample track-title fixtures across 7 test files to their "Track" equivalents (D-05) — pure test-data string-literal renames with no assertion-structure change. Task 2 closes the phase with a full-tree aggregate audit: zero surviving "song" references anywhere in `lib/` or `test/` except the explicitly deferred `lib/api/publicapi.yml` Songs tag, a green full test suite, a clean `flutter analyze`, and confirmation that no stale directory or generated artifact survives from Plan 16-01's work.

Purpose: complete RENAME-01 so the codebase is grep-clean of legacy "song" terminology in every file this milestone owns, while `publicapi.yml`'s dangling `Songs` tag is left untouched per D-04's explicit Phase 17 deferral.

Output: all 7 test files are "Track"-named; a full `flutter test` + `flutter analyze` pass confirms Phase 16 is complete.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-track-terminology-rename/16-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename "Song"-named test fixture data to "Track" equivalents across 7 test files</name>
  <reversibility rating="reversible">Pure test-fixture string-literal renames with no assertion-structure change; git revert restores the old strings instantly (D-05).</reversibility>
  <files>test/providers/setlists_provider_test.dart, test/features/setlists/setlist_detail_screen_test.dart, test/features/tracks/track_list_screen_test.dart, test/features/tracks/confirm_delete_track_dialog_test.dart, test/features/tracks/create_track_screen_test.dart, test/features/tracks/track_detail_screen_test.dart, test/cache/cache_service_test.dart</files>
  <read_first>
    test/providers/setlists_provider_test.dart (lines 662, 669, 693, 700, 726 — 'title': 'Song One'/'Song Two' fixtures)
    test/features/setlists/setlist_detail_screen_test.dart (19 occurrences, lines 129-707 — 'title': 'Song One'/'Two'/'Three' fixtures and their matching find.text assertions)
    test/features/tracks/track_list_screen_test.dart (7 occurrences, lines 62-252 — 'title': 'Cached Song' fixtures and the find.text('Cached Song') assertion)
    test/features/tracks/confirm_delete_track_dialog_test.dart (lines 67, 191 — trackTitle: 'My Song')
    test/features/tracks/create_track_screen_test.dart (lines 86, 113, 116, 246 — entered text, JSON fixture, and createTrackAddedSnackbar('My Song') assertion)
    test/features/tracks/track_detail_screen_test.dart (lines 63, 107, 206, 270, 275, 306 — bare 'title': 'Song' fixtures)
    test/cache/cache_service_test.dart (lines 134, 146, 159, 175, 275, 282, 299, 300 — 'title': 'Song One'/'Song Two' fixtures and their result[...]['title'] assertions)
    .planning/phases/16-track-terminology-rename/16-CONTEXT.md (D-05 — exact file list and rationale)
  </read_first>
  <action>
    Rename every arbitrary sample track-title fixture string containing "Song" to its "Track" equivalent, matching the exact suffix/prefix pattern already present (D-05) — a pure string-literal find/replace on test data, no assertion logic or structure changes:
    - `test/providers/setlists_provider_test.dart`: "Song One" -> "Track One" (lines 662, 693), "Song Two" -> "Track Two" (lines 669, 700, 726)
    - `test/features/setlists/setlist_detail_screen_test.dart`: "Song One" -> "Track One", "Song Two" -> "Track Two", "Song Three" -> "Track Three" at all 19 occurrences — keep each fixture's title and its corresponding find.text assertion in sync
    - `test/features/tracks/track_list_screen_test.dart`: "Cached Song" -> "Cached Track" at all 7 occurrences (fixture 'title' values and the find.text assertion)
    - `test/features/tracks/confirm_delete_track_dialog_test.dart`: "My Song" -> "My Track" at both trackTitle occurrences (lines 67, 191)
    - `test/features/tracks/create_track_screen_test.dart`: "My Song" -> "My Track" at all 4 occurrences — the entered text (line 86), the mocked JSON response 'title' (line 113), the snackbar assertion argument (line 116's createTrackAddedSnackbar argument), and the fixture 'title' (line 246)
    - `test/features/tracks/track_detail_screen_test.dart`: "Song" -> "Track" at all 6 occurrences (bare 'title': 'Song' and jsonEncode(...) fixtures)
    - `test/cache/cache_service_test.dart`: "Song One" -> "Track One", "Song Two" -> "Track Two" at all 8 occurrences (fixture 'title' values and their matching result[...]['title'] assertions)

    Do not touch any assertion structure, test descriptions/names, or non-title fixture fields (artist, id, band name) — only the "Song"-containing title/text literals change value.
  </action>
  <verify>
    <automated>test -z "$(grep -rliE 'song' test/providers/setlists_provider_test.dart test/features/setlists/setlist_detail_screen_test.dart test/features/tracks/track_list_screen_test.dart test/features/tracks/confirm_delete_track_dialog_test.dart test/features/tracks/create_track_screen_test.dart test/features/tracks/track_detail_screen_test.dart test/cache/cache_service_test.dart)" && flutter test test/providers/setlists_provider_test.dart test/features/setlists/setlist_detail_screen_test.dart test/features/tracks/track_list_screen_test.dart test/features/tracks/confirm_delete_track_dialog_test.dart test/features/tracks/create_track_screen_test.dart test/features/tracks/track_detail_screen_test.dart test/cache/cache_service_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - None of the 7 files contain the substring "song" (case-insensitive) anywhere
    - Every renamed fixture's title value and its corresponding find.text/expect argument stay in sync (e.g. a fixture 'title': 'Track One' is asserted against find.text('Track One'), never a mismatched pair)
    - `flutter test` on all 7 files passes with 0 failures
  </acceptance_criteria>
  <done>All "Song"-containing fixture literals across the 7 test files read "Track" equivalents; every test in these files still passes.</done>
</task>

<task type="auto">
  <name>Task 2: Phase-wide closing audit — zero "song" references, full suite green, no stale artifacts</name>
  <files>lib, test</files>
  <read_first>
    .planning/ROADMAP.md (Phase 16 section — the 4 success criteria this audit closes)
    .planning/phases/16-track-terminology-rename/16-CONTEXT.md (D-04 — the publicapi.yml exclusion this audit must respect)
  </read_first>
  <action>
    Run a full-tree audit confirming Phase 16's closing state: (1) no file under `lib/` or `test/` (`.dart`/`.arb`) contains the substring "song" case-insensitive, except `lib/api/publicapi.yml`'s explicitly out-of-scope `Songs` tag (D-04) — do not edit that file; (2) `lib/features/songs/` does not exist; (3) `flutter analyze` reports zero issues; (4) the full `flutter test` suite passes. This task performs no source edits of its own — it is the verification gate confirming Plan 16-01 and this plan's Task 1 together satisfy all 4 ROADMAP Phase 16 success criteria. If any check fails, identify and fix the specific surviving reference before re-running — do not weaken the audit scope to make it pass.
  </action>
  <verify>
    <automated>test -z "$(grep -rliE 'song' lib test --include='*.dart' --include='*.arb' 2>/dev/null | grep -v 'lib/api/publicapi.yml')" && test ! -e lib/features/songs && flutter analyze && flutter test</automated>
  </verify>
  <acceptance_criteria>
    - Zero files under `lib/` or `test/` (`.dart`/`.arb`), excluding `lib/api/publicapi.yml`, contain the substring "song" case-insensitive
    - `lib/features/songs/` does not exist
    - `flutter analyze` reports no issues
    - `flutter test` (full suite) passes with 0 failures
    - `lib/api/publicapi.yml` is not listed in either plan's `files_modified` (per D-04, left untouched)
  </acceptance_criteria>
  <done>The phase-wide audit confirms zero surviving "song" references anywhere in lib/ or test/ except the explicitly-deferred publicapi.yml Songs tag, the full test suite is green, flutter analyze is clean, and no stale songs/ directory or generated artifact remains — all 4 ROADMAP success criteria for Phase 16 hold.</done>
</task>

</tasks>

## Artifacts this plan produces

No new source files. This plan modifies 7 existing test files' fixture data in place and performs a read-only closing audit — no new classes, no new ARB keys, no new dependencies.

- `test/providers/setlists_provider_test.dart`, `test/features/setlists/setlist_detail_screen_test.dart`, `test/features/tracks/track_list_screen_test.dart`, `test/features/tracks/confirm_delete_track_dialog_test.dart`, `test/features/tracks/create_track_screen_test.dart`, `test/features/tracks/track_detail_screen_test.dart`, `test/cache/cache_service_test.dart` — "Song"-named fixture titles renamed to their "Track" equivalents

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | This plan only edits test-fixture string literals and runs read-only verification commands (grep, flutter analyze, flutter test) — no production code path, no new input surface, no new trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-16-03 | Tampering | Test-fixture title literals across 7 test files | low | accept | Test-only data with no production code path; the automated `<verify>` re-runs each file's full test suite after every rename, so any accidental assertion mismatch is caught immediately rather than shipped. |

No package-manager (npm/pip/cargo) installs occur in this plan; the package-legitimacy gate is not applicable.
</threat_model>

<verification>
All 7 test files are free of "Song"-named fixture data and pass. The Task 2 closing audit confirms zero "song" references remain anywhere in `lib/` or `test/` except the deferred `publicapi.yml` Songs tag, `flutter analyze` is clean, and the full `flutter test` suite is green — satisfying all 4 ROADMAP Phase 16 success criteria.
</verification>

<success_criteria>
- `flutter test` (full suite) — 0 failures
- `flutter analyze` — 0 issues
- `grep -ri song` across `lib/` and `test/` (`.dart`/`.arb`), excluding `lib/api/publicapi.yml`, returns zero matches
- `lib/features/songs/` does not exist
</success_criteria>

<output>
Create `.planning/phases/16-track-terminology-rename/16-02-SUMMARY.md` when done
</output>
