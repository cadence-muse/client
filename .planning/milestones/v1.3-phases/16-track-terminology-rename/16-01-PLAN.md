---
phase: 16
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/features/songs/tracks_screen.dart
  - lib/features/tracks/tracks_screen.dart
  - lib/navigation/root_scaffold.dart
  - lib/l10n/app_en.arb
  - lib/l10n/app_ru.arb
  - lib/generated/app_localizations.dart
  - lib/generated/app_localizations_en.dart
  - lib/generated/app_localizations_ru.dart
  - lib/features/home/home_screen.dart
  - lib/features/home/band_picker_sheet.dart
  - test/features/tracks/tracks_screen_test.dart
  - test/regression/offline_trust_regression_test.dart
  - test/features/home/home_screen_test.dart
autonomous: true
requirements: [RENAME-01]

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The homepage Quick Actions row's track button renders l10n.homeAddTrackButton, reading 'Add Track' in English and 'Добавить трек' in Russian — the app's last surviving 'Add Song'/'Добавить песню' text is gone (ROADMAP criterion 1, D-02)"
    - "lib/features/songs/ does not exist on disk; TracksScreen lives at lib/features/tracks/tracks_screen.dart and is wired into root_scaffold.dart's screens list unchanged (ROADMAP criterion 2, D-01)"
    - "No class under lib/ has a 'Song'-prefixed name — TracksScreen (already correctly named pre-phase) remains the only screen class serving the Tracks tab (ROADMAP criterion 2)"
    - "lib/l10n/app_en.arb and lib/l10n/app_ru.arb contain zero keys or values containing the substring 'song' (case-insensitive); homeAddTrackButton is the only key in that ARB position (ROADMAP criterion 3, D-02)"
    - "lib/generated/app_localizations.dart, app_localizations_en.dart, and app_localizations_ru.dart carry a homeAddTrackButton getter and no stale homeAddSongButton getter after flutter gen-l10n regeneration (ROADMAP criterion 4)"
  artifacts:
    - path: "lib/features/tracks/tracks_screen.dart"
      provides: "TracksScreen class, moved from lib/features/songs/ with unchanged content"
    - path: "lib/l10n/app_en.arb"
      provides: "homeAddTrackButton key (EN: 'Add Track'), replacing homeAddSongButton"
    - path: "lib/l10n/app_ru.arb"
      provides: "homeAddTrackButton key (RU: 'Добавить трек'), replacing homeAddSongButton"
    - path: "lib/generated/app_localizations.dart"
      provides: "regenerated homeAddTrackButton getter declaration (flutter gen-l10n output, not hand-edited)"
  key_links:
    - from: "lib/navigation/root_scaffold.dart import"
      to: "lib/features/tracks/tracks_screen.dart"
      via: "../features/tracks/tracks_screen.dart import"
      pattern: "import '../features/tracks/tracks_screen.dart';"
    - from: "lib/features/home/home_screen.dart's track quick-action ElevatedButton.icon"
      to: "l10n.homeAddTrackButton"
      via: "Text(l10n.homeAddTrackButton) label"
      pattern: "l10n.homeAddTrackButton"
  prohibitions:
    - statement: "MUST NOT touch or rename lib/api/publicapi.yml's Songs tag or any other publicapi.yml content — explicitly deferred to Phase 17 (API Contract Sync) per D-04"
      status: unresolved
    - statement: "MUST NOT leave a stale generated app_localizations*.dart getter referencing the old homeAddSongButton name after the ARB rename — flutter gen-l10n must regenerate all three generated files with zero old-name survivors"
      status: unresolved
    - statement: "MUST NOT change any runtime behavior or logic while renaming (button enablement conditions, navigation tab order/index, provider invalidation) — this is a pure identifier/string/directory rename with zero functional change"
      status: unresolved
---

<objective>
RENAME-01's client-code-only rename sweep, part 1 of 2. Task 1 (tracer) moves `lib/features/songs/tracks_screen.dart` into the already-existing `lib/features/tracks/` directory and wires the single import site plus its two forced-consequence test-file updates end-to-end (D-01/D-06). Task 2 (expansion) renames the last surviving "song" ARB key (`homeAddSongButton` -> `homeAddTrackButton`), regenerates the localization artifacts, sweeps the "Add Song" code comments, and updates the ARB key's dependent test assertions (D-02/D-03/D-06).

Purpose: eliminate the lib/ directory-name split and the one surviving user-facing "Add Song" string that predate the v1.0 track feature, per RENAME-01. Pure rename/cleanup — no new capability, no behavior change.

Output: `lib/features/songs/` is gone; `homeAddTrackButton` is the live ARB key everywhere; the Home feature and its tests contain zero "song" references.
</objective>

<flagged_assumptions>
Spec-less probe fallback (no phase SPEC.md exists for Phase 16) surfaced 2 generic edge-probe items that do not resolve against RENAME-01's actual shape — flagged here per protocol rather than silently dropped:

- Edge probe "empty/single-element/null input" — NOT APPLICABLE. RENAME-01 covers static renaming of source identifiers, directory names, and string literals; there is no runtime input-processing surface in this phase to exercise with an empty/single/null case.
- Edge probe "whose definition of length/equality applies — bytes, code points, grapheme clusters, or normalized form" — NOT APPLICABLE. This phase performs literal identifier/string-literal substitution only (e.g. `homeAddSongButton` -> `homeAddTrackButton`, "Add Song" -> "Add Track", "Добавить песню" -> "Добавить трек"); no code in scope compares string length or equality at runtime, so no encoding-sensitive logic exists to test.

Prohibition-probe recall (Stage 1/2, adversarial "what could this rename silently become that the author would not want") surfaced the 3 kept items now authored in this plan's `must_haves.prohibitions` (publicapi.yml scope creep, stale generated artifacts, and a behavior change smuggled into a rename). All other Stage-1 candidates (breaking unrelated tests, losing git blame, general code-style drift) were dropped as routine engineering already owned by the automated `<verify>` commands and code review.
</flagged_assumptions>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-track-terminology-rename/16-CONTEXT.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Merge lib/features/songs/ into lib/features/tracks/, wired through navigation and its dependent tests</name>
  <reversibility rating="reversible">A directory move plus one import update across 3 files, trivially undoable via git revert (per D-01).</reversibility>
  <files>lib/features/songs/tracks_screen.dart, lib/features/tracks/tracks_screen.dart, lib/navigation/root_scaffold.dart, test/features/tracks/tracks_screen_test.dart, test/regression/offline_trust_regression_test.dart</files>
  <read_first>
    lib/features/songs/tracks_screen.dart (the file being moved; TracksScreen ConsumerWidget class, content unchanged by the move)
    lib/navigation/root_scaffold.dart (sole import at line 8; TracksScreen() used at line 26 in the 5-item screens list, Home/Bands/Tracks/Setlists/Profile per D-21 — order unchanged)
    test/features/tracks/tracks_screen_test.dart (import at line 6)
    test/regression/offline_trust_regression_test.dart (cachedScreens list literal at line 34, positioned between band_detail_screen.dart and track_list_screen.dart)
    .planning/phases/16-track-terminology-rename/16-CONTEXT.md (D-01/D-06 — exact scope and reversibility rationale)
  </read_first>
  <action>
    Move `lib/features/songs/tracks_screen.dart` to `lib/features/tracks/tracks_screen.dart` via `git mv lib/features/songs/tracks_screen.dart lib/features/tracks/tracks_screen.dart` (D-01) — the file's content and its `TracksScreen` class are unchanged, only the parent directory changes; `lib/features/songs/` disappears once it holds no tracked files (delete the directory if `git mv` leaves it empty on disk).

    In `lib/navigation/root_scaffold.dart` line 8, change the import from `../features/songs/tracks_screen.dart` to `../features/tracks/tracks_screen.dart`. The `TracksScreen()` usage at line 26 in the `screens` list needs no change since the class name is unchanged.

    As a forced consequence of the move (D-06), update `test/features/tracks/tracks_screen_test.dart` line 6's import from `package:cadence/features/songs/tracks_screen.dart` to `package:cadence/features/tracks/tracks_screen.dart`, and `test/regression/offline_trust_regression_test.dart` line 34's `cachedScreens` list entry from `'lib/features/songs/tracks_screen.dart'` to `'lib/features/tracks/tracks_screen.dart'`, keeping its existing position in the list.
  </action>
  <verify>
    <automated>test ! -e lib/features/songs && flutter analyze lib/navigation/root_scaffold.dart lib/features/tracks/tracks_screen.dart test/features/tracks/tracks_screen_test.dart test/regression/offline_trust_regression_test.dart && flutter test test/features/tracks/tracks_screen_test.dart test/regression/offline_trust_regression_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `lib/features/songs/` no longer exists on disk (directory and its file both gone)
    - `lib/features/tracks/tracks_screen.dart` exists and its `TracksScreen` class body is unchanged from the pre-move file (no logic edits)
    - `lib/navigation/root_scaffold.dart` imports `../features/tracks/tracks_screen.dart` and still renders `TracksScreen()` third in the `screens` list
    - `test/features/tracks/tracks_screen_test.dart` and `test/regression/offline_trust_regression_test.dart` both reference the new `lib/features/tracks/tracks_screen.dart` path and pass
  </acceptance_criteria>
  <done>The songs/ directory is gone, tracks_screen.dart lives under tracks/, root_scaffold.dart and both dependent test files reference the new path, and flutter analyze/flutter test are clean for the touched files.</done>
</task>

<task type="auto">
  <name>Task 2: Rename homeAddSongButton ARB key to homeAddTrackButton and sweep "Add Song" comments</name>
  <reversibility rating="reversible">An ARB key rename plus a regenerated localization file and a handful of string literals; git revert restores the old key and label instantly (per D-02).</reversibility>
  <files>lib/l10n/app_en.arb, lib/l10n/app_ru.arb, lib/generated/app_localizations.dart, lib/generated/app_localizations_en.dart, lib/generated/app_localizations_ru.dart, lib/features/home/home_screen.dart, lib/features/home/band_picker_sheet.dart, test/features/home/home_screen_test.dart</files>
  <read_first>
    lib/l10n/app_en.arb (line 194, sibling keys homeAddBandButton at line 193 / homeAddSetlistButton at line 195)
    lib/l10n/app_ru.arb (line 194, same sibling pattern)
    lib/features/home/home_screen.dart (comment at lines 64-68, comment at line 128, button label at line 145)
    lib/features/home/band_picker_sheet.dart (doc comment at lines 9-10)
    test/features/home/home_screen_test.dart (lines 28, 79, 108-111, 146-150, 342, 365, 446)
    .planning/phases/16-track-terminology-rename/16-CONTEXT.md (D-02/D-03/D-06 — exact wording)
  </read_first>
  <action>
    In `lib/l10n/app_en.arb` line 194, rename the key `"homeAddSongButton": "Add Song"` to `"homeAddTrackButton": "Add Track"`, keeping its position between the `homeAddBandButton` and `homeAddSetlistButton` sibling keys (D-02). In `lib/l10n/app_ru.arb` line 194, rename `"homeAddSongButton": "Добавить песню"` to `"homeAddTrackButton": "Добавить трек"` (D-02) — "трек" matches the terminology already used in `trackCount`/`navTracks`/`commonAddTracks`. Run `flutter gen-l10n` to regenerate `lib/generated/app_localizations.dart`, `app_localizations_en.dart`, and `app_localizations_ru.dart` from the edited ARB sources — do not hand-edit these generated files; confirm the regeneration removes the old getter (currently declared at `app_localizations.dart:807`, defined at `app_localizations_en.dart:416` and `app_localizations_ru.dart:424`) and adds `homeAddTrackButton` in its place.

    In `lib/features/home/home_screen.dart`: change line 145's `label: Text(l10n.homeAddSongButton)` to `label: Text(l10n.homeAddTrackButton)`; change line 66's comment text from "Add Song"/"Add Setlist" to "Add Track"/"Add Setlist"; change line 128's comment text from "Add Song/Add Setlist" to "Add Track/Add Setlist" (D-03). In `lib/features/home/band_picker_sheet.dart` line 9's doc comment, change "Add Song" to "Add Track" (D-03).

    As a forced consequence of the ARB rename (D-06), update `test/features/home/home_screen_test.dart`: rename every `tester.strings.homeAddSongButton` reference (currently at lines 109, 148, 365, 446) to `tester.strings.homeAddTrackButton`; rename the local variable `addSong` (declared line 108, used line 111 as `addSong.onPressed`) to `addTrack`; update the test-description strings at line 79 (from "Add Song/Add Setlist" to "Add Track/Add Setlist") and line 342 (from "Add Song" to "Add Track"); update the inline comment at line 28 from "Song\"/\"Add Setlist\"" to "Track\"/\"Add Setlist\"".
  </action>
  <verify>
    <automated>test -z "$(grep -rliE 'song' lib/l10n lib/generated/app_localizations.dart lib/generated/app_localizations_en.dart lib/generated/app_localizations_ru.dart lib/features/home 2>/dev/null)" && flutter test test/features/home/home_screen_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `lib/l10n/app_en.arb` and `lib/l10n/app_ru.arb` contain the key `homeAddTrackButton` (not `homeAddSongButton`) with values "Add Track" / "Добавить трек" respectively
    - `lib/generated/app_localizations*.dart` expose a `homeAddTrackButton` getter and no `homeAddSongButton` getter
    - `home_screen.dart` and `band_picker_sheet.dart` contain zero occurrences of the substring "song" (case-insensitive) anywhere, including comments
    - `flutter test test/features/home/home_screen_test.dart` passes with 0 failures
  </acceptance_criteria>
  <done>homeAddTrackButton is the live ARB-backed key everywhere it's used (source, generated getters, and test assertions); every "Add Song" comment/string in the Home feature and its test is now "Add Track"; localization tests pass.</done>
</task>

</tasks>

## Artifacts this plan produces

- `lib/features/tracks/tracks_screen.dart` — moved from `lib/features/songs/tracks_screen.dart`; `TracksScreen` class and its content unchanged
- `homeAddTrackButton` — new ARB key/generated getter (EN "Add Track", RU "Добавить трек"), replacing `homeAddSongButton` in `lib/l10n/app_en.arb`, `lib/l10n/app_ru.arb`, and the three regenerated `lib/generated/app_localizations*.dart` files
- Updated import in `lib/navigation/root_scaffold.dart` (`../features/tracks/tracks_screen.dart`)
- Updated import in `test/features/tracks/tracks_screen_test.dart` and path literal in `test/regression/offline_trust_regression_test.dart`
- Renamed local variable `addTrack` (was `addSong`) in `test/features/home/home_screen_test.dart`

No new classes, no new files beyond the moved `tracks_screen.dart`, no new dependencies.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | This plan performs no new user-input handling, no new network calls, and introduces no new trust boundary — it moves a file, renames an ARB key, and edits comments/tests with no behavior change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-16-01 | Tampering | Directory move / import rewrite (`root_scaffold.dart`) | low | accept | Purely a file-path and import-string change with no new code path; `flutter analyze` + the existing/updated tests catch any accidental logic change; no new trust boundary is introduced. |
| T-16-02 | Information Disclosure | ARB key rename regenerating `lib/generated/app_localizations*.dart` | low | accept | Generated output is deterministic from `flutter gen-l10n` against the edited ARB source; only an existing button's label text changes, no new string or data surface is added. |

No package-manager (npm/pip/cargo) installs occur in this plan; the package-legitimacy gate is not applicable.
</threat_model>

<verification>
`lib/features/songs/` is gone and `TracksScreen` lives under `tracks/`, wired through `root_scaffold.dart` and both dependent tests. `homeAddTrackButton` is the live ARB key in source, generated output, and the Home feature's UI/tests, with zero "song" references remaining in any file this plan touches.
</verification>

<success_criteria>
- `flutter test test/features/tracks/tracks_screen_test.dart test/regression/offline_trust_regression_test.dart test/features/home/home_screen_test.dart` — 0 failures
- `lib/features/songs/` does not exist
- `grep -ri song` across `lib/l10n/`, `lib/generated/app_localizations*.dart`, and `lib/features/home/` returns zero matches
</success_criteria>

<output>
Create `.planning/phases/16-track-terminology-rename/16-01-SUMMARY.md` when done
</output>
