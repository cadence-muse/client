---
phase: 15-carried-over-fixes-setlist-date-picker
plan: 03
type: execute
gap_closure: true
wave: 1
depends_on: ["15-01"]
files_modified:
  - lib/features/setlists/edit_setlist_screen.dart
  - test/features/setlists/edit_setlist_screen_test.dart
autonomous: true
requirements: [SETL-13]

estimate:
  tokens: 15000
  raw_tokens: 15000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "Opening the edit-setlist date picker for a setlist with an eventDate older than 5 years or more than 2 years in the future does not throw and initialDate is clamped into [firstDate, lastDate]"
  artifacts:
    - path: "lib/features/setlists/edit_setlist_screen.dart"
      provides: "_showDatePickerDialog clamps a successfully-parsed initialDate into [firstDate, lastDate] before passing it to showDatePicker, in addition to the existing parse-failure fallback to now"
    - path: "test/features/setlists/edit_setlist_screen_test.dart"
      provides: "regression widget test proving a persisted eventDate more than 5 years in the past opens the picker without throwing and clamps initialDate to firstDate"
  key_links:
    - from: "edit_setlist_screen.dart _showDatePickerDialog's DateTime.parse(_dateController.text) success branch"
      to: "showDatePicker(initialDate: ...)"
      via: "if (initialDate.isBefore(firstDate)) / else if (initialDate.isAfter(lastDate)) clamp inserted between the parse and the showDatePicker call"
      pattern: "isBefore\\(firstDate\\)"
---

<objective>
Close the single CRITICAL gap from `15-VERIFICATION.md` (Gap 1, same root cause as code review CR-01): `EditSetlistScreen._showDatePickerDialog` parses a persisted `eventDate` successfully but never clamps it into the picker's `[firstDate, lastDate]` window (now ∓ 5/2 years) before calling `showDatePicker()`, which asserts that invariant and throws `AssertionError` for any setlist dated more than 5 years in the past or 2 years in the future.

Purpose: Fix the crash and add the regression test that was missing (the existing malformed-date test only covers unparseable strings, never a valid-but-out-of-range date), fully satisfying SETL-13's success criterion 3.

Output: `edit_setlist_screen.dart`'s clamping logic added; one new widget test in `edit_setlist_screen_test.dart` proving the out-of-range path no longer throws and clamps to `firstDate`.

Out of scope (explicitly not bundled — see 15-REVIEW.md WR-01, WR-02, IN-01): `CreateSetlistScreen`'s picker-reseeding UX regression, the missing `mounted` guard after `await showDatePicker`, and extracting the duplicated date-window computation. None of these are the verification blocker; bundling them would touch files/behavior outside this gap's scope.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-carried-over-fixes-setlist-date-picker/15-VERIFICATION.md
@.planning/phases/15-carried-over-fixes-setlist-date-picker/15-REVIEW.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Clamp EditSetlistScreen's initialDate into the picker's valid range</name>
  <files>lib/features/setlists/edit_setlist_screen.dart, test/features/setlists/edit_setlist_screen_test.dart</files>
  <read_first>
    lib/features/setlists/edit_setlist_screen.dart:117-142 (_showDatePickerDialog — firstDate/lastDate computed at lines 119-120, unclamped DateTime.parse at line 124, showDatePicker call at lines 131-136)
    test/features/setlists/edit_setlist_screen_test.dart:1-89 (currentSetlist fixture, buildApiClient/wrap/openEditSetlistScreen helpers, TextFormField index convention: name(0), location(1), date(2))
    test/features/setlists/edit_setlist_screen_test.dart:309-361 (the two existing date-picker tests this new test sits between: "confirming the picker with the existing date unchanged..." and "a malformed persisted eventDate falls back to today...")
  </read_first>
  <behavior>
    - Given a persisted `eventDate` more than 5 years before today (e.g. `'2020-01-01'` when today is 2026-08-27), tapping the date field opens `showDatePicker` without throwing, and confirming with `find.text('OK')` (no day changed) sets the date field's text to `firstDate` (`now.year - 5, now.month, now.day`) formatted as `YYYY-MM-DD`.
    - Given a persisted `eventDate` within `[firstDate, lastDate]` (existing test, unchanged), the picker still opens with `initialDate` equal to the parsed date — clamping must not alter in-range dates.
    - Given an unparseable `eventDate` (existing test, unchanged), the picker still falls back to `now` — clamping must not interfere with the existing catch-block fallback.
  </behavior>
  <action>
    In `lib/features/setlists/edit_setlist_screen.dart`'s `_showDatePickerDialog` (lines 117-142), inside the `try` block that currently only does `initialDate = DateTime.parse(_dateController.text);` (line 124), add clamping immediately after the successful parse: if `initialDate.isBefore(firstDate)`, set `initialDate = firstDate`; else if `initialDate.isAfter(lastDate)`, set `initialDate = lastDate`. Leave the surrounding `catch (_) { initialDate = now; }` and the `else { initialDate = now; }` branch (empty controller text) untouched — this only adds a post-parse clamp on the success path, matching the fix recommended in `15-VERIFICATION.md` Gap 1 and `15-REVIEW.md` CR-01 verbatim. Do not touch `create_setlist_screen.dart` — `CreateSetlistScreen` always seeds `initialDate: now`, which is already within `[firstDate, lastDate]` by construction, so it has no equivalent gap.

    In `test/features/setlists/edit_setlist_screen_test.dart`, add one new `testWidgets` case between the existing "confirming the picker with the existing date unchanged..." test (ends line 329) and "a malformed persisted eventDate falls back to today..." test (starts line 331). Name it `'a persisted eventDate more than 5 years in the past clamps initialDate to firstDate without throwing'`. Follow the malformed-date test's structure: pump `wrap(apiClient, setlistOverride: const {'id': 's1', 'name': 'Old Name', 'eventLocation': 'Old Venue', 'eventDate': '2020-01-01'})`, open the screen, tap `find.byType(TextFormField).at(2)`, `pumpAndSettle()`, and assert `tester.takeException()` is `null` (proves no `AssertionError`). Then tap `find.text('OK')` to confirm the picker with the clamped `initialDate` unchanged, `pumpAndSettle()`, and assert the date field's `TextFormField.controller!.text` equals `DateTime(now.year - 5, now.month, now.day).toIso8601String().split('T')[0]` (computed locally in the test via `DateTime.now()`), proving the clamp lands exactly on `firstDate`.
  </action>
  <verify>
    <automated>flutter test test/features/setlists/edit_setlist_screen_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `edit_setlist_screen.dart`'s `_showDatePickerDialog` try-block clamps a successfully-parsed `initialDate` into `[firstDate, lastDate]` via `isBefore`/`isAfter` checks, positioned after the `DateTime.parse` call and before the `showDatePicker` invocation
    - The existing `catch (_) { initialDate = now; }` fallback and the empty-controller `else` branch are unmodified
    - `test/features/setlists/edit_setlist_screen_test.dart` contains a new test asserting `tester.takeException()` is `null` when opening the picker for an `eventDate` of `'2020-01-01'`
    - That same test asserts the date field's text is clamped to `firstDate` (`now.year - 5, now.month, now.day`, `YYYY-MM-DD`) after confirming with `OK`
    - `flutter test test/features/setlists/edit_setlist_screen_test.dart` passes with 0 failures, including the new test and all pre-existing tests in the file
  </acceptance_criteria>
  <done>EditSetlistScreen's date picker no longer throws AssertionError for a persisted eventDate outside [firstDate, lastDate]; the out-of-range value is clamped to the nearest bound; a regression test proves both the no-throw and the clamp-to-firstDate behavior; the full test file passes.</done>
</task>

</tasks>

## Artifacts this phase produces

No new files, no new classes, no new public symbols. This plan modifies the body of an existing private method (`_showDatePickerDialog` in `_EditSetlistScreenState`) and adds one `testWidgets` case to an existing test file.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Persisted `eventDate` -> `DateTime.parse` -> `initialDate` | `EditSetlistScreen` parses a server-returned date string back into a `DateTime` to seed the picker; a value outside the picker's UI-enforced window previously reached `showDatePicker` unclamped |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-15-03 | Denial of Service | `edit_setlist_screen.dart`'s `_showDatePickerDialog` (initialDate resolution) | high | mitigate | A successfully-parsed but out-of-range `eventDate` (e.g. any setlist older than 5 years) previously crashed the screen with an uncaught `AssertionError` when the user tapped the date field, making the edit form unusable for that setlist (client-side availability loss). This plan clamps `initialDate` into `[firstDate, lastDate]` after parsing, closing the crash path. Covered by this plan's new regression test. |
</threat_model>

<verification>
`EditSetlistScreen`'s `_showDatePickerDialog` clamps a successfully-parsed `initialDate` into `[firstDate, lastDate]`; the existing malformed-date fallback and in-range parse behavior are unchanged; the new widget test proves an out-of-range persisted `eventDate` (2020-01-01, more than 5 years in the past) opens the picker without throwing and lands on `firstDate` when confirmed unchanged. Full `edit_setlist_screen_test.dart` suite passes.
</verification>

<success_criteria>
- `flutter test test/features/setlists/edit_setlist_screen_test.dart` — 0 failures, including the new out-of-range clamping test
- Opening the edit-setlist date picker for any persisted `eventDate` outside `[firstDate, lastDate]` no longer throws `AssertionError`
- SETL-13 success criterion 3 (native date picker, no crash) fully satisfied per `15-VERIFICATION.md`
</success_criteria>

<output>
Create `.planning/phases/15-carried-over-fixes-setlist-date-picker/15-03-SUMMARY.md` when done
</output>
</content>
