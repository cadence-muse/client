---
phase: 15
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/features/bands/band_detail_screen.dart
  - test/features/bands/band_detail_screen_test.dart
  - .planning/milestones/v1.0-phases/02-bands/02-VERIFICATION.md
autonomous: true
requirements: [BAND-13, QA-01]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "User can tap the copy-invite-code button on band_detail_screen.dart while offline (isOnline: false) and the code copies to the clipboard, showing the Copied! snackbar, with no isOnline gate blocking it (D-05)"
    - "Tapping Copy multiple times in rapid succession, including while offline, always results in the exact invite code on the clipboard with no error — Clipboard.setData is a single synchronous call with no shared mutable state (_isSubmitting, in-flight futures) guarding it, so there is no interruption/parallelism hazard to guard against (edge: concurrency, verification: explicit)"
    - "All other band actions on band_detail_screen.dart (rotate/regenerate invite code, edit, delete, leave, remove-member) remain gated behind isOnline exactly as before this fix (D-05 scope boundary)"
    - "02-VERIFICATION.md's four previously-flagged gaps (Hive deep-convert, mutation error handling, band-rename list propagation, background-refresh version guard) are independently re-verified against current code and re-stamped resolved with current file:line evidence (D-06/D-07)"
    - "When no local mutation occurs during a background refresh (capturedVersion == the live _version — the exact-equal/touching case), the version guard in bands_provider.dart falls through and applies the fetched data normally; it only discards on inequality, never on the equal case (edge: adjacency, verification: explicit)"
    - "_HiveStore.get() short-circuits a null Hive read to null before _deepConvert is ever called, and _deepConvert itself returns an empty typed Map/List (never throws) when given an empty Map or List input (edge: empty, verification: explicit)"
    - "BandsListData.renameBand()/patchBandOwner() preserve the original list's element order exactly — each patches the matching band in place via a positional for-comprehension over the current list, never re-sorting or re-inserting at a different index (edge: ordering, verification: explicit)"
  artifacts:
    - path: "lib/features/bands/band_detail_screen.dart"
      provides: "copy IconButton with an unconditional Tooltip.message and onPressed (no isOnline reference)"
    - path: "test/features/bands/band_detail_screen_test.dart"
      provides: "widget test proving offline copy works end-to-end (tap -> clipboard -> snackbar) with isOnline: false"
    - path: ".planning/milestones/v1.0-phases/02-bands/02-VERIFICATION.md"
      provides: "re-stamped status/score frontmatter and a new audit_acknowledged (v1.3) entry citing current evidence for all 4 gaps"
  key_links:
    - from: "band_detail_screen.dart copy IconButton.onPressed"
      to: "_copyInviteCode(context, inviteCode) -> Clipboard.setData"
      via: "unconditional callback (no isOnline ternary)"
      pattern: "onPressed: \\(\\) => _copyInviteCode"
    - from: "02-VERIFICATION.md audit_acknowledged frontmatter"
      to: "current source evidence (cache_service.dart, bands_provider.dart, edit_band_screen.dart, 6 mutation-catch sites)"
      via: "re-verification during Task 2, citing re-located file:line evidence"
      pattern: "audit_acknowledged"
  prohibitions:
    - statement: "MUST NOT remove the isOnline gate from any band action other than the invite-code copy button — rotate/regenerate, edit, delete, leave, and remove-member must remain gated exactly as before (D-05 scope boundary)"
      status: resolved
      verification: judgment
    - statement: "MUST NOT log, transmit to analytics, or persist the invite code anywhere besides the system clipboard as part of this offline fix — it stays a purely local, ephemeral copy operation"
      status: resolved
      verification: judgment
    - statement: "MUST NOT mark any of the 4 gaps resolved without independently re-reading the current source for that gap during this task — no fabricated or CONTEXT.md-trusted-without-verification evidence"
      status: resolved
      verification: judgment
    - statement: "MUST NOT delete or overwrite the original gap descriptions/dates in 02-VERIFICATION.md's history — updates append/patch in place, preserving the audit trail per the file's existing audit_acknowledged precedent"
      status: resolved
      verification: judgment
---

<objective>
Two independent, small carried-over fixes bundled per ROADMAP.md Phase 15 (matching the phase's own bundling rationale — both are low-risk and mutually independent, and neither touches files the other plan modifies). Task 1 (tracer) removes the incorrect `isOnline` gate from the band-detail invite-code copy button per BAND-13/D-05, restoring pre-v1.1 always-tappable clipboard-copy behavior end-to-end (tap -> clipboard -> snackbar), with no network dependency since clipboard writes are local. Task 2 re-verifies and re-stamps `02-VERIFICATION.md`'s 4 carried-over gaps per QA-01/D-06/D-07, using independently-gathered current-code evidence rather than trusting prior notes.

Purpose: BAND-13 fixes a real regression (WR-01 from v1.1 Phase 8 code review) that broke the app's offline-first Core Value on a purely-local action; QA-01 closes out a stale, since-superseded verification report so it no longer misrepresents the current (already-fixed) state of the codebase.

Output: Copy button works offline; `02-VERIFICATION.md` accurately reflects all 4 gaps as resolved with current evidence.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-carried-over-fixes-setlist-date-picker/15-CONTEXT.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: BandDetailScreen — copy invite code works offline, end-to-end</name>
  <files>lib/features/bands/band_detail_screen.dart, test/features/bands/band_detail_screen_test.dart</files>
  <read_first>
    lib/features/bands/band_detail_screen.dart (invite-code Row at lines 245-286: copy IconButton at 255-265, rotate/regenerate IconButton at 266-283; _copyInviteCode at 364-371)
    test/features/bands/band_detail_screen_test.dart (existing "tapping Copy places the trimmed invite code on the clipboard" test's SystemChannels.platform mock pattern, and the wrap()/band() fixture helpers)
    .planning/phases/15-carried-over-fixes-setlist-date-picker/15-CONTEXT.md (D-05 — exact scope boundary)
  </read_first>
  <action>
    In `_buildContent`'s invite-code `Row` (currently lines 255-265), remove the `isOnline` ternary from BOTH the copy `Tooltip.message` (change to the unconditional `l10n.bandDetailCopyTooltip`) and the copy `IconButton.onPressed` (change to the unconditional `() => _copyInviteCode(context, inviteCode)`). This is the `IconButton` with `icon: const Icon(Icons.content_copy)`.

    Leave every other control on this screen exactly as it is today: the rotate/regenerate `IconButton` (`icon: Icons.refresh`, currently lines 266-283, still gated `isOnline ? () => showDialog(...) : null`), the AppBar edit icon (lines 62-79), the Delete `ListTile` (lines 307-332), the Leave `ListTile` (lines 333-358), and the member-row `PopupMenuButton` (lines 164-234) — per D-05, only the copy button's gate is removed. Do not modify `_copyInviteCode` itself (lines 364-371); it already does `Clipboard.setData` + shows `bandDetailCopiedSnackbar` and needs no change.

    Add a widget test to `test/features/bands/band_detail_screen_test.dart`: pump `wrap(apiClient, cacheService, isOnline: false)` with a cached band, mock `SystemChannels.platform`'s `Clipboard.setData` call exactly as the existing online copy test does, tap the `Icons.content_copy` `IconButton`, and assert (a) its `onPressed` is non-null while offline, (b) its wrapping `Tooltip.message` equals `tester.strings.bandDetailCopyTooltip` (not `commonRequiresConnection`), (c) the mocked clipboard call received the trimmed invite code, and (d) `find.text(tester.strings.bandDetailCopiedSnackbar)` finds one widget.
  </action>
  <verify>
    <automated>flutter test test/features/bands/band_detail_screen_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - Copy `IconButton.onPressed` and its `Tooltip.message` contain no `isOnline` reference anywhere in `band_detail_screen.dart`
    - Rotate/edit/delete/leave/remove-member controls are byte-identical to their pre-task state (still gated on `isOnline`)
    - `flutter test test/features/bands/band_detail_screen_test.dart` passes with 0 failures, including the new offline-copy test
  </acceptance_criteria>
  <done>Copy button is unconditionally enabled and tappable with isOnline: false, writing the invite code to the clipboard and showing the Copied! snackbar; every other band action remains isOnline-gated; all tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: QA-01 — re-verify and re-stamp 02-VERIFICATION.md's 4 gaps</name>
  <files>.planning/milestones/v1.0-phases/02-bands/02-VERIFICATION.md</files>
  <read_first>
    .planning/milestones/v1.0-phases/02-bands/02-VERIFICATION.md (full current gap language, frontmatter, and the existing v1.2 audit_acknowledged block to mirror)
    lib/cache/cache_service.dart (Hive deep-convert gap — _deepConvert static method inside _HiveStore)
    lib/providers/bands_provider.dart (background-refresh version guard gap — _version field + capturedVersion check in both BandsListData and BandDetailData)
    lib/features/bands/edit_band_screen.dart (band-rename list propagation gap — ref.exists(bandsListDataProvider) guard before .renameBand())
    lib/features/bands/create_band_screen.dart, lib/features/bands/join_band_dialog.dart, lib/features/bands/confirm_delete_band_dialog.dart, lib/features/bands/confirm_leave_band_dialog.dart, lib/features/bands/confirm_remove_member_dialog.dart (mutation error handling gap — all 6+ catch sites)
  </read_first>
  <action>
    Independently re-verify each of the 4 gaps against current code — do not trust `15-CONTEXT.md`'s or `15-PATTERNS.md`'s line-number citations verbatim, they may have drifted; re-locate every citation from the files listed in `read_first`. Confirm: (1) Hive deep-convert — a `_deepConvert()` static method exists inside `_HiveStore` in `lib/cache/cache_service.dart`, recursively normalizing nested `Map`/`List` values from a raw Hive read before `.get()` returns them; (2) mutation error handling — every mutation call site (`create_band_screen.dart`, `join_band_dialog.dart`, `edit_band_screen.dart`, `confirm_delete_band_dialog.dart`, `confirm_leave_band_dialog.dart`, `confirm_remove_member_dialog.dart`) has a `catch (_)` fallback after `on ApiException catch (e)`, setting a generic `commonSomethingWentWrong`-style error message; (3) band-rename list propagation — `edit_band_screen.dart`'s `_submit()` guards `if (ref.exists(bandsListDataProvider))` before calling `.read(bandsListDataProvider.notifier).renameBand(bandId, name)`; (4) background-refresh version guard — `lib/providers/bands_provider.dart`'s `BandsListData` and `BandDetailData` each declare an `int _version` field bumped by every local-mutation method, and `_doRefresh()` captures it before the network await and discards the fetched result via `if (_version != capturedVersion) return;` if a mutation landed in the meantime.

    Update `.planning/milestones/v1.0-phases/02-bands/02-VERIFICATION.md`'s YAML frontmatter: for each of the 4 `gaps` entries, update its `status`/evidence text with the current file:line citations found during this re-verification (do not delete the original `reason`/`artifacts`/`missing` text — append/patch the resolution evidence alongside it, preserving the original gap description as historical record). Update the top-level `status` field to reflect all 4 gaps resolved (e.g. `resolved` or `4/4 gaps resolved`, matching the file's existing status-field vocabulary) and update `score` to state `4/4 previously-blocked requirements now resolved`. Add a new `audit_acknowledged` entry (the file already has one from the v1.2 re-check — do not overwrite it, append a new one): `milestone: v1.3`, `at: <today's date, ISO format>`, `status: resolved`.
  </action>
  <verify>
    <automated>grep -c "milestone: v1.3" .planning/milestones/v1.0-phases/02-bands/02-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `02-VERIFICATION.md`'s frontmatter `status` no longer reads `gaps_found`
    - All 4 gap entries in the frontmatter carry a current file:line citation gathered during this task (not copy-pasted from `15-CONTEXT.md`/`15-PATTERNS.md` unverified)
    - A new `audit_acknowledged` block with `milestone: v1.3` is appended after the existing v1.2 block, not replacing it
    - `grep -c "milestone: v1.3" .planning/milestones/v1.0-phases/02-bands/02-VERIFICATION.md` returns exactly `1`
  </acceptance_criteria>
  <done>02-VERIFICATION.md accurately reflects all 4 gaps as independently re-verified and resolved, with current evidence and a preserved audit trail.</done>
</task>

</tasks>

## Artifacts this phase produces

No new source files. This plan modifies 1 existing screen, 1 existing test file, and 1 existing documentation file in place:

- `lib/features/bands/band_detail_screen.dart` — copy `IconButton`'s `onPressed`/`Tooltip.message` lose their `isOnline` conditional
- `test/features/bands/band_detail_screen_test.dart` — 1 new `testWidgets` case (offline copy end-to-end)
- `.planning/milestones/v1.0-phases/02-bands/02-VERIFICATION.md` — frontmatter `status`/`score` updated, 4 gap entries carry current evidence, new `audit_acknowledged` (v1.3) entry appended

No new classes, no new files, no new ARB keys, no new dependencies.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Authenticated band member -> system clipboard | The invite code is already rendered in plaintext on `band_detail_screen.dart` regardless of connectivity; this task only changes whether the *copy* action is network-gated, not what data is visible |
| `02-VERIFICATION.md` re-stamp -> git history | Documentation-only change; no runtime trust boundary, but the audit-trail integrity itself is the asset being protected (see P-Q1/P-Q2 prohibitions) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-15-03 | Information Disclosure | `_copyInviteCode` — invite code now copyable without a network check | low | accept | The invite code is already displayed in plaintext on the same screen regardless of `isOnline` before this fix; removing the gate on the *copy* action alone exposes nothing that wasn't already visible, and restores the pre-v1.0/pre-v1.1 always-tappable behavior (this is a regression fix, not a new capability). |
| T-15-04 | Information Disclosure | Generic `catch (_)` fallback across the 6 re-verified band-mutation sites | low | accept | The fallback shows a generic "Something went wrong" message and never surfaces raw exception text or stack traces to the UI — confirmed unchanged/not regressed during this re-verification, not a new gap introduced by this task. |
</threat_model>

<verification>
Copy `IconButton` has no `isOnline` reference and works with `isOnline: false` in the widget test; every other band action's gate is unchanged. `02-VERIFICATION.md`'s frontmatter status/score/gap-evidence and a new `audit_acknowledged` (v1.3) block are updated, with the original gap history preserved.
</verification>

<success_criteria>
- `flutter test test/features/bands/band_detail_screen_test.dart` — 0 failures
- Copy button's `onPressed`/`Tooltip.message` contain no `isOnline` reference; every other band action's `isOnline` gate is unchanged
- `02-VERIFICATION.md` frontmatter `status` no longer reads `gaps_found`; a `milestone: v1.3` `audit_acknowledged` entry exists
</success_criteria>

<output>
Create `.planning/phases/15-carried-over-fixes-setlist-date-picker/15-02-SUMMARY.md` when done
</output>
