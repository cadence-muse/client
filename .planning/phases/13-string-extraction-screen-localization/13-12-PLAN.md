---
phase: 13-string-extraction-screen-localization
plan: 12
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - lib/features/tracks/create_track_screen.dart
  - test/features/tracks/create_track_screen_test.dart
  - lib/features/tracks/edit_track_screen.dart
  - test/features/tracks/edit_track_screen_test.dart
  - lib/features/songs/tracks_screen.dart
  - test/features/tracks/tracks_screen_test.dart
autonomous: true
requirements: [I18N-04]

estimate:
  tokens: 65000
  raw_tokens: 44000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Switching to Russian replaces every visible string on the create-track screen, edit-track screen, and the global cross-band Tracks tab (Songs feature) — no hardcoded English remains in any of the three"
    - "create_track_screen_test.dart, edit_track_screen_test.dart, and tracks_screen_test.dart assert against tester.strings.* instead of hardcoded English literals"
  artifacts:
    - lib/features/tracks/create_track_screen.dart
    - lib/features/tracks/edit_track_screen.dart
    - lib/features/songs/tracks_screen.dart
  key_links:
    - "tester.strings (from 13-01's test/test_strings.dart) -> all three migrated test files"
  prohibitions: []
---

<objective>
Localize the track create/edit forms (`create_track_screen.dart`, `edit_track_screen.dart` — near-identical duration/title/artist validation logic duplicated between the two, per their own existing doc comments) and the global cross-band Tracks tab (`lib/features/songs/tracks_screen.dart`, class `TracksScreen` — one of the 5 `IndexedStack`-kept-alive bottom-nav tabs), plus their tests.

Purpose: `create_track_screen.dart`/`edit_track_screen.dart` share nearly every validator message verbatim (title/artist/duration format/duration range — all already added as `commonX` shared keys by 13-01 Task 2); `tracks_screen.dart` is the last of the 5 bottom-nav tabs to be localized in this phase's Wave-2 plan set.
Output: All three files and their tests localized end-to-end.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-string-extraction-screen-localization/13-CONTEXT.md
@.planning/phases/13-string-extraction-screen-localization/13-01-PLAN.md
</context>

<artifacts_produced>
## Artifacts this plan adds to app_en.arb / app_ru.arb (landed by 13-01 Task 2, consumed here)

- createTrackAppBarTitle -> "Add track" -> "Добавить трек"
- createTrackSaveButton -> "Save track" -> "Сохранить трек"
- createTrackAddedSnackbar({title}, String) -> "{title} added!" -> "«{title}» добавлен!" (masculine agreement — "трек" is masculine, no gender-conflict risk unlike the band/setlist "created!" merge that was rejected in 13-01)
- editTrackAppBarTitle -> "Edit track" -> "Изменить трек"
- tracksTabEmptyTitle -> "No tracks" -> "Треков нет"
- tracksTabEmptyDescription -> "Create tracks in a band to see them here." -> "Создавайте треки в группе, чтобы они отображались здесь."
- tracksTabViewBandsButton -> "View bands" -> "Перейти к группам"

(`navTracks`, `commonTitleLabel`, `commonArtistLabel`, `commonDurationLabel`, `commonTempoLabel`, `commonKeyLabel`, `commonNotesLabel`, `commonEnterTrackTitle`, `commonEnterArtistName`, `commonEnterWholeNumber`, `commonDurationFormatHint`, `commonDurationNegative`, `commonDurationSecondsRange`, `commonDurationHelperText`, `commonRequiresConnection`, `commonSomethingWentWrong`, `commonSave`, `commonAllBandsFilter`, `commonCouldntLoadTracks`, `commonConnectionError`, `commonRetry` were all landed by 13-01 Task 2 — reused here, not redeclared.)

No task in this plan touches the ARB files.
</artifacts_produced>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: create_track_screen.dart + test</name>
  <files>lib/features/tracks/create_track_screen.dart, test/features/tracks/create_track_screen_test.dart</files>
  <read_first>
    lib/features/tracks/create_track_screen.dart
    test/features/tracks/create_track_screen_test.dart
    lib/l10n/app_en.arb
  </read_first>
  <action>
    Add `import '../../generated/app_localizations.dart';`. In `build()`, add `final l10n = AppLocalizations.of(context)!;`. Replace `AppBar(title: const Text('Add track'))` -> `AppBar(title: Text(l10n.createTrackAppBarTitle))`. `labelText: 'Title'` -> `labelText: l10n.commonTitleLabel`. The title validator's `'Enter a track title'` -> `l10n.commonEnterTrackTitle`. `labelText: 'Artist'` -> `labelText: l10n.commonArtistLabel`. The artist validator's `'Enter an artist name'` -> `l10n.commonEnterArtistName`. `labelText: 'Duration'` -> `labelText: l10n.commonDurationLabel`. `helperText: 'e.g. 2:30 for 2 minutes 30 seconds'` -> `helperText: l10n.commonDurationHelperText`. Leave `hintText: '0:00'` unchanged — a pure numeral/format example, not translatable prose. In `_durationValidator`: both `'Enter duration in mm:ss format (e.g. 0:30)'` branches -> `l10n.commonDurationFormatHint`; `'Duration cannot be negative'` -> `l10n.commonDurationNegative`; `'Seconds must be 0–59 (e.g. 2:30, not 2:75)'` -> `l10n.commonDurationSecondsRange`. Note `_durationValidator`/`_wholeNumberValidator` are instance methods on `_CreateTrackScreenState`, not passed a `context` — capture `l10n` once in `build()` and reference it from these methods via the enclosing `State`'s own `context` getter (add `final l10n = AppLocalizations.of(context)!;` as the first line of each validator method body, since they're called during `Form.validate()` outside `build()`'s scope). `labelText: 'Tempo (BPM)'` -> `labelText: l10n.commonTempoLabel`; `_wholeNumberValidator`'s `'Enter a whole number'` -> `l10n.commonEnterWholeNumber`. `labelText: 'Key'` -> `labelText: l10n.commonKeyLabel` (the `DropdownMenuItem`s' `Text(key)` render musical key names like "C#m" — data, not translatable, leave unchanged). `labelText: 'Notes'` -> `labelText: l10n.commonNotesLabel`. `message: isOnline ? '' : 'Requires connection'` -> `isOnline ? '' : l10n.commonRequiresConnection`. `Text(isOnline ? 'Save track' : 'Requires connection')` -> `Text(isOnline ? l10n.createTrackSaveButton : l10n.commonRequiresConnection)`. In `_submit()`, capture `l10n` via `AppLocalizations.of(context)!` and replace `SnackBar(content: Text('$title added!'))` -> `SnackBar(content: Text(l10n.createTrackAddedSnackbar(title)))`, and `_errorMessage = 'Something went wrong. Please try again.'` -> `_errorMessage = l10n.commonSomethingWentWrong`.

    Migrate the test file: add `import '../../test_strings.dart';`, replace every app-copy `find.text('...')` with `tester.strings.keyName` (composing `tester.strings.createTrackAddedSnackbar(title)` with the same test-fixture title). Test fixtures stay hardcoded.
  </action>
  <verify>
    <automated>flutter test test/features/tracks/create_track_screen_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "l10n\." lib/features/tracks/create_track_screen.dart` >= 16
    - `grep -c "tester.strings" test/features/tracks/create_track_screen_test.dart` >= 1
    - `flutter test test/features/tracks/create_track_screen_test.dart` passes
  </acceptance_criteria>
  <done>create_track_screen.dart and its test are fully localized; flutter test passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: edit_track_screen.dart + test</name>
  <files>lib/features/tracks/edit_track_screen.dart, test/features/tracks/edit_track_screen_test.dart</files>
  <read_first>
    lib/features/tracks/edit_track_screen.dart
    test/features/tracks/edit_track_screen_test.dart
    lib/l10n/app_en.arb
  </read_first>
  <action>
    Same substitutions as Task 1's `create_track_screen.dart`, applied to `edit_track_screen.dart`'s identical field structure, with these differences: `AppBar(title: const Text('Edit track'))` -> `AppBar(title: Text(l10n.editTrackAppBarTitle))`; there is no "added!" SnackBar in this file (edit has no success SnackBar per the existing code — `_submit()` just pops the screen); `Text(isOnline ? 'Save' : 'Requires connection')` -> `Text(isOnline ? l10n.commonSave : l10n.commonRequiresConnection)` (bare "Save", reusing the shared key — NOT `createTrackSaveButton`, which is the compound "Save track" text unique to the create screen). Every other label/validator substitution (`commonTitleLabel`/`commonEnterTrackTitle`/`commonArtistLabel`/`commonEnterArtistName`/`commonDurationLabel`/`commonDurationHelperText`/`commonDurationFormatHint`/`commonDurationNegative`/`commonDurationSecondsRange`/`commonTempoLabel`/`commonEnterWholeNumber`/`commonKeyLabel`/`commonNotesLabel`/`commonRequiresConnection`/`commonSomethingWentWrong`) is identical to Task 1 — add `import '../../generated/app_localizations.dart';` and capture `l10n` in `build()` and in each validator method the same way.

    Migrate the test file: add `import '../../test_strings.dart';`, replace every app-copy `find.text('...')` with `tester.strings.keyName`. Test fixtures stay hardcoded.
  </action>
  <verify>
    <automated>flutter test test/features/tracks/edit_track_screen_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "l10n\." lib/features/tracks/edit_track_screen.dart` >= 15
    - `grep -c "tester.strings" test/features/tracks/edit_track_screen_test.dart` >= 1
    - `flutter test test/features/tracks/edit_track_screen_test.dart` passes
  </acceptance_criteria>
  <done>edit_track_screen.dart and its test are fully localized; flutter test passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: lib/features/songs/tracks_screen.dart (global tab) + test</name>
  <files>lib/features/songs/tracks_screen.dart, test/features/tracks/tracks_screen_test.dart</files>
  <read_first>
    lib/features/songs/tracks_screen.dart
    test/features/tracks/tracks_screen_test.dart
    lib/l10n/app_en.arb
  </read_first>
  <action>
    Add `import '../../generated/app_localizations.dart';`. In `build()`, add `final l10n = AppLocalizations.of(context)!;` and re-derive in `_buildFilterDropdown`/`_buildTracksBody`/`_buildContent`/`_buildEmptyState`/`_buildError` (each has its own `context`). Replace `AppBar(title: const Text('Tracks'))` -> `AppBar(title: Text(l10n.navTracks))` (reuse — this is the "Songs" feature's screen file but its user-facing AppBar title text is identical to the nav-bar "Tracks" label). `const Text('All bands')` -> `Text(l10n.commonAllBandsFilter)` (reuse — exact-match text with `setlists_screen.dart`, per D-01). `'No tracks'` -> `l10n.tracksTabEmptyTitle`. `'Create tracks in a band to see them here.'` -> `l10n.tracksTabEmptyDescription`. `const Text('View bands')` -> `Text(l10n.tracksTabViewBandsButton)`. `_buildError`'s `"Couldn't load tracks"`/`'Please check your connection and try again.'`/`'Retry'` -> `l10n.commonCouldntLoadTracks`/`l10n.commonConnectionError`/`l10n.commonRetry`. The `Chip` showing `bandName` and the `ListTile` `title`/`subtitle`/`trailing` showing `title`/`artist`/`durationSeconds.asMinutesSeconds` are data, not localizable strings — leave unchanged.

    Migrate the test file: add `import '../../test_strings.dart';`, replace every app-copy `find.text('...')` with `tester.strings.keyName`. Test fixtures (track/band names) stay hardcoded.
  </action>
  <verify>
    <automated>flutter test test/features/tracks/tracks_screen_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "l10n\." lib/features/songs/tracks_screen.dart` >= 8
    - `grep -c "tester.strings" test/features/tracks/tracks_screen_test.dart` >= 1
    - `flutter test test/features/tracks/tracks_screen_test.dart` passes
  </acceptance_criteria>
  <done>tracks_screen.dart (global tab) and its test are fully localized; flutter test passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | All three files render already-approved ARB strings interpolated with user-typed/server-returned title/artist/band-name values (already-trusted display data used identically before this phase). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|--------------|------------------|
| T-13-14 | Information Disclosure | `createTrackAddedSnackbar({title})` interpolation | low | accept | `title` is the track the current user just submitted — no new data surfaced. |

No package installs in this plan; package-legitimacy gate not applicable.
</threat_model>

<verification>
- `flutter test test/features/tracks/create_track_screen_test.dart test/features/tracks/edit_track_screen_test.dart test/features/tracks/tracks_screen_test.dart` — all pass
- `flutter analyze lib/features/tracks/create_track_screen.dart lib/features/tracks/edit_track_screen.dart lib/features/songs/tracks_screen.dart` — no new errors
</verification>

<success_criteria>
All three files and their tests fully localized; combined with 13-11, the entire Tracks/Songs domain's localization sweep is complete, and the last of the 5 bottom-nav tabs (global Tracks tab) is now localized.
</success_criteria>

<output>
Create `.planning/phases/13-string-extraction-screen-localization/13-12-SUMMARY.md` when done
</output>
