---
phase: 13-string-extraction-screen-localization
plan: 08
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - lib/widgets/offline_no_cache_view.dart
  - lib/features/auth/login_screen.dart
autonomous: true
requirements: [I18N-04]

estimate:
  tokens: 45000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Switching to Russian replaces the offline-empty-state widget's two strings — this single shared widget covers every consuming list screen at once, per D-14"
    - "A Russian-speaking user who logs out lands on a fully localized login screen — no screen retains hardcoded English even after the auth boundary, per D-16"
  artifacts:
    - lib/widgets/offline_no_cache_view.dart
    - lib/features/auth/login_screen.dart
  key_links:
    - "offlineNoCacheTitle/offlineNoCacheDescription (from 13-01) -> every consuming screen's own test file (migrated in each of those screens' own plans, per D-07's touched-file scope)"
  prohibitions:
    - { statement: "Placeholder, machine-stub, or untranslated text must not ship in app_ru.arb as if it were reviewed Russian copy.", status: unverified, verification: manual }
---

<objective>
Localize `lib/widgets/offline_no_cache_view.dart` (D-14 — the shared "offline with nothing cached yet" empty state consumed by ~6 list screens) and `lib/features/auth/login_screen.dart` (D-16 — in scope even though ROADMAP's success-criteria list names only Home/Songs/Tracks/Bands/Setlists/Profile, because Phase 12's D-04 means the language preference survives logout, so a Russian-speaking user landing back on login after logout would otherwise see English).

Neither file has its own dedicated widget test today (`OfflineNoCacheView` is asserted from within each CONSUMING screen's test file — those assertions are migrated in each consuming screen's own plan per D-07's "touched-file" scope, not here; `login_screen.dart` similarly has no dedicated test file in this codebase).

Purpose: These are the two remaining always-visible-or-widely-consumed shared surfaces CONTEXT.md's discretion section calls out ("Any other lib/widgets/ shared files not surfaced during discussion that turn out to have hardcoded strings visible from in-scope screens — apply the same reasoning established by D-13 through D-16").
Output: Both files fully localized.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-string-extraction-screen-localization/13-CONTEXT.md
@.planning/phases/13-string-extraction-screen-localization/13-01-PLAN.md
</context>

<artifacts_produced>
## Artifacts this plan adds to app_en.arb / app_ru.arb (landed by 13-01 Task 2, consumed here)

- offlineNoCacheTitle -> "No cached data" -> "Нет сохранённых данных"
- offlineNoCacheDescription -> "Connect to the internet to load this" -> "Подключитесь к интернету, чтобы загрузить данные"
- loginAppTitle -> "Cadence" -> "Cadence" (brand name, untranslated)
- loginUsernameLabel -> "Username" -> "Имя пользователя"
- loginUsernameValidator -> "Enter a username" -> "Введите имя пользователя"
- loginPasswordLabel -> "Password" -> "Пароль"
- loginSignUpButton -> "Sign up" -> "Зарегистрироваться"
- loginLogInButton -> "Log in" -> "Войти"
- loginToggleToLogin -> "Already have an account? Log in" -> "Уже есть аккаунт? Войти"
- loginToggleToSignUp -> "Don't have an account? Sign up" -> "Нет аккаунта? Зарегистрироваться"
- loginUsernameTakenError -> "This username is already taken" -> "Это имя пользователя уже занято"
- loginInvalidCredentialsError -> "Invalid credentials" -> "Неверные учётные данные"

No task in this plan touches the ARB files.
</artifacts_produced>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: offline_no_cache_view.dart</name>
  <files>lib/widgets/offline_no_cache_view.dart</files>
  <read_first>
    lib/widgets/offline_no_cache_view.dart
    lib/l10n/app_en.arb
  </read_first>
  <action>
    Add `import '../generated/app_localizations.dart';`. Currently `OfflineNoCacheView extends StatelessWidget` with a plain `Widget build(BuildContext context)` — no Riverpod dependency needed for localization since `AppLocalizations.of(context)` only needs a `BuildContext`. Add `final l10n = AppLocalizations.of(context)!;` at the top of `build()`. Replace `'No cached data'` with `l10n.offlineNoCacheTitle`. Replace `'Connect to the internet to load this'` with `l10n.offlineNoCacheDescription`.
  </action>
  <verify>
    <automated>flutter analyze lib/widgets/offline_no_cache_view.dart</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "l10n.offlineNoCache" lib/widgets/offline_no_cache_view.dart` == 2
    - `flutter analyze lib/widgets/offline_no_cache_view.dart` reports no new errors
  </acceptance_criteria>
  <done>offline_no_cache_view.dart renders both strings via AppLocalizations.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: login_screen.dart</name>
  <files>lib/features/auth/login_screen.dart</files>
  <read_first>
    lib/features/auth/login_screen.dart
    lib/l10n/app_en.arb
  </read_first>
  <action>
    Add `import '../../generated/app_localizations.dart';`. In `build()`, add `final l10n = AppLocalizations.of(context)!;`. Replace `Text('Cadence', ...)` with `Text(l10n.loginAppTitle, ...)` (keep the existing `textAlign`/`style`). Replace `labelText: 'Username'` with `labelText: l10n.loginUsernameLabel`. Replace the username validator's `'Enter a username'` with `l10n.loginUsernameValidator`. Replace `labelText: 'Password'` with `labelText: l10n.loginPasswordLabel`. Replace the password validator's `'At least 8 characters'` with `l10n.commonAtLeast8Chars` (reuse — exact-match text with change_password_screen.dart's validator, per D-01). Replace `Text(isSignUp ? 'Sign up' : 'Log in')` with `Text(isSignUp ? l10n.loginSignUpButton : l10n.loginLogInButton)`. Replace the toggle-mode text (`isSignUp ? 'Already have an account? Log in' : "Don't have an account? Sign up"`) with `isSignUp ? l10n.loginToggleToLogin : l10n.loginToggleToSignUp`.

    In `_submit()`, capture `l10n` via `AppLocalizations.of(context)!` (this method is called from `build()`'s widget tree, `context` is available on the `State` object via `this.context`). Replace the thrown `ApiException(..., message: 'This username is already taken')` with `message: l10n.loginUsernameTakenError`. Replace `message: 'Invalid credentials'` with `message: l10n.loginInvalidCredentialsError`.
  </action>
  <verify>
    <automated>flutter analyze lib/features/auth/login_screen.dart</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "l10n\." lib/features/auth/login_screen.dart` >= 8
    - `flutter analyze lib/features/auth/login_screen.dart` reports no new errors
  </acceptance_criteria>
  <done>login_screen.dart renders every string via AppLocalizations, including the two thrown-exception messages surfaced through `_errorMessage`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Client -> Auth API (login/register) | `login_screen.dart` submits credentials to `publicApi.login`/`.register`; this plan only touches display copy, not the request/validation path itself. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|--------------|------------------|
| T-13-10 | Information Disclosure | `loginUsernameTakenError`/`loginInvalidCredentialsError` copy | low | accept | Both messages already disclosed the same information pre-phase (username-taken on signup, invalid-credentials on login) — localizing the text changes nothing about what information is revealed. |

No package installs in this plan; package-legitimacy gate not applicable.
</threat_model>

<verification>
- `flutter analyze lib/widgets/offline_no_cache_view.dart lib/features/auth/login_screen.dart` — no new errors
- Manual: pump `LoginScreen` and `OfflineNoCacheView` under `Locale('ru')` and confirm no English string remains (neither file has a dedicated widget test in this codebase; flutter analyze + code review is the available backstop, and each consuming screen's own migrated test — e.g. bands_screen_test.dart's OfflineNoCacheView assertions — provides indirect coverage of offline_no_cache_view.dart's two strings)
</verification>

<success_criteria>
Both shared surfaces fully localized; no hardcoded English string literal remains in either.
</success_criteria>

<output>
Create `.planning/phases/13-string-extraction-screen-localization/13-08-SUMMARY.md` when done
</output>
