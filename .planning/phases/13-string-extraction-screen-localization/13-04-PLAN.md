---
phase: 13-string-extraction-screen-localization
plan: 04
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - lib/features/bands/create_band_screen.dart
  - test/features/bands/create_band_screen_test.dart
  - lib/features/bands/edit_band_screen.dart
  - test/features/bands/edit_band_screen_test.dart
  - lib/features/bands/join_band_dialog.dart
  - test/features/bands/join_band_dialog_test.dart
autonomous: true
requirements: [I18N-04]

estimate:
  tokens: 55000
  raw_tokens: 37000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Switching to Russian replaces every visible string on the create-band screen, edit-band screen, and join-band dialog — no hardcoded English remains in any of the three"
    - "create_band_screen_test.dart, edit_band_screen_test.dart, and join_band_dialog_test.dart assert against tester.strings.* instead of hardcoded English literals"
  artifacts:
    - lib/features/bands/create_band_screen.dart
    - lib/features/bands/edit_band_screen.dart
    - lib/features/bands/join_band_dialog.dart
  key_links:
    - "tester.strings (from 13-01's test/test_strings.dart) -> all three migrated test files"
  prohibitions: []
---

<objective>
Localize the remaining band-creation/editing/joining surfaces: `create_band_screen.dart`, `edit_band_screen.dart`, `join_band_dialog.dart`, and their tests.

Purpose: Completes the Bands feature's localization sweep (bands_screen/band_detail_screen done in 13-01, the confirm dialogs in 13-02/13-03, this plan covers the remaining CRUD entry points).
Output: All three files and their tests localized end-to-end.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-string-extraction-screen-localization/13-CONTEXT.md
@.planning/phases/13-string-extraction-screen-localization/13-01-PLAN.md
</context>

<artifacts_produced>
## Artifacts this plan adds to app_en.arb / app_ru.arb (landed by 13-01 Task 2, consumed here)

- createBandAppBarTitle -> "Create a new band" -> "Создать новую группу"
- createBandSuccessSnackbar({name}) -> "{name} created!" -> "«{name}» создана!" (feminine agreement — "группа" is feminine)
- editBandAppBarTitle -> "Edit band" -> "Изменить группу"
- joinBandTitle -> "Join a band" -> "Присоединиться к группе"
- joinBandCodeLabel -> "Invite code" -> "Код приглашения"
- joinBandCodeHint -> "Paste the code here" -> "Вставьте код сюда"
- joinBandCodeValidator -> "Enter an invite code" -> "Введите код приглашения"
- joinBandButton -> "Join" -> "Присоединиться"
- joinBandAmbiguousSnackbar -> "Joined band!" -> "Вы присоединились к группе!"
- joinBandSuccessSnackbar({bandName}) -> "You've joined {bandName}!" -> "Вы присоединились к группе «{bandName}»!"

No task in this plan touches the ARB files.
</artifacts_produced>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: create_band_screen.dart + test</name>
  <files>lib/features/bands/create_band_screen.dart, test/features/bands/create_band_screen_test.dart</files>
  <read_first>
    lib/features/bands/create_band_screen.dart
    test/features/bands/create_band_screen_test.dart
    lib/l10n/app_en.arb
  </read_first>
  <action>
    Add `import '../../generated/app_localizations.dart';`. In `build()`, add `final l10n = AppLocalizations.of(context)!;`. Replace `AppBar(title: const Text('Create a new band'))` with `AppBar(title: Text(l10n.createBandAppBarTitle))`. Replace `labelText: 'Band name'` with `labelText: l10n.commonBandNameLabel`. Replace the validator's `'Enter a band name'` with `l10n.commonEnterBandName`. Replace `Text(isOnline ? 'Create' : 'Requires connection')` with `Text(isOnline ? l10n.commonCreate : l10n.commonRequiresConnection)`. Replace `message: isOnline ? '' : 'Requires connection'` with `isOnline ? '' : l10n.commonRequiresConnection`. In `_submit()`, capture `l10n` via `AppLocalizations.of(context)!` and replace `SnackBar(content: Text('$name created!'))` with `SnackBar(content: Text(l10n.createBandSuccessSnackbar(name)))` and `_errorMessage = 'Something went wrong. Please try again.'` with `_errorMessage = l10n.commonSomethingWentWrong`.

    Migrate the test file: add `import '../../test_strings.dart';`, replace every app-copy `find.text('...')` with `tester.strings.keyName` (or `.createBandSuccessSnackbar(name)` for the SnackBar assertion, using the same `name` test-fixture value already in the test). Test fixtures stay hardcoded.
  </action>
  <verify>
    <automated>flutter test test/features/bands/create_band_screen_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "l10n\." lib/features/bands/create_band_screen.dart` >= 6
    - `grep -c "tester.strings" test/features/bands/create_band_screen_test.dart` >= 1
    - `flutter test test/features/bands/create_band_screen_test.dart` passes
  </acceptance_criteria>
  <done>create_band_screen.dart and its test are fully localized; flutter test passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: edit_band_screen.dart + test</name>
  <files>lib/features/bands/edit_band_screen.dart, test/features/bands/edit_band_screen_test.dart</files>
  <read_first>
    lib/features/bands/edit_band_screen.dart
    test/features/bands/edit_band_screen_test.dart
    lib/l10n/app_en.arb
  </read_first>
  <action>
    Add `import '../../generated/app_localizations.dart';`. In `build()`, add `final l10n = AppLocalizations.of(context)!;`. Replace `AppBar(title: const Text('Edit band'))` with `AppBar(title: Text(l10n.editBandAppBarTitle))`. Replace `labelText: 'Band name'` with `labelText: l10n.commonBandNameLabel`. Replace the validator's `'Enter a band name'` with `l10n.commonEnterBandName`. Replace `Text(isOnline ? 'Save' : 'Requires connection')` with `Text(isOnline ? l10n.commonSave : l10n.commonRequiresConnection)`. Replace `message: isOnline ? '' : 'Requires connection'` with `isOnline ? '' : l10n.commonRequiresConnection`. In `_submit()`, capture `l10n` and replace `_errorMessage = 'Something went wrong. Please try again.'` with `_errorMessage = l10n.commonSomethingWentWrong`.

    Migrate the test file: add `import '../../test_strings.dart';`, replace every app-copy `find.text('...')` with `tester.strings.keyName`. Test fixtures stay hardcoded.
  </action>
  <verify>
    <automated>flutter test test/features/bands/edit_band_screen_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "l10n\." lib/features/bands/edit_band_screen.dart` >= 5
    - `grep -c "tester.strings" test/features/bands/edit_band_screen_test.dart` >= 1
    - `flutter test test/features/bands/edit_band_screen_test.dart` passes
  </acceptance_criteria>
  <done>edit_band_screen.dart and its test are fully localized; flutter test passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: join_band_dialog.dart + test</name>
  <files>lib/features/bands/join_band_dialog.dart, test/features/bands/join_band_dialog_test.dart</files>
  <read_first>
    lib/features/bands/join_band_dialog.dart
    test/features/bands/join_band_dialog_test.dart
    lib/l10n/app_en.arb
  </read_first>
  <action>
    Add `import '../../generated/app_localizations.dart';`. `showJoinBandDialog` is a top-level function (not a widget) — it already has a `BuildContext context` parameter, so capture `final l10n = AppLocalizations.of(context)!;` at its top before the two SnackBar branches. Replace `const SnackBar(content: Text('Joined band!'))` with `SnackBar(content: Text(l10n.joinBandAmbiguousSnackbar))`. Replace `SnackBar(content: Text("You've joined ${outcome.bandName}!"))` with `SnackBar(content: Text(l10n.joinBandSuccessSnackbar(outcome.bandName!)))`.

    In `_JoinBandDialogState.build()`, add `final l10n = AppLocalizations.of(context)!;`. Replace `const Text('Join a band')` with `Text(l10n.joinBandTitle)`. Replace `labelText: 'Invite code'` with `labelText: l10n.joinBandCodeLabel`. Replace `hintText: 'Paste the code here'` with `hintText: l10n.joinBandCodeHint`. Replace the validator's `'Enter an invite code'` with `l10n.joinBandCodeValidator`. Replace `const Text('Cancel')` with `Text(l10n.commonCancel)`. Replace `message: isOnline ? '' : 'Requires connection'` with `isOnline ? '' : l10n.commonRequiresConnection`. Replace `Text(isOnline ? 'Join' : 'Requires connection')` with `Text(isOnline ? l10n.joinBandButton : l10n.commonRequiresConnection)`. In `_submit()`, capture `l10n` via `AppLocalizations.of(context)!` and replace `_errorMessage = 'Something went wrong. Please try again.'` with `_errorMessage = l10n.commonSomethingWentWrong`.

    Migrate the test file: add `import '../../test_strings.dart';`, replace every app-copy `find.text('...')` with `tester.strings.keyName`. Test fixtures (invite codes, band names) stay hardcoded.
  </action>
  <verify>
    <automated>flutter test test/features/bands/join_band_dialog_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "l10n\." lib/features/bands/join_band_dialog.dart` >= 8
    - `grep -c "tester.strings" test/features/bands/join_band_dialog_test.dart` >= 1
    - `flutter test test/features/bands/join_band_dialog_test.dart` passes
  </acceptance_criteria>
  <done>join_band_dialog.dart and its test are fully localized; flutter test passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | These three files render already-approved ARB strings, none interpolating anything beyond server-returned/user-typed display names already trusted pre-phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|--------------|------------------|
| T-13-06 | Tampering | User-typed band name / invite code, echoed in SnackBar/validators | low | accept | Text is rendered via Flutter's `Text` widget (no HTML/JS execution context); no injection risk. Server-side validation of band names/invite codes is unchanged by this phase. |

No package installs in this plan; package-legitimacy gate not applicable.
</threat_model>

<verification>
- `flutter test test/features/bands/create_band_screen_test.dart test/features/bands/edit_band_screen_test.dart test/features/bands/join_band_dialog_test.dart` — all pass
- `flutter analyze lib/features/bands/create_band_screen.dart lib/features/bands/edit_band_screen.dart lib/features/bands/join_band_dialog.dart` — no new errors
</verification>

<success_criteria>
All three files and their tests fully localized; the Bands feature's entire localization sweep is now complete across 13-01/13-02/13-03/13-04.
</success_criteria>

<output>
Create `.planning/phases/13-string-extraction-screen-localization/13-04-SUMMARY.md` when done
</output>
