---
phase: 13-string-extraction-screen-localization
plan: 10
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - lib/features/setlists/edit_setlist_screen.dart
  - test/features/setlists/edit_setlist_screen_test.dart
  - lib/features/setlists/confirm_delete_setlist_dialog.dart
  - test/features/setlists/confirm_delete_setlist_dialog_test.dart
  - lib/features/setlists/setlist_list_screen.dart
  - test/features/setlists/setlist_list_screen_test.dart
autonomous: true
requirements: [I18N-04]

estimate:
  tokens: 60000
  raw_tokens: 40000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Switching to Russian replaces every visible string on the edit-setlist screen, delete-setlist confirm dialog, and the per-band setlist list screen — no hardcoded English remains in any of the three"
    - "edit_setlist_screen_test.dart, confirm_delete_setlist_dialog_test.dart, and setlist_list_screen_test.dart assert against tester.strings.* instead of hardcoded English literals"
  artifacts:
    - lib/features/setlists/edit_setlist_screen.dart
    - lib/features/setlists/confirm_delete_setlist_dialog.dart
    - lib/features/setlists/setlist_list_screen.dart
  key_links:
    - "tester.strings (from 13-01's test/test_strings.dart) -> all three migrated test files"
  prohibitions: []
---

<objective>
Localize the remaining Setlists-domain files that do NOT depend on 13-09's `setlist_formatting.dart` consolidation: `edit_setlist_screen.dart`, `confirm_delete_setlist_dialog.dart`, `setlist_list_screen.dart` (per-band list — uses `formatEventDate` only, not `pluralizeTracks`/`tracksAndDuration`, so it has no dependency on 13-09).

Purpose: Completes the Setlists domain's localization sweep alongside 13-09.
Output: All three files and their tests localized end-to-end.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-string-extraction-screen-localization/13-CONTEXT.md
@.planning/phases/13-string-extraction-screen-localization/13-01-PLAN.md
</context>

<artifacts_produced>
## Artifacts this plan adds to app_en.arb / app_ru.arb (landed by 13-01 Task 2, consumed here)

- editSetlistAppBarTitle -> "Edit setlist" -> "Изменить сетлист"
- editSetlistFailedError -> "Failed to save setlist. Try again." -> "Не удалось сохранить сетлист. Попробуйте ещё раз."
- confirmDeleteSetlistTitle -> "Delete setlist?" -> "Удалить сетлист?"
- confirmDeleteSetlistFailedError -> "Delete failed. Try again." -> "Не удалось удалить. Попробуйте ещё раз."
- setlistListAddButton -> "Add setlist" -> "Добавить сетлист"
- setlistListEmptyTitle -> "No setlists yet" -> "Пока нет сетлистов"
- setlistListEmptyDescription -> "Create a setlist or ask a bandmate to add one." -> "Создайте сетлист или попросите участника группы добавить его."

(`commonNameLabel`, `commonLocationLabel`, `commonDateLabel`, `commonNameRequired`, `commonSave`, `commonRequiresConnection`, `commonCancel`, `commonDelete`, `commonActionCannotBeUndone`, `commonFailedToLoadSetlists`, `commonRetry`, `navSetlists` were all landed by 13-01 Task 2 — reused here, not redeclared.)

No task in this plan touches the ARB files.
</artifacts_produced>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: edit_setlist_screen.dart + test</name>
  <files>lib/features/setlists/edit_setlist_screen.dart, test/features/setlists/edit_setlist_screen_test.dart</files>
  <read_first>
    lib/features/setlists/edit_setlist_screen.dart
    test/features/setlists/edit_setlist_screen_test.dart
    lib/l10n/app_en.arb
  </read_first>
  <action>
    Add `import '../../generated/app_localizations.dart';`. In `build()`, add `final l10n = AppLocalizations.of(context)!;`. Replace `AppBar(title: const Text('Edit setlist'))` -> `AppBar(title: Text(l10n.editSetlistAppBarTitle))`. `labelText: 'Name'` -> `labelText: l10n.commonNameLabel` (reuse — exact-match text with create_setlist_screen.dart, per D-01). The name validator's `'Name is required'` -> `l10n.commonNameRequired` (reuse). `labelText: 'Location'` -> `labelText: l10n.commonLocationLabel` (reuse). `labelText: 'Date'` -> `labelText: l10n.commonDateLabel` (reuse). `message: isOnline ? '' : 'Requires connection'` -> `isOnline ? '' : l10n.commonRequiresConnection`. `Text(isOnline ? 'Save' : 'Requires connection')` -> `Text(isOnline ? l10n.commonSave : l10n.commonRequiresConnection)`. In `_submit()`, capture `l10n` via `AppLocalizations.of(context)!` and replace `_errorMessage = 'Failed to save setlist. Try again.'` with `_errorMessage = l10n.editSetlistFailedError` (this key already exists in `lib/l10n/app_en.arb`/`app_ru.arb` — landed by 13-01 Task 2's batch per this plan's "Artifacts this plan adds" list above; if `flutter analyze` reports the getter undefined, re-run `flutter pub run build_runner build --delete-conflicting-outputs` to confirm the generated code is current, but do not edit either ARB file from this task).

    Migrate the test file: add `import '../../test_strings.dart';`, replace every app-copy `find.text('...')` with `tester.strings.keyName`. Test fixtures stay hardcoded.
  </action>
  <verify>
    <automated>flutter test test/features/setlists/edit_setlist_screen_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "l10n\." lib/features/setlists/edit_setlist_screen.dart` >= 7
    - `grep -c "tester.strings" test/features/setlists/edit_setlist_screen_test.dart` >= 1
    - `flutter test test/features/setlists/edit_setlist_screen_test.dart` passes
  </acceptance_criteria>
  <done>edit_setlist_screen.dart and its test are fully localized; flutter test passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: confirm_delete_setlist_dialog.dart + test</name>
  <files>lib/features/setlists/confirm_delete_setlist_dialog.dart, test/features/setlists/confirm_delete_setlist_dialog_test.dart</files>
  <read_first>
    lib/features/setlists/confirm_delete_setlist_dialog.dart
    test/features/setlists/confirm_delete_setlist_dialog_test.dart
    lib/l10n/app_en.arb
  </read_first>
  <action>
    Add `import '../../generated/app_localizations.dart';`. In `build()`, add `final l10n = AppLocalizations.of(context)!;`. Replace `const Text('Delete setlist?')` -> `Text(l10n.confirmDeleteSetlistTitle)`. `const Text('This action cannot be undone.')` -> `Text(l10n.commonActionCannotBeUndone)` (reuse — exact-match text with confirm_delete_track_dialog.dart, per D-01). `const Text('Cancel')` -> `Text(l10n.commonCancel)`. `message: isOnline ? '' : 'Requires connection'` -> `isOnline ? '' : l10n.commonRequiresConnection`. `Text(isOnline ? 'Delete' : 'Requires connection')` -> `Text(isOnline ? l10n.commonDelete : l10n.commonRequiresConnection)`. In `_delete()`, capture `l10n` via `AppLocalizations.of(context)!` and replace `_errorMessage = 'Delete failed. Try again.'` with `_errorMessage = l10n.confirmDeleteSetlistFailedError`.

    Migrate the test file: add `import '../../test_strings.dart';`, replace every app-copy `find.text('...')` with `tester.strings.keyName`. Test fixtures stay hardcoded.
  </action>
  <verify>
    <automated>flutter test test/features/setlists/confirm_delete_setlist_dialog_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "l10n\." lib/features/setlists/confirm_delete_setlist_dialog.dart` >= 6
    - `grep -c "tester.strings" test/features/setlists/confirm_delete_setlist_dialog_test.dart` >= 1
    - `flutter test test/features/setlists/confirm_delete_setlist_dialog_test.dart` passes
  </acceptance_criteria>
  <done>confirm_delete_setlist_dialog.dart and its test are fully localized; flutter test passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: setlist_list_screen.dart + test</name>
  <files>lib/features/setlists/setlist_list_screen.dart, test/features/setlists/setlist_list_screen_test.dart</files>
  <read_first>
    lib/features/setlists/setlist_list_screen.dart
    test/features/setlists/setlist_list_screen_test.dart
    lib/l10n/app_en.arb
  </read_first>
  <action>
    Add `import '../../generated/app_localizations.dart';`. In `build()`, add `final l10n = AppLocalizations.of(context)!;` and re-derive in `_buildContent`/`_buildError` (own `context`). Replace `AppBar(title: const Text('Setlists'))` -> `AppBar(title: Text(l10n.navSetlists))` (reuse). `tooltip: isOnline ? 'Add setlist' : 'Requires connection'` -> `isOnline ? l10n.setlistListAddButton : l10n.commonRequiresConnection`. `'No setlists yet'` -> `l10n.setlistListEmptyTitle`. `'Create a setlist or ask a bandmate to add one.'` -> `l10n.setlistListEmptyDescription`. `const Text('Add setlist')` (empty-state button — same text as the FAB tooltip above, reuse the same key within this file) -> `Text(l10n.setlistListAddButton)`. `_buildError`'s `'Failed to load setlists. Tap to try again.'`/`'Retry'` -> `l10n.commonFailedToLoadSetlists`/`l10n.commonRetry`. `formatEventDate(eventDate)` and the raw `eventLocation` SnackBar content are formatted/data values, not localizable strings — leave unchanged.

    Migrate the test file: add `import '../../test_strings.dart';`, replace every app-copy `find.text('...')` with `tester.strings.keyName`. Test fixtures (setlist names, locations) stay hardcoded.
  </action>
  <verify>
    <automated>flutter test test/features/setlists/setlist_list_screen_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "l10n\." lib/features/setlists/setlist_list_screen.dart` >= 6
    - `grep -c "tester.strings" test/features/setlists/setlist_list_screen_test.dart` >= 1
    - `flutter test test/features/setlists/setlist_list_screen_test.dart` passes
  </acceptance_criteria>
  <done>setlist_list_screen.dart and its test are fully localized; flutter test passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | All three files render already-approved ARB strings; no new data paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|--------------|------------------|
| T-13-12 | Tampering | `AppLocalizations.of(context)!.editSetlistFailedError` getter availability | low | mitigate | Depends on 13-01 Task 2 having landed the key before this plan's Wave 2 executes (enforced by this plan's `depends_on: ["13-01"]`); `flutter analyze` catches an undefined getter if that ordering were ever violated. |

No package installs in this plan; package-legitimacy gate not applicable.
</threat_model>

<verification>
- `flutter test test/features/setlists/edit_setlist_screen_test.dart test/features/setlists/confirm_delete_setlist_dialog_test.dart test/features/setlists/setlist_list_screen_test.dart` — all pass
- `flutter analyze lib/features/setlists/edit_setlist_screen.dart lib/features/setlists/confirm_delete_setlist_dialog.dart lib/features/setlists/setlist_list_screen.dart` — no new errors
</verification>

<success_criteria>
All three files and their tests fully localized; combined with 13-09, the entire Setlists domain's localization sweep is complete.
</success_criteria>

<output>
Create `.planning/phases/13-string-extraction-screen-localization/13-10-SUMMARY.md` when done
</output>
