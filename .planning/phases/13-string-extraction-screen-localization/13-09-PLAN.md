---
phase: 13-string-extraction-screen-localization
plan: 09
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - lib/features/setlists/setlist_formatting.dart
  - lib/features/setlists/setlist_detail_screen.dart
  - test/features/setlists/setlist_detail_screen_test.dart
  - lib/features/setlists/setlists_screen.dart
  - test/features/setlists/setlists_screen_test.dart
  - lib/features/setlists/create_setlist_screen.dart
  - test/features/setlists/create_setlist_screen_test.dart
  - lib/features/setlists/add_setlist_tracks_dialog.dart
  - test/features/setlists/add_setlist_tracks_dialog_test.dart
autonomous: true
requirements: [I18N-04, I18N-06]

estimate:
  tokens: 130000
  raw_tokens: 88000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "trackCount() follows the Russian 1/2-4/5+ plural rule everywhere it's used in the setlist domain (setlist detail track-count header uses a plain interpolated number per D-12's confirmed reading, not a plural — see key_links); the three max-track-ceiling messages (setlist_detail_screen, create_setlist_screen, add_setlist_tracks_dialog) all route through the same trackCount() plural method for grammatically consistent Russian, per D-11"
    - "The `_maxSetlistTracks` constant, previously independently declared in 3 files, now has one shared definition in setlist_formatting.dart, per D-12"
    - "Switching to Russian replaces every visible string on the setlist detail screen, the global cross-band Setlists tab, the create-setlist screen, and the add-tracks dialog — no hardcoded English remains in any of the four"
    - "setlist_detail_screen_test.dart, setlists_screen_test.dart, create_setlist_screen_test.dart, and add_setlist_tracks_dialog_test.dart assert against tester.strings.* instead of hardcoded English literals"
  artifacts:
    - lib/features/setlists/setlist_formatting.dart
    - lib/features/setlists/setlist_detail_screen.dart
    - lib/features/setlists/setlists_screen.dart
    - lib/features/setlists/create_setlist_screen.dart
    - lib/features/setlists/add_setlist_tracks_dialog.dart
  key_links:
    - "l10n.trackCount(maxSetlistTracks) composed into setlistDetailReorderTooManyTracks/setlistTracksLimit/addSetlistTracksMaxReached template strings -> grammatically correct RU across all 3 max-track messages"
    - "setlistDetailTracksHeader({count}) is a plain int-placeholder template ('Tracks ({count})'), NOT an ICU plural — CONTEXT.md's code_context section flags this exact string as 'likely just needs the number to stay interpolated, not routed through trackCount()'; confirmed here since it is a numeral-in-parens section-header idiom, not natural-language plural prose"
    - "setlist_formatting.dart's public maxSetlistTracks constant -> imported by create_setlist_screen.dart and add_setlist_tracks_dialog.dart (Task 2/3), replacing each file's own private _maxSetlistTracks"
  prohibitions:
    - { statement: "RU ICU plural ARB entries must not silently omit distinct 'few'/'many' forms and fall through to 'other', producing grammatically incorrect Russian outside the specific counts exercised by tests.", status: unverified, verification: manual }
    - { statement: "Semantically distinct messages must not be silently conflated under one shared/deduplicated ARB key such that users lose meaningful distinction between different error/status conditions.", status: unverified, verification: manual }
---

<!-- planner-discipline-allow: _maxSetlistTracks -->

<objective>
Consolidate the 3-file-duplicated `_maxSetlistTracks` constant into `setlist_formatting.dart` (D-12), delete the now-superseded `pluralizeTracks`/`tracksAndDuration` helpers (D-11 — replaced by `AppLocalizations.trackCount()`), then localize `setlist_detail_screen.dart`, `setlists_screen.dart` (global cross-band tab — its ONLY consumer of `tracksAndDuration`, so it must migrate in lockstep with the helper's deletion), `create_setlist_screen.dart`, and `add_setlist_tracks_dialog.dart`.

This plan intentionally bundles these 4 screens together (rather than splitting `setlists_screen.dart` into a separate plan) because all 4 depend on the SAME edit to `setlist_formatting.dart` — a shared file only one plan may safely touch per wave without forcing a cross-plan dependency (see 13-01's objective for why ARB files get the same treatment; the same "one file, one plan" rule applies to `setlist_formatting.dart` within Wave 2).

Purpose: This is the primary I18N-06 implementation site alongside 13-01 — every count-bearing string in the Setlists domain (the visible track-count display AND the 3 max-track-ceiling messages) must render grammatically correct Russian plural forms.
Output: setlist_formatting.dart consolidated; all 4 screens + their tests fully localized.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-string-extraction-screen-localization/13-CONTEXT.md
@.planning/phases/13-string-extraction-screen-localization/13-RESEARCH.md
@.planning/phases/13-string-extraction-screen-localization/13-01-PLAN.md
</context>

<artifacts_produced>
## Artifacts this plan adds to app_en.arb / app_ru.arb (landed by 13-01 Task 2, consumed here)

- setlistDetailFallbackTitle -> "Setlist" -> "Сетлист"
- setlistDetailEditTooltip -> "Edit setlist" -> "Изменить сетлист"
- setlistDetailDoneButton -> "Done" -> "Готово"
- setlistDetailTracksHeader({count}, int) -> "Tracks ({count})" -> "Треки ({count})"
- setlistDetailNoTracks -> "No tracks in this setlist" -> "В этом сетлисте нет треков"
- setlistDetailRemoveTrackTooltip -> "Remove" -> "Удалить"
- setlistDetailRemoveTrackFailedSnackbar -> "Failed to remove track. Try again." -> "Не удалось удалить трек. Попробуйте ещё раз."
- setlistDetailReorderFailedSnackbar -> "Failed to reorder tracks. Refreshing..." -> "Не удалось изменить порядок треков. Обновление..."
- setlistDetailReorderTooManyTracks({tracksPhrase}, String) -> "Can't reorder — this setlist has more than {tracksPhrase}." -> "Невозможно изменить порядок — в этом сетлисте больше, чем {tracksPhrase}."
- setlistTracksLimit({tracksPhrase}, String) -> "Setlists can have at most {tracksPhrase}." -> "В сетлисте может быть не более {tracksPhrase}."
- setlistsTabEmptyTitle -> "No setlists" -> "Сетлистов нет"
- setlistsTabEmptyDescription -> "Create setlists in a band to see them here." -> "Создавайте сетлисты в группе, чтобы они отображались здесь."
- createSetlistAppBarTitle -> "Create setlist" -> "Создать сетлист"
- createSetlistDateHint -> "YYYY-MM-DD" -> "ГГГГ-ММ-ДД"
- createSetlistAddTracksOptionalHeader -> "Add tracks (optional)" -> "Добавить треки (необязательно)"
- createSetlistNoTracksInBand -> "No tracks in this band yet" -> "В этой группе пока нет треков"
- createSetlistFailedError -> "Failed to create setlist. Try again." -> "Не удалось создать сетлист. Попробуйте ещё раз."
- createSetlistSuccessSnackbar({name}, String) -> "{name} created!" -> "«{name}» создан!" (masculine agreement — "сетлист" is masculine)
- addSetlistTracksSearchHint -> "Search by title or artist" -> "Поиск по названию или исполнителю"
- addSetlistTracksMaxReached({tracksPhrase}, String) -> "This setlist already has the maximum of {tracksPhrase}." -> "В этом сетлисте уже максимум — {tracksPhrase}."
- addSetlistTracksNoMatch -> "No tracks match your search" -> "Нет треков, соответствующих поиску"
- addSetlistTracksNoneAvailable -> "No more tracks available" -> "Больше нет доступных треков"
- addSetlistTracksRemainingMessage({tracksPhrase},{slotsPhrase}, both String) -> "Setlists can have at most {tracksPhrase} — {slotsPhrase} remaining." -> "В сетлисте может быть не более {tracksPhrase} — осталось {slotsPhrase}."
- addSetlistTracksSubmitButton -> "Add" -> "Добавить"
- addSetlistTracksSuccessSnackbar -> "Tracks added!" -> "Треки добавлены!"
- addSetlistTracksFailedError -> "Failed to add tracks. Try again." -> "Не удалось добавить треки. Попробуйте ещё раз."

(`trackCount`/`slotCount` ICU plurals, `commonAllBandsFilter`, `commonAddTracks`, `commonDelete`, `commonEdit`, `commonRetry`, `commonRequiresConnection`, `commonSomethingWentWrong`, `commonCancel`, `commonCouldntLoadTracks`, `commonFailedToLoadSetlists`, `commonNameLabel`, `commonLocationLabel`, `commonDateLabel`, `commonNameRequired`, `navSetlists` were all landed by 13-01 Task 2 too — reused here, not redeclared.)

No task in this plan touches the ARB files.
</artifacts_produced>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: setlist_formatting.dart consolidation + setlist_detail_screen.dart + setlists_screen.dart</name>
  <files>lib/features/setlists/setlist_formatting.dart, lib/features/setlists/setlist_detail_screen.dart, test/features/setlists/setlist_detail_screen_test.dart, lib/features/setlists/setlists_screen.dart, test/features/setlists/setlists_screen_test.dart</files>
  <read_first>
    lib/features/setlists/setlist_formatting.dart
    lib/features/setlists/setlist_detail_screen.dart
    test/features/setlists/setlist_detail_screen_test.dart
    lib/features/setlists/setlists_screen.dart
    test/features/setlists/setlists_screen_test.dart
    lib/l10n/app_en.arb
  </read_first>
  <action>
    In `setlist_formatting.dart`: add `const int maxSetlistTracks = 100;` (public top-level constant — this file already imports `track_formatting.dart`, add this alongside `_monthAbbreviations`). Delete `String pluralizeTracks(int count) => ...` and `String tracksAndDuration(int tracksCount, int durationSeconds) => ...` entirely — both are superseded by `AppLocalizations.trackCount()` called directly at each former call site. Leave `_monthAbbreviations`/`formatEventDate` untouched — per REQUIREMENTS.md's "Date/time localization" Out-of-Scope entry, date formatting (including the "No date set" fallback) is explicitly out of this phase's scope; localizing only the fallback string while leaving month abbreviations in English would produce an inconsistent half-localized date string, so this function is left entirely as-is.

    In `setlist_detail_screen.dart`: add `import '../../generated/app_localizations.dart';` and `import 'setlist_formatting.dart' show maxSetlistTracks;` (the file already imports `setlist_formatting.dart` for other symbols — add `maxSetlistTracks` to that existing import's `show` clause, or add the import if none exists yet). Delete the private `static const int _maxSetlistTracks = 100;` and replace both of its 2 usages (`_handleReorder`'s length check, and the interpolated reorder-failure message) with `maxSetlistTracks`. Add `final l10n = AppLocalizations.of(context)!;` in `build()` and re-derive it in `_buildContent`/`_buildError` (own `context` params) and in `_removeTrack`/`_handleReorder` (via `context` on the `State` object, guarded by the existing `if (!mounted) return;` checks already in place before each SnackBar). Replace: `Text(name ?? 'Setlist')` -> `Text(name ?? l10n.setlistDetailFallbackTitle)`. `tooltip: isOnline ? 'Edit setlist' : 'Requires connection'` -> `isOnline ? l10n.setlistDetailEditTooltip : l10n.commonRequiresConnection`. `const SnackBar(content: Text('Failed to remove track. Try again.'))` -> `SnackBar(content: Text(l10n.setlistDetailRemoveTrackFailedSnackbar))`. The reorder-too-many SnackBar's `"Can't reorder — this setlist has more than $_maxSetlistTracks tracks."` -> `l10n.setlistDetailReorderTooManyTracks(l10n.trackCount(maxSetlistTracks))`. `const SnackBar(content: Text('Failed to reorder tracks. Refreshing...'))` -> `SnackBar(content: Text(l10n.setlistDetailReorderFailedSnackbar))`. `message: isOnline || _editMode ? '' : 'Requires connection'` -> `isOnline || _editMode ? '' : l10n.commonRequiresConnection`. `Text(_editMode ? 'Done' : 'Edit')` -> `Text(_editMode ? l10n.setlistDetailDoneButton : l10n.commonEdit)`. `'Tracks (${tracks.length})'` -> `l10n.setlistDetailTracksHeader(tracks.length)` (plain interpolated count, not a plural — confirmed by CONTEXT.md). `const Center(child: Text('No tracks in this setlist'))` -> `Center(child: Text(l10n.setlistDetailNoTracks))`. `tooltip: 'Remove'` (per-row remove icon) -> `tooltip: l10n.setlistDetailRemoveTrackTooltip`. `const Text('Add tracks')` -> `Text(l10n.commonAddTracks)`. `Text('Delete', ...)` -> `Text(l10n.commonDelete, ...)`. `_buildError`'s `'Failed to load setlists. Tap to try again.'`/`'Retry'` -> `l10n.commonFailedToLoadSetlists`/`l10n.commonRetry`.

    In `setlists_screen.dart`: add `import '../../generated/app_localizations.dart';`. Add `final l10n = AppLocalizations.of(context)!;` in `build()` and re-derive in `_buildFilterDropdown`/`_buildContent`/`_buildEmptyState`/`_buildError` (each has its own `context`). Replace `const Text('Setlists')` -> `Text(l10n.navSetlists)` (reuse — exact-match text with the nav-bar label, per D-01). `const Text('All bands')` -> `Text(l10n.commonAllBandsFilter)`. The trailing `Text(tracksAndDuration(tracksCount, durationSeconds))` -> since `tracksAndDuration`/`pluralizeTracks` no longer exist, compose directly: `Text('${l10n.trackCount(tracksCount)}, ${durationSeconds.asMinutesSeconds}')`. `'No setlists'` -> `l10n.setlistsTabEmptyTitle`. `'Create setlists in a band to see them here.'` -> `l10n.setlistsTabEmptyDescription`. `_buildError`'s `'Failed to load setlists. Tap to try again.'`/`'Retry'` -> `l10n.commonFailedToLoadSetlists`/`l10n.commonRetry`. Remove the now-dangling `import 'setlist_formatting.dart';` if `tracksAndDuration` was its only used symbol from that import (check other usages first — `formatEventDate` is NOT used in this file per the current read, so the import can likely be dropped entirely; confirm via `flutter analyze` reporting an unused-import warning if kept unnecessarily).

    Migrate both test files: add `import '../../test_strings.dart';` to each, replace every app-copy `find.text('...')` with `tester.strings.keyName` (composing `tester.strings.trackCount(n)` + `', '` + the duration string for the setlists_screen trailing-text assertions, and `tester.strings.setlistDetailReorderTooManyTracks(tester.strings.trackCount(maxSetlistTracks))` — importing `maxSetlistTracks` from `package:cadence/features/setlists/setlist_formatting.dart` into the test file if not already imported — for the reorder-limit assertion in setlist_detail_screen_test.dart, if such an assertion exists). Test fixtures (setlist names, track titles, locations) stay hardcoded.
  </action>
  <verify>
    <automated>flutter test test/features/setlists/setlist_detail_screen_test.dart test/features/setlists/setlists_screen_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "pluralizeTracks\|tracksAndDuration" lib/features/setlists/setlist_formatting.dart` == 0
    - `grep -c "const int maxSetlistTracks" lib/features/setlists/setlist_formatting.dart` == 1
    - `grep -c "_maxSetlistTracks" lib/features/setlists/setlist_detail_screen.dart` == 0
    - `grep -c "l10n.trackCount" lib/features/setlists/setlist_detail_screen.dart lib/features/setlists/setlists_screen.dart` >= 2
    - `grep -c "tester.strings" test/features/setlists/setlist_detail_screen_test.dart test/features/setlists/setlists_screen_test.dart` >= 2
    - `flutter test test/features/setlists/setlist_detail_screen_test.dart test/features/setlists/setlists_screen_test.dart` passes
  </acceptance_criteria>
  <done>setlist_formatting.dart's pluralizeTracks/tracksAndDuration are deleted in favor of AppLocalizations.trackCount(); maxSetlistTracks is a single public constant; setlist_detail_screen.dart and setlists_screen.dart are fully localized and their tests pass.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: create_setlist_screen.dart + test</name>
  <files>lib/features/setlists/create_setlist_screen.dart, test/features/setlists/create_setlist_screen_test.dart</files>
  <read_first>
    lib/features/setlists/create_setlist_screen.dart
    test/features/setlists/create_setlist_screen_test.dart
    lib/l10n/app_en.arb
  </read_first>
  <action>
    Add `import '../../generated/app_localizations.dart';` and `import 'setlist_formatting.dart' show maxSetlistTracks;`. Delete the private `static const int _maxSetlistTracks = 100;` and replace its 2 usages (the `atCap` check, and the at-cap message's interpolation) with `maxSetlistTracks`. Add `final l10n = AppLocalizations.of(context)!;` in `build()`. Replace `AppBar(title: const Text('Create setlist'))` -> `AppBar(title: Text(l10n.createSetlistAppBarTitle))`. `labelText: 'Name'` -> `labelText: l10n.commonNameLabel`. The name validator's `'Name is required'` -> `l10n.commonNameRequired`. `labelText: 'Location'` -> `labelText: l10n.commonLocationLabel`. `labelText: 'Date'` -> `labelText: l10n.commonDateLabel`. `hintText: 'YYYY-MM-DD'` -> `hintText: l10n.createSetlistDateHint`. `'Add tracks (optional)'` -> `l10n.createSetlistAddTracksOptionalHeader`. `const Text('No tracks in this band yet')` -> `Text(l10n.createSetlistNoTracksInBand)`. The at-cap message `'Setlists can have at most $_maxSetlistTracks tracks.'` -> `l10n.setlistTracksLimit(l10n.trackCount(maxSetlistTracks))`. `const Expanded(child: Text("Couldn't load tracks"))` -> `Expanded(child: Text(l10n.commonCouldntLoadTracks))`. `const Text('Retry')` -> `Text(l10n.commonRetry)`. `message: isOnline ? '' : 'Requires connection'` -> `isOnline ? '' : l10n.commonRequiresConnection`. `Text(isOnline ? 'Create' : 'Requires connection')` -> `Text(isOnline ? l10n.commonCreate : l10n.commonRequiresConnection)`. In `_submit()`, capture `l10n` via `AppLocalizations.of(context)!` and replace `SnackBar(content: Text('$name created!'))` -> `SnackBar(content: Text(l10n.createSetlistSuccessSnackbar(name)))`, and `_errorMessage = 'Failed to create setlist. Try again.'` -> `_errorMessage = l10n.createSetlistFailedError`.

    Migrate the test file: add `import '../../test_strings.dart';`, replace every app-copy `find.text('...')` with `tester.strings.keyName` (composing the at-cap and success-snackbar messages the same way the screen does). Test fixtures stay hardcoded.
  </action>
  <verify>
    <automated>flutter test test/features/setlists/create_setlist_screen_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "_maxSetlistTracks" lib/features/setlists/create_setlist_screen.dart` == 0
    - `grep -c "l10n\." lib/features/setlists/create_setlist_screen.dart` >= 10
    - `grep -c "tester.strings" test/features/setlists/create_setlist_screen_test.dart` >= 1
    - `flutter test test/features/setlists/create_setlist_screen_test.dart` passes
  </acceptance_criteria>
  <done>create_setlist_screen.dart imports the shared maxSetlistTracks constant, is fully localized, and its test passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: add_setlist_tracks_dialog.dart + test</name>
  <files>lib/features/setlists/add_setlist_tracks_dialog.dart, test/features/setlists/add_setlist_tracks_dialog_test.dart</files>
  <read_first>
    lib/features/setlists/add_setlist_tracks_dialog.dart
    test/features/setlists/add_setlist_tracks_dialog_test.dart
    lib/l10n/app_en.arb
  </read_first>
  <action>
    Add `import '../../generated/app_localizations.dart';` and `import 'setlist_formatting.dart' show maxSetlistTracks;`. Delete the private `static const int _maxSetlistTracks = 100;` and replace both usages (the `remainingSlots` calculation, and the "already has the maximum" message's interpolation) with `maxSetlistTracks`. Add `final l10n = AppLocalizations.of(context)!;` in `build()`. Replace `const Text('Add tracks')` (dialog title) -> `Text(l10n.commonAddTracks)` (reuse — exact-match text with `setlist_detail_screen.dart`'s "Add tracks" button, per D-01). `hintText: 'Search by title or artist'` -> `hintText: l10n.addSetlistTracksSearchHint`. `const Text('This setlist already has the maximum of 100 tracks.')` -> `Text(l10n.addSetlistTracksMaxReached(l10n.trackCount(maxSetlistTracks)))`. `const Center(child: Text('No tracks match your search'))` -> `Center(child: Text(l10n.addSetlistTracksNoMatch))`. `const Text('No more tracks available')` -> `Text(l10n.addSetlistTracksNoneAvailable)`. The remaining-slots message `'Setlists can have at most 100 tracks — $remainingSlots slot${remainingSlots == 1 ? '' : 's'} remaining.'` -> `l10n.addSetlistTracksRemainingMessage(l10n.trackCount(maxSetlistTracks), l10n.slotCount(remainingSlots))`. `const Expanded(child: Text("Couldn't load tracks"))` -> `Expanded(child: Text(l10n.commonCouldntLoadTracks))`. `const Text('Retry')` -> `Text(l10n.commonRetry)`. `const Text('Cancel')` -> `Text(l10n.commonCancel)`. `message: isOnline ? '' : 'Requires connection'` -> `isOnline ? '' : l10n.commonRequiresConnection`. `Text(isOnline ? 'Add' : 'Requires connection')` -> `Text(isOnline ? l10n.addSetlistTracksSubmitButton : l10n.commonRequiresConnection)`. In `_submit()`, capture `l10n` and replace `const SnackBar(content: Text('Tracks added!'))` -> `SnackBar(content: Text(l10n.addSetlistTracksSuccessSnackbar))`, and `_errorMessage = 'Failed to add tracks. Try again.'` -> `_errorMessage = l10n.addSetlistTracksFailedError`.

    Migrate the test file: add `import '../../test_strings.dart';`, replace every app-copy `find.text('...')` with `tester.strings.keyName` (composing the max-reached and remaining-slots messages the same way the screen does). Test fixtures stay hardcoded.
  </action>
  <verify>
    <automated>flutter test test/features/setlists/add_setlist_tracks_dialog_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "_maxSetlistTracks" lib/features/setlists/add_setlist_tracks_dialog.dart` == 0
    - `grep -c "l10n\." lib/features/setlists/add_setlist_tracks_dialog.dart` >= 11
    - `grep -c "tester.strings" test/features/setlists/add_setlist_tracks_dialog_test.dart` >= 1
    - `flutter test test/features/setlists/add_setlist_tracks_dialog_test.dart` passes
  </acceptance_criteria>
  <done>add_setlist_tracks_dialog.dart imports the shared maxSetlistTracks constant, is fully localized (including both slotCount and trackCount plurals composed into the remaining-slots message), and its test passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | All 4 screens render already-approved ARB strings; ICU plural resolution happens entirely client-side against a client-known integer (`maxSetlistTracks`, `remainingSlots`, `tracksCount`), never user-supplied text. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|--------------|------------------|
| T-13-11 | Tampering | `setlist_formatting.dart`'s public `maxSetlistTracks` constant | low | mitigate | Single source of truth (was 3 independently-declared private constants that could drift); `flutter analyze` catches any remaining reference to the deleted private constants across all 3 consuming files. |

No package installs in this plan; package-legitimacy gate not applicable.
</threat_model>

<verification>
- `flutter test test/features/setlists/setlist_detail_screen_test.dart test/features/setlists/setlists_screen_test.dart test/features/setlists/create_setlist_screen_test.dart test/features/setlists/add_setlist_tracks_dialog_test.dart` — all pass
- `flutter analyze lib/features/setlists/` — no new errors, no unused-import warnings on `setlist_formatting.dart`'s consumers
</verification>

<success_criteria>
The `_maxSetlistTracks` triplication is eliminated; every count-bearing string in the Setlists domain renders grammatically correct Russian plural forms; all 4 screens and their tests are fully localized.
</success_criteria>

<output>
Create `.planning/phases/13-string-extraction-screen-localization/13-09-SUMMARY.md` when done
</output>
