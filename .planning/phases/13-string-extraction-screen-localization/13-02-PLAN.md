---
phase: 13-string-extraction-screen-localization
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - lib/features/bands/confirm_delete_band_dialog.dart
  - lib/features/bands/confirm_leave_band_dialog.dart
  - lib/features/bands/confirm_remove_member_dialog.dart
autonomous: true
requirements: [I18N-04]

estimate:
  tokens: 45000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Switching to Russian replaces every visible string in the delete-band, leave-band, and remove-member confirm dialogs — no hardcoded English remains in any of the three"
  artifacts:
    - lib/features/bands/confirm_delete_band_dialog.dart
    - lib/features/bands/confirm_leave_band_dialog.dart
    - lib/features/bands/confirm_remove_member_dialog.dart
  key_links:
    - "AppLocalizations.of(context)!.confirmDeleteBandTitle(bandName) -> ARB key already added by 13-01 Task 2"
  prohibitions:
    - { statement: "Semantically distinct messages must not be silently conflated under one shared/deduplicated ARB key such that users lose meaningful distinction between different error/status conditions.", status: unverified, verification: manual }
---

<objective>
Localize the three band-membership confirm dialogs with no dedicated widget test today (`confirm_delete_band_dialog.dart`, `confirm_leave_band_dialog.dart`, `confirm_remove_member_dialog.dart`) — replacing every hardcoded English string with the shared/per-dialog ARB keys 13-01 already established.

Purpose: These are the three owner/member-facing destructive-action dialogs (BAND-05/BAND-08/BAND-09); localizing them closes a real gap since none currently have automated coverage catching a stray hardcoded string.
Output: All three dialogs render entirely through `AppLocalizations`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-string-extraction-screen-localization/13-CONTEXT.md
@.planning/phases/13-string-extraction-screen-localization/13-01-PLAN.md
</context>

<artifacts_produced>
## Artifacts this plan adds to app_en.arb / app_ru.arb (landed by 13-01 Task 2, consumed here)

- confirmDeleteBandTitle({bandName}) -> "Delete {bandName}?" -> "Удалить «{bandName}»?"
- confirmDeleteBandBody -> "Type the band name to confirm. This action cannot be undone and will remove the band for all members." -> "Введите название группы для подтверждения. Это действие нельзя отменить — группа будет удалена для всех участников."
- confirmLeaveBandTitle({bandName}) -> "Leave {bandName}?" -> "Покинуть «{bandName}»?"
- confirmLeaveBandBody -> "You will no longer be a member of this band." -> "Вы больше не будете участником этой группы."
- confirmRemoveMemberTitle({memberUsername},{bandName}) -> "Remove {memberUsername} from {bandName}?" -> "Исключить {memberUsername} из группы «{bandName}»?"
- confirmRemoveMemberBody({memberUsername}) -> "{memberUsername} will no longer be a member of this band." -> "{memberUsername} больше не будет участником этой группы."

These 6 keys, plus `@key` placeholder-type metadata for each, must exist in `lib/l10n/app_en.arb`/`app_ru.arb` before this plan runs (added by 13-01 Task 2's key batch). No task in this plan touches the ARB files.
</artifacts_produced>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: confirm_delete_band_dialog.dart</name>
  <files>lib/features/bands/confirm_delete_band_dialog.dart</files>
  <read_first>
    lib/features/bands/confirm_delete_band_dialog.dart
    lib/l10n/app_en.arb
  </read_first>
  <action>
    Add `import '../../generated/app_localizations.dart';`. In `build()`, add `final l10n = AppLocalizations.of(context)!;`. Replace `Text('Delete ${widget.bandName}?')` with `Text(l10n.confirmDeleteBandTitle(widget.bandName))`. Replace the `const Text('Type the band name to confirm. This action cannot be undone and will remove the band for all members.')` with `Text(l10n.confirmDeleteBandBody)`. Replace `labelText: 'Band name'` with `labelText: l10n.commonBandNameLabel`. Replace `const Text('Cancel')` with `Text(l10n.commonCancel)`. Replace `message: isOnline ? '' : 'Requires connection'` with `isOnline ? '' : l10n.commonRequiresConnection`. Replace `Text(isOnline ? 'Delete' : 'Requires connection')` with `Text(isOnline ? l10n.commonDelete : l10n.commonRequiresConnection)`. Replace the catch-all `_errorMessage = 'Something went wrong. Please try again.'` with `_errorMessage = l10n.commonSomethingWentWrong` — this line is inside `_delete()`, not `build()`, so capture `l10n` via `AppLocalizations.of(context)!` at the top of `_delete()` too (same `context` the `State` class exposes).
  </action>
  <verify>
    <automated>flutter analyze lib/features/bands/confirm_delete_band_dialog.dart</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "l10n\." lib/features/bands/confirm_delete_band_dialog.dart` >= 7
    - `flutter analyze lib/features/bands/confirm_delete_band_dialog.dart` reports no new errors
  </acceptance_criteria>
  <done>confirm_delete_band_dialog.dart renders every string via AppLocalizations.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: confirm_leave_band_dialog.dart</name>
  <files>lib/features/bands/confirm_leave_band_dialog.dart</files>
  <read_first>
    lib/features/bands/confirm_leave_band_dialog.dart
    lib/l10n/app_en.arb
  </read_first>
  <action>
    Add `import '../../generated/app_localizations.dart';`. In `build()`, add `final l10n = AppLocalizations.of(context)!;`. Replace `Text('Leave ${widget.bandName}?')` with `Text(l10n.confirmLeaveBandTitle(widget.bandName))`. Replace `const Text('You will no longer be a member of this band.')` with `Text(l10n.confirmLeaveBandBody)`. Replace `const Text('Cancel')` with `Text(l10n.commonCancel)`. Replace `message: isOnline ? '' : 'Requires connection'` with `isOnline ? '' : l10n.commonRequiresConnection`. Replace `Text(isOnline ? 'Leave' : 'Requires connection')` with `Text(isOnline ? l10n.commonLeave : l10n.commonRequiresConnection)`. Replace `_errorMessage = 'Something went wrong. Please try again.'` (inside `_leave()`) with `_errorMessage = l10n.commonSomethingWentWrong`, capturing `l10n` at the top of `_leave()` via `AppLocalizations.of(context)!`.
  </action>
  <verify>
    <automated>flutter analyze lib/features/bands/confirm_leave_band_dialog.dart</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "l10n\." lib/features/bands/confirm_leave_band_dialog.dart` >= 6
    - `flutter analyze lib/features/bands/confirm_leave_band_dialog.dart` reports no new errors
  </acceptance_criteria>
  <done>confirm_leave_band_dialog.dart renders every string via AppLocalizations.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: confirm_remove_member_dialog.dart</name>
  <files>lib/features/bands/confirm_remove_member_dialog.dart</files>
  <read_first>
    lib/features/bands/confirm_remove_member_dialog.dart
    lib/l10n/app_en.arb
  </read_first>
  <action>
    Add `import '../../generated/app_localizations.dart';`. In `build()`, add `final l10n = AppLocalizations.of(context)!;`. Replace `Text('Remove ${widget.memberUsername} from ${widget.bandName}?')` with `Text(l10n.confirmRemoveMemberTitle(widget.memberUsername, widget.bandName))`. Replace `'${widget.memberUsername} will no longer be a member of this band.'` with `l10n.confirmRemoveMemberBody(widget.memberUsername)`. Replace `const Text('Cancel')` with `Text(l10n.commonCancel)`. Replace `message: isOnline ? '' : 'Requires connection'` with `isOnline ? '' : l10n.commonRequiresConnection`. Replace `Text(isOnline ? 'Remove' : 'Requires connection')` with `Text(isOnline ? l10n.commonRemove : l10n.commonRequiresConnection)`. Replace `_errorMessage = 'Something went wrong. Please try again.'` (inside `_remove()`) with `_errorMessage = l10n.commonSomethingWentWrong`, capturing `l10n` at the top of `_remove()`.
  </action>
  <verify>
    <automated>flutter analyze lib/features/bands/confirm_remove_member_dialog.dart</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "l10n\." lib/features/bands/confirm_remove_member_dialog.dart` >= 6
    - `flutter analyze lib/features/bands/confirm_remove_member_dialog.dart` reports no new errors
  </acceptance_criteria>
  <done>confirm_remove_member_dialog.dart renders every string via AppLocalizations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | These dialogs only render already-approved ARB strings interpolated with server-returned `bandName`/`memberUsername` values (already-trusted display data used identically before this phase). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|--------------|------------------|
| T-13-04 | Information Disclosure | Dialog body text | low | accept | No new data exposure — dialogs interpolate the same `bandName`/`memberUsername` values already rendered pre-phase, now via a localized template instead of a raw string literal. |

No package installs in this plan; package-legitimacy gate not applicable.
</threat_model>

<verification>
- `flutter analyze lib/features/bands/confirm_delete_band_dialog.dart lib/features/bands/confirm_leave_band_dialog.dart lib/features/bands/confirm_remove_member_dialog.dart` — no new errors
- Manual: pump each dialog under `Locale('ru')` and confirm no English string remains (no automated widget test exists for these three files pre-phase; flutter analyze + code review is the available backstop)
</verification>

<success_criteria>
All three dialogs fully localized; no hardcoded English string literal remains in any of them.
</success_criteria>

<output>
Create `.planning/phases/13-string-extraction-screen-localization/13-02-SUMMARY.md` when done
</output>
