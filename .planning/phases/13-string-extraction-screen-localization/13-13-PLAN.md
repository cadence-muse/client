---
phase: 13-string-extraction-screen-localization
plan: 13
type: execute
wave: 3
depends_on: ["13-07", "13-12"]
files_modified:
  - test/widget_test.dart
autonomous: true
requirements: [I18N-04]

estimate:
  tokens: 20000
  raw_tokens: 13000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "The full-app smoke test (test/widget_test.dart) asserts against tester.strings.* for nav labels and the empty-state title instead of hardcoded English literals, and continues to pass"
  artifacts: []
  key_links:
    - "tester.strings.navHome/.navBands/.navTracks (from 13-07's localized root_scaffold.dart) -> test/widget_test.dart"
    - "tester.strings.tracksTabEmptyTitle (from 13-12's localized songs/tracks_screen.dart) -> test/widget_test.dart"
  prohibitions: []
---

<objective>
Migrate `test/widget_test.dart` — the project's full-app smoke test, which pumps `CadenceApp` and taps across the Home/Bands/Tracks tabs — off its 4 hardcoded English literals (`'Home'`, `'Bands'`, `'Tracks'`, `'No tracks'`), now that both source screens it depends on (`root_scaffold.dart` from 13-07, `songs/tracks_screen.dart` from 13-12) are localized.

This plan is Wave 3 (depends on both 13-07 and 13-12 landing first) because `widget_test.dart` is the one cross-cutting test in this codebase that needs BOTH plans' output simultaneously — the nav-bar labels (13-07) to navigate between tabs, and the Tracks tab's empty-state text (13-12) to assert the final state. No other plan in this phase has that combined dependency, so it gets its own minimal Wave-3 plan rather than delaying either 13-07 or 13-12's other work by an extra wave.

Purpose: Keeps the project's baseline smoke test green after this phase's localization sweep — a regression here would silently break `flutter test`'s fastest, most-run test.
Output: test/widget_test.dart passes, asserting through tester.strings.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-string-extraction-screen-localization/13-CONTEXT.md
@.planning/phases/13-string-extraction-screen-localization/13-01-PLAN.md
</context>

<artifacts_produced>
## Artifacts this plan adds to app_en.arb / app_ru.arb

None — this plan only touches a test file and consumes keys already landed by 13-01 Task 2 (`navHome`, `navBands`, `navTracks`, `tracksTabEmptyTitle`).
</artifacts_produced>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: test/widget_test.dart — migrate nav-label and empty-state assertions</name>
  <files>test/widget_test.dart</files>
  <read_first>
    test/widget_test.dart
  </read_first>
  <action>
    Add `import 'test_strings.dart';` (same-directory import, this file lives at `test/widget_test.dart`). Replace `expect(find.text('Home'), findsWidgets)` with `expect(find.text(tester.strings.navHome), findsWidgets)`. Replace `await tester.tap(find.text('Bands'))` with `await tester.tap(find.text(tester.strings.navBands))`. Replace `await tester.tap(find.text('Tracks'))` with `await tester.tap(find.text(tester.strings.navTracks))`. Replace `expect(find.text('No tracks'), findsOneWidget)` with `expect(find.text(tester.strings.tracksTabEmptyTitle), findsOneWidget)`. Leave `expect(find.text('B.A.T.H.'), findsOneWidget)` unchanged — `'B.A.T.H.'` is a test-fixture band name, not UI copy, per D-07.
  </action>
  <verify>
    <automated>flutter test test/widget_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "tester.strings" test/widget_test.dart` >= 4
    - `grep -c "find.text('Home')\|find.text('Bands')\|find.text('Tracks')\|find.text('No tracks')" test/widget_test.dart` == 0
    - `flutter test test/widget_test.dart` passes
  </acceptance_criteria>
  <done>test/widget_test.dart asserts through tester.strings for every nav-label and empty-state check; the test passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | Test-only change; no production code touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|--------------|------------------|
| N/A | — | — | — | accept | Test-file-only plan; no production trust boundary is touched. |

No package installs in this plan; package-legitimacy gate not applicable.
</threat_model>

<verification>
- `flutter test test/widget_test.dart` — passes
- `flutter test` (full suite) — recommended at phase-gate to confirm no other test file was missed by this phase's plan set
</verification>

<success_criteria>
The project's baseline smoke test passes fully migrated to tester.strings, closing out this phase's plan set.
</success_criteria>

<output>
Create `.planning/phases/13-string-extraction-screen-localization/13-13-SUMMARY.md` when done
</output>
