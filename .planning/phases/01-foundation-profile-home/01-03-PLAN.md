---
phase: 01-foundation-profile-home
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - lib/cache/cache_service.dart
  - lib/providers/homepage_provider.dart
  - lib/features/home/home_screen.dart
  - test/cache/cache_service_test.dart
  - test/providers/homepage_provider_test.dart
  - test/features/home/home_screen_test.dart
autonomous: true
requirements: [USER-02, OFFL-01]

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Home screen renders the real welcome message and bandsCount fetched from GET /api/homepage"
    - "Home screen displays a 'No bands yet' empty state (heading, body, 'Create Band' button) when bandsCount is 0"
    - "Home screen displays exactly '1 band' (singular) when bandsCount is 1, and 'N bands' (plural) when bandsCount is 2 or more"
    - "Home screen displays large band counts as the exact number with comma thousands-separators and no K/M abbreviation (e.g. '1,250 bands')"
    - "bandsCount is rendered as-is from the server integer response, with no client-side rounding, truncation, or arithmetic applied"
    - "Tapping the Home refresh icon twice in quick succession triggers exactly one GET /api/homepage network call"
    - "First-ever Home load with no cache and no connectivity shows the 'Couldn't load home' / 'Please check your connection and try again.' error state with a Retry button"
    - "Home screen truncates a username longer than 20 characters in the welcome message to a single line with an ellipsis"
    - "CacheService.clearAll() empties both profileBox and homepageBox"
    - statement: "On screen load with a warm cache, Home screen shows cached data immediately with no loading spinner, and a background refresh updates the display silently in place with no animation or toast"
      verification: backstop
  artifacts:
    - path: "lib/providers/homepage_provider.dart"
      provides: "HomepageData AsyncNotifier: cache-first GET /api/homepage with deduped refresh()"
      exports: ["homepageDataProvider"]
    - path: "lib/features/home/home_screen.dart"
      provides: "ConsumerWidget rendering real homepage data with loading/error/empty/populated states"
    - path: "test/cache/cache_service_test.dart"
      provides: "Real Hive-backed roundtrip coverage for both profileBox and homepageBox, plus clearAll()"
    - path: "test/providers/homepage_provider_test.dart"
      provides: "Cache-first + refresh-dedup unit coverage for HomepageData"
    - path: "test/features/home/home_screen_test.dart"
      provides: "Widget-level coverage of populated/empty/error/pluralization/overflow states"
  key_links:
    - from: "lib/features/home/home_screen.dart"
      to: "lib/providers/homepage_provider.dart"
      via: "ref.watch(homepageDataProvider)"
      pattern: "ref\\.watch\\(homepageDataProvider\\)"
    - from: "lib/providers/homepage_provider.dart"
      to: "lib/cache/cache_service.dart"
      via: "ref.watch(cacheServiceProvider) for homepageBox read/write"
      pattern: "cacheServiceProvider"
  prohibitions:
    - statement: "MUST NOT retain and display a previously-signed-in user's cached profile/homepage data for a different user who signs in afterward on the same device"
---

## Continues Phase 1 Goal

See 01-01-PLAN.md's "Phase Goal" user story. This plan proves the story's Homepage half, applying the cache-first + Riverpod pattern 01-01-PLAN.md established for Profile to a second endpoint (per D-02: each cached endpoint gets its own Hive box, reusing the same provider shape).

<objective>
Wire the Home tab to real `GET /api/homepage` data using the exact cache-first AsyncNotifier + Hive-box pattern proven in 01-01-PLAN.md, add the `homepageBox` to `CacheService`, and cover both the new endpoint and the previously-untested `CacheService`/cache-first behaviors with automated tests.

Purpose: Complete OFFL-01 for both Phase 1 endpoints (Profile + Home) and prove the per-endpoint-box pattern (D-02) generalizes cleanly — the exact proof point Phase 2 (Bands) needs before it adds `bandsBox`.
Output: Working Home screen backed by GET /api/homepage, cache-first via Hive, full test coverage for the cache layer.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-profile-home/01-01-SUMMARY.md
@.planning/phases/01-foundation-profile-home/01-UI-SPEC.md
@lib/api/publicapi.yml
@lib/cache/cache_service.dart
@lib/providers/profile_provider.dart
@lib/features/profile/profile_screen.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Home screen — cache-first GET /api/homepage, real UI</name>
  <read_first>
    lib/cache/cache_service.dart, lib/providers/profile_provider.dart, lib/features/profile/profile_screen.dart, lib/features/home/home_screen.dart, lib/api/publicapi.yml (HomepageData schema, lines ~596-605), .planning/phases/01-foundation-profile-home/01-UI-SPEC.md (Copywriting Contract: Home Screen; State & Interaction Coverage)
  </read_first>
  <files>
    lib/cache/cache_service.dart, lib/providers/homepage_provider.dart, lib/features/home/home_screen.dart
  </files>
  <behavior>
    - `HomepageData.build()` returns cached data immediately when `homepageBox` has an entry, and triggers a silent background refresh (mirrors `ProfileData.build()`)
    - `HomepageData.build()` returns `AsyncError` when there is no cached entry and the network fetch fails (D-07)
    - `HomepageData.refresh()` de-duplicates concurrent calls the same way `ProfileData.refresh()` does (shared in-flight `Future`)
    - `HomeScreen` renders `"Welcome, {username}"` and `"{N} band(s)"` from live provider data
  </behavior>
  <action>
    1. Extend `lib/cache/cache_service.dart`: add a `Box<Map> _homepageBox` field, initialize it in `initialize()` via `Hive.openBox<Map>('homepageBox')`, add `Future<Map<String, dynamic>?> readHomepage()` / `Future<void> writeHomepage(Map<String, dynamic> data)` mirroring `readProfile`/`writeProfile` exactly (same try/catch shape). Extend `clearAll()` to also `await _homepageBox.clear();`.

    2. Create `lib/providers/homepage_provider.dart` with `part 'homepage_provider.g.dart';`, structurally identical to `lib/providers/profile_provider.dart`'s `ProfileData` class but named `HomepageData`, calling `cache.readHomepage()`/`writeHomepage()` and `apiClient.send('GET', '/api/homepage')`. Same `_inFlightRefresh` de-dup pattern in `refresh()`. Exposes `homepageDataProvider`.

    3. Rewrite `lib/features/home/home_screen.dart`: `class HomeScreen extends ConsumerWidget { const HomeScreen({super.key}); @override Widget build(BuildContext context, WidgetRef ref) { final homeAsync = ref.watch(homepageDataProvider); return Scaffold(appBar: AppBar(title: const Text('Home'), actions: [IconButton(icon: const Icon(Icons.refresh), tooltip: 'Refresh', onPressed: () => ref.read(homepageDataProvider.notifier).refresh())]), body: homeAsync.when(data: (data) => _buildContent(context, data), loading: () => const Center(child: CircularProgressIndicator()), error: (error, stackTrace) => _buildError(context, () => ref.invalidate(homepageDataProvider)))); } }`.

    `_buildContent(BuildContext, Map<String, dynamic> data)`: extract `final username = data['username'] as String; final bandsCount = data['bandsCount'] as int;`. If `bandsCount == 0`, render the empty state: heading `"No bands yet"`, body `"Create or join a band to get started. Tap the '+' icon to create one or ask a bandmate for an invite code."`, `ElevatedButton` labeled `"Create Band"` with `onPressed: () => Navigator.of(context).push(MaterialPageRoute(builder: (_) => const BandsScreen()))` (navigates to the existing Bands tab content — the same `Navigator.push` pattern profile_screen.dart already uses for Settings; there is no real band-creation flow yet, so this is a real, working navigation to the nearest available screen, not a stub). Otherwise render: welcome text `"Welcome, $username"` in `headlineMedium` with `maxLines: 1, overflow: TextOverflow.ellipsis`, a `"Your bands"` label, and the pluralized/formatted count via a private helper `String _formatBandsCount(int n) => n == 1 ? '1 band' : '${_withThousandsSeparator(n)} bands';` where `String _withThousandsSeparator(int n) => n.toString().replaceAllMapped(RegExp(r'\B(?=(\d{3})+(?!\d))'), (m) => ',');` (no new dependency — a standard Dart regex idiom for comma-grouping).

    `_buildError(BuildContext, VoidCallback onRetry)`: heading `"Couldn't load home"`, body `"Please check your connection and try again."`, `ElevatedButton` labeled `"Retry"` — exact strings from 01-UI-SPEC.md.

    Import `../bands/bands_screen.dart` for the empty-state button's navigation target.
  </action>
  <acceptance_criteria>
    - `lib/cache/cache_service.dart` exposes `readHomepage`/`writeHomepage`, and `clearAll()` clears both `_profileBox` and `_homepageBox`
    - `homepageDataProvider` exists and follows the identical cache-first + dedup-refresh contract as `profileDataProvider`
    - Home screen shows "No bands yet" + "Create Band" for `bandsCount == 0`, "1 band" for `bandsCount == 1`, "N bands" for `bandsCount >= 2`, with comma grouping for large N
  </acceptance_criteria>
  <verify>
    <automated>flutter analyze</automated>
  </verify>
  <done>Home screen is wired end-to-end to GET /api/homepage through the same cache-first Riverpod pattern as Profile, with cache_service.dart now serving both endpoints.</done>
</task>

<task type="auto">
  <name>Task 2: Test coverage — cache_service (both boxes), homepage_provider, home_screen</name>
  <read_first>
    lib/cache/cache_service.dart, lib/providers/homepage_provider.dart, lib/features/home/home_screen.dart, test/features/profile/profile_screen_test.dart (pattern to mirror), test/providers/auth_provider_test.dart (ProviderContainer + fake pattern), .planning/phases/01-foundation-profile-home/01-RESEARCH.md (Validation Architecture)
  </read_first>
  <files>
    test/cache/cache_service_test.dart, test/providers/homepage_provider_test.dart, test/features/home/home_screen_test.dart
  </files>
  <action>
    In `test/cache/cache_service_test.dart`: use real Hive (no fakes) — `final tempDir = await Directory.systemTemp.createTemp(); Hive.init(tempDir.path);` in `setUp`, `await tempDir.delete(recursive: true);` in `tearDown` (after `Hive.close()`... note `CacheService.initialize()` opens boxes as a side effect of the SAME public API path production code uses, satisfying the read_first pattern without needing a special test constructor). Cases: `writeProfile` then `readProfile` roundtrips the same map; `readProfile` returns null when nothing was written; same two cases for `writeHomepage`/`readHomepage`; after writing both profile and homepage data, `clearAll()` makes both `readProfile()` and `readHomepage()` return null.

    In `test/providers/homepage_provider_test.dart`: mirror whatever pattern 01-01-PLAN.md's tracer test used to exercise `ProfileData`'s cache-first + refresh-dedup behavior (read `lib/providers/profile_provider.dart` and its test coverage in `test/features/profile/profile_screen_test.dart` for the exact override/mock shape), applied to `HomepageData`/`homepageDataProvider`: cache-hit returns immediately, no-cache+network-failure yields `AsyncError`, two rapid `refresh()` calls produce exactly one `send('GET', '/api/homepage', ...)` call on the mocked `ApiClient`.

    In `test/features/home/home_screen_test.dart`: mirror `test/features/profile/profile_screen_test.dart`'s `ProviderScope` override setup (fake `cacheServiceProvider`, `apiClientProvider` with `MockClient`). Cases: `bandsCount: 0` shows "No bands yet" + "Create Band"; `bandsCount: 1` shows "1 band"; `bandsCount: 2` shows "2 bands"; `bandsCount: 1250` shows "1,250 bands"; no-cache + network failure shows "Couldn't load home" + "Retry"; a username longer than 20 characters renders with `TextOverflow.ellipsis` and `maxLines: 1`.
  </action>
  <acceptance_criteria>
    - `flutter test test/cache/cache_service_test.dart test/providers/homepage_provider_test.dart test/features/home/home_screen_test.dart` passes
    - `cache_service_test.dart`'s `clearAll()` case fails if either box is left non-empty (verified by temporarily removing the `_homepageBox.clear()` line during development and confirming the test goes red)
  </acceptance_criteria>
  <verify>
    <automated>flutter test test/cache/cache_service_test.dart test/providers/homepage_provider_test.dart test/features/home/home_screen_test.dart</automated>
  </verify>
  <done>cache_service.dart, homepage_provider.dart, and home_screen.dart are all covered by passing automated tests proving OFFL-01 for both Phase 1 endpoints and the prohibition mitigation across both boxes.</done>
</task>

</tasks>

## Artifacts This Phase Produces (this plan)

- `lib/providers/homepage_provider.dart` — `HomepageData` class / `homepageDataProvider`
- `CacheService.readHomepage()` / `CacheService.writeHomepage()` — new methods on the existing class from 01-01-PLAN.md
- `test/cache/cache_service_test.dart`, `test/providers/homepage_provider_test.dart`, `test/features/home/home_screen_test.dart` — new files/directories

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> API (`GET /api/homepage`) | Session cookie sent to `AppConfig.apiBaseUrl`; untrusted JSON response parsed and rendered |
| Client -> Hive `homepageBox` | App writes/reads locally-persisted, unencrypted JSON blob (username + bandsCount only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Disclosure | `lib/cache/cache_service.dart` `homepageBox` | medium | mitigate | Extends the same cross-user cache-bleed threat identified in 01-01-PLAN.md (T-01-03) to the new box. `clearAll()` is extended in this plan to clear `_homepageBox` too, exercised by the same `AuthSession.signOut()` call path; `cache_service_test.dart`'s `clearAll()` case explicitly asserts both boxes end empty. |
| T-03-02 | Tampering | `lib/features/home/home_screen.dart` bandsCount display | low | accept | Display-only — no client-side arithmetic is performed on `bandsCount` (USER-02 precision truth), and a corrupted Hive read already falls back to cache-miss -> network fetch (same acceptance rationale as T-01-04). |

</threat_model>

<verification>
`flutter test test/cache/cache_service_test.dart test/providers/homepage_provider_test.dart test/features/home/home_screen_test.dart` passes; `flutter analyze` is clean.
</verification>

<success_criteria>
Home screen is fully functional against the real API contract, cache-first via Hive, matching every state in 01-UI-SPEC.md's State & Interaction Coverage table, with automated test coverage for the cache layer completing OFFL-01.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-profile-home/01-03-SUMMARY.md` when done
</output>
