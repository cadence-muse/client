---
phase: 01-foundation-profile-home
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - test/providers/auth_provider_test.dart
  - test/providers/theme_provider_test.dart
autonomous: true
requirements: [OFFL-06]

estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "AuthSession.build() restores the correct token from TokenStorage on cold start, verified with no ChangeNotifier involved"
    - "AuthSession.signIn() persists the token via TokenStorage.write() and updates provider state to AsyncData(token)"
    - "AuthSession.signOut() calls TokenStorage.delete() and cacheServiceProvider's clearAll(), proven via a fake CacheService spy, and updates state to AsyncData(null)"
    - "ThemeController.build() defaults to ThemeMode.system and setThemeMode() updates state without any ValueNotifier involved"
    - "grep -rn 'extends ChangeNotifier\\|extends ValueNotifier' lib/ returns zero matches"
  artifacts:
    - path: "test/providers/auth_provider_test.dart"
      provides: "Unit coverage for AuthSession restore/signIn/signOut, incl. the cache-clear-on-signOut privacy mitigation"
    - path: "test/providers/theme_provider_test.dart"
      provides: "Unit coverage for ThemeController Riverpod migration"
  key_links:
    - from: "test/providers/auth_provider_test.dart"
      to: "lib/providers/auth_provider.dart"
      via: "ProviderContainer with tokenStorageProvider/cacheServiceProvider overridden by fakes"
      pattern: "ProviderContainer"
---

## Continues Phase 1 Goal

See 01-01-PLAN.md's "Phase Goal" user story. This plan hardens the Riverpod auth/theme migration with automated regression coverage — it does not add new user-facing behavior.

<objective>
Add the unit-test coverage for `AuthSession` and `ThemeController` that 01-RESEARCH.md's Wave 0 Gaps flagged as missing, closing the OFFL-06 requirement with automated proof (not just manual inspection) that the migration is complete and the cache-clear-on-signOut privacy mitigation actually fires.

Purpose: Give future phases (which will also read/write `authSessionProvider`/`themeControllerProvider`) a regression net, and machine-verify the "no dual source of truth" / "no ChangeNotifier remains" claims from 01-01-PLAN.md's must_haves.
Output: `test/providers/auth_provider_test.dart`, `test/providers/theme_provider_test.dart`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-profile-home/01-01-SUMMARY.md
@lib/providers/auth_provider.dart
@lib/providers/theme_provider.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Unit-test AuthSession and ThemeController Riverpod providers</name>
  <read_first>
    lib/providers/auth_provider.dart, lib/providers/theme_provider.dart, lib/cache/cache_service.dart, lib/api/token_storage.dart, test/widget_test.dart (existing `_FakeSecureStorage` pattern to reuse), .planning/phases/01-foundation-profile-home/01-RESEARCH.md (Validation Architecture -> Testing approach, `ProviderContainer` example)
  </read_first>
  <files>
    test/providers/auth_provider_test.dart, test/providers/theme_provider_test.dart
  </files>
  <behavior>
    - Test: cold start with a token already written via `TokenStorage().write('seed-token')` before `ProviderContainer` creation -> `container.read(authSessionProvider.future)` resolves to `'seed-token'`
    - Test: cold start with no token written -> `container.read(authSessionProvider.future)` resolves to `null`
    - Test: `container.read(authSessionProvider.notifier).signIn('new-token')` -> `container.read(authSessionProvider).value == 'new-token'` AND `TokenStorage().read()` (via the fake secure storage) returns `'new-token'`
    - Test: after `signIn`, calling `container.read(authSessionProvider.notifier).signOut()` -> `container.read(authSessionProvider).value == null`, `TokenStorage().read()` returns `null`, AND the overridden fake `CacheService`'s `clearAll()` was invoked exactly once (a spy/fake with a `bool cleared` flag or call counter, injected via `cacheServiceProvider.overrideWithValue(...)`)
    - Test: `container.read(themeControllerProvider) == ThemeMode.system` by default
    - Test: `container.read(themeControllerProvider.notifier).setThemeMode(ThemeMode.dark)` -> `container.read(themeControllerProvider) == ThemeMode.dark`
  </behavior>
  <!-- planner-discipline-allow: extends ChangeNotifier -->
  <!-- planner-discipline-allow: extends ValueNotifier -->
  <action>
    In `test/providers/auth_provider_test.dart`: reuse the `_FakeSecureStorage` pattern from `test/widget_test.dart` (copy it or extract — copying is fine here, it's a small self-contained class), set `FlutterSecureStoragePlatform.instance = _FakeSecureStorage()` in each test's setup. Build a `ProviderContainer(overrides: [cacheServiceProvider.overrideWithValue(_FakeCacheService())])` per test, where `_FakeCacheService implements CacheService` (Dart allows implementing a class with a private constructor — only public members need concrete implementations) with an in-memory `Map<String, dynamic>? _profile` and a `bool clearAllCalled = false` field set inside its `clearAll()` override. Dispose the container (`container.dispose()`) after each test via `addTearDown`.

    In `test/providers/theme_provider_test.dart`: a plain `ProviderContainer()` (no overrides needed, `ThemeController` has no external dependencies) covering the two behavior cases above.

    Add a final assertion test (can live in either file, e.g. `auth_provider_test.dart`) running `grep -rn "extends ChangeNotifier\|extends ValueNotifier" lib/` via `Process.runSync('grep', [...])` from `dart:io` and asserting `result.stdout` (as `String`) is empty — this is the automated regression guard for the OFFL-06 must-have. Alternative if `Process.runSync` proves awkward in the test sandbox: a plain widget-test-free `test()` that walks `Directory('lib')` recursively (`.listSync(recursive: true)`), reads each `.dart` file, and asserts none contain the literal substrings `extends ChangeNotifier` or `extends ValueNotifier`.
  </action>
  <acceptance_criteria>
    - `flutter test test/providers/auth_provider_test.dart test/providers/theme_provider_test.dart` passes with all cases in `<behavior>` covered
    - The ChangeNotifier/ValueNotifier regression-guard test fails if either literal is reintroduced anywhere under `lib/` (verified by temporarily reintroducing one during development and confirming the test goes red, then reverting)
  </acceptance_criteria>
  <verify>
    <automated>flutter test test/providers/auth_provider_test.dart test/providers/theme_provider_test.dart</automated>
  </verify>
  <done>Both provider test files exist, pass, and the regression guard proves lib/ contains no ChangeNotifier/ValueNotifier subclass.</done>
</task>

</tasks>

## Artifacts This Phase Produces (this plan)

- `test/providers/auth_provider_test.dart` — new file/directory
- `test/providers/theme_provider_test.dart` — new file

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test harness -> `AuthSession`/`ThemeController` providers | Fully in-process, no network or real secure storage involved (fakes throughout) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Repudiation | `lib/providers/auth_provider.dart` `AuthSession.signOut()` | medium | mitigate | A future regression could silently reintroduce ChangeNotifier or drop the cache-clear-on-signOut mitigation without detection. This plan's `clearAllCalled` spy test and the grep-based regression guard give automated, always-run protection against both. |

</threat_model>

<verification>
`flutter test test/providers/auth_provider_test.dart test/providers/theme_provider_test.dart` passes.
</verification>

<success_criteria>
OFFL-06's "no ChangeNotifier/ValueNotifier, no dual source of truth" claim is proven by an automated test, not just code review; the privacy-prohibition mitigation (cache clear on sign-out) is proven by an automated test.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-profile-home/01-02-SUMMARY.md` when done
</output>
