---
phase: 14-api-error-localization
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - lib/features/bands/edit_band_screen.dart
  - lib/features/bands/confirm_delete_band_dialog.dart
  - lib/features/bands/confirm_remove_member_dialog.dart
  - lib/features/bands/confirm_leave_band_dialog.dart
  - lib/features/bands/confirm_rotate_invite_code_dialog.dart
  - lib/features/bands/confirm_transfer_ownership_dialog.dart
  - lib/features/bands/join_band_dialog.dart
  - test/features/bands/edit_band_screen_test.dart
  - test/features/bands/confirm_rotate_invite_code_dialog_test.dart
autonomous: true
requirements: [I18N-05]

estimate:
  tokens: 40000
  raw_tokens: 27000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Every band-feature `on ApiException catch (e)` site (edit band, delete band, remove member, leave band, rotate invite code, transfer ownership, join band) shows the ARB-localized generic message for a known error code, matching the mechanism proven in 14-01."
    - "None of these 7 sites had a screen-specific override before this phase, so none need the `overrides` parameter — a bare `e.localizedMessage(l10n)` call is correct for all 7."
  artifacts:
    - "All 7 listed files call `e.localizedMessage(l10n)` in place of `e.message` inside their `on ApiException catch (e)` block."
  key_links:
    - "Each of the 7 sites already has `l10n` fetched via `AppLocalizations.of(context)!` before its `try` block (verified per-file during planning) — no new `l10n` fetch needs to be added, unlike 14-04's setlist sites."
---

<objective>
Wire the `ApiExceptionLocalization.localizedMessage()` extension (built and proven in 14-01) into all 7 remaining band-feature catch sites — none of which have a screen-specific override, so each is a mechanical one-line swap.

Purpose: Expand localized error-code coverage across the Bands feature area (I18N-05).
Output: 7 files updated; 2 existing widget-test files extended with a known-error-code regression case.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-api-error-localization/14-CONTEXT.md
@lib/api/api_exception.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire edit/delete-band/remove-member/leave-band catch sites</name>
  <files>lib/features/bands/edit_band_screen.dart, lib/features/bands/confirm_delete_band_dialog.dart, lib/features/bands/confirm_remove_member_dialog.dart, lib/features/bands/confirm_leave_band_dialog.dart, test/features/bands/edit_band_screen_test.dart</files>
  <read_first>
    lib/api/api_exception.dart (confirm the `ApiExceptionLocalization.localizedMessage(l10n)` signature from 14-01)
    lib/features/bands/edit_band_screen.dart lines 40-82 (`_submit()` — `l10n` fetched at line 49, before `try`)
    lib/features/bands/confirm_delete_band_dialog.dart lines 44-68 (`_delete()` — `l10n` fetched at line 45, before `try`)
    lib/features/bands/confirm_remove_member_dialog.dart lines 38-62 (`_remove()` — `l10n` fetched at line 39, before `try`)
    lib/features/bands/confirm_leave_band_dialog.dart lines 35-65 (`_leave()` — `l10n` fetched via widget/dialog build path already in scope before `try`, mirrors the other 3)
    test/features/bands/edit_band_screen_test.dart (existing test scaffold/patterns to model the new test on)
  </read_first>
  <action>
    In each of `edit_band_screen.dart`, `confirm_delete_band_dialog.dart`, `confirm_remove_member_dialog.dart`, `confirm_leave_band_dialog.dart`: change the `on ApiException catch (e)` block's body from `setState(() => _errorMessage = e.message);` to `setState(() => _errorMessage = e.localizedMessage(l10n));`. `l10n` is already a local in scope in every one of these 4 methods — do not add a new fetch. Leave the `catch (_)` fallback block (already localized) and the `finally` block untouched in all 4 files.

    In `test/features/bands/edit_band_screen_test.dart`, add one new `testWidgets` block modeled on the file's existing "renders an inline error" test: mock the `updateBand` PUT to return `http.Response(jsonEncode({'code': 'permission_denied', 'message': 'raw server text'}), 400)`, submit the edit form, and assert `find.text(tester.strings.commonErrorPermissionDenied)` is found while `find.text('raw server text')` is not found.
  </action>
  <verify>
    <automated>flutter test test/features/bands/edit_band_screen_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - All 4 files' `on ApiException catch (e)` blocks call `e.localizedMessage(l10n)`, no file introduces a new `l10n` fetch (all 4 already have one in scope).
    - `flutter test test/features/bands/edit_band_screen_test.dart` passes, including the new `permission_denied` case.
    - `flutter analyze` reports no new issues in the 4 modified `.dart` files.
  </acceptance_criteria>
  <reversibility rating="reversible">Client-side string-display swap only; trivially revertable via git.</reversibility>
  <done>edit_band_screen.dart, confirm_delete_band_dialog.dart, confirm_remove_member_dialog.dart, and confirm_leave_band_dialog.dart all show the localized generic message for known error codes; proven by a passing widget test on the edit-band path.</done>
</task>

<task type="auto">
  <name>Task 2: Wire rotate-invite-code/transfer-ownership/join-band catch sites</name>
  <files>lib/features/bands/confirm_rotate_invite_code_dialog.dart, lib/features/bands/confirm_transfer_ownership_dialog.dart, lib/features/bands/join_band_dialog.dart, test/features/bands/confirm_rotate_invite_code_dialog_test.dart</files>
  <read_first>
    lib/api/api_exception.dart (confirm the `ApiExceptionLocalization.localizedMessage(l10n)` signature from 14-01)
    lib/features/bands/confirm_rotate_invite_code_dialog.dart lines 34-70 (`_rotate()` — `l10n` fetched at line 35, before `try`)
    lib/features/bands/confirm_transfer_ownership_dialog.dart lines 40-81 (`_transfer()` — `l10n` fetched at line 41, before `try`)
    lib/features/bands/join_band_dialog.dart lines 77-124 (`_submit()` — `l10n` fetched at line 87, before `try`)
    test/features/bands/confirm_rotate_invite_code_dialog_test.dart (existing test scaffold/patterns to model the new test on)
  </read_first>
  <action>
    In each of `confirm_rotate_invite_code_dialog.dart`, `confirm_transfer_ownership_dialog.dart`, `join_band_dialog.dart`: change the `on ApiException catch (e)` block's body from `setState(() => _errorMessage = e.message);` to `setState(() => _errorMessage = e.localizedMessage(l10n));`. `l10n` is already a local in scope in every one of these 3 methods — do not add a new fetch. Leave the `catch (_)` fallback block and `finally` block untouched in all 3 files.

    In `test/features/bands/confirm_rotate_invite_code_dialog_test.dart`, add one new `testWidgets` block modeled on the file's existing error-rendering test: mock the rotate-invite-code POST to return `http.Response(jsonEncode({'code': 'operation_rejected', 'message': 'raw server text'}), 400)`, trigger the rotate action, and assert `find.text(tester.strings.commonErrorOperationRejected)` is found while `find.text('raw server text')` is not found.
  </action>
  <verify>
    <automated>flutter test test/features/bands/confirm_rotate_invite_code_dialog_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - All 3 files' `on ApiException catch (e)` blocks call `e.localizedMessage(l10n)`, no file introduces a new `l10n` fetch (all 3 already have one in scope).
    - `flutter test test/features/bands/confirm_rotate_invite_code_dialog_test.dart` passes, including the new `operation_rejected` case.
    - `flutter analyze` reports no new issues in the 3 modified `.dart` files.
  </acceptance_criteria>
  <reversibility rating="reversible">Client-side string-display swap only; trivially revertable via git.</reversibility>
  <done>confirm_rotate_invite_code_dialog.dart, confirm_transfer_ownership_dialog.dart, and join_band_dialog.dart all show the localized generic message for known error codes; proven by a passing widget test on the rotate-invite-code path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server HTTP error response → `ApiException.code`/`message` → client UI text | Same boundary as 14-01, now exercised at 7 more Bands-feature call sites via the already-mitigated shared extension. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-05 | Tampering | 7 Bands-feature catch sites calling `localizedMessage` | low | mitigate | No new logic introduced at these call sites beyond the already-mitigated extension from 14-01 (T-14-01) — each site is a mechanical call-site swap with no new switch/lookup logic of its own. |

No new authentication, authorization, or data-flow surface is introduced.
</threat_model>

<verification>
- `flutter test test/features/bands/edit_band_screen_test.dart` passes.
- `flutter test test/features/bands/confirm_rotate_invite_code_dialog_test.dart` passes.
- `flutter analyze` shows no new issues across the 7 modified files.
</verification>

<success_criteria>
- All 7 band-feature `on ApiException catch (e)` sites call `e.localizedMessage(l10n)`.
- 2 representative sites (edit-band, rotate-invite-code) have automated proof of a known-code → localized-message mapping.
</success_criteria>

<output>
Create `.planning/phases/14-api-error-localization/14-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

(Complete list across all of Phase 14's plans — 14-01 through 14-04.)

- `ApiExceptionLocalization` extension on `ApiException`, method `String localizedMessage(AppLocalizations l10n, {Map<String, String>? overrides})` — defined in `lib/api/api_exception.dart` (created in 14-01).
- ARB keys `commonErrorInvalidInput`, `commonErrorNotFound`, `commonErrorPermissionDenied`, `commonErrorOperationRejected`, `commonErrorAlreadyExists` in `lib/l10n/app_en.arb` and `lib/l10n/app_ru.arb`, and their generated `AppLocalizations` getters in `lib/generated/app_localizations*.dart` (created in 14-01).
- The `overrides` named parameter on `localizedMessage` (the D-04 override mechanism) (created in 14-01, consumed in 14-03).
- `test/features/auth/login_screen_test.dart` — new test file, did not exist before this phase (created in 14-03).
