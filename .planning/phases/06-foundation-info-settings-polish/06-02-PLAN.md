---
phase: 06-foundation-info-settings-polish
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - lib/features/bands/band_detail_screen.dart
  - lib/features/bands/bands_screen.dart
  - test/features/bands/bands_screen_test.dart
  - test/features/bands/band_detail_screen_test.dart
autonomous: true
requirements: [BAND-10]

estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "On the Bands list, each row's trailing text shows the member count and, once BandListItem.ownerId is present and the current user's profile has loaded, the user's role: 'N members' alone or 'N members • Owner'/'N members • Member' (BAND-10, UI-SPEC populated row)"
    - "member-count pluralization is correct: '1 member' (singular) vs 'N members' (plural), N != 1"
    - "If BandListItem.ownerId is absent (backend hasn't shipped it yet) the role suffix is omitted entirely -- only the member count shows (D-06 graceful degradation, UI-SPEC partial row)"
    - "If ownerId is present but the current user's profile is still loading, the role suffix is also omitted until profile data resolves (tri-state guard, RESEARCH.md Pitfall 4) -- it must never flash 'Member' before flipping to 'Owner'"
    - "On the Band detail screen, a role + member-count row ('Owner • N members' or 'Member • N members') appears below the band name/avatar, shown only once ownership status has resolved (UI-SPEC populated row, Band detail)"
    - "Bands list and Bands detail reuse the same ownership tri-state logic (BandDetailScreen.isOwner / BandDetailScreen.ownershipStatus) rather than reimplementing it -- per D-05's explicit reuse instruction"
    - "Bands list and detail screens reuse their existing screen-level loading/error states (no new per-row skeleton or per-row error UI) for this addition"
  artifacts:
    - lib/features/bands/band_detail_screen.dart
    - lib/features/bands/bands_screen.dart
  key_links:
    - "BandListItem.ownerId (added in 06-01) -> BandsScreen row -> BandDetailScreen.ownershipStatus(profileAsync, ownerId) -> role suffix text"
    - "BandDetailScreen._buildContent's existing `members`/`isOwner` locals -> new role/count Text row"
---

<objective>
Deliver BAND-10: show each band's member count and the current user's role (Owner/Member) on both the Bands list and the Band detail screen, reusing `BandDetailScreen`'s existing ownership tri-state helpers rather than reimplementing role logic on the list screen (D-05).

Purpose: Establishes the "reuse an existing tri-state helper across two screens" display pattern for the phase, using `BandListItem.ownerId` (added in Plan 06-01) which is now available to both screens.
Output: Bands list rows and the Band detail screen both surface member count + role.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-foundation-info-settings-polish/06-CONTEXT.md
@.planning/phases/06-foundation-info-settings-polish/06-UI-SPEC.md
@lib/features/bands/band_detail_screen.dart
@lib/features/bands/bands_screen.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make BandDetailScreen's ownership helpers reusable and add its role/count row</name>
  <files>lib/features/bands/band_detail_screen.dart, test/features/bands/band_detail_screen_test.dart</files>
  <read_first>
    lib/features/bands/band_detail_screen.dart (lines 22-43 -- `_isOwner`/`_ownershipStatus` static helpers and their doc comments; lines 94-123 -- `_buildContent`'s name/avatar header and existing `isOwner` local var)
    test/features/bands/band_detail_screen_test.dart (the `band()` test-data builder and `buildRoutedApiClient` helper that routes `/api/me` vs the band GET -- reuse both for the new test)
    .planning/phases/06-foundation-info-settings-polish/06-UI-SPEC.md (`## Band Member Count & Role Display` -> `Detail Screen Pattern` section)
  </read_first>
  <action>
    IMPORTANT correctness fix: 06-PATTERNS.md's suggested snippet for `bands_screen.dart` calls `BandDetailScreen._isOwner(...)` directly from a different file -- that does not compile, because a leading-underscore identifier is library-private to its own file in Dart, not merely class-private. Do NOT copy that snippet as-is.

    In `lib/features/bands/band_detail_screen.dart`, rename the two static helpers to drop their leading underscore, making them public so Task 2 (in `bands_screen.dart`) can call them qualified as `BandDetailScreen.isOwner(...)` / `BandDetailScreen.ownershipStatus(...)`: `_isOwner` -> `isOwner`, `_ownershipStatus` -> `ownershipStatus`. Update every in-file call site (the doc comment's `[_ownershipStatus]` reference, the internal `_ownershipStatus` -> `_isOwner` call, and `_buildContent`'s `final isOwner = _ownershipStatus(profileAsync, ownerId);` line) to the new names. Keep both methods' signatures, tri-state semantics, and doc comments otherwise unchanged.

    In `_buildContent`, insert a new role/count row directly below the existing band-name `Text` (after its `SizedBox(height: 24)`, before the `Divider`), guarded on `isOwner != null` (never render while unresolved, per the existing tri-state discipline this file already uses for the Delete/Leave tiles): `Center(child: Text('${isOwner ? 'Owner' : 'Member'} • ${members.length} member${members.length == 1 ? '' : 's'}'))`, followed by `const SizedBox(height: 24)`. Use `members.length` (the already-parsed `members` list) as the count -- the full `Band` detail response has no separate `membersCount` field, only `BandListItem` (list response) does.

    Add a test to `test/features/bands/band_detail_screen_test.dart` reusing the existing `band()`/`buildRoutedApiClient()` helpers: pump as the owner profile (`'id': 'u1'`, matching `band()`'s default `ownerId: 'u1'`) and assert `find.text('Owner • 1 members')` (the default `band()` fixture has one member); pump as a non-owner profile (`'id': 'u2'`) and assert `find.text('Member • 1 members')`.
  </action>
  <verify>
    <automated>flutter test test/features/bands/band_detail_screen_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `BandDetailScreen.isOwner` and `BandDetailScreen.ownershipStatus` are public static methods (no leading underscore), with the same tri-state contract as before.
    - `_buildContent` renders `'{Owner|Member} • N members'` below the band name only when `isOwner != null`.
    - No remaining reference to `_isOwner`/`_ownershipStatus` anywhere in `band_detail_screen.dart`.
    - `flutter test test/features/bands/band_detail_screen_test.dart` passes, including the two new role-row assertions and every pre-existing test in the file (the rename must not break the file's other 20+ tests that exercise `isOwner`-gated Delete/Leave visibility).
  </acceptance_criteria>
  <done>Band detail screen shows "Owner • N members" or "Member • N members" below the band name, and the ownership helpers are public so the Bands list can reuse them without reimplementation.</done>
</task>

<task type="auto">
  <name>Task 2: Bands list -- member count + role trailing text</name>
  <files>lib/features/bands/bands_screen.dart, test/features/bands/bands_screen_test.dart</files>
  <read_first>
    lib/features/bands/bands_screen.dart (full file -- `build`'s current `bandsAsync`/`isOnline` watches and `_buildContent`'s `ListTile` with the bare `Icons.chevron_right` trailing)
    lib/features/bands/band_detail_screen.dart (Task 1's now-public `isOwner`/`ownershipStatus` -- read after Task 1 completes, since this task depends on it)
    lib/providers/profile_provider.dart (`profileDataProvider` -- the provider to watch for the current user id)
    test/features/bands/bands_screen_test.dart (existing `buildApiClient`/`wrap` helper -- note it currently does NOT route `/api/me`, since `BandsScreen` didn't previously need the profile)
  </read_first>
  <action>
    In `lib/features/bands/bands_screen.dart`, import `../../providers/profile_provider.dart` and `band_detail_screen.dart`. In `build`, add `final profileAsync = ref.watch(profileDataProvider);` and pass it through to `_buildContent(context, bands, profileAsync)`.

    In `_buildContent`, for each band row read `final ownerId = band['ownerId'] as String?;` and `final membersCount = band['membersCount'] as int;`, compute `final isOwner = ownerId == null ? null : BandDetailScreen.ownershipStatus(profileAsync, ownerId);`. Replace the current bare `trailing: const Icon(Icons.chevron_right)` with a plain `Text` trailing (dropping the chevron -- Tracks and Setlists list rows already use a plain text trailing with no chevron, so this keeps all three list screens visually consistent): `trailing: Text(isOwner == null ? _membersLabel(membersCount) : '${_membersLabel(membersCount)} • ${isOwner ? 'Owner' : 'Member'}')`. Add a small private top-level helper in this file: `String _membersLabel(int count) => '$count member${count == 1 ? '' : 's'}';`.

    Update `test/features/bands/bands_screen_test.dart`'s `buildApiClient`/`wrap` helper to route `/api/me` the same way `band_detail_screen_test.dart`'s `buildRoutedApiClient` does (since `BandsScreen` now watches `profileDataProvider`, every existing test's mock handler must respond to `GET /api/me` or the profile provider will error/hang) -- add a `profile` parameter (default `{'id': 'u1', 'username': 'tester'}`) to `wrap`, and route the mock handler: `if (request.url.path == '/api/me') return http.Response(jsonEncode(profile), 200);` before the existing band-list handling. Add new tests: (1) a band with `ownerId` matching the pumped profile id shows `'1 member • Owner'` in trailing; (2) a band with `ownerId` NOT matching shows `'1 member • Member'`; (3) a band with no `ownerId` key shows just `'1 member'` (no bullet, no role); (4) a band with `membersCount: 5` shows `'5 members'` (plural).
  </action>
  <verify>
    <automated>flutter test test/features/bands/</automated>
  </verify>
  <acceptance_criteria>
    - `BandsScreen` watches `profileDataProvider` and computes per-row ownership via `BandDetailScreen.ownershipStatus`, never reimplementing the id comparison.
    - Trailing text shows `'N member(s)'` alone when `ownerId` is absent, and `'N member(s) • Owner|Member'` once resolved.
    - `flutter test test/features/bands/` passes, including all pre-existing `bands_screen_test.dart` tests (now updated to route `/api/me`) and the four new role/count tests.
  </acceptance_criteria>
  <done>Bands list rows show member count and (once available) role, reusing BandDetailScreen's ownership helper.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| BandListItem.ownerId / profileDataProvider -> role text | Server-returned ids are compared client-side and rendered as a role label; no new mutation surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-02-01 | Information Disclosure | BandsScreen trailing role text | low | accept | `ownerId` is already returned (and thus already visible to the same authenticated band member) via the full `Band` detail response consumed by `BandDetailScreen` today; surfacing it on the list adds no new data exposure, only a display convenience. |
| T-06-02-02 | Elevation of Privilege | Role label rendering | low | accept | The role text is purely informational (read-only display); it does not gate any action, grant any capability, or change existing owner-only mutation gates (Delete/rotate-invite/transfer-ownership remain server- and existing-client-enforced, unchanged by this plan, per REQUIREMENTS.md's Out-of-Scope note that owner gates were already verified compliant). |
</threat_model>

<verification>
- `flutter test test/features/bands/` passes (both files).
- `flutter analyze` reports no new errors.
</verification>

<success_criteria>
- Bands list shows member count + role per row, degrading gracefully when `ownerId` is missing or profile hasn't loaded.
- Band detail screen shows a role + member-count row below the band name.
</success_criteria>

<output>
Create `.planning/phases/06-foundation-info-settings-polish/06-02-SUMMARY.md` when done
</output>
