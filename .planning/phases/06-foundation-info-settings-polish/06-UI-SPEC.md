---
phase: 6
slug: foundation-info-settings-polish
status: draft
shadcn_initialized: false
platform: flutter
created: 2026-08-20
---

# Phase 6 — UI Design Contract

> Visual and interaction contract for Foundation Info & Settings Polish. Generated by gsd-ui-researcher.

---

## Design System

| Property | Value |
|----------|-------|
| Platform | Flutter (Material 3) |
| Theme | `AppTheme` (light/dark via `ColorScheme.fromSeed`) |
| Component library | Flutter Material Design |
| Icon library | Material Design Icons (`Icons.*`) |
| Font | System default (Roboto on Android, SF Pro Display on iOS) |

**Notes:**
- `AppTheme` defined in `lib/theme/app_theme.dart` (light/dark themes)
- Color scheme seeded from `Colors.green`
- No third-party component libraries (shadcn/radix N/A for Flutter)

---

## Spacing Scale

Declared values (multiples of 4, consistent with existing codebase):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon-to-text gaps in inline rows |
| sm | 8px | Compact element spacing, dialog padding |
| md | 16px | Default field spacing, list item padding |
| lg | 24px | Screen/section padding, major spacing |
| xl | 32px | Layout gaps, header spacing |
| 2xl | 48px | Top spacing for large sections |

Exceptions: None (all hardcoded spacing in existing code already respects multiples of 4).

**Rationale:** Existing codebase uses 8, 12, 16, 24, 32, 48 — all multiples of 4. Phase 6 maintains this scale for consistency with existing screens.

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 14px | 400 (regular) | 1.5 |
| Label | 14px | 500 (medium) | 1.4 |
| Heading (medium) | 28px | 400 (regular) | 1.2 |
| Display | 32px | 400 (regular) | 1.2 |

**Source:** Flutter Material 3 defaults via `ThemeData` and `textTheme.*` properties.

**Usage:**
- `textTheme.bodyMedium` (14px/400) → body copy in forms, lists, detail screens
- `textTheme.labelMedium` (14px/500) → field labels, buttons
- `textTheme.headlineMedium` (28px/400) → screen titles, section headings (already used in existing Profile screen)
- Form fields use labelText + `OutlineInputBorder` (matches existing LoginScreen pattern)

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `Colors.green` seed → light/dark surfaces | App background, default surfaces |
| Secondary (30%) | `ColorScheme.secondary` (derived from seed) | Card backgrounds, subtle UI elements |
| Accent (10%) | `ColorScheme.primary` (green-derived) | Primary buttons, active selections, highlights |
| Destructive | `ColorScheme.error` | Logout button, destructive actions, error messages |

**Accent reserved for:**
- Primary action buttons (Save, Change Password, Add, Create)
- Active navigation selection
- Error state text/borders (red)
- Logout/sign-out action

**Rationale:**
- Flutter's `ColorScheme.fromSeed(seedColor: Colors.green)` auto-generates harmonious light/dark schemes
- Existing codebase already uses `colorScheme.error` for error messages (seen in LoginScreen)
- No custom colors introduced; all derived from Material Design system

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA (Password Save) | "Change password" (button label) |
| Primary CTA (Band Member Count) | "View details" (navigation, implicit via ListTile tap) |
| Form label (Current Password) | "Current password" |
| Form label (New Password) | "New password" |
| Form label (Confirm Password) | "Confirm new password" |
| Success state (Password Changed) | "Password changed successfully" (SnackBar) |
| Error state (Wrong Current Password) | "Current password is incorrect" (error message below form) |
| Error state (Passwords Don't Match) | "Passwords don't match" (validator on confirm field) |
| Error state (Password Too Short) | "At least 8 characters" (validator) |
| Empty state (No Bands) | "No bands yet\nCreate a band or ask a bandmate to add you." (already implemented in v1.0) |
| List label (Member Count) | "{count} member{s}" (inline text, e.g., "3 members") |
| List label (User Role) | "Owner" or "Member" (badge or label, inline) |
| Track metadata labels | "Key: {key}" (optional, from `key` field if present) — icon display only if field present |
| Track metadata labels | "Duration: {mm:ss}" (always shown via `DurationFormatting.asMinutesSeconds`) |
| Track metadata labels | "Notes" (icon only; notes text shown on detail screen) |
| Setlist metadata labels | "Location: {eventLocation}" (icon display only if field present) |
| Setlist metadata labels | "Duration" (icon display; total duration calculated from tracks) |

---

## UI Components & Patterns

### Password Change Form

**Location:** Profile screen (`lib/features/profile/profile_screen.dart`)

**Layout:**
- Three TextFormField inputs stacked vertically
- 16px spacing between fields (`const SizedBox(height: 16)`)
- Each field uses `OutlineInputBorder()` (matches existing LoginScreen pattern)
- Submit button: `FilledButton` spanning full width (matches LoginScreen pattern)
- Disabled while submitting (loading spinner on button)

**Fields:**
1. Current password (label: "Current password", obscureText: true, required)
2. New password (label: "New password", obscureText: true, required, min 8 chars)
3. Confirm new password (label: "Confirm new password", obscureText: true, required, must match new password)

**Validators:**
- Current password: required (non-empty)
- New password: required, min 8 characters
- Confirm password: required, must match new password exactly

**Success Feedback:**
- On successful change, show SnackBar: "Password changed successfully"
- Remain on Profile screen (no navigation)

**Error Handling:**
- Server returns 401 → "Current password is incorrect" (displayed as red text below form)
- Validation failures → field-level error messages displayed inline
- Network error → "An error occurred. Please try again."

**State:**
- Initially: all fields empty, button enabled
- While submitting: button disabled, loading spinner
- After success: fields cleared, SnackBar shown
- On error: red text displayed below the form, fields preserved (user can retry)

### Band Member Count & Role Display

**Location:** 
- `lib/features/bands/bands_screen.dart` (list view)
- `lib/features/bands/band_detail_screen.dart` (detail view)

**List View Pattern:**
- ListTile with `trailing` widget containing: `"{count} member{s}" • "Owner"` or `"{count} member{s}" • "Member"`
- Icon before role label: optional role badge (visual indicator)
- Example: `"3 members • Owner"` or `"5 members • Member"`
- Font: `textTheme.bodySmall` or `textTheme.labelMedium` in trailing area
- Spacing: single line, compact

**Detail Screen Pattern:**
- Display below band name/avatar
- Layout: role badge on left, member count on right (two-column grid or single row)
- Example row: `👑 Owner  •  3 members`
- Icon choices (visual only, not clickable):
  - Owner: `Icons.star` (gold/yellow accent color) or no icon, just text
  - Member: no icon, plain text

**Implementation Notes:**
- Role computed via existing helper `_isOwner(currentUserId, ownerId)` in band_detail_screen.dart
- Reuse for list screen without reimplementing
- Member count comes from `BandListItem.membersCount` (already present in v1.0 API)
- Owner ID comes from new `BandListItem.ownerId` field (added in Phase 6 spec)

### Track List Metadata Icons

**Location:** `lib/features/tracks/track_list_screen.dart` and `track_detail_screen.dart`

**Icon Row Style (List Screen):**
- Displayed in ListTile `trailing` area as horizontal row
- Format: `[Icon] value  [Icon] value  [Icon] value` (if all fields present)
- Spacing between icon-value pairs: 8px (`const SizedBox(width: 8)`)
- Spacing between icon and text: 4px
- Font: `textTheme.labelSmall` or `bodySmall` for conciseness
- Icons: `Icons.music_note` (key), `Icons.timer` (duration), `Icons.notes` (notes)

**Fields:**
1. **Key** (optional) — shown only if `TrackListItem.key` is present
   - Icon: `Icons.music_note` (color: `colorScheme.primary`)
   - Value: `"{key}"` (e.g., "C" or "Cm" from the musicKeys list)
   
2. **Duration** (always shown)
   - Icon: `Icons.timer` (color: `colorScheme.primary`)
   - Value: formatted via `DurationFormatting.asMinutesSeconds` (e.g., "3:45")
   
3. **Notes** (detail screen only, not in list)
   - Icon: `Icons.notes` (color: `colorScheme.primary`)
   - Shown only on track detail screen as a section header or inline

**Graceful Degradation:**
- If `key` field is missing/null from server, omit the music_note icon and value
- If `notes` field is missing/null, omit the notes section (never show placeholder text like "No notes")
- Duration always present (required field in schema)

**Example List Row:**
```
Track Title (title)        🎵 C   ⏱ 3:45
```

### Setlist List Metadata Icons

**Location:** `lib/features/setlists/setlist_list_screen.dart` and `setlist_detail_screen.dart`

**Icon Row Style (List Screen):**
- Same as Track list (horizontal row in `trailing` area)
- Spacing between icon-value pairs: 8px
- Spacing between icon and text: 4px
- Font: `textTheme.labelSmall` or `bodySmall`
- Icons: `Icons.location_on` (location), `Icons.timer` (duration)

**Fields:**
1. **Location** (optional) — shown only if `SetlistListItem.eventLocation` is present
   - Icon: `Icons.location_on` (color: `colorScheme.primary`)
   - Value: `"{eventLocation}"` (e.g., "Whisky Barrel" or "The Fillmore")
   
2. **Duration** (always shown)
   - Icon: `Icons.timer` (color: `colorScheme.primary`)
   - Value: calculated from sum of all track durations in the setlist (e.g., "45:30")
   - Use `DurationFormatting.asMinutesSeconds` for consistent formatting

**Graceful Degradation:**
- If `eventLocation` field is missing/null from server, omit the location_on icon and value
- Duration always calculated (derived from existing track data)

**Example List Row:**
```
Tonight's Show (title)     📍 The Fillmore   ⏱ 45:30
```

---

## State Coverage

| State | Elements | Resolution |
|-------|----------|-----------|
| **Empty bands list** | Bands screen | "No bands yet\nCreate a band or ask a bandmate to add you." (copy + button) — already implemented in v1.0 |
| **Empty tracks list** | Track list screen | "No tracks yet\nCreate a track or ask a bandmate to add one." (copy + button) — already implemented in v1.0 |
| **Empty setlists list** | Setlist list screen | "No setlists yet\nCreate a setlist or ask a bandmate to add one." (implicit pattern) — handled by existing screen pattern |
| **Loading password form** | Profile screen (password section) | Form disabled, submit button disabled until page loads |
| **Password field invalid** | Profile screen | Red validation text below field (matches LoginScreen error pattern) |
| **Submitting password change** | Profile screen (password button) | Button disabled, loading spinner shown (matches LoginScreen pattern) |
| **Password change success** | Profile screen | SnackBar: "Password changed successfully"; fields cleared |
| **Server error on password change** | Profile screen | Red text: "Current password is incorrect" or generic "An error occurred" (matches LoginScreen pattern) |
| **Optional fields missing** | Track/Setlist list rows | Icon row omits missing fields gracefully (e.g., if no key, show only ⏱ 3:45, not 🎵  ⏱ 3:45) |
| **Owner ID missing from list** | Band list rows | Role display omitted until data loads or backend provides ownerId |

---

## Icon Specifications

**Material Design Icons Used:**

| Icon | Code | Usage | Color |
|------|------|-------|-------|
| Music note | `Icons.music_note` | Track key indicator | `colorScheme.primary` |
| Timer | `Icons.timer` | Duration indicator (track/setlist) | `colorScheme.primary` |
| Notes | `Icons.notes` | Track notes indicator (detail screen only) | `colorScheme.primary` |
| Location on | `Icons.location_on` | Setlist location indicator | `colorScheme.primary` |

**Icon Size:** 18-20dp (standard Material icon size in lists; Flutter default is 24dp, use `IconTheme.of(context).size` or wrap in `IconTheme(data: IconThemeData(size: 20), child: Icon(...))` if needed for compact trailing areas)

**Color:** All icons use `colorScheme.primary` (green-derived from seed) for consistency with accent color.

---

## Spacing Checklist

Verify all hardcoded spacing values respect the 4-point scale:

- [ ] Form field vertical spacing: 16px ✓
- [ ] Form section padding: 24px ✓
- [ ] List item trailing icon spacing: 4px (icon-to-text), 8px (pair-to-pair) ✓
- [ ] Button vertical spacing: 8px (below form), 16px (form to button) ✓
- [ ] Modal/dialog padding: 8-16px ✓
- [ ] SizedBox heights in lists: 8px, 12px, 16px, 24px, 32px ✓

---

## Form Validation Rules

### Password Change Form

| Field | Rules |
|-------|-------|
| Current password | Required (non-empty). No length minimum (user may have set any password). Error: "This field is required" (generic validator). |
| New password | Required, min 8 characters (matches auth requirements). Error: "At least 8 characters". |
| Confirm new password | Required, must match "New password" field exactly. Error: "Passwords don't match". |
| Overall form | Submit only if all fields pass validation and not already submitting. |

**API Integration:**
- After validation passes, call `PublicApi.changePassword(currentPassword: ..., newPassword: ...)`
- Server may return 401 if currentPassword is incorrect → display "Current password is incorrect"
- Server may accept the request successfully → show SnackBar and clear form

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS (N/A — no third-party registries in Flutter)

**Approval:** pending

---

## Implementation Notes

### Requirements Mapping

| Requirement | Section | Status |
|-------------|---------|--------|
| USER-03: Password change | Password Change Form | Specified |
| BAND-10: Member count & role | Band Member Count & Role Display | Specified |
| TRACK-07: Key/duration/notes icons | Track List Metadata Icons | Specified |
| SETL-11: Location/duration icons | Setlist List Metadata Icons | Specified |

### API Schema Additions (Client-First)

Phase 6 extends `lib/api/publicapi.yml` with the following client-first additions (backend implements separately):

1. **ChangeUserPasswordRequestBody** — add field `currentPassword` (string, required)
2. **TrackListItem** — add field `key` (string, optional)
3. **SetlistListItem** — add field `eventLocation` (string, optional)
4. **BandListItem** — add field `ownerId` (string, optional)

**Precedent:** SETL-12 (Phase 10) already established this pattern. Fields not yet returned by server come back absent/null; UI must degrade gracefully.

### Code Reuse

- `DurationFormatting.asMinutesSeconds` → reuse for track and setlist duration display
- `musicalKeys` list → reuse for track key dropdown/display
- `_isOwner(currentUserId, ownerId)` helper → reuse for band role computation
- LoginScreen's TextFormField + validator pattern → reuse for password change form
- ListTile + trailing widget pattern → reuse for metadata icon rows

### Deferred (Phase 7+)

- Staleness badge removal (Phase 7: Cache Behavior Flip)
- Offline warning banner (Phase 7)
- Owner-only controls (Band owner tools in Phase 8)
