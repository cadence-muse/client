---
phase: 06-foundation-info-settings-polish
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - lib/features/tracks/track_list_screen.dart
  - lib/features/tracks/track_detail_screen.dart
  - test/features/tracks/track_list_screen_test.dart
  - test/features/tracks/track_detail_screen_test.dart
autonomous: true
requirements: [TRACK-07]

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Track list rows show a key icon (Icons.music_note) + value only when TrackListItem.key is present, and a duration icon (Icons.timer) + mm:ss value always -- e.g. 'Track Title  <note> C  <timer> 3:45' (TRACK-07, UI-SPEC populated row)"
    - "When key is null/absent, only the duration icon+value renders -- no placeholder, no empty note icon (D-06 graceful degradation, UI-SPEC partial row)"
    - "Track detail screen shows icon rows (not label-prefixed text) for duration (always) and key (when present), replacing the old 'Duration: ...'/'Key: ...' prefixed text"
    - "Track detail's notes section is omitted entirely when notes is null/empty -- never a placeholder (UI-SPEC 'Track detail notes' empty row, unchanged from existing behavior)"
    - "Track detail's notes, when present, renders with a notes icon (Icons.notes), truncates to 2 lines with an ellipsis, and tapping it shows the full untruncated text in a SnackBar (UI-SPEC 'Track detail notes' overflow/long-text rows)"
    - "Track list/detail reuse their existing screen-level loading/error states -- no new per-row skeleton or per-row error UI for this addition"
  artifacts:
    - lib/features/tracks/track_list_screen.dart
    - lib/features/tracks/track_detail_screen.dart
  key_links:
    - "TrackListItem.key (added in 06-01) -> TrackListScreen trailing icon row"
    - "TrackDetailScreen notes Text -> GestureDetector -> ScaffoldMessenger SnackBar (tap-to-expand)"
---

<objective>
Deliver TRACK-07: musical key, duration, and notes as icon-based at-a-glance indicators on the per-band Track list and detail screens, per D-03/D-07's icon+inline-row style.

Purpose: Establishes the icon-row display pattern (SizedBox-constrained trailing Row on list screens, unprefixed icon+text rows on detail screens) that Plan 06-04 mirrors for Setlists.
Output: Track list rows and the Track detail screen both show icon-based key/duration/notes indicators.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-foundation-info-settings-polish/06-CONTEXT.md
@.planning/phases/06-foundation-info-settings-polish/06-UI-SPEC.md
@lib/features/tracks/track_list_screen.dart
@lib/features/tracks/track_detail_screen.dart
@lib/features/tracks/track_formatting.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Track list -- key + duration icon row</name>
  <files>lib/features/tracks/track_list_screen.dart, test/features/tracks/track_list_screen_test.dart</files>
  <read_first>
    lib/features/tracks/track_list_screen.dart (lines 87-114 -- `_buildContent`'s `ListView.separated`/`itemBuilder`, and the current `trailing: Text(durationSeconds?.asMinutesSeconds ?? '—')` at line 102)
    lib/features/tracks/track_formatting.dart (`DurationFormatting.asMinutesSeconds` -- already imported)
    .planning/phases/06-foundation-info-settings-polish/06-UI-SPEC.md (`## Track List Metadata Icons` and `## Icon Specifications` sections -- icon choices/color/size)
    test/features/tracks/track_list_screen_test.dart (existing `buildApiClient`/`wrap` helper and the 'populated list...' test at line 41, which must keep passing unmodified since duration's rendered string doesn't change)
  </read_first>
  <action>
    In `lib/features/tracks/track_list_screen.dart`'s `_buildContent`, inside `itemBuilder`, replace `trailing: Text(durationSeconds?.asMinutesSeconds ?? '—')` with an icon row. Read `final key = track['key'] as String?;` alongside the existing `durationSeconds` read. Build: `final colorScheme = Theme.of(context).colorScheme;` then `trailing: SizedBox(width: 130, child: Row(mainAxisSize: MainAxisSize.min, mainAxisAlignment: MainAxisAlignment.end, children: [ if (key != null) ...[Icon(Icons.music_note, size: 18, color: colorScheme.primary), const SizedBox(width: 4), Text(key, style: const TextStyle(fontSize: 12), maxLines: 1, overflow: TextOverflow.ellipsis), const SizedBox(width: 8)], Icon(Icons.timer, size: 18, color: colorScheme.primary), const SizedBox(width: 4), Text(durationSeconds?.asMinutesSeconds ?? '—', style: const TextStyle(fontSize: 12)) ]))`. Duration's icon+text always renders (matches the current unconditional behavior); the key icon+text pair only renders when `key != null` -- no `SizedBox(width: 8)` gap before duration when key is absent (the conditional spread `...[]` already achieves this by only emitting its trailing gap alongside the key pair).

    Add two tests to `test/features/tracks/track_list_screen_test.dart`: (1) a cached track with `'key': 'C'` shows `find.byIcon(Icons.music_note)` (`findsOneWidget`), `find.text('C')`, and `find.byIcon(Icons.timer)`; (2) a cached track with no `key` entry in its map shows `find.byIcon(Icons.music_note)` `findsNothing` while `find.byIcon(Icons.timer)` still `findsOneWidget`.
  </action>
  <verify>
    <automated>flutter test test/features/tracks/track_list_screen_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - Track list trailing renders `Icons.music_note` + key text only when `track['key'] != null`.
    - Track list trailing always renders `Icons.timer` + the existing `asMinutesSeconds` duration text (unchanged string format from before this plan).
    - `flutter test test/features/tracks/track_list_screen_test.dart` passes, including the pre-existing duration-text assertion (`find.text('3:45')`) and the two new key-icon tests.
  </acceptance_criteria>
  <done>Track list rows show a key icon+value (when present) and a duration icon+value (always), replacing the old bare duration text.</done>
</task>

<task type="auto">
  <name>Task 2: Track detail -- icon rows for duration/key, notes with truncation + tap-to-expand</name>
  <files>lib/features/tracks/track_detail_screen.dart, test/features/tracks/track_detail_screen_test.dart</files>
  <read_first>
    lib/features/tracks/track_detail_screen.dart (lines 67-124 -- `_buildContent`'s existing `Text('Duration: ...')`/`Text('Tempo: ...')`/`Text('Key: $key')`/`Text('Notes: $notes')` rows and the already-declared `colorScheme` local var)
    .planning/phases/06-foundation-info-settings-polish/06-UI-SPEC.md (`## Track List Metadata Icons` fields list and `## UI Considerations` -- "Track detail notes" overflow/long-text rows: "tap expands to full text")
    test/features/tracks/track_detail_screen_test.dart (lines 88-118 -- the 'a full BandTrack response...' test, which asserts the OLD `'Duration: 3:45'`/`'Key: C'`/`'Notes: Some notes'` label-prefixed strings that this task's format change will break)
  </read_first>
  <action>
    In `lib/features/tracks/track_detail_screen.dart`'s `_buildContent`, replace the plain `Text('Duration: ${durationSeconds?.asMinutesSeconds ?? '—'}')` with an icon row (no "Duration:" prefix -- the icon carries that meaning per D-07): `Row(children: [Icon(Icons.timer, size: 20, color: colorScheme.primary), const SizedBox(width: 8), Text(durationSeconds?.asMinutesSeconds ?? '—')])`. Leave the `Text('Tempo: $tempo BPM')` block completely unchanged -- tempo is out of TRACK-07's scope (only key/duration/notes are required).

    Replace the `if (key != null) ...[..., Text('Key: $key')]` block's `Text` with `Row(children: [Icon(Icons.music_note, size: 20, color: colorScheme.primary), const SizedBox(width: 8), Text(key)])` (no "Key:" prefix).

    Replace the `if (notes != null && notes.isNotEmpty) ...[..., Text('Notes: $notes')]` block's `Text` with a tap-to-expand icon row: wrap in `GestureDetector(onTap: () => ScaffoldMessenger.of(context).showSnackBar(SnackBar(content: Text(notes))), child: Row(crossAxisAlignment: CrossAxisAlignment.start, children: [Icon(Icons.notes, size: 20, color: colorScheme.primary), const SizedBox(width: 8), Expanded(child: Text(notes, maxLines: 2, overflow: TextOverflow.ellipsis))]))` (no "Notes:" prefix -- the icon carries that meaning). Tapping shows the full, untruncated `notes` string in a SnackBar regardless of whether the 2-line truncation actually triggered.

    Update the existing test 'a full BandTrack response renders title/artist/duration/tempo/key/notes' in `test/features/tracks/track_detail_screen_test.dart`: change `expect(find.text('Duration: 3:45'), findsOneWidget);` to `expect(find.text('3:45'), findsOneWidget);`, `expect(find.text('Key: C'), findsOneWidget);` to `expect(find.text('C'), findsOneWidget);`, `expect(find.text('Notes: Some notes'), findsOneWidget);` to `expect(find.text('Some notes'), findsOneWidget);`; leave the `'Tempo: 120 BPM'` assertion untouched. Add assertions for `find.byIcon(Icons.timer)`, `find.byIcon(Icons.music_note)`, `find.byIcon(Icons.notes)` all `findsOneWidget` in that same test.

    Add a new test: pump a track with a long `notes` string (e.g. 200+ characters), `await tester.tap(find.byIcon(Icons.notes))`, `await tester.pump()`, and assert `find.text(<the full long notes string>)` `findsOneWidget` (the SnackBar).
  </action>
  <verify>
    <automated>flutter test test/features/tracks/track_detail_screen_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - Duration/key rows on the detail screen are icon+value with no "Duration:"/"Key:" text prefix; Tempo remains prefixed and unchanged.
    - Notes row shows a notes icon, truncates to 2 lines with an ellipsis, and tapping it shows the full text via a SnackBar.
    - `flutter test test/features/tracks/track_detail_screen_test.dart` passes, including the updated full-response test and the new tap-to-expand test.
  </acceptance_criteria>
  <done>Track detail screen shows icon rows for duration/key/notes, with notes truncating and expanding to full text on tap.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| TrackListItem.key / BandTrack.notes -> icon rows | Server-returned track metadata is rendered as-is; no new mutation surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-03-01 | Information Disclosure | TrackListScreen key icon row | low | accept | `key` was already visible to the same authenticated band member via `TrackDetailScreen` (full `BandTrack`); surfacing it on the list is a display convenience, not new exposure. |
| T-06-03-02 | Information Disclosure | Notes tap-to-expand SnackBar | low | accept | `notes` is already fully visible on the detail screen before this change; the SnackBar only re-displays the same already-rendered string (untruncated), not additional data. |
</threat_model>

<verification>
- `flutter test test/features/tracks/` passes (both files).
- `flutter analyze` reports no new errors.
</verification>

<success_criteria>
- Track list rows show key (when present) + duration icons.
- Track detail screen shows icon rows for duration/key, and notes with truncation + tap-to-expand.
</success_criteria>

<output>
Create `.planning/phases/06-foundation-info-settings-polish/06-03-SUMMARY.md` when done
</output>
