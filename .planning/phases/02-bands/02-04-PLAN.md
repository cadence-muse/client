---
phase: 02-bands
plan: 04
type: execute
wave: 4
depends_on: ["02-02", "02-03"]
files_modified:
  - lib/api/public_api.dart
  - lib/providers/bands_provider.dart
  - lib/features/bands/band_detail_screen.dart
  - lib/features/bands/edit_band_screen.dart
  - test/providers/band_detail_provider_test.dart
  - test/features/bands/edit_band_screen_test.dart
  - test/features/bands/band_detail_screen_test.dart
autonomous: true
requirements: [BAND-04]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "BandDetailScreen shows an 'Edit' action (available to any member, not owner-gated — BAND-04 has no ownership restriction per REQUIREMENTS.md and ROADMAP.md Phase 2 success criteria #4)"
    - "Tapping Edit opens EditBandScreen pre-filled with the band's current name"
    - "EditBandScreen's name field uses the same non-empty validator as CreateBandScreen (value.trim().isEmpty rejected inline); Save is blocked until the field is non-empty (BAND-04 edge probe: empty)"
    - "Long/multi-byte-script band names in the edit field render correctly via Flutter's default text layout — no custom encoding/length logic (BAND-04 edge probe: encoding)"
    - "PUT /api/band/{bandId} is REST-idempotent — submitting the same unchanged name twice is a no-op; the Save button is _isSubmitting-gated so a double-tap fires exactly one PUT request per submit (BAND-04 edge probe: idempotency/concurrency)"
    - "On success, BandDetailData(bandId) reflects the new name (either by direct state update or provider invalidation+refetch) and the user returns to BandDetailScreen showing the updated name"
  artifacts:
    - lib/features/bands/edit_band_screen.dart
  key_links:
    - "BandDetailScreen Edit action -> Navigator.push(EditBandScreen(bandId, currentName)) -> PublicApi.updateBand(bandId, name) -> ref.invalidate(bandDetailDataProvider(bandId)) -> Navigator.pop back to BandDetailScreen"
---

<objective>
Wire `PUT /api/band/{bandId}` end-to-end — an Edit action on `BandDetailScreen` opening a pre-filled form that updates the band's name (BAND-04).

Purpose: The fourth band mutation capability, extending the create-form pattern (02-03) to an edit flow and proving `BandDetailData`'s cache can be correctly invalidated/updated after a mutation on an already-viewed entity.
Output: A working Edit flow reachable from any band's detail screen, available to every member (not owner-gated).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-bands/02-CONTEXT.md
@.planning/phases/02-bands/02-RESEARCH.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end PUT /api/band/{bandId} — EditBandScreen + Edit action on detail (BAND-04)</name>
  <files>lib/api/public_api.dart, lib/providers/bands_provider.dart, lib/features/bands/band_detail_screen.dart, lib/features/bands/edit_band_screen.dart, test/providers/band_detail_provider_test.dart, test/features/bands/edit_band_screen_test.dart</files>
  <read_first>
    lib/features/bands/create_band_screen.dart (form/submit/validation pattern to adapt — same non-empty validator, same _isSubmitting/_errorMessage shape),
    lib/features/bands/band_detail_screen.dart (current structure — where to add the Edit action, and how band name is currently displayed),
    lib/providers/bands_provider.dart (BandDetailData — how to update its state after a successful mutation),
    lib/api/publicapi.yml lines 152-169 and 681-685 (UpdateBand operation: UpdateBandRequestBody {name}, response '200' has no content — no updated Band returned)
  </read_first>
  <behavior>
    - Test: `EditBandScreen` opens with its name field pre-filled with the `currentName` constructor argument.
    - Test: submitting an empty/whitespace-only name is rejected by validation before any API call fires.
    - Test: submitting a valid new name calls `PublicApi.updateBand(bandId: ..., name: ...)`; on success the screen pops back.
    - Test: `BandDetailScreen` shows an "Edit" action that navigates to `EditBandScreen` with the current band's `bandId` and `name`.
  </behavior>
  <action>
    Add `updateBand({required String bandId, required String name})` to `PublicApi` (`lib/api/public_api.dart`): `PUT /api/band/$bandId` with body `{'name': name}`, return `void` (per `publicapi.yml`, `UpdateBand`'s `'200'` response has no content schema — the client does not receive the updated `Band` back).

    Create `lib/features/bands/edit_band_screen.dart`: a `ConsumerStatefulWidget` `EditBandScreen` with required `bandId` and `currentName` `String` constructor fields, mirroring `CreateBandScreen`'s Form/validator/`_isSubmitting`/`_errorMessage` shape exactly, except the `TextEditingController` is initialized with `text: currentName` (pre-filled) and the submit button is labeled "Save". On submit: call `ref.read(publicApiProvider).updateBand(bandId: bandId, name: controller.text.trim())`; on success, since `UpdateBand` returns no body, update `BandDetailData(bandId)`'s cached state directly with the new name — either `ref.read(bandDetailDataProvider(bandId).notifier).state = AsyncData({...previous, 'name': newName})` merging the new name into the existing map, or call `ref.invalidate(bandDetailDataProvider(bandId))` to force a full refetch (prefer the direct-merge approach since it avoids an extra network round-trip and matches the "no response body" reality) — then `Navigator.of(context).pop()` back to `BandDetailScreen`.

    In `lib/features/bands/band_detail_screen.dart`, add an "Edit" action (e.g. an `IconButton(Icons.edit)` in the `AppBar.actions` or a `ListTile` in the actions section) visible to every viewer (no ownership check — BAND-04 is not owner-gated, unlike Delete/Remove-member) that navigates via `Navigator.push(MaterialPageRoute(builder: (_) => EditBandScreen(bandId: bandId, currentName: band['name'] as String)))`.
  </action>
  <acceptance_criteria>
    - `flutter test test/features/bands/edit_band_screen_test.dart` passes: pre-fill, empty-validation, submit-success-pops.
    - `grep -n "updateBand" lib/api/public_api.dart` finds the new method.
    - `grep -n "Edit" lib/features/bands/band_detail_screen.dart` finds the new action, with no `ownerId ==` comparison gating it (confirms non-owner-gated per BAND-04).
    - `flutter analyze` reports no new errors on touched files.
  </acceptance_criteria>
  <verify>
    <automated>flutter test test/features/bands/edit_band_screen_test.dart</automated>
  </verify>
  <done>Any band member can open Edit from the detail screen, change the band's name, and see the update reflected on return.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Edit-flow edge-state and cache-update coverage (BAND-04)</name>
  <files>test/features/bands/edit_band_screen_test.dart, test/providers/band_detail_provider_test.dart, test/features/bands/band_detail_screen_test.dart</files>
  <read_first>
    lib/features/bands/edit_band_screen.dart (from Task 1),
    lib/features/bands/band_detail_screen.dart (from Task 1),
    lib/providers/bands_provider.dart (BandDetailData state-update logic from Task 1)
  </read_first>
  <behavior>
    - Test: an `updateBand()` API failure renders an inline error and re-enables the Save button (mirrors `create_band_screen_test.dart`'s equivalent).
    - Test: a long/multi-byte band name (e.g. containing non-Latin script) is accepted by the field without a layout exception.
    - Test: submitting the SAME unchanged name still calls `updateBand()` exactly once per tap and pops successfully (idempotency — no client-side short-circuit skips the call, since the client can't know server-side whether it's truly a no-op).
    - Test: after a successful edit, `BandDetailScreen` displays the new name (verifying the cache-state update from Task 1, not a stale cached value).
  </behavior>
  <action>
    Extend `test/features/bands/edit_band_screen_test.dart` (created in Task 1) with the API-failure-inline-error test and the long/multi-byte-name-no-exception test, mirroring `create_band_screen_test.dart`'s equivalents.

    Extend `test/providers/band_detail_provider_test.dart` with a test that directly exercises the post-`updateBand`-success state-merge path: seed `BandDetailData(bandId)` with an initial name, simulate a successful `updateBand` call, and assert the provider's state reflects the new name without an additional network fetch (assert the mock `getBand` handler was NOT called again if the direct-merge approach was used).

    Extend `test/features/bands/band_detail_screen_test.dart` with an end-to-end widget test: navigate `BandDetailScreen` -> tap Edit -> `EditBandScreen` -> change name -> Save -> pop back to `BandDetailScreen` -> assert the new name renders (not the old cached one).
  </action>
  <acceptance_criteria>
    - `flutter test test/features/bands/edit_band_screen_test.dart test/providers/band_detail_provider_test.dart test/features/bands/band_detail_screen_test.dart` all pass.
  </acceptance_criteria>
  <verify>
    <automated>flutter test test/features/bands/edit_band_screen_test.dart test/providers/band_detail_provider_test.dart test/features/bands/band_detail_screen_test.dart</automated>
  </verify>
  <done>Edit flow's error/overflow/idempotency edge cases and the post-edit cache-state update are all covered by passing tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Flutter app <-> API server | `PUT /api/band/{bandId}` crosses the network boundary; user-typed `name` is the untrusted input sent, with no server response body to validate on return |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-07b | Tampering (invalid input) | `lib/features/bands/edit_band_screen.dart` (band `name` field) | low | accept | Same accepted posture as T-02-07 in 02-03-PLAN.md — non-empty client validation is UX-only, server is authoritative for real validation; no server response body means the client optimistically merges its own submitted value into the cache rather than trusting an echoed value, which is the correct trust direction (client only shows what it itself sent and the server accepted with a 200) |
</threat_model>

<verification>
`flutter test test/features/bands/edit_band_screen_test.dart test/providers/band_detail_provider_test.dart test/features/bands/band_detail_screen_test.dart` green; `flutter analyze` clean; manual check that editing a band's name persists and displays correctly after navigating away and back.
</verification>

<success_criteria>
BAND-04 fully satisfied: any band member can update the band's name from the detail screen, with the change reflected immediately.
</success_criteria>

## Artifacts this phase produces

- `PublicApi.updateBand({required String bandId, required String name})` (method) — `lib/api/public_api.dart`
- `EditBandScreen` (class, ConsumerStatefulWidget, constructor params `bandId`/`currentName`) — `lib/features/bands/edit_band_screen.dart`
- "Edit" action added to `BandDetailScreen` — `lib/features/bands/band_detail_screen.dart`

<output>
Create `.planning/phases/02-bands/02-04-SUMMARY.md` when done
</output>
