---
phase: 03-tracks
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/api/public_api.dart
  - lib/features/tracks/edit_track_screen.dart
  - lib/features/tracks/create_track_screen.dart
  - lib/features/tracks/confirm_delete_track_dialog.dart
  - lib/features/songs/tracks_screen.dart
  - lib/navigation/root_scaffold.dart
  - lib/providers/navigation_provider.dart
  - lib/providers/navigation_provider.g.dart
  - test/features/tracks/edit_track_screen_test.dart
  - test/features/tracks/create_track_screen_test.dart
  - test/features/tracks/confirm_delete_track_dialog_test.dart
  - test/providers/navigation_provider_test.dart
  - test/widget_test.dart
autonomous: true
gap_closure: true
requirements: [TRACK-02, TRACK-04, TRACK-05, TRACK-06]

estimate:
  tokens: 115000
  raw_tokens: 115000
  tasks: 5
  confidence: low

must_haves:
  truths:
    - "TRACK-01 (view list of tracks in a band): ✓ VERIFIED in 03-VERIFICATION.md, untouched by this plan — TrackListScreen is out of scope."
    - "TRACK-02 (add a track, title/artist required, duration/tempo/key/notes optional): ⚠️ PRESENT_BEHAVIOR_UNVERIFIED in 03-VERIFICATION.md — this plan closes the WR-02 half (non-numeric Duration/Tempo now rejected with feedback) and the CR-03 half (a created track now appears in the global Tracks tab without a manual filter change)."
    - "TRACK-03 (view a track's full detail): ✓ VERIFIED in 03-VERIFICATION.md, untouched by this plan — TrackDetailScreen is out of scope."
    - "TRACK-04 (edit a track's info and have changes persist): ✗ FAILED in 03-VERIFICATION.md (CR-01 crash + CR-02 data loss) — THIS PLAN FIXES BOTH: EditTrackScreen no longer crashes on an unrecognized key value, and clearing an optional field (Duration/Tempo/Notes) now actually persists as cleared."
    - "TRACK-05 (delete a track via a lightweight confirm dialog): ✓ VERIFIED in 03-VERIFICATION.md — this plan only adds the CR-03 global-tab invalidation to ConfirmDeleteTrackDialog; the delete flow itself is untouched."
    - "TRACK-06 (view all tracks across every band, filterable by band, via a global Tracks tab): ✗ FAILED in 03-VERIFICATION.md (CR-03 staleness + WR-01 no-op button) — THIS PLAN FIXES BOTH: all three per-band mutation flows now invalidate the global list provider, and the empty-state 'View bands' button now switches to the Bands tab per 03-UI-SPEC.md's citation."
  artifacts:
    - lib/providers/navigation_provider.dart (new — SelectedTabIndex Riverpod notifier backing selectedTabIndexProvider)
    - lib/providers/navigation_provider.g.dart (new — riverpod_generator output for navigation_provider.dart)
    - lib/api/public_api.dart (updateBandTrack rewritten to always send all 6 editable fields, title/artist now required)
    - lib/features/tracks/edit_track_screen.dart (CR-01 dropdown guard, CR-02 always-send patch map, CR-03 global invalidate)
    - lib/features/tracks/create_track_screen.dart (CR-03 global invalidate, WR-02 numeric validators)
    - lib/features/tracks/confirm_delete_track_dialog.dart (CR-03 global invalidate)
    - lib/features/songs/tracks_screen.dart (WR-01 "View bands" wired to selectedTabIndexProvider)
    - lib/navigation/root_scaffold.dart (ConsumerWidget reading selectedTabIndexProvider for IndexedStack index and NavigationBar.selectedIndex)
  key_links:
    - "EditTrackScreen._selectedKey init -> musicalKeys.contains() guard -> DropdownButtonFormField never receives an initialValue outside its items list (closes CR-01)"
    - "EditTrackScreen._submit() patch map -> PublicApi.updateBandTrack request body -> both unconditionally include all 6 fields, so an emptied field sends JSON null instead of being omitted (closes CR-02)"
    - "CreateTrackScreen/EditTrackScreen/ConfirmDeleteTrackDialog mutation success -> ref.exists(userTracksListDataProvider) guarded ref.invalidate(userTracksListDataProvider) -> TracksScreen's global list refetches without a manual filter change (closes CR-03)"
    - "TracksScreen 'View bands' ElevatedButton.onPressed -> ref.read(selectedTabIndexProvider.notifier).setIndex(2) -> RootScaffold.build() watches selectedTabIndexProvider for both IndexedStack.index and NavigationBar.selectedIndex, switching to the Bands tab (closes WR-01, per 03-UI-SPEC.md line 231's 'navigates to Bands tab' citation)"
---

<objective>
Close all 5 gaps from `.planning/phases/03-tracks/03-VERIFICATION.md` (status: `gaps_found`, 2/6 must-haves verified) that block Phase 03's goal: "Band members can maintain their band's song catalog." Phase 03 was executed across `03-01-PLAN.md`/`03-02-PLAN.md`/`03-03-PLAN.md`; this plan is purely additive — it does not replan any of their scope, only fixes the 5 correctness/UX bugs code review (`03-REVIEW.md`) and verification found in the already-built track CRUD feature set.

Purpose: TRACK-04 (edit) and TRACK-06 (global Tracks tab) are currently BLOCKED per 03-VERIFICATION.md's Requirements Coverage table. This plan unblocks both by fixing:
- CR-01: EditTrackScreen crashes if a track's `key` isn't one of the 24 hardcoded `musicalKeys` values.
- CR-02: Editing a track can never clear a previously-set optional field (Duration/Tempo/Notes) — the PUT request silently omits cleared fields instead of sending `null`.
- CR-03: Track create/edit/delete never invalidate the global cross-band Tracks tab's data provider, leaving it stale for the rest of the session.
- WR-01: The "View bands" button in the global Tracks tab's zero-bands empty state is a no-op (`onPressed: () {}`).
- WR-02: Duration/Tempo text fields silently discard non-numeric input with no validator/error feedback.

Output: `lib/api/public_api.dart`, `lib/features/tracks/edit_track_screen.dart`, `lib/features/tracks/create_track_screen.dart`, `lib/features/tracks/confirm_delete_track_dialog.dart`, and `lib/features/songs/tracks_screen.dart` fixed in place; a new `lib/providers/navigation_provider.dart` (+ generated `.g.dart`) backing tab-switching; `lib/navigation/root_scaffold.dart` converted to consume it; regression tests added to `test/features/tracks/edit_track_screen_test.dart`, `test/features/tracks/create_track_screen_test.dart`, `test/features/tracks/confirm_delete_track_dialog_test.dart`, `test/widget_test.dart`, and new `test/providers/navigation_provider_test.dart` — each proving the specific failure mode from 03-VERIFICATION.md no longer reproduces.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-tracks/03-VERIFICATION.md
@.planning/phases/03-tracks/03-REVIEW.md
@lib/providers/tracks_provider.dart
@lib/providers/theme_provider.dart
@lib/api/publicapi.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix CR-01 — guard EditTrackScreen's key dropdown against unrecognized values</name>
  <read_first>
    lib/features/tracks/edit_track_screen.dart (line 46's `_selectedKey` initializer and the `DropdownButtonFormField<String>` at lines 178-188)
    lib/features/tracks/track_formatting.dart (the 24-entry `musicalKeys` const list `_selectedKey` must be validated against)
    test/features/tracks/edit_track_screen_test.dart (existing `currentTrack` const, `wrap()` helper, and `openEditTrackScreen()` helper — the new test reuses this scaffolding)
  </read_first>
  <files>lib/features/tracks/edit_track_screen.dart, test/features/tracks/edit_track_screen_test.dart</files>
  <action>
    In edit_track_screen.dart, change the `_selectedKey` field initializer (currently `late String? _selectedKey = widget.currentTrack['key'] as String?;`) so it only pre-selects the track's `key` value when that value is a member of `musicalKeys`; otherwise it initializes to `null`. Use `musicalKeys.contains(widget.currentTrack['key'])` as the guard condition. Do not change the `DropdownButtonFormField`'s `items` list or `onChanged` handler — only the initial value assignment changes. This intentionally does NOT preserve the unrecognized value anywhere in the UI (matching 03-VERIFICATION.md's Next Steps #1 exactly) — a user editing such a track will need to explicitly pick a key from the list if they want one set.

    In test/features/tracks/edit_track_screen_test.dart, parametrize the existing `wrap()` helper to accept an optional `Map<String, dynamic>? currentTrack` parameter (defaulting to the existing top-level `currentTrack` const) so a test can open `EditTrackScreen` with a different track map without duplicating the whole helper. Add a new `testWidgets` case: build a `currentTrack`-shaped map whose `'key'` value is a string not present in `musicalKeys` (e.g. `'F#m(maj7)'`), open `EditTrackScreen` with it via the parametrized `wrap()`, and assert the screen builds without throwing and the key dropdown shows no pre-selected value.
  </action>
  <verify>
    <automated>flutter test test/features/tracks/edit_track_screen_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - Source: `_selectedKey`'s initializer in edit_track_screen.dart reads `widget.currentTrack['key']` only after checking membership in `musicalKeys`; any value outside that list resolves the field to `null`.
    - Test: the new test opens EditTrackScreen with an out-of-list key value; `tester.takeException()` returns `null` (no assertion failure from the DropdownButtonFormField) and the built `DropdownButtonFormField<String>` widget's `initialValue` is `null`.
    - All pre-existing tests in edit_track_screen_test.dart (including "starts pre-populated with currentTrack's values", which uses the in-list key `'C'`) still pass unchanged.
    - `flutter test test/features/tracks/edit_track_screen_test.dart` passes with 0 failures.
  </acceptance_criteria>
  <done>EditTrackScreen never throws when a track's key falls outside the client's 24-value list; a regression test proves it.</done>
</task>

<task type="auto">
  <name>Task 2: Fix CR-02 — always send all editable fields on track update so null unambiguously means "clear"</name>
  <read_first>
    lib/api/public_api.dart (current `updateBandTrack` method, lines 156-178, with its conditional `if (x != null)` guards on every body key)
    lib/features/tracks/edit_track_screen.dart (the `updateFields` patch map built in `_submit()`, lines 98-105, which mirrors the same conditional-guard bug for the local cache merge)
    lib/api/publicapi.yml (`UpdateBandTrackRequestBody` schema, ~line 799 — confirms `durationSeconds`/`tempo`/`key`/`notes` are `nullable: true`, i.e. the API contract already supports an explicit JSON `null` meaning "clear this field")
    test/features/tracks/edit_track_screen_test.dart (existing "submitting a changed title calls updateBandTrack with the exact request body" test — its expected `jsonEncode` key order is title, artist, durationSeconds, tempo, key, notes; the fix must preserve this exact order so that test keeps passing)
  </read_first>
  <files>lib/api/public_api.dart, lib/features/tracks/edit_track_screen.dart, test/features/tracks/edit_track_screen_test.dart</files>
  <action>
    In public_api.dart, change `updateBandTrack`'s signature so `title` and `artist` become `required String` (non-nullable) instead of optional nullable — `EditTrackScreen` is the method's only caller and always has both values from its form. Keep `durationSeconds`, `tempo`, `key`, and `notes` as nullable optional named parameters. Rewrite the request body map so all 6 keys (`title`, `artist`, `durationSeconds`, `tempo`, `key`, `notes`, in that exact order) are always present in the map, assigned unconditionally — remove every `if (x != null) 'field': x` guard from this method's body. Update the method's doc comment to state that `durationSeconds`/`tempo`/`key`/`notes` being `null` is now sent explicitly and the server-side merge treats an explicit `null` as "clear this field" per `UpdateBandTrackRequestBody`'s `nullable: true` schema (contrast with the still-conditional `createBandTrack`, which is intentionally unchanged — there is no "old value" to clear on create).

    In edit_track_screen.dart's `_submit()`, change the `updateFields` patch map (currently guarded with `if (durationSeconds != null) 'durationSeconds': durationSeconds` etc.) to unconditionally include all 6 keys — `title`, `artist`, `durationSeconds`, `tempo`, `key`, `notes` — mirroring the new `updateBandTrack` body shape, so the local `TrackDetailData` cache merge (`tracks_provider.dart`'s `updateFields`, which does a flat `{...current, ...patch}` merge) also picks up a cleared field instead of silently keeping the stale cached value.

    In test/features/tracks/edit_track_screen_test.dart, add a new test: open EditTrackScreen with the existing `currentTrack` const (Duration=200, Tempo=120, Key='C', Notes='Old notes'), clear the Duration and Tempo text fields and the Notes field (leave Key unchanged), tap Save, and assert the captured PUT request body equals the JSON-encoded map `{'title': 'Old Title', 'artist': 'Old Artist', 'durationSeconds': null, 'tempo': null, 'key': 'C', 'notes': null}` — proving the cleared fields are sent as explicit `null`, not omitted.
  </action>
  <verify>
    <automated>flutter test test/features/tracks/edit_track_screen_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - Source: `updateBandTrack`'s request body map in public_api.dart assigns all 6 keys (title/artist/durationSeconds/tempo/key/notes) with no conditional guard on any of them; `title`/`artist` parameters are `required String`.
    - Source: edit_track_screen.dart's `updateFields` patch map in `_submit()` likewise assigns all 6 keys unconditionally.
    - Test: the new "clearing optional fields sends explicit null" test passes, proving the PUT body contains `"durationSeconds":null,"tempo":null,...,"notes":null` rather than omitting those keys.
    - The pre-existing "submitting a changed title calls updateBandTrack with the exact request body and pops back" test continues to pass unmodified (its expected body already lists all 6 non-null fields, since that test's fields are all non-null — the fix doesn't change that test's outcome, only exercises the previously-silent omission path).
    - `flutter analyze` reports no new issues (the `title`/`artist` required-parameter change has exactly one call site — edit_track_screen.dart — which already passes both).
    - `flutter test test/features/tracks/edit_track_screen_test.dart` passes with 0 failures.
  </acceptance_criteria>
  <done>Clearing an optional track field on edit and saving actually persists the clear (sends explicit null instead of omitting the field); a regression test proves it.</done>
</task>

<task type="auto">
  <name>Task 3: Fix CR-03 — invalidate the global Tracks tab after every track mutation</name>
  <read_first>
    lib/providers/tracks_provider.dart (`UserTracksListData`/`userTracksListDataProvider` — the global non-family provider; and the existing `ref.exists(trackDetailDataProvider(...))` guard precedent already used elsewhere in this codebase for a provider that may not have been instantiated yet)
    lib/features/tracks/create_track_screen.dart (line 64 — the current unconditional `ref.invalidate(trackListDataProvider(widget.bandId));` in `_submit()`)
    lib/features/tracks/edit_track_screen.dart (lines 107-112 — the existing `if (ref.exists(trackListDataProvider(widget.bandId)))` guarded invalidate block in `_submit()`, from Task 2's edits)
    lib/features/tracks/confirm_delete_track_dialog.dart (lines 44-50 — the existing if/else in `_delete()` that either patches `trackListDataProvider` via `removeFromList` or invalidates it)
    test/features/tracks/create_track_screen_test.dart (for the `ApiClient`/`MockClient`/`ProviderScope` test-wiring pattern to reuse)
  </read_first>
  <files>lib/features/tracks/create_track_screen.dart, lib/features/tracks/edit_track_screen.dart, lib/features/tracks/confirm_delete_track_dialog.dart, test/features/tracks/create_track_screen_test.dart, test/features/tracks/edit_track_screen_test.dart, test/features/tracks/confirm_delete_track_dialog_test.dart</files>
  <action>
    In all three files — create_track_screen.dart's `_submit()`, edit_track_screen.dart's `_submit()`, and confirm_delete_track_dialog.dart's `_delete()` — add, immediately after the existing per-band `trackListDataProvider` invalidate/patch logic and before the `if (!mounted) return;` line, a guarded invalidate of the global provider: check `ref.exists(userTracksListDataProvider)` and, only if true, call `ref.invalidate(userTracksListDataProvider)`. Guard with `ref.exists()` (not an unconditional invalidate) because the global Tracks tab may not have been visited yet in the current session, and reading `.notifier`/invalidating a never-instantiated provider would trigger an unwanted network fetch as a side effect — mirroring the existing `ref.exists(trackDetailDataProvider(...))` guard already used in edit_track_screen.dart. Import `userTracksListDataProvider` from `../../providers/tracks_provider.dart` in each file (already imported in all three via the existing `tracks_provider.dart` import — just add the new provider reference, no new import statement needed).

    In each of the three corresponding test files, add one new `testWidgets` case that proves the global provider was actually invalidated (not just that the guard code exists): pre-populate the in-memory cache with `cacheService.writeUserTracks(null, [])` (or a suitable small seed list), wrap the screen's trigger button in a `Consumer` that calls `ref.watch(userTracksListDataProvider)` before the mutation screen is opened — this is what makes `ref.exists(userTracksListDataProvider)` true inside the mutation flow — and have the mock `ApiClient` handler count `GET` requests to `/api/track/list`. Perform the mutation (create/edit/delete) through the normal UI flow and `pumpAndSettle()`, then assert the `/api/track/list` call count increased versus its value right before the mutation — proving `ref.invalidate(userTracksListDataProvider)` fired a fresh fetch rather than leaving stale data displayed.
  </action>
  <verify>
    <automated>flutter test test/features/tracks/create_track_screen_test.dart test/features/tracks/edit_track_screen_test.dart test/features/tracks/confirm_delete_track_dialog_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - Source: all three mutation flows (create/edit/delete) contain a `ref.exists(userTracksListDataProvider)`-guarded `ref.invalidate(userTracksListDataProvider)` call, placed after their existing per-band provider update and before navigation pops.
    - Test: each of the three test files has a new test proving a widget that already watches `userTracksListDataProvider` observes an additional `/api/track/list` GET request after the mutation completes (call count strictly greater than the pre-mutation count).
    - The pre-existing tests in all three files (invalidate/removeFromList behavior for the per-band provider, error handling, disabled-while-submitting states) continue to pass unmodified.
    - `flutter test test/features/tracks/create_track_screen_test.dart test/features/tracks/edit_track_screen_test.dart test/features/tracks/confirm_delete_track_dialog_test.dart` passes with 0 failures.
  </acceptance_criteria>
  <done>Creating, editing, or deleting a track from a per-band screen now invalidates the global Tracks tab's data provider (when it has already been instantiated), so the global tab reflects the mutation without requiring a manual filter change; regression tests in all three affected test files prove it.</done>
</task>

<task type="auto">
  <name>Task 4: Fix WR-01 — wire the "View bands" empty-state button to the Bands tab</name>
  <read_first>
    lib/navigation/root_scaffold.dart (current `StatefulWidget`/`_RootScaffoldState` with local `_selectedIndex`, `IndexedStack`, and `NavigationBar` — Home=index 0, Tracks=index 1, Bands=index 2, Profile=index 3)
    lib/features/songs/tracks_screen.dart (the `_buildEmptyState` method, lines 112-143, whose `ElevatedButton(onPressed: () {}, ...)` at line 135 is the no-op to fix, and its two call sites at lines 23 and 79)
    lib/providers/theme_provider.dart (the exact `@riverpod` class-with-public-setter-method shape to mirror for the new provider — a single-field notifier with a default `build()` and one setter method, same pattern as `ThemeController`)
    lib/providers/tracks_provider.dart (`SelectedBandIdFilter`'s doc comment explaining why a public setter method is used instead of direct `notifier.state = value` assignment — same reasoning applies here)
    test/providers/theme_provider_test.dart (the exact `ProviderContainer`-based unit test pattern to mirror for the new provider's test)
    test/widget_test.dart (the existing full-`CadenceApp()` pump pattern with `_FakeSecureStorage`, `MockClient`, and bottom-nav-tab-tap-by-label — the pattern the new WR-01 regression test extends)
    03-UI-SPEC.md line 231 (confirms "View bands" is spec'd to navigate to the Bands tab, not to be removed)
  </read_first>
  <files>lib/providers/navigation_provider.dart, lib/navigation/root_scaffold.dart, lib/features/songs/tracks_screen.dart, test/providers/navigation_provider_test.dart, test/widget_test.dart</files>
  <action>
    Create lib/providers/navigation_provider.dart with a new `@riverpod` class `SelectedTabIndex` (part `navigation_provider.g.dart`), whose `build()` returns `int` defaulting to `0`, and a public `setIndex(int index)` method that sets `state = index` — mirroring `ThemeController`'s exact shape in theme_provider.dart (default value + one public setter, no other state). Document in the class doc comment: 0=Home, 1=Tracks, 2=Bands, 3=Profile, matching `RootScaffold`'s `NavigationDestination` order, and that this lets `TracksScreen`'s "View bands" empty-state CTA switch tabs without a direct reference to `RootScaffold`'s state — same reasoning as `SelectedBandIdFilter`'s public-setter precedent. Run `dart run build_runner build --delete-conflicting-outputs` to generate `navigation_provider.g.dart`.

    Convert `RootScaffold` from `StatefulWidget`/`_RootScaffoldState` to `ConsumerWidget` (drop the State class entirely). In `build(BuildContext context, WidgetRef ref)`, read `final selectedIndex = ref.watch(selectedTabIndexProvider);` and use it for both `IndexedStack(index: selectedIndex, ...)` and `NavigationBar(selectedIndex: selectedIndex, ...)`. Change `onDestinationSelected` from the current `setState` callback to `(index) => ref.read(selectedTabIndexProvider.notifier).setIndex(index)`. Import `../providers/navigation_provider.dart`.

    In tracks_screen.dart, thread `WidgetRef ref` into `_buildEmptyState` (add it as a parameter alongside `BuildContext context` and the existing `required bool showViewBandsButton`) and update both call sites (`build()`'s zero-bands branch and `_buildContent()`'s zero-tracks branch) to pass `ref` through. Change the `ElevatedButton`'s `onPressed: () {}` to `onPressed: () => ref.read(selectedTabIndexProvider.notifier).setIndex(2)` (2 = Bands tab index per `root_scaffold.dart`'s destination order). Import `../../providers/navigation_provider.dart`.

    Create test/providers/navigation_provider_test.dart mirroring theme_provider_test.dart exactly: a `group('SelectedTabIndex', ...)` with one test asserting `container.read(selectedTabIndexProvider)` defaults to `0`, and one test asserting `container.read(selectedTabIndexProvider.notifier).setIndex(2)` then `container.read(selectedTabIndexProvider)` equals `2`.

    In test/widget_test.dart, add a new `testWidgets` case reusing the existing full-`CadenceApp()` pump pattern (`_FakeSecureStorage`, `TokenStorage().write('test-token')`, `ProviderScope` with `cacheServiceProvider`/`apiClientProvider` overrides): mock `/api/band/list` to return `{'items': []}` (zero bands, so `TracksScreen` renders its zero-bands empty state directly without needing a `/api/track/list` mock) and keep the existing `/api/homepage`/default-case mocks. After `pumpAndSettle()`, tap the bottom-nav "Tracks" label, `pumpAndSettle()`, assert `find.text('No tracks')` finds one widget, tap the "View bands" `ElevatedButton`, `pumpAndSettle()`, then assert `tester.widget<NavigationBar>(find.byType(NavigationBar)).selectedIndex` equals `2`.
  </action>
  <verify>
    <automated>flutter test test/providers/navigation_provider_test.dart test/widget_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - Source: lib/providers/navigation_provider.dart exists with a `SelectedTabIndex` `@riverpod` class exposing `selectedTabIndexProvider`, defaulting to `0`, with a public `setIndex(int)` method; `navigation_provider.g.dart` exists and is generated (not hand-written).
    - Source: `RootScaffold` is a `ConsumerWidget` (no `StatefulWidget`/`_RootScaffoldState` remains in root_scaffold.dart); both `IndexedStack.index` and `NavigationBar.selectedIndex` read from `ref.watch(selectedTabIndexProvider)`; `onDestinationSelected` calls `selectedTabIndexProvider.notifier.setIndex`.
    - Source: tracks_screen.dart's "View bands" `ElevatedButton.onPressed` calls `ref.read(selectedTabIndexProvider.notifier).setIndex(2)`, not an empty closure.
    - Test: test/providers/navigation_provider_test.dart passes, proving default-0 and setIndex behavior at the provider level.
    - Test: the new test/widget_test.dart case passes, proving that tapping "View bands" on the Tracks tab's empty state results in `NavigationBar.selectedIndex == 2` (Bands tab) in the running app.
    - The pre-existing "bottom navigation switches between tabs" test in widget_test.dart continues to pass unmodified.
    - `flutter analyze` reports no new issues.
    - `dart run build_runner build --delete-conflicting-outputs` completes without conflicts.
  </acceptance_criteria>
  <done>Tapping "View bands" in the global Tracks tab's zero-bands empty state switches the app to the Bands tab, proven by an end-to-end widget test; the button is no longer a no-op.</done>
</task>

<task type="auto">
  <name>Task 5: Fix WR-02 — validate non-numeric Duration/Tempo input</name>
  <read_first>
    lib/features/tracks/create_track_screen.dart (the Duration/Tempo `TextFormField`s at lines 115-131, and the Title/Artist fields immediately above them for the `validator:` pattern to mirror)
    lib/features/tracks/edit_track_screen.dart (the Duration/Tempo `TextFormField`s at lines 160-176, same pattern)
    test/features/tracks/create_track_screen_test.dart (the "empty title and artist are rejected without an API call" test pattern to mirror for the new validator tests)
    test/features/tracks/edit_track_screen_test.dart (same pattern)
  </read_first>
  <files>lib/features/tracks/create_track_screen.dart, lib/features/tracks/edit_track_screen.dart, test/features/tracks/create_track_screen_test.dart, test/features/tracks/edit_track_screen_test.dart</files>
  <action>
    In both create_track_screen.dart and edit_track_screen.dart, add a `validator` to the Duration and Tempo `TextFormField`s (4 additions total across the 2 files). The validator function: trim the incoming value (treat `null` as an empty string); if the trimmed text is empty, return `null` (field stays optional, no error — matches existing behavior for a genuinely blank field); otherwise, if `int.tryParse` on the trimmed text returns `null` (i.e. it doesn't parse as an integer), return the error string `'Enter a whole number'`; otherwise return `null` (valid). Do not change `keyboardType: TextInputType.number` or any other property on these fields — only add the `validator`.

    In test/features/tracks/create_track_screen_test.dart, add a new test: open CreateTrackScreen, fill in Title and Artist, enter non-numeric text (e.g. `'abc'`) into the Duration field, tap "Save track", and assert no API call was made (call count stays 0) and `find.text('Enter a whole number')` finds a widget.

    In test/features/tracks/edit_track_screen_test.dart, add an analogous test: open EditTrackScreen (pre-populated from the existing `currentTrack` const), enter non-numeric text into the Tempo field, tap "Save", and assert no API call was made and `find.text('Enter a whole number')` finds a widget.
  </action>
  <verify>
    <automated>flutter test test/features/tracks/create_track_screen_test.dart test/features/tracks/edit_track_screen_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - Source: both Duration `TextFormField`s and both Tempo `TextFormField`s (4 fields across create_track_screen.dart and edit_track_screen.dart) have a `validator` that returns `'Enter a whole number'` for non-empty, non-integer-parsable input and `null` otherwise.
    - Test: the new create_track_screen_test.dart case proves non-numeric Duration input blocks submission (0 API calls) and shows the error text.
    - Test: the new edit_track_screen_test.dart case proves non-numeric Tempo input blocks submission and shows the error text.
    - The pre-existing "empty title and artist are rejected without an API call" tests in both files continue to pass unmodified (an empty Duration/Tempo field still passes validation with the new validator, since the empty-string branch returns `null`).
    - `flutter test test/features/tracks/create_track_screen_test.dart test/features/tracks/edit_track_screen_test.dart` passes with 0 failures.
  </acceptance_criteria>
  <done>Non-numeric Duration/Tempo input is rejected with a visible "Enter a whole number" error instead of being silently discarded; regression tests in both create and edit test files prove it.</done>
</task>

</tasks>

## Artifacts Produced

New symbols/files this plan introduces (none existed before this plan):

- `lib/providers/navigation_provider.dart` — new file. `SelectedTabIndex` (`@riverpod` class, `build() -> int` defaulting to `0`, `setIndex(int index)` method) backing `selectedTabIndexProvider` (`AutoDisposeNotifierProvider<SelectedTabIndex, int>`, generated).
- `lib/providers/navigation_provider.g.dart` — new file, riverpod_generator output for the above.
- `test/providers/navigation_provider_test.dart` — new file, `ProviderContainer`-based unit tests for `SelectedTabIndex`.

Existing symbols this plan changes the signature/behavior of:

- `PublicApi.updateBandTrack` (`lib/api/public_api.dart`) — `title`/`artist` change from optional nullable (`String? title`, `String? artist`) to required non-nullable (`required String title`, `required String artist`); request body now unconditionally includes all 6 fields instead of only non-null ones.
- `RootScaffold` (`lib/navigation/root_scaffold.dart`) — changes from `StatefulWidget` to `ConsumerWidget`; `_RootScaffoldState` is removed entirely; tab-selection state moves from local `setState` to `selectedTabIndexProvider`.
- `TracksScreen._buildEmptyState` (`lib/features/songs/tracks_screen.dart`) — gains a `WidgetRef ref` parameter; both call sites updated.

## Artifacts modified without a new symbol

- `lib/features/tracks/edit_track_screen.dart` — `_selectedKey` init guard (CR-01); `updateFields` patch map always-send (CR-02); global-provider invalidate added to `_submit()` (CR-03).
- `lib/features/tracks/create_track_screen.dart` — global-provider invalidate added to `_submit()` (CR-03); Duration/Tempo validators added (WR-02).
- `lib/features/tracks/confirm_delete_track_dialog.dart` — global-provider invalidate added to `_delete()` (CR-03).

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mobile client → Public API (`PUT /api/band/{bandId}/track/{trackId}`) | Same boundary established in 03-01/03-02's threat model — unchanged by this plan. This plan changes what the client sends across that boundary (CR-02), not the boundary itself. |
| Local Riverpod/Hive cache (in-process, on-device) | Not a network trust boundary; touched only by CR-03's invalidate calls (no new read/write surface). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-04-01 | Tampering | `PublicApi.updateBandTrack` request body (CR-02 fix) | low | mitigate | `title`/`artist` are typed `required String` (non-nullable) in the Dart method signature, so the compiler prevents ever sending `null` for those two required fields even though the body map is now built unconditionally; only the genuinely optional `durationSeconds`/`tempo`/`key`/`notes` fields (already `nullable: true` in `publicapi.yml`'s `UpdateBandTrackRequestBody`) can carry an explicit `null`. |
| T-03-04-02 | Tampering | Duration/Tempo `TextFormField` validators (WR-02 fix) | low | accept | Client-side numeric validation is a UX convenience only, not a security control — the server is and remains the authoritative validator for `durationSeconds`/`tempo` (per 03-01's existing threat model for `createBandTrack`/`updateBandTrack`, unchanged here). No new server trust is implied by adding this client-side check. |
| T-03-04-03 | Denial of Service (client-side) | `RootScaffold`'s `ConsumerWidget` conversion + `selectedTabIndexProvider` (WR-01 fix) | low | accept | Purely local UI state (an `int` 0-3, in-memory, `AutoDispose`, no persistence, no network I/O). No new attack surface — cannot be influenced by any external input. |

No `npm`/`pip`/`cargo` package installs occur in this plan (no new dependency added — `riverpod_generator`/`build_runner` codegen for the new provider file uses packages already present in `pubspec.yaml` since Phase 1). Package Legitimacy Gate is not applicable.
</threat_model>

<verification>
Run the full test suite and static analysis after all 5 tasks are complete:
- `flutter analyze` — clean, no new warnings/errors (in particular: no `invalid_use_of_protected_member`/`visible_for_testing` from the `RootScaffold` conversion, no missing-required-argument errors from `updateBandTrack`'s signature change).
- `flutter test` — full suite passes, including the pre-existing 135 tests from 03-03-SUMMARY.md plus the new regression tests added by this plan (at minimum: 1 CR-01 test, 1 CR-02 test, 3 CR-03 tests, 2 WR-01 tests, 2 WR-02 tests = 9+ new tests).
- Manually cross-check each of the 5 gaps against 03-VERIFICATION.md's "Next Steps (Unblocking)" section — every numbered fix (1-5) there has a corresponding task above.
</verification>

<success_criteria>
- All 5 gaps (CR-01, CR-02, CR-03, WR-01, WR-02) from 03-VERIFICATION.md are closed with source changes and passing regression tests that reproduce the original failure mode pre-fix and prove it resolved post-fix.
- TRACK-04 (edit a track) and TRACK-06 (global Tracks tab) — both `BLOCKED` in 03-VERIFICATION.md's Requirements Coverage table — are unblocked: editing no longer crashes or silently drops clears, and the global tab stays in sync with per-band mutations with a working "View bands" affordance.
- `flutter analyze` and `flutter test` both pass clean after this plan's changes.
- No scope beyond the 5 named gaps was introduced (e.g. no "clear key via dropdown" UI affordance was added — 03-VERIFICATION.md's fix instructions for CR-02 only require the always-send-all-fields mechanism, not a new UI control).
</success_criteria>

<output>
Create `.planning/phases/03-tracks/03-04-SUMMARY.md` when done
</output>
