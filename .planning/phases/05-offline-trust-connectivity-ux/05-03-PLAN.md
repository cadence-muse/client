---
phase: 05-offline-trust-connectivity-ux
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - lib/providers/tracks_provider.dart
  - lib/features/songs/tracks_screen.dart
  - lib/features/tracks/track_list_screen.dart
  - lib/features/tracks/track_detail_screen.dart
  - lib/features/tracks/create_track_screen.dart
  - lib/features/tracks/edit_track_screen.dart
  - lib/features/tracks/confirm_delete_track_dialog.dart
  - test/providers/tracks_provider_test.dart
  - test/features/tracks/tracks_screen_test.dart
  - test/features/tracks/track_list_screen_test.dart
  - test/features/tracks/track_detail_screen_test.dart
  - test/features/tracks/create_track_screen_test.dart
  - test/features/tracks/edit_track_screen_test.dart
  - test/features/tracks/confirm_delete_track_dialog_test.dart
autonomous: true
requirements: [OFFL-03, OFFL-04]

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Every mutation control disabled offline in the Tracks feature is fully inert on double-tap (same onPressed: null guarantee established in 05-02 for Bands)."
    - "The global cross-band Tracks tab (tracks_screen.dart), the per-band track list, and track detail each show their own SyncStatusBadge, independently timestamped (OFFL-04)."
    - "The global Tracks tab has no mutation entry points (it is filter+view only per TRACK-06) — OFFL-03 gating applies only to track_list_screen.dart's Add-track FAB and track_detail_screen.dart's Edit/Delete controls."
    - "track_list_screen.dart's Add-track FAB and track_detail_screen.dart's Edit icon are disabled at the source while offline (D-12) — a user never reaches CreateTrackScreen/EditTrackScreen without connectivity."
  artifacts:
    - lib/providers/tracks_provider.dart (TrackListSyncedAt, TrackDetailSyncedAt, UserTracksSyncedAt notifiers)
  key_links:
    - "isOnlineProvider (05-01) -> track_list_screen.dart FAB / track_detail_screen.dart Edit+Delete -> create/edit/delete forms"
    - "tracks_provider.dart writeBandTracks/writeBandTrackDetail/writeUserTracks call sites -> *SyncedAt notifiers -> SyncStatusBadge on 3 screens"
---

<objective>
Apply the identical Wave 1 (staleness badge) and Wave 2/Plan 05-02 (connectivity-gated mutation) patterns to the Tracks entity across all three of its screens: the global cross-band Tracks tab, the per-band track list, and track detail.

Purpose: Replicate 05-02's proven mutation-gating shape onto Tracks' simpler entry-point surface (one FAB, one Edit icon, one Delete tile — no bottom-sheet split, no per-row destructive actions) with zero new design decisions, while also covering the one screen type Bands doesn't have: a filter-only, mutation-free global list (`tracks_screen.dart`).
Output: `tracks_provider.dart` gains per-key `syncedAt` exposure across its 3 cache keys; all 3 Tracks screens show the staleness badge; the per-band Add-track FAB and track-detail Edit/Delete controls are connectivity-gated with live in-form reactivity.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-offline-trust-connectivity-ux/05-CONTEXT.md
@.planning/phases/05-offline-trust-connectivity-ux/05-UI-SPEC.md

lib/providers/tracks_provider.dart
lib/providers/connectivity_provider.dart
lib/widgets/sync_status_badge.dart
lib/features/songs/tracks_screen.dart
lib/features/tracks/track_list_screen.dart
lib/features/tracks/track_detail_screen.dart
lib/features/tracks/create_track_screen.dart
lib/features/tracks/edit_track_screen.dart
lib/features/tracks/confirm_delete_track_dialog.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tracks syncedAt exposure + staleness badges (3 screens) + source-blocked FAB/Edit/Delete</name>
  <files>lib/providers/tracks_provider.dart, lib/features/songs/tracks_screen.dart, lib/features/tracks/track_list_screen.dart, lib/features/tracks/track_detail_screen.dart, test/providers/tracks_provider_test.dart, test/features/tracks/tracks_screen_test.dart, test/features/tracks/track_list_screen_test.dart, test/features/tracks/track_detail_screen_test.dart</files>
  <read_first>lib/providers/bands_provider.dart (05-02's BandsListSyncedAt/BandDetailSyncedAt pattern — mirror exactly, including the unconditional-bump-before-version-guard placement), lib/providers/tracks_provider.dart, lib/providers/connectivity_provider.dart, lib/widgets/sync_status_badge.dart, lib/features/songs/tracks_screen.dart, lib/features/tracks/track_list_screen.dart, lib/features/tracks/track_detail_screen.dart</read_first>
  <action>
    1. In `lib/providers/tracks_provider.dart`, add three companion notifiers mirroring 05-02's `bands_provider.dart` shape:
       - `@riverpod class TrackListSyncedAt extends _$TrackListSyncedAt { @override DateTime? build(String bandId) => null; void set(DateTime? value) => state = value; }` (family, matches `TrackListData.build(String bandId)`).
       - `@riverpod class TrackDetailSyncedAt extends _$TrackDetailSyncedAt { @override DateTime? build(String bandId, String trackId) => null; void set(DateTime? value) => state = value; }` (family, matches `TrackDetailData.build(String bandId, String trackId)`).
       - `@riverpod class UserTracksSyncedAt extends _$UserTracksSyncedAt { @override DateTime? build() => null; void set(DateTime? value) => state = value; }` (non-family, matches `UserTracksListData.build()`).
       For each of `TrackListData`, `TrackDetailData`, `UserTracksListData`: on the cache-hit branch in `build()`, call the matching `readXSyncedAt(...)` and `.set(...)` it into the companion notifier before returning `cached`; in `_fetchAndCache()`, after the paired `writeX(...)` call succeeds, call `.set(DateTime.now())` on the companion notifier unconditionally — placed before any `_version`-guard check, exactly like 05-02's `BandsListData` pattern. Also add the same unconditional bump inside `TrackListData.removeFromList(...)` (it already calls `writeBandTracks(...)`) and `TrackDetailData.updateFields(...)` (it already calls `writeBandTrackDetail(...)`), right after each existing `write*` call.
    2. In `lib/features/songs/tracks_screen.dart`: add `final syncedAt = ref.watch(userTracksSyncedAtProvider);` inside `build()`. Wrap the `data:` branch's content the same way as every other screen so far: `data: (tracks) => Column(children: [SyncStatusBadge(syncedAt: syncedAt), Expanded(child: _buildContent(context, tracks))])` (adjust to this file's actual `_buildContent` call signature). This screen has no FAB and no mutation entry point (TRACK-06 is view-only) — do not add any connectivity gating here.
    3. In `lib/features/tracks/track_list_screen.dart`: add `final syncedAt = ref.watch(trackListSyncedAtProvider(bandId));` and `final isOnline = ref.watch(isOnlineProvider);` inside `build()`. Wrap the `data:` branch's content with the badge. Change the `FloatingActionButton`'s `onPressed: () => Navigator.of(context).push(MaterialPageRoute(builder: (_) => CreateTrackScreen(bandId: bandId)))` to `onPressed: isOnline ? () => Navigator.of(context).push(...) : null`, and change its existing `tooltip: 'Add track'` to `tooltip: isOnline ? 'Add track' : 'Requires connection'` (D-12/D-13).
    4. In `lib/features/tracks/track_detail_screen.dart`: add `final syncedAt = ref.watch(trackDetailSyncedAtProvider(bandId, trackId));` and `final isOnline = ref.watch(isOnlineProvider);` inside `build()`. Wrap the `data:` branch with the badge. Wrap the AppBar's Edit `IconButton` so `onPressed: isOnline ? () => Navigator.of(context).push(...) : null` and `tooltip: isOnline ? 'Edit track' : 'Requires connection'` (source-block D-12). Change the Delete `ListTile` to add `enabled: isOnline` and `onTap: isOnline ? () => showDialog(...) : null`.
  </action>
  <verify>
    <automated>flutter test test/providers/tracks_provider_test.dart test/features/tracks/tracks_screen_test.dart test/features/tracks/track_list_screen_test.dart test/features/tracks/track_detail_screen_test.dart && flutter analyze</automated>
  </verify>
  <acceptance_criteria>
    - `test/providers/tracks_provider_test.dart`'s existing tests pass unmodified, plus new assertions: `trackListSyncedAtProvider(bandId)`, `trackDetailSyncedAtProvider(bandId, trackId)`, and `userTracksSyncedAtProvider` each resolve to the cache's stored `syncedAt` on a cache hit and update after a successful refresh.
    - `test/features/tracks/tracks_screen_test.dart`: `find.byType(SyncStatusBadge)` is found once the global list loads; no `FloatingActionButton` is present on this screen (unchanged from before this plan).
    - `test/features/tracks/track_list_screen_test.dart`: with `isOnlineProvider` overridden `false`, the FAB's `onPressed` is `null`; overridden `true`, non-null.
    - `test/features/tracks/track_detail_screen_test.dart`: with `isOnlineProvider` overridden `false`, the Edit `IconButton`'s `onPressed` is `null` and the Delete `ListTile`'s `enabled` is `false`; overridden `true`, both are enabled.
    - `flutter analyze` reports zero issues.
  </acceptance_criteria>
  <done>All 3 Tracks screens show independently-timestamped staleness badges; the per-band Add-track FAB and track-detail Edit/Delete controls are disabled + tooltipped while offline; the global cross-band tab is untouched beyond its new badge.</done>
</task>

<task type="auto">
  <name>Task 2: Live connectivity reactivity (D-14) on the 3 in-form/in-dialog Tracks mutation buttons</name>
  <files>lib/features/tracks/create_track_screen.dart, lib/features/tracks/edit_track_screen.dart, lib/features/tracks/confirm_delete_track_dialog.dart, test/features/tracks/create_track_screen_test.dart, test/features/tracks/edit_track_screen_test.dart, test/features/tracks/confirm_delete_track_dialog_test.dart</files>
  <read_first>lib/features/bands/create_band_screen.dart (05-02's finished D-14 pattern — mirror exactly), lib/features/tracks/create_track_screen.dart, lib/features/tracks/edit_track_screen.dart, lib/features/tracks/confirm_delete_track_dialog.dart, lib/providers/connectivity_provider.dart</read_first>
  <action>
    Mirror 05-02 Task 2's exact treatment: in each of the 3 files, add `final isOnline = ref.watch(isOnlineProvider);` at the top of `build()`, change the existing `_isSubmitting`-gated `onPressed` from `_isSubmitting ? null : _submit` (or `_delete`) to `(!isOnline || _isSubmitting) ? null : _submit`, wrap the `FilledButton` in a `Tooltip(message: isOnline ? '' : 'Requires connection', child: ...)` (D-13), and swap the button's non-submitting label text to read 'Requires connection' when `!isOnline`:
    - `create_track_screen.dart`: `FilledButton` calling `_submit` (labelled "Create" when online).
    - `edit_track_screen.dart`: `FilledButton` calling `_submit` (labelled "Save" when online).
    - `confirm_delete_track_dialog.dart`: `FilledButton` calling `_delete` (labelled "Delete" when online) — preserve any existing non-connectivity gating condition already on this button unchanged, only add the `!isOnline` term.
  </action>
  <verify>
    <automated>flutter test test/features/tracks/create_track_screen_test.dart test/features/tracks/edit_track_screen_test.dart test/features/tracks/confirm_delete_track_dialog_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `test/features/tracks/create_track_screen_test.dart`: with `isOnlineProvider` overridden `false`, the "Create" `FilledButton`'s `onPressed` is `null`; overridden `true` with a valid filled form, non-null.
    - `test/features/tracks/edit_track_screen_test.dart`: same shape for the "Save" button.
    - `test/features/tracks/confirm_delete_track_dialog_test.dart`: same shape for the "Delete" button.
    - `flutter analyze` reports zero issues.
  </acceptance_criteria>
  <done>Every Tracks create/edit/delete button watches connectivity live — opening a form online then losing connectivity before submitting disables the button immediately, matching 05-02's Bands pattern with zero deviation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| User intent (tap) -> mutation dispatch | Same boundary as 05-02, applied to Tracks' create/edit/delete surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-07 | Tampering | Tracks create/edit/delete controls | medium | mitigate | Add-track FAB, Edit icon, and Delete tile/dialog are all gated on `isOnlineProvider` (D-11/D-12/D-14), identical to 05-02's Bands mitigation |
| T-05-08 | Tampering + Information Disclosure | `tracks_provider.dart`'s `_version`-guarded refresh | low | mitigate | `TrackListSyncedAt`/`TrackDetailSyncedAt`/`UserTracksSyncedAt` bump unconditionally on every successful cache write, independent of the `_version` guard (same Pitfall 6 mitigation as 05-02) |

</threat_model>

<verification>
- `flutter test` (full suite) passes.
- `flutter analyze` reports zero issues.
- Manual: with airplane mode on, the global Tracks tab shows its badge with no FAB; the per-band track list's FAB and track detail's Edit/Delete are grayed with "Requires connection" tooltips.
</verification>

<success_criteria>
- All 3 Tracks screens show independent staleness badges.
- Every Tracks mutation entry point and in-form submit button is connectivity-gated with tooltip feedback, matching 05-02's Bands shape.
</success_criteria>

<output>
Create `.planning/phases/05-offline-trust-connectivity-ux/05-03-SUMMARY.md` when done
</output>
