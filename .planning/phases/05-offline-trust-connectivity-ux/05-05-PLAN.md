---
phase: 05-offline-trust-connectivity-ux
plan: 05
type: execute
wave: 3
depends_on: ["05-02", "05-03", "05-04"]
files_modified:
  - test/regression/offline_trust_regression_test.dart
  - test/offline_cross_tab_test.dart
autonomous: true
requirements: [OFFL-02, OFFL-03, OFFL-04, OFFL-05]

estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Every one of the 10 cached screens (Profile, Home, Bands list/detail, Tracks global/per-band-list/detail, Setlists global/per-band-list/detail) renders SyncStatusBadge — verified by a single deterministic regression guard, not by re-eyeballing each plan's individual tests."
    - "Every one of the 19 mutation-control files across Bands/Tracks/Setlists references isOnlineProvider — same deterministic guard, mirroring Phase 1's OFFL-06 Directory.listSync() regression-guard pattern."
    - "The global offline banner is reachable from, and stays visible across, every one of the app's 5 bottom-nav tabs without re-mounting or flickering when switching tabs while offline (ROADMAP Phase 5 success criterion #3)."
  artifacts:
    - test/regression/offline_trust_regression_test.dart
    - test/offline_cross_tab_test.dart
  key_links:
    - "Static file-content regression guard (Task 1) -> every screen/control file touched across 05-01 through 05-04"
    - "Cross-tab widget test (Task 2) -> RootScaffold + OfflineBanner + isOnlineProvider, exercised through all 5 tabs in one running app"
---

<objective>
Close out the phase with a single deterministic, automated pass that verifies the ROADMAP's phase-level success criteria hold consistently across every screen and every mutation control — not just within each entity plan's own scoped tests, but as a cross-cutting regression guard that would fail loudly if a future change (in this phase or a later one) silently drops the badge or the connectivity gate from any one file.

Purpose: 05-01 through 05-04 each verified their own slice in isolation; nothing yet proves the *aggregate* claim the ROADMAP phase goal makes ("verified consistently across profile, bands, tracks, and setlists"). This plan is that proof, structured the same way Phase 1's OFFL-06 regression guard was (a portable, pure-Dart static scan) plus one real running-app cross-tab test.
Output: `test/regression/offline_trust_regression_test.dart` (static content guard) and `test/offline_cross_tab_test.dart` (live cross-tab banner-consistency test).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-offline-trust-connectivity-ux/05-CONTEXT.md

test/widget_test.dart
lib/navigation/root_scaffold.dart
lib/widgets/offline_banner.dart
lib/providers/connectivity_provider.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Static regression guard — every cached screen has the badge, every mutation control has the connectivity gate</name>
  <files>test/regression/offline_trust_regression_test.dart</files>
  <read_first>test/providers/*_test.dart (grep for the existing Phase 1 OFFL-06 regression-guard test — likely in test/providers/auth_provider_test.dart or test/providers/theme_provider_test.dart — read whichever file contains it to mirror its exact Directory.listSync()-based approach), lib/features/profile/profile_screen.dart, lib/navigation/root_scaffold.dart</read_first>
  <behavior>
    - Given the list of 10 screen files below, each file's contents (read via `File(path).readAsStringSync()`) contains the substring `'SyncStatusBadge'`.
    - Given the list of 19 mutation-control files below, each file's contents contains the substring `'isOnlineProvider'`.
    - `lib/navigation/root_scaffold.dart`'s contents contain the substring `'OfflineBanner'`.
    - `lib/cache/cache_service.dart`'s contents contain all 10 `readXSyncedAt` method names (`readProfileSyncedAt`, `readHomepageSyncedAt`, `readBandsSyncedAt`, `readBandDetailSyncedAt`, `readBandTracksSyncedAt`, `readBandTrackDetailSyncedAt`, `readUserTracksSyncedAt`, `readBandSetlistsSyncedAt`, `readSetlistDetailSyncedAt`, `readUserSetlistsSyncedAt`).
    - If any file in either list does not exist at its expected path, the test fails with a message naming the missing file (not a generic `FileSystemException`).
  </behavior>
  <action>
    Create `test/regression/offline_trust_regression_test.dart` as a plain `flutter_test` file (no widget pumping, no Riverpod — pure `dart:io` file reads), mirroring Phase 1's `OFFL-06` regression-guard test's structure (locate and read that existing test first per `read_first` — reuse its helper-function shape rather than inventing a new one). Define two `const` lists of relative paths from the project root:
    - `_screensWithBadge`: `lib/features/profile/profile_screen.dart`, `lib/features/home/home_screen.dart`, `lib/features/bands/bands_screen.dart`, `lib/features/bands/band_detail_screen.dart`, `lib/features/songs/tracks_screen.dart`, `lib/features/tracks/track_list_screen.dart`, `lib/features/tracks/track_detail_screen.dart`, `lib/features/setlists/setlists_screen.dart`, `lib/features/setlists/setlist_list_screen.dart`, `lib/features/setlists/setlist_detail_screen.dart`.
    - `_mutationControlsWithConnectivityGate`: `lib/features/bands/bands_screen.dart`, `lib/features/bands/band_detail_screen.dart`, `lib/features/bands/create_band_screen.dart`, `lib/features/bands/join_band_dialog.dart`, `lib/features/bands/edit_band_screen.dart`, `lib/features/bands/confirm_delete_band_dialog.dart`, `lib/features/bands/confirm_leave_band_dialog.dart`, `lib/features/bands/confirm_remove_member_dialog.dart`, `lib/features/tracks/track_list_screen.dart`, `lib/features/tracks/track_detail_screen.dart`, `lib/features/tracks/create_track_screen.dart`, `lib/features/tracks/edit_track_screen.dart`, `lib/features/tracks/confirm_delete_track_dialog.dart`, `lib/features/setlists/setlist_list_screen.dart`, `lib/features/setlists/setlist_detail_screen.dart`, `lib/features/setlists/create_setlist_screen.dart`, `lib/features/setlists/edit_setlist_screen.dart`, `lib/features/setlists/confirm_delete_setlist_dialog.dart`, `lib/features/setlists/add_setlist_tracks_dialog.dart`.
    Write one `test(...)` per list (2 tests) that iterates the list, asserts `File(path).existsSync()` (with a failure message naming the missing path), then asserts the file's content `.contains('SyncStatusBadge')` / `.contains('isOnlineProvider')` respectively. Add a third `test(...)` asserting `File('lib/navigation/root_scaffold.dart').readAsStringSync().contains('OfflineBanner')`. Add a fourth `test(...)` asserting `File('lib/cache/cache_service.dart').readAsStringSync()` contains all 10 `readXSyncedAt` method-name substrings listed in `<behavior>` above.
  </action>
  <verify>
    <automated>flutter test test/regression/offline_trust_regression_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - All 4 tests in `test/regression/offline_trust_regression_test.dart` pass against the codebase as left by 05-01 through 05-04.
    - Re-running the same test file a second time (no code changes) produces identical pass results — the guard is deterministic, no flakiness from file-system ordering.
    - `flutter analyze` reports zero issues.
  </acceptance_criteria>
  <done>A single test file exists that will fail loudly, naming the exact file, if any future change removes the staleness badge from a cached screen, the connectivity gate from a mutation control, the banner from `RootScaffold`, or a `syncedAt` accessor from `cache_service.dart`.</done>
</task>

<task type="auto">
  <name>Task 2: Cross-tab offline banner consistency — one running app, all 5 tabs</name>
  <files>test/offline_cross_tab_test.dart</files>
  <read_first>test/widget_test.dart (the existing bottom-navigation tab-switching test — mirror its `ProviderScope`/`MockClient` setup exactly), lib/navigation/root_scaffold.dart, lib/widgets/offline_banner.dart, lib/providers/connectivity_provider.dart</read_first>
  <action>
    Create `test/offline_cross_tab_test.dart` as a `testWidgets` test mirroring `test/widget_test.dart`'s existing `ProviderScope` overrides (`cacheServiceProvider.overrideWithValue(CacheService.inMemory())`, a `MockClient`-backed `apiClientProvider` returning minimal valid JSON for `/api/homepage`, `/api/band/list`, `/api/track/list`, `/api/setlist/list`, and the default `GET /api/me` fallback — reuse `test/widget_test.dart`'s exact `MockClient` handler shape), plus one additional override: `connectivityProvider.overrideWith((ref) => Stream.value(ConnectivityStatus.offline))`. Pump `CadenceApp` inside that `ProviderScope`, `await tester.pumpAndSettle()`, then for each of the 5 `NavigationDestination` labels (`'Home'`, `'Bands'`, `'Tracks'`, `'Setlists'`, `'Profile'`) in turn: tap it, `pumpAndSettle()`, and assert `find.text("You're offline — showing cached data")` finds exactly one widget. After cycling through all 5 tabs, rebuild the `ProviderScope` with `connectivityProvider.overrideWith((ref) => Stream.value(ConnectivityStatus.online))` (via `tester.pumpWidget` with the new override) and assert the banner text is no longer found on the currently-visible tab.
  </action>
  <verify>
    <automated>flutter test test/offline_cross_tab_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - The offline banner text is found on all 5 tabs when `connectivityProvider` is overridden offline, with no tab producing zero matches or more than one match.
    - After overriding `connectivityProvider` to online and rebuilding, the banner text is not found.
    - `flutter analyze` reports zero issues.
  </acceptance_criteria>
  <done>One real running-app test proves the offline banner is the single, consistent, app-wide signal the ROADMAP phase goal requires — not 5 independently-wired per-tab instances that happen to look the same.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Test coverage itself | The only "boundary" this plan protects is regression risk — a future edit silently dropping a badge/gate without any test catching it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-11 | Repudiation | Cross-plan consistency claim | low | mitigate | The static regression guard (Task 1) makes the "every screen has the badge, every control has the gate" claim independently verifiable and re-runnable, rather than resting on each plan's own isolated test coverage |

</threat_model>

<verification>
- `flutter test` (full suite, including this plan's 2 new files) passes.
- `flutter analyze` reports zero issues.
</verification>

<success_criteria>
- A single automated run proves ROADMAP Phase 5's full success-criteria set holds across every screen this phase touched.
</success_criteria>

<output>
Create `.planning/phases/05-offline-trust-connectivity-ux/05-05-SUMMARY.md` when done
</output>
