---
phase: 05-offline-trust-connectivity-ux
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - lib/providers/bands_provider.dart
  - lib/features/bands/bands_screen.dart
  - lib/features/bands/band_detail_screen.dart
  - lib/features/bands/create_band_screen.dart
  - lib/features/bands/join_band_dialog.dart
  - lib/features/bands/edit_band_screen.dart
  - lib/features/bands/confirm_delete_band_dialog.dart
  - lib/features/bands/confirm_leave_band_dialog.dart
  - lib/features/bands/confirm_remove_member_dialog.dart
  - test/providers/bands_provider_test.dart
  - test/providers/band_detail_provider_test.dart
  - test/features/bands/bands_screen_test.dart
  - test/features/bands/band_detail_screen_test.dart
  - test/features/bands/create_band_screen_test.dart
  - test/features/bands/join_band_dialog_test.dart
  - test/features/bands/edit_band_screen_test.dart
autonomous: true
requirements: [OFFL-03, OFFL-04]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Rapidly double-tapping a disabled (offline) mutation control fires zero submit calls — Flutter's onPressed: null makes the control fully inert, not just visually dimmed (OFFL-03 idempotency)."
    - "Bands list and Band detail each show their own SyncStatusBadge, independently timestamped (OFFL-04), matching Profile/Home's Wave 1 pattern exactly."
    - "The single FAB on BandsScreen gates both Create-band and Join-band entry points at once (D-12) — both live behind the same bottom-sheet trigger, so disabling that one FAB blocks both mutation paths."
    - "Band detail's Edit icon, Delete tile, Leave tile, and per-member Remove icon are each disabled (grayed, Tooltip 'Requires connection') while offline — a user can never reach a form/dialog they can't submit (D-11/D-12)."
    - statement: "If connectivity drops while a create/join/edit/delete/leave/remove request is already in flight (submitted while online), the in-flight request is allowed to complete rather than being cancelled — the button's disabled state only prevents NEW submissions, not already-dispatched ones."
      verification: backstop
  artifacts:
    - lib/providers/bands_provider.dart (BandsListSyncedAt, BandDetailSyncedAt family notifiers)
  key_links:
    - "isOnlineProvider (05-01) -> bands_screen.dart FAB / band_detail_screen.dart action tiles -> confirm/create/edit dialogs' Save buttons"
    - "bands_provider.dart writeBands/writeBandDetail call sites -> BandsListSyncedAt/BandDetailSyncedAt -> SyncStatusBadge"
---

<objective>
Apply Wave 1's proven patterns (staleness badge, connectivity-gated mutation controls) to the Bands entity — the first full create/edit/delete/join/leave CRUD surface in the app, and the template every other Wave 2 plan (Tracks, Setlists) replicates.

Purpose: Prove OFFL-03's "disabled + blocked at source" mutation-gating pattern (D-11 through D-14) once, on Bands' full range of entry-point shapes (FAB behind a bottom sheet, an AppBar icon, plain `ListTile`s, and confirm dialogs) so Tracks/Setlists apply the identical shape with no new design decisions.
Output: `bands_provider.dart` gains per-key `syncedAt` exposure; every Bands screen shows the staleness badge; every Bands create/update/delete/join/leave entry point is visibly disabled offline with live reactivity if connectivity drops mid-form.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-offline-trust-connectivity-ux/05-CONTEXT.md
@.planning/phases/05-offline-trust-connectivity-ux/05-UI-SPEC.md

lib/providers/bands_provider.dart
lib/providers/connectivity_provider.dart
lib/widgets/sync_status_badge.dart
lib/features/bands/bands_screen.dart
lib/features/bands/band_detail_screen.dart
lib/features/bands/create_band_screen.dart
lib/features/bands/join_band_dialog.dart
lib/features/bands/edit_band_screen.dart
lib/features/bands/confirm_delete_band_dialog.dart
lib/features/bands/confirm_leave_band_dialog.dart
lib/features/bands/confirm_remove_member_dialog.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bands syncedAt exposure + staleness badges + source-blocked mutation entry points</name>
  <files>lib/providers/bands_provider.dart, lib/features/bands/bands_screen.dart, lib/features/bands/band_detail_screen.dart, test/providers/bands_provider_test.dart, test/providers/band_detail_provider_test.dart, test/features/bands/bands_screen_test.dart, test/features/bands/band_detail_screen_test.dart</files>
  <read_first>lib/providers/profile_provider.dart (05-01's ProfileSyncedAt pattern — mirror exactly), lib/providers/bands_provider.dart, lib/providers/connectivity_provider.dart, lib/widgets/sync_status_badge.dart, lib/features/bands/bands_screen.dart, lib/features/bands/band_detail_screen.dart, test/features/bands/bands_screen_test.dart, test/features/bands/band_detail_screen_test.dart</read_first>
  <action>
    1. In `lib/providers/bands_provider.dart`, add two companion notifiers mirroring 05-01's `ProfileSyncedAt` shape:
       - `@riverpod class BandsListSyncedAt extends _$BandsListSyncedAt { @override DateTime? build() => null; void set(DateTime? value) => state = value; }`
       - `@riverpod class BandDetailSyncedAt extends _$BandDetailSyncedAt { @override DateTime? build(String bandId) => null; void set(DateTime? value) => state = value; }` (family, matching `BandDetailData`'s `build(String bandId)` shape).
       In `BandsListData.build()`, on the cache-hit branch, add `ref.read(bandsListSyncedAtProvider.notifier).set(await cache.readBandsSyncedAt());` before returning `cached`. In `BandsListData._fetchAndCache()`, after `writeBands(bands)` succeeds, add `ref.read(bandsListSyncedAtProvider.notifier).set(DateTime.now());` unconditionally (before the version-guard's `if (_version == capturedVersion)` check in `_refresh`/`_doRefresh` — the syncedAt bump is NOT gated by the version guard, per 05-RESEARCH.md Pitfall 6: the cache write already succeeded regardless of whether the fetched data gets discarded). Also add the same unconditional bump inside `setBands(...)` and `renameBand(...)` — both already call `writeBands(...)`, so add `ref.read(bandsListSyncedAtProvider.notifier).set(DateTime.now());` right after each `writeBands(...)` call (a successful local mutation is itself a fresh sync point).
       Repeat the identical shape for `BandDetailData` with `bandDetailSyncedAtProvider(bandId)`/`writeBandDetail`/`updateName`.
    2. In `lib/features/bands/bands_screen.dart`: add `final syncedAt = ref.watch(bandsListSyncedAtProvider);` and `final isOnline = ref.watch(isOnlineProvider);` inside `build()`. Wrap the `data:` branch's `_buildContent(context, bands)` the same way as 05-01 Task 1: `data: (bands) => Column(children: [SyncStatusBadge(syncedAt: syncedAt), Expanded(child: _buildContent(context, bands))])`. Change the `FloatingActionButton`'s `onPressed: () => _showCreateJoinMenu(context, ref)` to `onPressed: isOnline ? () => _showCreateJoinMenu(context, ref) : null`, and add `tooltip: isOnline ? null : 'Requires connection'` (D-11/D-12/D-13 — this single FAB gates both Create and Join, since both live behind its bottom sheet).
    3. In `lib/features/bands/band_detail_screen.dart`: add `final syncedAt = ref.watch(bandDetailSyncedAtProvider(bandId));` and `final isOnline = ref.watch(isOnlineProvider);` inside `build()`. Wrap the `data:` branch the same way. Wrap the AppBar's Edit `IconButton` in a `Tooltip(message: isOnline ? 'Edit' : 'Requires connection', child: IconButton(onPressed: isOnline ? () => Navigator.of(context).push(...) : null, ...))` (source-block D-12). Change the Delete `ListTile` and Leave `ListTile` to add `enabled: isOnline` (Flutter's `ListTile.enabled: false` grays the tile automatically) — wrap each in a `Tooltip(message: isOnline ? '' : 'Requires connection', child: ListTile(...))` and change each `onTap:` to `isOnline ? () => showDialog(...) : null`. Change the per-member Remove `IconButton` (only shown when `showRemove` is true) the same way: `onPressed: isOnline ? () => showDialog(...) : null`, tooltip becomes `isOnline ? 'Remove' : 'Requires connection'`.
  </action>
  <verify>
    <automated>flutter test test/providers/bands_provider_test.dart test/providers/band_detail_provider_test.dart test/features/bands/bands_screen_test.dart test/features/bands/band_detail_screen_test.dart && flutter analyze</automated>
  </verify>
  <acceptance_criteria>
    - `test/providers/bands_provider_test.dart`/`band_detail_provider_test.dart`'s existing tests pass unmodified, plus new assertions: `bandsListSyncedAtProvider`/`bandDetailSyncedAtProvider(bandId)` resolve to the cache's stored `syncedAt` on a cache hit and update after a successful refresh.
    - `test/features/bands/bands_screen_test.dart`: with `isOnlineProvider` overridden `false`, the FAB's `onPressed` is `null` (verify via `tester.widget&lt;FloatingActionButton&gt;(...).onPressed`, not `null`); overridden `true`, it is non-null. `find.byType(SyncStatusBadge)` is found once the list loads.
    - `test/features/bands/band_detail_screen_test.dart`: with `isOnlineProvider` overridden `false`, the Edit `IconButton`'s `onPressed` is `null`, the Delete/Leave `ListTile`s' `enabled` is `false`, and (when a removable member is rendered) the Remove `IconButton`'s `onPressed` is `null`; overridden `true`, all are enabled.
    - `flutter analyze` reports zero issues.
  </acceptance_criteria>
  <done>Bands list and detail screens show the staleness badge and every mutation entry point on them is disabled + tooltipped while offline, with zero effect from repeated taps on a disabled control.</done>
</task>

<task type="auto">
  <name>Task 2: Live connectivity reactivity (D-14) on the 6 in-form/in-dialog Bands mutation buttons</name>
  <files>lib/features/bands/create_band_screen.dart, lib/features/bands/join_band_dialog.dart, lib/features/bands/edit_band_screen.dart, lib/features/bands/confirm_delete_band_dialog.dart, lib/features/bands/confirm_leave_band_dialog.dart, lib/features/bands/confirm_remove_member_dialog.dart, test/features/bands/create_band_screen_test.dart, test/features/bands/join_band_dialog_test.dart, test/features/bands/edit_band_screen_test.dart</files>
  <read_first>lib/features/bands/create_band_screen.dart, lib/features/bands/join_band_dialog.dart, lib/features/bands/edit_band_screen.dart, lib/features/bands/confirm_delete_band_dialog.dart, lib/features/bands/confirm_leave_band_dialog.dart, lib/features/bands/confirm_remove_member_dialog.dart, lib/providers/connectivity_provider.dart</read_first>
  <action>
    D-14: if a form/dialog was opened while online (its entry point was enabled) and connectivity drops before the user taps Save/Create/Join/Delete/Leave/Remove, that button must disable itself live, not just at the entry point. In each of the 6 files below, add `final isOnline = ref.watch(isOnlineProvider);` at the top of `build()` (all 6 are already `ConsumerStatefulWidget`s with a `build(BuildContext context)` override that has `ref` in scope), then change the existing `_isSubmitting`-gated `onPressed` condition from `_isSubmitting ? null : _submit` to `(!isOnline || _isSubmitting) ? null : _submit` (same pattern, different function name per file):
    - `create_band_screen.dart`: `FilledButton` calling `_submit` — also change its child `Text('Create')` to show `Text(isOnline ? 'Create' : 'Requires connection')` when not submitting.
    - `join_band_dialog.dart`: `FilledButton` calling `_submit` (the "Join" button) — same text swap to `'Join'`/`'Requires connection'`.
    - `edit_band_screen.dart`: `FilledButton` calling `_submit` (the "Save" button) — same text swap to `'Save'`/`'Requires connection'`.
    - `confirm_delete_band_dialog.dart`: the destructive `FilledButton` calling `_delete` — its condition becomes `(!matches || !isOnline || _isSubmitting) ? null : _delete` (keep the existing exact-name-match gate `matches` — do not loosen it); text swap `'Delete'`/`'Requires connection'`.
    - `confirm_leave_band_dialog.dart`: `FilledButton` calling `_leave` — text swap `'Leave'`/`'Requires connection'`.
    - `confirm_remove_member_dialog.dart`: `FilledButton` calling `_remove` — text swap `'Remove'`/`'Requires connection'`.
    Every changed `FilledButton` also gets a `Tooltip(message: isOnline ? '' : 'Requires connection', child: FilledButton(...))` wrapper (D-13).
  </action>
  <verify>
    <automated>flutter test test/features/bands/create_band_screen_test.dart test/features/bands/join_band_dialog_test.dart test/features/bands/edit_band_screen_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `test/features/bands/create_band_screen_test.dart`: with `isOnlineProvider` overridden `false`, the "Create" `FilledButton`'s `onPressed` is `null` (and its label reads "Requires connection"); overridden `true` with a filled-in valid form, `onPressed` is non-null.
    - `test/features/bands/join_band_dialog_test.dart`/`edit_band_screen_test.dart`: same shape as above for their respective Join/Save buttons.
    - A new or extended test in `band_detail_screen_test.dart`/dialog coverage confirms: opening `ConfirmDeleteBandDialog` with `isOnlineProvider` initially `true`, then overriding it to `false` via a rebuild, disables the Delete button even when the typed-name `matches` condition is satisfied.
    - `flutter analyze` reports zero issues.
  </acceptance_criteria>
  <done>Every Bands create/edit/delete/join/leave/remove-member button watches connectivity live — opening a form online then losing connectivity before submitting disables the button immediately, matching D-14, with no special-casing across the 6 files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| User intent (tap) -> mutation dispatch | The disabled-control gate is the only thing preventing a doomed offline `POST`/`PUT`/`DELETE` from being attempted |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-05 | Tampering | Bands create/edit/delete/join/leave/remove-member controls | medium | mitigate | Every entry point and every in-form submit button is gated on `isOnlineProvider` (D-11/D-12/D-14) — a user can never dispatch a mutation request while confirmed offline, so no silent-failure-looks-like-success scenario |
| T-05-06 | Tampering + Information Disclosure | `bands_provider.dart`'s `_version`-guarded refresh | low | mitigate | `BandsListSyncedAt`/`BandDetailSyncedAt` are bumped unconditionally on every successful cache write, independent of the `_version` guard discarding stale-vs-local-edit data (05-RESEARCH.md Pitfall 6) — the staleness badge never lags behind a fetch that actually succeeded |

</threat_model>

<verification>
- `flutter test` (full suite) passes.
- `flutter analyze` reports zero issues.
- Manual: with airplane mode on, every Bands FAB/icon/tile/dialog button is visibly grayed and shows "Requires connection" on tap; toggling airplane mode off re-enables them without needing to reopen the screen.
</verification>

<success_criteria>
- Bands list and detail screens show independent staleness badges.
- Every Bands mutation entry point (source) and in-form submit button (live) is connectivity-gated with tooltip feedback.
</success_criteria>

<output>
Create `.planning/phases/05-offline-trust-connectivity-ux/05-02-SUMMARY.md` when done
</output>
