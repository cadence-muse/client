---
phase: 07-cache-behavior-flip-online-first
plan: 05
type: execute
wave: 3
depends_on: ["07-01", "07-02", "07-03", "07-04"]
files_modified:
  - lib/widgets/sync_status_badge.dart
  - test/widgets/sync_status_badge_test.dart
  - test/regression/offline_trust_regression_test.dart
autonomous: true
requirements: [OFFL-07, OFFL-08]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "SyncStatusBadge no longer exists anywhere in the codebase — no file, no import, no reference (OFFL-08)"
    - "The phase-level regression guard (offline_trust_regression_test.dart) asserts the NEW aggregate claim — every cached screen is free of SyncStatusBadge and every offline-capable screen references OfflineNoCacheException — instead of the old, now-false, 'every cached screen renders SyncStatusBadge' claim"
    - "flutter analyze reports zero unused_import/unused_element warnings across the entire lib/ tree after every prior Phase 7 plan's edits"
  artifacts: []
  key_links:
    - "test/regression/offline_trust_regression_test.dart -> 10 screen files (aggregate absence-of-SyncStatusBadge + presence-of-OfflineNoCacheException regression guard)"
---

<objective>
Close out Phase 7: delete the now-fully-unused `SyncStatusBadge` widget and
its dedicated test, and rewrite the cross-cutting Phase-5 regression guard
(`offline_trust_regression_test.dart`) whose hard-coded "every cached screen
renders SyncStatusBadge" assertion is now categorically false — every one of
the 10 call sites removed it across 07-01 through 07-04.

Purpose: this is the phase's final aggregate verification — proving, in one
independently re-runnable test file, that OFFL-08's "the old badge system is
removed entirely" claim holds across all 10 screens simultaneously, not just
per-plan in isolation. Runs last (wave 3) because it depends on every prior
plan's badge removal having landed.

Output: `sync_status_badge.dart` and its test deleted; the regression test
rewritten to assert the new aggregate claim; `flutter analyze` clean across
the full `lib/` tree.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-cache-behavior-flip-online-first/07-CONTEXT.md
@.planning/phases/07-cache-behavior-flip-online-first/07-01-SUMMARY.md
@.planning/phases/07-cache-behavior-flip-online-first/07-02-SUMMARY.md
@.planning/phases/07-cache-behavior-flip-online-first/07-03-SUMMARY.md
@.planning/phases/07-cache-behavior-flip-online-first/07-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete SyncStatusBadge and confirm zero dangling references</name>
  <files>lib/widgets/sync_status_badge.dart, test/widgets/sync_status_badge_test.dart</files>
  <read_first>lib/widgets/sync_status_badge.dart, test/widgets/sync_status_badge_test.dart</read_first>
  <precondition>07-01 through 07-04 have already removed every SyncStatusBadge instantiation from all 10 screen files (verify with `grep -rl "SyncStatusBadge" lib/features/` returning no matches before deleting the widget file itself)</precondition>
  <action>
    Run `grep -rn "SyncStatusBadge" lib/` first to confirm zero remaining
    references outside `lib/widgets/sync_status_badge.dart` itself (every
    call site across the 10 screens was removed by 07-01 Task 1/Task 2,
    07-02 Task 1/Task 2, 07-03 Task 2, 07-04 Task 2). If any reference
    remains, STOP and do not delete the widget file — that screen's badge
    removal from an earlier plan was incomplete and must be fixed first
    (name the specific file/line found). Once confirmed clean, delete
    `lib/widgets/sync_status_badge.dart` and
    `test/widgets/sync_status_badge_test.dart` entirely (the widget's
    dedicated unit test has nothing left to test once the widget itself is
    gone — OFFL-08 requires the badge system removed, not kept-but-unused).
  </action>
  <verify>
    <automated>test ! -f lib/widgets/sync_status_badge.dart && test ! -f test/widgets/sync_status_badge_test.dart && ! grep -rn "SyncStatusBadge" lib/ test/ && flutter analyze</automated>
  </verify>
  <acceptance_criteria>
    - `lib/widgets/sync_status_badge.dart` and
      `test/widgets/sync_status_badge_test.dart` do not exist.
    - `grep -rn "SyncStatusBadge" lib/ test/` returns zero matches anywhere
      in the repository (including the regression test — updated in Task 2).
    - `flutter analyze` reports zero warnings/errors across the entire
      `lib/` and `test/` trees.
  </acceptance_criteria>
  <done>SyncStatusBadge and its dedicated test are deleted; no file in the
  repository references it; flutter analyze is fully clean.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite the OFFL-07/OFFL-08 aggregate regression guard</name>
  <files>test/regression/offline_trust_regression_test.dart</files>
  <read_first>test/regression/offline_trust_regression_test.dart, lib/providers/offline_no_cache_exception.dart, lib/widgets/offline_banner.dart, lib/navigation/root_scaffold.dart</read_first>
  <precondition>Task 1 of this plan has already deleted lib/widgets/sync_status_badge.dart</precondition>
  <action>
    Rewrite `test/regression/offline_trust_regression_test.dart`'s first
    test, `'every cached screen renders SyncStatusBadge (OFFL-04 regression
    guard)'` — this Phase-5 assertion is now categorically false and must be
    replaced, not just patched. Keep the same `screensWithBadge` file-list
    constant (rename to `cachedScreens` — still the canonical 10-screen
    list this phase's OFFL-07/OFFL-08 apply to) but invert and extend the
    assertion into two checks per file: (1) the file no longer contains the
    substring `'SyncStatusBadge'` (proves OFFL-08's badge removal held); (2)
    the file DOES contain the substring `'OfflineNoCacheException'` (proves
    every cached screen wires the D-06 offline-no-cache error branch this
    phase added — every one of the 10 screens across 07-01/07-02/07-03/
    07-04 added this import/check). Rename the test description to
    reference OFFL-07/OFFL-08 instead of the retired OFFL-04. Leave the
    `mutationControlsWithConnectivityGate` test (`isOnlineProvider`
    presence) and the `readSyncedAtMethodNames` `cache_service.dart` test
    completely unchanged — both are still true and unaffected by this
    phase. Leave the `'RootScaffold renders the global OfflineBanner'` test
    unchanged too — `OfflineBanner` itself still renders in
    `root_scaffold.dart`, only its internal text changed (07-01), which
    this file-existence-plus-substring-match test does not assert on.
  </action>
  <verify>
    <automated>flutter test test/regression/offline_trust_regression_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - The rewritten test iterates the same 10 cached-screen files and
      asserts BOTH the absence of `'SyncStatusBadge'` and the presence of
      `'OfflineNoCacheException'` in each.
    - The `mutationControlsWithConnectivityGate` and `readSyncedAtMethodNames`
      tests are byte-for-byte unchanged from before this plan.
    - `flutter test test/regression/offline_trust_regression_test.dart`
      passes with 4 tests green (badge-absence+exception-presence,
      connectivity-gate, readSyncedAt-accessors, OfflineBanner-in-
      RootScaffold).
  </acceptance_criteria>
  <done>The phase-level regression guard asserts the phase's actual final
  state (no badge, universal offline-no-cache handling) instead of the
  retired Phase-5 claim; all 4 of its tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | This plan only deletes dead code and rewrites a test assertion — no runtime trust boundary is introduced or changed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-10 | Tampering | Regression-guard assertion drift | low | mitigate | This plan's Task 2 exists specifically to prevent the regression suite from asserting a false claim (the deleted badge's presence) going forward — closing the gap immediately rather than leaving a permanently-red or silently-stale test |
</threat_model>

<verification>
Run `flutter test` (full suite) — all green, including the rewritten regression test.
Run `flutter analyze` on the full repository — zero warnings/errors.
`grep -rn "SyncStatusBadge" lib/ test/` returns zero matches.
</verification>

<success_criteria>
- SyncStatusBadge and its dedicated test are fully removed from the repository.
- The aggregate OFFL-07/OFFL-08 regression guard asserts the phase's actual final state across all 10 cached screens.
- Full `flutter test` suite and `flutter analyze` are clean, closing out Phase 7.
</success_criteria>

<output>
Create `.planning/phases/07-cache-behavior-flip-online-first/07-05-SUMMARY.md` when done
</output>
