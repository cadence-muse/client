---
phase: 07-cache-behavior-flip-online-first
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - lib/providers/tracks_provider.dart
  - test/providers/tracks_provider_test.dart
  - lib/features/songs/tracks_screen.dart
  - lib/features/tracks/track_list_screen.dart
  - lib/features/tracks/track_detail_screen.dart
  - test/features/tracks/tracks_screen_test.dart
  - test/features/tracks/track_list_screen_test.dart
  - test/features/tracks/track_detail_screen_test.dart
autonomous: true
requirements: [OFFL-07, OFFL-08]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Opening the global cross-band Tracks tab while online always shows freshly-fetched server data, and re-selecting it after it was already built triggers a fresh fetch (OFFL-07, D-01)"
    - "Opening a band's per-band track list or a track detail screen (pushed routes) while online always fetches fresh, with no new tab-switch wiring needed (D-02)"
    - "All three track screens show cached data offline instead of a blank/error screen, and the dedicated 'No cached data' state (no Retry) when nothing was ever cached, auto-recovering on reconnect (OFFL-08, D-06, D-07)"
    - "SyncStatusBadge no longer renders on any of the 3 track screens (OFFL-08)"
  artifacts: []
  key_links:
    - "TrackListData/TrackDetailData/UserTracksListData.build() -> ref.watch(isOnlineProvider) (online-first fetch decision, D-01/D-03)"
    - "TracksScreen.build() -> ref.listen(selectedTabIndexProvider) -> ref.invalidate(userTracksListDataProvider) (D-01 tab-switch refetch, tab index 2)"
---

<objective>
Apply the online-first pattern established by 07-01's tracer to the three
track providers (`TrackListData` per-band, `TrackDetailData` per-track,
`UserTracksListData` cross-band tab) and their three screens.

Purpose: extend OFFL-07/OFFL-08 coverage to every track-related screen using
the exact `build()` rewrite shape 07-01 proved, distinguishing the one tab
screen (cross-band `TracksScreen`, D-01 tab-switch wiring) from the two
pushed-route screens (`TrackListScreen`/`TrackDetailScreen`, D-02 no new
wiring).

Output: `tracks_provider.dart`'s three providers rewritten to online-first;
`tracks_screen.dart`/`track_list_screen.dart`/`track_detail_screen.dart`
rewired accordingly.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-cache-behavior-flip-online-first/07-CONTEXT.md
@.planning/phases/07-cache-behavior-flip-online-first/07-UI-SPEC.md
@.planning/phases/07-cache-behavior-flip-online-first/07-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: tracks_provider.dart — TrackListData, TrackDetailData, UserTracksListData online-first</name>
  <files>lib/providers/tracks_provider.dart, test/providers/tracks_provider_test.dart</files>
  <read_first>lib/providers/bands_provider.dart (07-01's rewritten BandsListData/BandDetailData.build() — canonical template), lib/providers/tracks_provider.dart, test/providers/tracks_provider_test.dart, lib/providers/offline_no_cache_exception.dart</read_first>
  <behavior>
    Apply 07-01's 6 provider-level cases (online+no-cache fetches directly;
    online+stale-cache still returns fresh; online+fetch-fails+cache falls
    back silently D-03; online+fetch-fails+no-cache rethrows;
    offline+cache returns cached; offline+no-cache throws
    `OfflineNoCacheException` D-06) to all THREE providers in this file:
    - `TrackListData.build(bandId)` — has a `_version` guard, preserve it
      exactly like `BandsListData` did.
    - `TrackDetailData.build(bandId, trackId)` — has a `_version` guard,
      same preservation.
    - `UserTracksListData.build()` — watches `selectedBandIdFilterProvider`
      for its `bandIdFilter`; has a `final int _version = 0;` field that is
      never mutated (pre-existing, out of scope to change) — preserve the
      capture/compare pattern as-is even though it is trivially always-true.
  </behavior>
  <action>
    Rewrite all three `build()` methods in `lib/providers/tracks_provider.dart`
    to the online-first shape from 07-01 (watch `isOnlineProvider` and
    `cacheServiceProvider`; online branch tries `_fetchAndCache(...)` falling
    back to the matching `cache.readX(...)` call on failure, setting the
    matching `XxxSyncedAt` provider from `cache.readXSyncedAt(...)` on a
    cache hit; offline branch reads cache the same way or throws
    `const OfflineNoCacheException()`):
    - `TrackListData.build(String bandId)`: online try
      `_fetchAndCache(bandId)`, catch falls back to
      `cache.readBandTracks(bandId)`; offline reads
      `cache.readBandTracks(bandId)` or throws.
    - `TrackDetailData.build(String bandId, String trackId)`: online try
      `_fetchAndCache(bandId, trackId)`, catch falls back to
      `cache.readBandTrackDetail(bandId, trackId)`; offline reads the same
      or throws.
    - `UserTracksListData.build()`: keep
      `final bandIdFilter = ref.watch(selectedBandIdFilterProvider);` as the
      first line (unchanged — the filter dependency must still trigger a
      rebuild on change), then apply online-first: online try
      `_fetchAndCache(bandIdFilter)`, catch falls back to
      `cache.readUserTracks(bandIdFilter)`; offline reads the same or
      throws.
    Delete all three now-dead `_refresh(...)` private methods (each one's
    only call site — the old cache-hit branch in its own `build()` — is
    gone; leaving them triggers `flutter analyze`'s `unused_element` lint).
    Leave every `_fetchAndCache`, `refresh()`, `_doRefresh()`, `_version`,
    and local-mutation method (`removeFromList`, `updateFields`) completely
    untouched. Add imports for `connectivity_provider.dart` and
    `offline_no_cache_exception.dart` (both relative to
    `lib/providers/`, matching `bands_provider.dart`'s post-07-01 import
    style).

    Update `test/providers/tracks_provider_test.dart` (612 lines,
    pre-existing coverage for all three providers): apply the 07-01
    -established rewrite pattern to each provider's build()-related test
    block (replace "cache-hit returns cached data immediately with a silent
    background refresh" with the new online-first/offline/fallback cases;
    keep every `_version`-guard/dedup/local-mutation test unchanged in
    intent).
  </action>
  <verify>
    <automated>flutter test test/providers/tracks_provider_test.dart && flutter analyze lib/providers/tracks_provider.dart</automated>
  </verify>
  <acceptance_criteria>
    - `TrackListData.build(bandId)`, `TrackDetailData.build(bandId, trackId)`,
      and `UserTracksListData.build()` each call their `_fetchAndCache(...)`
      directly when online, even with a populated cache present.
    - Each falls back silently to its matching cache read when online but
      the fetch throws (D-03), and each throws `OfflineNoCacheException`
      when offline with no cache (D-06).
    - All pre-existing `_version`-guard/dedup/local-mutation tests still
      pass unmodified in intent.
    - `flutter analyze lib/providers/tracks_provider.dart` reports zero
      `unused_element` warnings (all three `_refresh()` methods removed).
  </acceptance_criteria>
  <done>TrackListData, TrackDetailData, and UserTracksListData are all
  online-first with D-03/D-06 fallback, `_version` guards preserved
  unchanged; all listed tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Track screens — TracksScreen (tab), TrackListScreen + TrackDetailScreen (pushed routes)</name>
  <files>lib/features/songs/tracks_screen.dart, lib/features/tracks/track_list_screen.dart, lib/features/tracks/track_detail_screen.dart, test/features/tracks/tracks_screen_test.dart, test/features/tracks/track_list_screen_test.dart, test/features/tracks/track_detail_screen_test.dart</files>
  <read_first>lib/features/bands/bands_screen.dart (07-01's rewritten tab screen — canonical template), lib/features/bands/band_detail_screen.dart (07-01's rewritten pushed-route screen — canonical template), lib/features/songs/tracks_screen.dart, lib/features/tracks/track_list_screen.dart, lib/features/tracks/track_detail_screen.dart, test/features/tracks/tracks_screen_test.dart, test/features/tracks/track_list_screen_test.dart, test/features/tracks/track_detail_screen_test.dart, lib/providers/offline_no_cache_exception.dart, lib/widgets/offline_no_cache_view.dart</read_first>
  <behavior>
    - `TracksScreen` (cross-band tab, index 2): mirror `BandsScreen`'s
      07-01 shape exactly — tab-switch listener invalidating
      `userTracksListDataProvider` on `current == 2` (D-01), `AppBar.bottom`
      `LinearProgressIndicator` gated on
      `userTracksAsync.isLoading && userTracksAsync.hasValue` (D-08),
      `OfflineNoCacheView` on `OfflineNoCacheException` (D-06).
    - `TrackListScreen`/`TrackDetailScreen` (pushed routes, family
      providers): mirror `BandDetailScreen`'s 07-01 shape — NO tab-switch
      listener (D-02), NO `AppBar.bottom` indicator (same rationale as
      07-01 Task 2 — `hasValue` starts false on every push), just badge
      removal + `OfflineNoCacheView` error branch.
  </behavior>
  <action>
    Rewire `lib/features/songs/tracks_screen.dart`: this screen's AppBar is
    built in the top-level `build()` while
    `userTracksListDataProvider`/`userTracksSyncedAtProvider` are currently
    only watched inside the nested `_buildTracksBody` helper — lift the
    `ref.watch(userTracksListDataProvider)` call (or an equivalent) up to
    `build()` so the `AppBar.bottom` indicator can read its
    `isLoading`/`hasValue` state; remove the
    `final syncedAt = ref.watch(userTracksSyncedAtProvider);` line and the
    `sync_status_badge.dart` import. Add
    `ref.listen<int>(selectedTabIndexProvider, (previous, current) { if
    (current == 2) ref.invalidate(userTracksListDataProvider); });` at the
    top of `build()` (D-01; tab index 2 = Tracks). Simplify
    `_buildTracksBody`'s `data:` branch to directly
    `_buildContent(context, ref, tracks)` (drop the
    `Column([SyncStatusBadge, Expanded])` wrapper). Add the
    `OfflineNoCacheView` branch to `error:`. Note the existing
    `bands.isEmpty` zero-bands early-return in `build()` is unrelated to
    this refactor — leave it as-is, but the tab-switch listener must still
    be registered before that early return so a tab-switch always
    re-triggers a fetch attempt regardless of the bands-empty state. Add
    imports for `navigation_provider.dart`, `offline_no_cache_exception.dart`,
    `offline_no_cache_view.dart`.

    Rewire `lib/features/tracks/track_list_screen.dart` and
    `lib/features/tracks/track_detail_screen.dart` identically to how
    07-01 Task 2 rewired `band_detail_screen.dart`: remove the
    `sync_status_badge.dart` import and each screen's
    `final syncedAt = ref.watch(...SyncedAtProvider(...));` line; simplify
    the `data:` branch to drop the `Column([SyncStatusBadge, Expanded])`
    wrapper; add the `OfflineNoCacheView` branch to `error:`; add imports
    for `offline_no_cache_exception.dart` and `offline_no_cache_view.dart`.
    No tab-switch listener, no `AppBar.bottom` indicator on either (D-02).

    Update `test/features/tracks/tracks_screen_test.dart`,
    `test/features/tracks/track_list_screen_test.dart`, and
    `test/features/tracks/track_detail_screen_test.dart`: replace/add tests
    per the `<behavior>` block, mirroring 07-01's `bands_screen_test.dart`/
    `band_detail_screen_test.dart` updates. `tracks_screen_test.dart`'s
    `wrap()` helper does not currently override `isOnlineProvider` — add the
    same default-true override parameter 07-02 added to
    `home_screen_test.dart`, for the same reason (an unmocked
    `isOnlineProvider` now silently resolves to offline and changes which
    branch every existing test exercises). `track_list_screen_test.dart`
    and `track_detail_screen_test.dart` already override `isOnlineProvider`
    — no change needed there. Watch for the same
    `tester.pump()` -> `await tester.pumpAndSettle()` adjustment 07-01/07-02
    flagged for any cache-seeded assertion that previously relied on
    cache-first synchronous-ish rendering.
  </action>
  <verify>
    <automated>flutter test test/features/tracks/tracks_screen_test.dart test/features/tracks/track_list_screen_test.dart test/features/tracks/track_detail_screen_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `TracksScreen` refetches on a second Tracks-tab selection; shows the
      `AppBar` in-flight indicator only when loading with existing data;
      shows `OfflineNoCacheView` on `OfflineNoCacheException`.
    - `TrackListScreen`/`TrackDetailScreen` show `OfflineNoCacheView` (no
      Retry) on the same exception, with no tab-switch listener and no
      `AppBar.bottom` indicator present in either file.
    - No file under `lib/features/songs/` or `lib/features/tracks/` imports
      `sync_status_badge.dart` anymore.
  </acceptance_criteria>
  <done>All 3 track screens are wired to the online-first providers from
  Task 1: TracksScreen refetches on tab-switch with an in-flight indicator,
  TrackListScreen/TrackDetailScreen need no new wiring (D-02), all show the
  offline-no-cache view, none render SyncStatusBadge; all listed tests
  pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Device connectivity signal -> fetch/cache policy | `isOnlineProvider` now gates whether all three track providers trust the network or fall back to local cache |
| Cross-band filter state -> fetch key | `selectedBandIdFilterProvider` (user-controlled UI state, not external input) still determines `UserTracksListData`'s cache/fetch key — unchanged by this phase, no new trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-06 | Tampering | `TrackListData`/`TrackDetailData` `_version` guard | medium | mitigate | Preserved verbatim across the online-first rewrite, identical mitigation to T-07-01 in 07-01-PLAN.md — a slow in-flight fetch cannot clobber a newer `removeFromList`/`updateFields` local mutation |
| T-07-07 | Spoofing | `isOnlineProvider` (radio state, not reachability) | low | accept | Same accepted tradeoff as T-07-02/T-07-04 — D-03's silent cache fallback absorbs a wrong online read |
</threat_model>

<verification>
Run `flutter test test/providers/tracks_provider_test.dart test/features/tracks/tracks_screen_test.dart test/features/tracks/track_list_screen_test.dart test/features/tracks/track_detail_screen_test.dart` — all green.
Run `flutter analyze` on the full `lib/` tree — zero new warnings from this plan's files.
</verification>

<success_criteria>
- All three track providers and their three screens are fully online-first (OFFL-07) with offline-cache fallback, offline-no-cache state, and no SyncStatusBadge (OFFL-08).
- TracksScreen has D-01 tab-switch wiring; TrackListScreen/TrackDetailScreen correctly have none (D-02).
- All automated tests pass; `flutter analyze` is clean on every file this plan touches.
</success_criteria>

<output>
Create `.planning/phases/07-cache-behavior-flip-online-first/07-03-SUMMARY.md` when done
</output>
