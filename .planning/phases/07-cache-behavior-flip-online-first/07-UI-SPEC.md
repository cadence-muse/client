---
phase: 7
slug: cache-behavior-flip-online-first
status: draft
shadcn_initialized: false
preset: not_applicable
created: 2026-08-21
---

# Phase 7 — UI Design Contract: Cache Behavior Flip — Online-First

> Visual and interaction contract for cache-behavior inversion from cache-first to online-first.
> This phase is a **messaging + state-coverage refactor**, not a visual redesign.
> Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | Flutter Material 3 (no shadcn — native Flutter app) |
| Preset | not applicable |
| Component library | Flutter Material Design (built-in) |
| Icon library | Material Icons 3 (Flutter default) |
| Font | Roboto (Material 3 default) |

**Framework notes:** This is a Dart/Flutter mobile app (Android/iOS/web). No web component registry applies. All UI components use Material 3 via Flutter's built-in `material` package. No third-party component registries used.

---

## Spacing Scale

**Source:** CLAUDE.md (project conventions) + Material Design defaults

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, minimal padding |
| sm | 8px | Compact element spacing, inline padding |
| md | 16px | Default element spacing, standard padding |
| lg | 24px | Section padding, card spacing |
| xl | 32px | Layout gaps, major spacing |
| 2xl | 48px | Major section breaks |
| 3xl | 64px | Page-level spacing |

**Exceptions:** None — all Phase 7 UI uses standard Material spacing multiples of 4.

---

## Typography

**Source:** Material 3 defaults via Flutter `material` package

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 14px | 400 (regular) | 1.5 |
| Body (emphasis) | 14px | 500 (medium) | 1.5 |
| Label | 12px | 500 (medium) | 1.0 |
| Heading (small) | 16px | 500 (medium) | 1.25 |
| Heading (medium) | 20px | 500 (medium) | 1.2 |
| Display | 24px | 400 (regular) | 1.0 |

**Notes:**
- All text uses Flutter's `TextTheme` from the active `ThemeData.textTheme` (set at `CadenceApp` root).
- No custom font files introduced by Phase 7.
- Line heights are implicit via Material 3 defaults; no explicit line-height override needed in Phase 7 widgets.

---

## Color

**Source:** Material 3 color scheme seeded from green (app_theme.dart)

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | ColorScheme.surface (light: #FFFBFE, dark: #1A1B20) | Screen backgrounds, app canvas |
| Secondary (30%) | ColorScheme.surfaceContainer (light: #F3EEF8, dark: #312E37) | Cards, lists, containers |
| Accent (10%) | ColorScheme.primary (green, Material 3 default) | Reserved for **primary CTAs only** (band create, track create, setlist create buttons) |
| Destructive | ColorScheme.error (Material 3 red) | Delete/leave band, remove track, clear setlist |

**Accent reserved for:**
- "Add band" button (FloatingActionButton on Bands tab)
- "Create band" CTA (band creation flow)
- "Add track" button (FloatingActionButton on Tracks tab)
- "Create track" CTA (track creation flow)
- "Add setlist" button (FloatingActionButton on Setlists tab)
- "Create setlist" CTA (setlist creation flow)
- Form submission buttons (login/signup on auth screen)

---

## Copywriting Contract

### Primary Banner: Offline State (D-04)

**Element:** `OfflineBanner` widget (global, `RootScaffold` level)

| Aspect | Specification |
|--------|---------------|
| **Copy** | `Showing cached data — may be out of date` |
| **Previous copy** (removed) | `You're offline — showing cached data` |
| **Rationale** | D-04: Reworded to be connectivity-agnostic — covers both true-offline and online-but-fetch-failed cases. Does not claim "you're offline" literally since silent fallback to cache can happen while device is technically online (connectivity_plus reports radio state, not active internet). |
| **Icon** | `Icons.cloud_off` (Material Icons) |
| **Colors** | `errorContainer` background + `onErrorContainer` text/icon |
| **Placement** | Top of `RootScaffold.body`, above `IndexedStack` (all 5 tab screens) |
| **Visibility** | Shown only when `isOnlineProvider` is false (device-level connectivity from `connectivity_plus`) |
| **Padding** | Horizontal 16px, vertical 8px (standard Material spacing) |
| **Duration** | Persistent (no auto-dismiss); visible for entire offline session |

**Layout example:**
```
┌────────────────────────────┐
│ 🌥️  Showing cached data — may be out of date │  ← OfflineBanner
├────────────────────────────┤
│                            │
│   IndexedStack (Home/     │
│   Bands/Tracks/Setlists/  │
│   Profile content)         │
│                            │
└────────────────────────────┘
```

---

### Secondary State: Offline + No-Cache Error (D-06, D-07)

**Element:** New error state in each tab screen's `AsyncValue.when()` error branch

| Aspect | Specification |
|--------|---------------|
| **Trigger condition** | Screen opened offline (`isOnlineProvider` false) AND no cache data exists for this screen's provider |
| **Exception type** | `OfflineNoCacheException` (custom exception in provider layer) |
| **Screen state** | Full-screen centered error layout (reusing existing `_buildError()` widget structure) |
| **Heading** | `No cached data` |
| **Body text** | `Connect to the internet to load this` |
| **Icon** | `Icons.cloud_off_outlined` (outlined variant, larger size 64px) |
| **Icon color** | `colorScheme.onSurfaceVariant` |
| **Retry button** | **None** — no retry possible while offline |
| **Recovery behavior** | Automatic (D-07) — screen listens to `isOnlineProvider`, and the instant it flips to online, it refetches automatically with no user action needed |

**Layout example:**
```
┌────────────────────────────┐
│ Showing cached data —      │  ← OfflineBanner
├────────────────────────────┤
│                            │
│           ☁️❌             │  ← cloud_off_outlined icon (64px)
│                            │
│     No cached data         │  ← heading (headlineSmall)
│                            │
│  Connect to the internet   │  ← body text (bodyMedium)
│  to load this              │
│                            │
└────────────────────────────┘
```

---

### Tertiary State: In-Flight Refresh Indicator (D-08, D-09)

**Element:** Subtle progress indicator in AppBar while refetching with cached data present

| Aspect | Specification |
|--------|---------------|
| **Trigger** | When provider's `AsyncValue.isLoading` is true AND `AsyncValue.hasValue` is true (refreshing with old data present) |
| **Indicator type** | `LinearProgressIndicator` (thin, indeterminate progress bar) |
| **Placement** | AppBar's `bottom` property (below title text, above screen content) |
| **Height** | 2px (thin, non-intrusive) |
| **Color** | `colorScheme.primary` (green, Material 3 default) |
| **Visibility timing** | Only shown during background refresh **after** first successful load (D-08). On cold start (no data yet), show full-screen `CircularProgressIndicator` centered in screen (D-09 exception). |
| **Duration** | Visible until fetch completes and new data arrives; automatic removal when state transitions to `AsyncData` |

**Screen state examples:**

**Cold start (D-09):**
```
┌────────────────────────────┐
│ Showing cached data —      │  ← OfflineBanner
├────────────────────────────┤
│                            │
│      ⟳ (spinner)           │  ← Full-screen centered CircularProgressIndicator
│                            │
│    Loading bands...        │  ← Implicit message (or none, per Material)
│                            │
└────────────────────────────┘
```

**Refresh with cached data present (D-08):**
```
┌────────────────────────────┐
│ Showing cached data —      │
├────────────────────────────┤
│ Bands                    ⋮ │  ← AppBar title
│ ══════════════════════════ │  ← LinearProgressIndicator (2px, thin)
│                            │
│ • Band A                   │  ← Old cached band list still visible
│ • Band B                   │
│ • Band C                   │
│                            │
└────────────────────────────┘
```

---

### Removed: SyncStatusBadge (All Instances)

**Element:** `SyncStatusBadge` widget (to be completely removed)

| Aspect | Specification |
|--------|---------------|
| **Removal scope** | Delete widget from 10 call-sites (all tab screens and their detail variants) |
| **Files affected** | `home_screen.dart`, `bands_screen.dart`, `band_detail_screen.dart`, `tracks_screen.dart`, `track_list_screen.dart`, `track_detail_screen.dart`, `setlists_screen.dart`, `setlist_list_screen.dart`, `setlist_detail_screen.dart`, `profile_screen.dart` |
| **Replacement** | None — D-04/D-08 (offline banner + subtle progress indicator) entirely replace SyncStatusBadge's purpose |
| **Removed behavior** | No more "Synced Xm ago" badges; no more 10min/30min staleness tiers (warning color escalation); no more per-screen sync timestamp display |
| **Widget file** | `lib/widgets/sync_status_badge.dart` — file can be deleted entirely after all 10 call-site removals |
| **Imports** | Remove `import 'package:cadence/widgets/sync_status_badge.dart'` from all 10 affected screens |

---

## UI Considerations

Applicable state considerations resolved: **5 covered, 0 backstop, 0 unresolved**

| Category | Element(s) | Status | Resolution / Reason |
|----------|------------|--------|---------------------|
| Empty (no-cache) | All tab & detail screens | ✅ covered | Offline-no-cache state (D-06) renders dedicated "No cached data — Connect to the internet" message via `OfflineNoCacheException` branch in error handler |
| Loading (cold start) | All tab screens (5) + detail screens (5) | ✅ covered | D-09: Cold start shows full-screen centered `CircularProgressIndicator`; no old data to keep visible |
| Loading (refresh) | All tab screens (5) + detail screens (5) | ✅ covered | D-08: Refresh with cached data shows old content + subtle `LinearProgressIndicator` in AppBar.bottom (no blank flash on tab switch) |
| Offline | All screens (all 5 tab + all detail variants) | ✅ covered | D-04: Global `OfflineBanner` ("Showing cached data — may be out of date") visible when `isOnlineProvider` is false, regardless of which screen is active |
| Error (fetch failed) | All screens with API dependency | ✅ covered | D-03: Online-but-fetch-failed silently falls back to cache; only surfaces error if no cache exists (`OfflineNoCacheException`) |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| Flutter Material | (built-in) | not required — no third-party component registry used |
| Material Icons | (built-in via Flutter) | not required — standard icon library |

**Note:** This is a native Flutter app, not a web app using shadcn or other component libraries. No third-party Dart package registries consulted for UI components.

---

## Pre-Population Source Audit

| Artifact | Decisions Used | Count |
|----------|----------------|-------|
| CONTEXT.md (Phase 7 decisions) | D-01 (tab-switch trigger), D-03 (fetch-fails-online fallback), D-04 (banner wording), D-05 (banner trigger), D-06 (offline-no-cache state), D-07 (offline recovery), D-08 (in-flight indicator), D-09 (cold-start exception) | 8 |
| RESEARCH.md (technical stack) | Material 3 + connectivity_plus + Riverpod async patterns | 3 |
| Existing codebase (`offline_banner.dart`, `sync_status_badge.dart`, `app_theme.dart`) | Icon choices, color scheme, padding/spacing values, typography | 5 |
| REQUIREMENTS.md (OFFL-07, OFFL-08) | Scope: remove badge, add banner messaging for offline state | 2 |

**Total pre-populated:** 18/18 fields from upstream sources. **No user questions needed.**

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending (awaiting gsd-ui-checker verification)

---

## Phase-Specific Notes

### What This Phase Changes (UI Layer)

1. **OfflineBanner text:** Reword from "You're offline — showing cached data" to "Showing cached data — may be out of date" (D-04)
2. **SyncStatusBadge removal:** Delete widget and all 10 instantiations
3. **Offline-no-cache error state:** Add new `OfflineNoCacheException` branch to error handling in all screens
4. **In-flight refresh indicator:** Add subtle `LinearProgressIndicator` to AppBar when refetching with data present (D-08)

### What This Phase Does NOT Change (Non-UI)

- Data layer provider logic (cache-first → online-first inversion) — spec'd in RESEARCH.md, implemented in provider layer
- Navigation structure or routing
- Authentication or token handling
- API contract or HTTP client
- Screen layout or component tree structure
- App theme (colors, fonts, spacing scale)

### Integration Points

- **OfflineBanner:** Already exists in `RootScaffold`; only text needs update
- **SyncStatusBadge:** Already exists in 10 screens; needs complete removal
- **Tab-screen refetch trigger (D-01):** Wired in provider layer (not UI layer) via `selectedTabIndexProvider` listener
- **In-flight indicator:** Rendered in each screen's AppBar conditionally based on provider's `AsyncValue.isLoading && hasValue`
- **Offline-no-cache state:** Handled in each screen's error branch, throwing/catching `OfflineNoCacheException`

---

*Phase 7 UI contract: messaging + state-coverage refactor (cache-behavior-inversion, not visual redesign)*
*Created: 2026-08-21 | Status: draft*
