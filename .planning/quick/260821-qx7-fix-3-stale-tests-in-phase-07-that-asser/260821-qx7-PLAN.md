---
title: Fix 3 stale tests asserting the retired cache-first offline-disabled-tiles pattern
description: >
  07-01 flipped BandDetailData/BandDetailScreen (and BandsListData/BandsScreen)
  from cache-first to online-first: offline-with-nothing-cached now renders
  OfflineNoCacheView instead of the old disabled-but-visible tile pattern, and
  BandsListData.build() reads isOnlineProvider directly (fail-safe default:
  false). Two tests in band_detail_screen_test.dart still assert the retired
  disabled-tile behavior, and widget_test.dart's bottom-nav test never
  overrides isOnlineProvider, so it silently exercises the offline branch and
  never finds the seeded band. Rewrite the two stale tests to assert the
  current online-first contract (offline -> OfflineNoCacheView, no tile) while
  preserving their original intent (owner sees Delete, member sees Leave,
  Remove icon on member rows) via an added online assertion; add the missing
  isOnlineProvider override to the bottom-nav test.
files_modified:
  - test/features/bands/band_detail_screen_test.dart
  - test/widget_test.dart
---

<objective>
Fix 3 tests that fail on `main` after phase 07 (cache-behavior flip to
online-first) merged completely, without deleting their original coverage
intent and without touching any other test file or any non-test source file.

Purpose: these are pre-existing, root-caused failures (see
`.planning/phases/07-cache-behavior-flip-online-first/deferred-items.md`,
items 2 and 3) left over from 07-01's online-first flip of
`BandDetailData`/`BandDetailScreen`/`BandsListData`. They are test-only
staleness, not product bugs.
Output: `test/features/bands/band_detail_screen_test.dart` and
`test/widget_test.dart` updated; `flutter test` passes with 0 failures where
previously these 3 failed.
</objective>

<context>
@test/features/bands/band_detail_screen_test.dart
@test/widget_test.dart
@test/features/home/home_screen_test.dart
@lib/widgets/offline_no_cache_view.dart
@lib/providers/connectivity_provider.dart
@.planning/phases/07-cache-behavior-flip-online-first/deferred-items.md
@.planning/phases/07-cache-behavior-flip-online-first/07-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the 2 stale offline-disabled-tile tests in band_detail_screen_test.dart</name>
  <files>test/features/bands/band_detail_screen_test.dart</files>
  <action>
    This file already imports `OfflineNoCacheView` and already has a `wrap()`
    helper accepting an `isOnline` bool (default `true`), a `band()` fixture
    helper, and a `buildRoutedApiClient()` helper — reuse all three exactly as
    the file's other tests do. Two tests in this file assert a stale
    cache-first pattern (a disabled-but-visible `ListTile`/`IconButton` while
    offline) that no longer exists under 07-01's online-first
    `BandDetailScreen`: offline-with-no-cache now renders `OfflineNoCacheView`
    with no tile at all, not a disabled tile.

    Rewrite the first stale test (currently named "Delete and Leave tiles are
    disabled while offline (owner sees Delete, member sees Leave)"). Rename it
    to reflect the new contract, e.g. "owner sees an enabled Delete tile
    online, member sees an enabled Leave tile online; offline shows
    OfflineNoCacheView instead". For the owner case: build the owner
    `CacheService`/`ApiClient` pair via `band()` and `buildRoutedApiClient()`
    exactly as the existing test does, pump `wrap(ownerApiClient,
    ownerCacheService)` (online, the helper's default), `pumpAndSettle`, then
    assert the `ListTile` found via `find.widgetWithText(ListTile, 'Delete')`
    has `enabled == true` and `onTap != null` — this preserves the original
    "owner sees Delete" intent while asserting the correct (online, enabled)
    state instead of the retired (offline, disabled) one. Then pump a second
    widget tree reusing `ownerApiClient` with a fresh
    `CacheService.inMemory()` seeded the same way via `writeBandDetail`, this
    time via `wrap(ownerApiClient, freshCacheService, isOnline: false)`,
    `pumpAndSettle`, and assert `find.byType(OfflineNoCacheView)` finds one
    widget while `find.widgetWithText(ListTile, 'Delete')` finds nothing.
    Repeat the same online-then-offline two-step sequence for the member case
    (profile id `u2`, a fresh `memberApiClient`/`memberCacheService` pair),
    asserting the `Leave` `ListTile` is enabled with `onTap != null` while
    online, then that `OfflineNoCacheView` renders with the `Leave` `ListTile`
    absent while offline.

    Rewrite the second stale test (currently named "Remove icon on a member
    row is disabled while offline, enabled while online"). Rename it to
    reflect the new contract, e.g. "Remove icon appears on a member row while
    online; offline shows OfflineNoCacheView instead". Seed a `members` list
    of an owner (`u1`) and a member (`u2`) via `band(members: members)`
    exactly as the existing test does, pump `wrap(apiClient, cacheService)`
    online first, and assert the `person_remove` `IconButton` (found via
    `find.widgetWithIcon(IconButton, Icons.person_remove)`) has `onPressed !=
    null` — preserving the "Remove icon exists on member rows" intent. Then
    pump a second widget tree reusing the same `apiClient` with a fresh
    offline `CacheService` (`wrap(apiClient, freshCacheService, isOnline:
    false)`), `pumpAndSettle`, and assert `OfflineNoCacheView` renders while
    `find.byIcon(Icons.person_remove)` finds nothing.

    Follow this file's established two-widget-tree-pump-in-one-test pattern
    (see the existing "Edit icon is disabled while offline and enabled while
    online" test for the shape of pumping a second `ProviderScope` tree mid
    test with a fresh `CacheService`). Do not change any other test in this
    file — the currently-passing tests ("owner sees a 'Delete' action,
    non-owner does not", "non-owner sees a 'Leave' action, owner does not",
    "owner sees a 'Remove' icon on other members' rows...") already cover
    online Delete/Leave/Remove-icon presence and must keep passing unmodified.
    Do not touch any non-test source file.
  </action>
  <verify>
    <automated>flutter test test/features/bands/band_detail_screen_test.dart</automated>
  </verify>
  <done>Both rewritten tests pass, asserting online-enabled Delete/Leave tiles and an online-enabled Remove icon (preserving original coverage intent), plus offline rendering OfflineNoCacheView with the corresponding tile/icon absent. All other tests in the file remain unmodified and passing.</done>
</task>

<task type="auto">
  <name>Task 2: Add missing isOnlineProvider override to widget_test.dart's bottom-nav test</name>
  <files>test/widget_test.dart</files>
  <action>
    Add an import for `isOnlineProvider` (`package:cadence/providers/connectivity_provider.dart`)
    alongside this file's existing `package:cadence/...` imports. In the
    "bottom navigation switches between tabs" test only — the first test in
    this file; leave the second test ("WR-01: tapping 'View bands'...")
    untouched, since it never asserts on rendered band data and does not need
    this override — add `isOnlineProvider.overrideWithValue(true)` to that
    test's `ProviderScope` `overrides` list, alongside the existing
    `cacheServiceProvider.overrideWithValue(...)` and
    `apiClientProvider.overrideWithValue(...)` entries. This matches the
    override pattern already established in
    `test/features/home/home_screen_test.dart`'s `wrap()` helper and
    `test/features/bands/band_detail_screen_test.dart`'s `wrap()` helper.
    Under online-first (07-01), `BandsListData.build()` reads
    `isOnlineProvider` directly, which defaults to `false` (fail-safe) with no
    platform-channel mock in this test's sandbox; without the override the
    Bands tab renders `OfflineNoCacheView` instead of the seeded `'B.A.T.H.'`
    band the test expects to find and tap. Do not touch any non-test source
    file.
  </action>
  <verify>
    <automated>flutter test test/widget_test.dart</automated>
  </verify>
  <done>The "bottom navigation switches between tabs" test passes: the Bands tab renders the seeded 'B.A.T.H.' band after the override, and the tab-switch assertion succeeds. The WR-01 test in the same file remains unmodified and passing.</done>
</task>

</tasks>

<verification>
- `flutter test` (full suite) reports 0 failures — the 3 previously-failing
  tests now pass and no other test regresses.
- `flutter analyze` reports no new issues in the 2 modified files.
- Manual check: `git diff --stat` shows changes limited to
  `test/features/bands/band_detail_screen_test.dart` and `test/widget_test.dart`
  — no non-test source file touched.
</verification>

<success_criteria>
- `test/features/bands/band_detail_screen_test.dart`'s two rewritten tests
  assert the current online-first contract (OfflineNoCacheView when offline
  with no cache; enabled Delete/Leave tile and Remove icon when online) and
  pass.
- `test/widget_test.dart`'s "bottom navigation switches between tabs" test
  passes with an explicit `isOnlineProvider.overrideWithValue(true)`.
- Full `flutter test` suite is green (0 failures), with no source-file changes
  outside the 2 listed test files.
</success_criteria>

<output>
Create `.planning/quick/260821-qx7-fix-3-stale-tests-in-phase-07-that-asser/260821-qx7-SUMMARY.md` when done
</output>
